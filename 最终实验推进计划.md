# 最终实验推进计划

更新时间：2026-06-10

## 当前结论

当前项目已经跑通主训练、评估、逐图对比、ROI 可视化和 flow 诊断链路。`v4_robust_edge_10ep` 是目前最强主模型候选，1000 样本上相对 v3 主线在 crack/global EPE、Dice、edge fidelity 和 folding rate 上整体更好。

但当前还不能直接说已经满足“三区及以上论文实验完整度”。主要差距是：最终模型还需要锁定，外部新近方法对比还没有跑，消融实验还没有形成完整表格，论文实验部分需要的统计表、可视化和失败分析还没有统一整理。

## 目标拆分

### 1. 代码收口

目标：保证训练、评估、可视化和结果汇总都能按统一入口复现。

需要完成：

- 固定最终主模型候选：优先使用 `v4_robust_edge_10ep/best_epe.pth`。
- 统一最终评估口径：主结果以 1000 样本或完整测试集为准，120 样本只作为快速诊断。
- 生成可复用命令：使用 `utils/generate_final_experiment_commands.py` 输出主实验和消融实验 Slurm 命令。
- 后续补一个总表汇总脚本：从多个 `eval_summary.json` 聚合为论文表格 CSV / Markdown。

### 2. 主模型最终训练与评估

目标：确认 `v4_robust_edge_10ep` 是否可以作为最终主模型。

建议优先做：

- 对 `v4_robust_edge_10ep/best_epe.pth` 做完整评估，至少覆盖当前固定的 1000 样本；如果服务器时间允许，再做全量评估。
- 输出主表指标：crack EPE、global EPE、Dice、crack edge fidelity、global edge fidelity、folding rate。
- 输出可视化：整体提升样本、共同失败但改善样本、局限/退化样本。

### 3. 外部对比实验

目标：补齐近年强相关方法，而不是只放旧算法。

优先级建议：

| 优先级 | 方法方向 | 候选方法 | 选择原因 | 当前动作 |
| --- | --- | --- | --- | --- |
| A | 近年高精度光流 | [SEA-RAFT / SEA-RAFT-M](https://github.com/princeton-vl/SEA-RAFT) | 2024 方法，速度和精度都强，适合做 dense flow 对比 | 优先调研开源代码并适配当前输入输出 |
| A | 统一匹配/光流 | [UniMatch](https://github.com/autonomousvision/unimatch) | 2023 方法，代码成熟，可作为强基线 | 优先跑通评估适配 |
| B | 记忆光流 | [MemFlow](https://github.com/DQiaole/MemFlow) | 2024 方法，适合视频/序列，若当前数据能组织伪序列可作为扩展对比 | 先评估适配成本 |
| B | 快速轻量光流 | [NeuFlow / NeuFlow v2](https://github.com/neufieldrobotics/NeuFlow_v2) | 2024-2025 方向，适合作为速度对比 | 先确认代码和权重可用 |
| C | 经典强基线 | RAFT / GMFlow / FlowFormer | 不够新，但可作为历史强基线或补充 | 不建议作为唯一对比 |
| C | 水下/湍流恢复 | 2024-2025 水下畸变或湍流恢复方法 | 领域相关，但适配成本不确定 | 先做文献和代码可用性筛选 |

对比方法统一要求：

- 使用同一数据划分和同一评估脚本。
- 如果方法输出的是像素位移，需要转换为当前项目的归一化逆映射坐标场。
- 如果方法只能输出校正图，则至少补充图像级指标和 ROI 可视化，但主表应明确“非同类输出”。
- 最终主表建议保留 4-6 个外部方法，避免堆太多弱相关方法。

### 4. 消融实验

目标：证明 v4 的关键改动确实有效。

建议最小消融矩阵：

| 实验名 | 初始化 | `W_CRACK_MAG` | `CRACK_MAG_ROBUST_DELTA` | `CRACK_MAG_OVER_WEIGHT` | `W_CRACK_EDGE` | 目的 |
| --- | --- | ---: | ---: | ---: | ---: | --- |
| `abl_v3_base` | v2 best | 0.05 | 0 | 0 | 0 | 已有 v3 主线，作为消融基底 |
| `abl_robust_only` | v3 best | 0.05 | 0.01 | 0.5 | 0 | 验证鲁棒位移幅度和过大位移惩罚 |
| `abl_edge_only` | v3 best | 0 | 0 | 0 | 0.05 | 验证裂缝边缘一致性 |
| `abl_no_over_penalty` | v3 best | 0.05 | 0.01 | 0 | 0.05 | 验证过大位移惩罚是否必要 |
| `abl_edge_w003` | v3 best | 0.05 | 0.01 | 0.5 | 0.03 | 验证边缘损失更保守权重 |
| `abl_v4_full` | v3 best | 0.05 | 0.01 | 0.5 | 0.05 | v4 当前完整方案 |

说明：

- 已经跑过的 v3/v4 结果可以直接纳入表格，不需要重复浪费算力。
- 新消融建议先跑 2 epoch smoke，再跑 8-10 epoch 小规模 fine-tune。
- 每个消融至少做 1000 样本评估，再和 v3/v4 做逐图对比。

### 5. 论文实验材料

必须补齐：

- 主结果表：最终模型与内部版本、外部方法的统一指标。
- 消融表：每个模块对 EPE、Dice、edge fidelity、folding 的贡献。
- 可视化图：整体校正、ROI 局部、EPE 热图、folding 热区、位移幅度图。
- 失败案例：展示仍未解决的边界、大形变和裂缝 ROI 错位。
- 训练曲线：loss、Val EPE、crack EPE、edge/folding 变化。
- 运行成本：参数量、推理速度、训练耗时、显存占用，至少给出近似统计。

## 下一步执行顺序

1. 在服务器上用 `utils/generate_final_experiment_commands.py --stage all` 生成命令清单。
2. 先跑 `main_eval`，确认 v4 最终主结果。
3. 跑 2-3 个优先消融：`abl_robust_only`、`abl_edge_only`、`abl_no_over_penalty`。
4. 同步调研并适配外部对比方法，优先 SEA-RAFT 和 UniMatch。
5. 汇总主实验、对比实验和消融实验表格，再决定是否继续做 v5。
