# 项目级 AGENTS.md

## 项目目标

本项目用于整理、分析、训练和后续优化“水下裂缝图像扭曲校正”算法。当前仓库已经包含用户补充的代码、合成数据、原始裂缝图、checkpoint、训练日志、评估结果和两份 Word 报告。核心任务是提升水下裂缝扭曲复原效果，重点解决当前可视化效果不好、裂缝复原不足、预测恢复量偏小、工程门限未达标等问题。

## 技术栈

- 文档格式：Microsoft Word `.docx`
- 报告生成痕迹：文档元数据均显示由 `python-docx` 生成
- 运行环境：已创建项目本地 Python 虚拟环境 `.venv`；当前 `.venv` 已安装文档处理依赖和 `numpy`，但尚未安装训练所需的 PyTorch、OpenCV、SciPy、tqdm、natsort、scikit-image 等依赖
- 深度学习框架：PyTorch 代码结构，训练入口为 `train_v2.py`
- 图像与数据：OpenCV / NumPy / Pillow 风格的数据生成、读取、推理和可视化流程
- 已新增 Slurm 训练包装入口和提交脚本；集群训练应优先使用 `slurm_train_crackwarp.sbatch`，通过 `run_train_slurm.py` 覆盖输出目录和训练参数，避免误清空历史结果

## 当前架构

- `水下裂缝扭曲校正实验报告(1).docx`：普通版报告，侧重训练收敛、典型样本可视化和综合结论。
- `水下裂缝扭曲校正实验报告_详细版(1).docx`：详细版报告，额外包含 checkpoint 横向评估、逐图像误差分布、ROI 局部观察、门限失败原因和优化建议。
- `generate_underwater_v2.py`：根据真实裂缝图合成水下扭曲训练样本与 `.npy` 逆映射标签。
- `MyDataSet.py`：读取成对的 `.png` / `.png.npy` 样本，并实现水下颜色、散射、噪声、模糊和几何增强。
- `models/crack_warp_net.py`：主网络，包含编码器-解码器、Transformer、CBAM、NAFBlock 和迭代 flow refinement 等结构。
- `loss_crack.py`：裂缝优先损失，包含坐标损失、边缘感知平滑、folding penalty、频域损失、光度一致性和 crack mask 加权项。
- `train_v2.py`：训练入口，使用 `config_crack.py` 配置，包含训练/验证划分、AMP、EMA、warm restart、mixup、checkpoint 保存等逻辑。
- `infer_epoch.py`：推理与可视化入口；现有文档中仍有历史脚本名 `infer_epoch80.py`，后续需要统一。
- `utils/evaluate_metrics.py` 与 `utils/checkpoint_gate_report.py`：裂缝中心指标评估和 checkpoint 门限诊断。
- `utils/compare_eval_per_image.py`：对比新旧模型 `eval_per_image.csv`，自动挑选新模型更好、旧模型更好和共同失败的诊断样本，并输出图片清单。
- `utils/export_crack_roi_visuals.py`：裂缝 ROI 可视化导出脚本，现已支持 `--image_list`，可按诊断样本清单批量出局部放大图。
- `utils/export_flow_diagnostics.py`：整图 flow 诊断脚本，用于同批样本的新旧模型对比，导出校正图、EPE 热图、folding 热区、位移幅度图和逐图诊断 CSV。
- `under-crack-images/images/`：原始真实裂缝图，共 1,037 张 `.jpg`。
- `underwater_crack_v3/`：主合成训练集，共 10,360 张 `.png` 与 10,360 个 `.png.npy` 标签，`manifest.json` 记录 `samples_per_image=10` 与 `total_generated=10360`。
- `output_crackwarp/`：已有训练输出，包含 `train.log`、13 个 `.pth` checkpoint、评估 CSV/JSON、gate report 和可视化结果。
- `.gitignore`：忽略本地虚拟环境、Python 缓存、编辑器目录和系统生成文件。
- `.venv/`：项目本地 Python 虚拟环境，不应入库。

## 开发规范

- 所有 Python 操作必须使用项目本地 `.venv`，不要使用系统 Python。
- 编辑文件前必须先读取和理解现有内容。
- 保持最小修改，不做无关重构。
- 不直接删除文件；如需清理文件，只提供建议命令或先征求确认。
- 新增代码或脚本时，应包含较详细的中文注释，说明用途、关键逻辑、参数、返回值和重要分支。
- 后续若新增训练代码、推理脚本或长时间运行任务，应评估是否需要 Slurm 脚本；GPU 任务默认优先使用 `gpu` 分区，账号默认 `gpo-ifv7xx`，QOS 默认 `normal`。
- 当前 `train_v2.py` 中 `restart_training=True` 会删除目标输出目录；Slurm 训练必须使用独立输出目录，默认写入 `output_crackwarp_slurm/<jobid>`，不要直接覆盖已有 `output_crackwarp/`。

## Current Status

2026-07-15 已按用户选择的“方案 A”补齐客观评价表中 Roboflow 两个数据集的外部四模型 + v107 指标。新增 `scripts/run_roboflow_external_v107_eval.sh`、`scripts/summarize_roboflow_external_v107_eval.py` 和 `scripts/submit_roboflow_external_v107_eval.sbatch`，通过 GitHub Release `roboflow-external-v107-scripts-20260715` 同步到 usmidet，提交短任务 `43079810` 并完成；普通重复排队任务 `43079790` 已取消。补评估结果通过 Release `roboflow-external-v107-results-20260715` 拉回本地 `roboflow_external_v107_eval_results_local/`。最终 CSV 共 10 行：Roboflow underwater crack 160 样本、Roboflow concrete/blue crack 48 样本，各包含 RAFT、UniMatch、SEA-RAFT、GMA/RAFT-small fallback、V107。已更新 `scripts/fill_user_final_docx_tables_and_figures.py` 并生成 `实验部分整理最终_填表补图_补齐指标.docx`；校验结果：表格结构保持 `[(6,4), (4,8), (4,6)]`，图片数 `2`，文档不再包含“未单独评估”。报告口径仍需说明外部四模型为 oracle-pair dense matching 参考/上界，v107 是单图模型，二者不是完全同输入公平横比。

2026-07-15 已按用户要求将最终 Word `实验部分整理最终_填表补图_补齐指标.docx` 从微信临时目录复制到项目根目录，准备随方案 A 补评估脚本、汇总脚本和项目状态记录一起提交。提交范围只包含本次交付相关文件，避免混入仓库中大量历史结果包和未归类输出。

2026-07-15 用户澄清：需要保留用户原 Word 的表格结构，同时把表格内容填进去，并补图1/图2。已新增 `scripts/fill_user_final_docx_tables_and_figures.py`，从微信消息目录原始文件 `C:/Users/billy.zhou/Documents/xwechat_files/wxid_bkwta3uyaj7f11_44fc/msg/file/2026-07/实验部分整理最终.docx` 重新生成填表补图版，输出到 `C:/Users/billy.zhou/Documents/xwechat_files/wxid_bkwta3uyaj7f11_44fc/temp/RWTemp/2026-07/bcb15cc8ba39d84c08631605f8a62f2e/实验部分整理最终_填表补图.docx`。处理内容：保留 3 张表格的原行列结构 `[(6, 4), (4, 8), (4, 6)]`，填入外部四模型、v107、v108-A/B 数据消融和“未单独评估”标注；在原有图1/图2图注前插入外部四模型可视化图和 v107 训练曲线图。校验结果：输出文件 `paragraphs=20`、`tables=3`、`inline_shapes=2`、大小 `774,699 bytes`，包含关键数据 `1.6220`、`3.8106`、`14.8459`、`未单独评估`、`100 epoch`，不包含 `v109`、`v110`。

2026-07-15 用户要求在其 Word 中补充图1和图2。已新增 `scripts/fill_user_final_docx_figures.py`，直接打开用户给定路径 `C:/Users/billy.zhou/Documents/xwechat_files/wxid_bkwta3uyaj7f11_44fc/temp/RWTemp/2026-07/bcb15cc8ba39d84c08631605f8a62f2e/实验部分整理最终.docx`，只在已有“图1 各种水下裂缝图像复原算法的结果对比”和“图2 各种模型训练曲线”图注前插入图片，不重写文档、不修改表格结构。图1 使用 `external_four_model_1000_postprocess_local/external_four_model_1000_postprocess/visual_external4_1000/crack0001_00_visual_compare.png`，图2 使用 `delivery_v107_v108_html/assets/curves/v107_training_curves.png`。执行后校验：`paragraphs=20`、`tables=3`、`inline_shapes=2`、文档大小 `776,990 bytes`，表格结构保持 `[(6, 4), (4, 8), (4, 6)]`，图1/图2图注仍存在。

2026-07-15 用户要求不要修改其 Word 中的表格结构，只直接填数据到现有表格。已新增 `scripts/fill_user_final_docx_tables.py`，直接打开用户给定路径 `C:/Users/billy.zhou/Documents/xwechat_files/wxid_bkwta3uyaj7f11_44fc/temp/RWTemp/2026-07/bcb15cc8ba39d84c08631605f8a62f2e/实验部分整理最终.docx`，只替换现有 3 张表格单元格文本，不新增/删除表格、不改变行列数；没有真实实验数据的位置明确写“未单独评估”。已执行脚本并保存回用户 Word 原路径，校验结果：`paragraphs=18`、`tables=3`、`inline_shapes=0`，表格结构保持 `[(6, 4), (4, 8), (4, 6)]`，关键数据 `1.6220`、`3.8106`、`14.8459`、`未单独评估`、`100 epoch` 已写入，且表格内容不包含 `v109`、`v110`。

2026-07-15 用户最新要求调整最终报告口径：内部模型不再补四模型长训对比，只保留内部最新 v107；对比实验改为四个外部模型 RAFT、UniMatch、SEA-RAFT、GMA/RAFT-small fallback 与 v107 对比。已取消 v109/v110 长训 jobs `21136455`、`21136456`，二者 Slurm 状态为 `CANCELLED`。已新增 `scripts/build_final_experiment_docx.py`，以用户指定模板 `实验部分整理最终.docx` 为基础，保留模板页面/样式格式并替换正文内容；新文档结构包含主观评价、可视化结果、客观评价、训练曲线、消融实验、结论。已重新生成 `实验部分整理最终.docx`，校验结果：包含 RAFT、UniMatch、SEA-RAFT、GMA/RAFT-small fallback、V107算法、外部参考/上界、100 epoch；不包含 `v109`、`v110`；文档 `paragraphs=25`、`tables=3`、`inline_shapes=3`、大小 `831,194 bytes`。最终报告中明确外部模型为 oracle-pair 参考/上界，v107 为本文 100 epoch 主模型，不能把外部模型写成与 v107 完全公平同输入对比。

2026-07-15 已复查 v109/v110 80 epoch 长训进度。plcyf jobs `21136455`、`21136456` 均仍在 `plcyf-com-prod-gpu001` 上 `RUNNING`，已运行约 4 小时。v109（v9 路线 80 epoch 补长训）当前日志显示完成 `Epoch 5/80`，已保存 `best_loss.pth`、`best_epe.pth`、`best_crack_epe.pth` 和 `epoch_5.pth`；当前最佳 Val EPE 约 `111.919px`，最佳 CrackEPE 约 `114.334px`，整体仍接近原 v9 早期基线水平，尚未明显改善。v110（v106 路线 80 epoch 补长训）当前日志显示完成 `Epoch 10/80`，已保存 `best_loss.pth`、`best_epe.pth`、`best_crack_epe.pth`、`epoch_5.pth` 和 `epoch_10.pth`；当前最佳 Val EPE 约 `18.135px`，最佳 CrackEPE 约 `17.329px`，比 v106 初始阶段略有改善但尚未达到 v107 水平。后续继续等待长训完成，再补 120/1000 指标和内部四模型长训对比表。

2026-07-15 已将外部四模型 1000 样本扩展评估并入客户实验部分 Word 材料。更新 `scripts/build_experiment_section_docx.py`：在可视化章节新增 `external_four_model_1000_postprocess_local/external_four_model_1000_postprocess/visual_external4_1000/crack0001_00_visual_compare.png` 作为外部四模型 1000 样本代表图；在指标章节新增外部 baseline 120/1000 双范围对比表，包含 RAFT、GMA/RAFT-small fallback、UniMatch、SEA-RAFT 的 Crack EPE、Global EPE、Dice、Crack edge、Folding，并明确说明外部模型为 oracle-pair 参考/上界，不是与内部单图模型完全公平同输入对比。已重新生成 `水下裂缝复原算法实验部分整理.docx`，校验结果：文档大小 `3,503,887 bytes`，`tables=4`，`inline_shapes=8`，关键外部指标 `1.5161/1.6220/2.1497/2.3947/2.7924/3.8106/2.8955/3.1400` 均已写入。

2026-07-15 已按用户要求完成外部四模型 1000 样本扩展评估后处理，用于补强外部 baseline 的稳定性。由于 usmidet 项目目录缺少最新外部对比脚本，已按用户“文件同步使用 git/gh，不用 scp”的要求，将 `scripts/run_external_eight_model_comparison.sh`、`scripts/submit_external_eight_model_comparison.sbatch`、`scripts/summarize_eight_model_comparison.py`、`utils/export_torchvision_raft_predictions.py`、`utils/export_gma_predictions.py` 和 `utils/export_visual_comparison_grid.py` 打包为 `external_eight_model_scripts_20260715.tar.gz`，上传到 GitHub Release `external-eight-model-scripts-20260715`，再在 usmidet 远端用 `gh release download` 拉取并解压。usmidet Slurm job `43031348` 输出目录为 `output_crackwarp_slurm/external_four_model_comparison_1000`；该 job 运行 `00:07:07` 后 Slurm 标记 `FAILED`、ExitCode `13:0`，但 `run_status.log` 显示 RAFT-large、GMA/RAFT-small fallback、UniMatch、SEA-RAFT 的 1000 样本导出/评估均已 `DONE`，内部 v9/v87/v106/v107 的 1000 样本评估也已 `DONE`；失败发生在原八模型可视化阶段。已单独补跑汇总和外部四模型可视化：生成 `eight_model_metrics_1000.csv`、`eight_model_comparison_1000.md`、`visual_external4_1000/` 和 `EXTERNAL4_1000_POSTPROCESS_DONE.md`。外部四模型 1000 样本指标：RAFT `Crack EPE=1.621974`、`Global EPE=0.789960`、`Dice=0.730836`、`Crack edge=0.790021`、`Folding=0.030280`；GMA/RAFT-small fallback `Crack EPE=2.394695`、`Dice=0.724985`；UniMatch `Crack EPE=3.810645`、`Dice=0.715517`；SEA-RAFT `Crack EPE=3.140003`、`Dice=0.718757`。轻量后处理包已通过 GitHub Release `external-four-model-1000-postprocess-20260715` 同步回本地 `external_four_model_1000_postprocess_local/`，SHA256 校验通过，包含 8 行指标表和 12 张外部四模型代表图。外部模型仍属于 oracle-pair 外部参考/上界，不能写成与内部单图模型完全公平同输入对比。

2026-07-15 已按用户要求补做“未充分长训内部基线”的 80 epoch 长训练：v9 早期基线从 `output_crackwarp_slurm/v9_from_v4_edge_w0025_jac_w0003_10ep/best_epe.pth` 继续训练 80 epoch，输出目录 `output_crackwarp_slurm/v109_v9_baseline_long80_from_v9`，plcyf Slurm job `21136455`；v106 detail-head probe 从 `output_crackwarp_slurm/v106_detail_strength_from_v105_1ep_plcyf/best_epe.pth` 继续训练 80 epoch，输出目录 `output_crackwarp_slurm/v110_v106_detail_long80_from_v106`，plcyf Slurm job `21136456`。两项任务均提交到 `plcyf-gpu` 分区、account `mfk-gsr2`、QOS `normal`，已在 `plcyf-com-prod-gpu001` 上进入 `RUNNING` 状态。v87 已有 80 epoch 长训，v107 已有 100 epoch 长训，因此本轮只补 v9 与 v106，后续完成后需重新生成内部四模型长训指标表，并更新八模型对比中的内部基线结果。

2026-07-14 已按客户要求补外部算法对比，新增并完成八模型 120 样本横评链路：外部 4 个为 RAFT、GMA/RAFT-small fallback、UniMatch、SEA-RAFT，内部 4 个为 v9、v87、v106、v107。新增 `utils/export_torchvision_raft_predictions.py`、`utils/export_gma_predictions.py`、`scripts/run_external_eight_model_comparison.sh`、`scripts/submit_external_eight_model_comparison.sbatch`、`scripts/summarize_eight_model_comparison.py` 和 plcyf/usmidet bridge 辅助脚本；新增 `scripts/plcyf_finalize_ext8_inner.sh`、`scripts/plcyf_summarize_ext8_inner.sh` 用于补齐 plcyf 上的内部评估和汇总。plcyf job `21093404` 曾因可视化样本清单包含 RAFT 未导出的样本而失败；修复后已补齐 UniMatch、SEA-RAFT、v9、v87、v106、v107，并生成 `eight_model_metrics_120.csv`、`eight_model_comparison.md`、`eight_model_status.md` 和 `visual_comparison_120/`。关键指标：RAFT `Crack EPE=1.516065`、`Dice=0.715649`、`Folding=0.032152`；GMA 因官方 checkpoint/repo 缺失按预案使用 RAFT-small fallback，`Crack EPE=2.149747`、`Dice=0.708206`；UniMatch `Crack EPE=2.792439`、`Dice=0.706881`；SEA-RAFT `Crack EPE=2.895537`、`Dice=0.701087`；内部 v9 `Crack EPE=106.244987`、v87 `24.744062`、v106 `18.956278`、v107 `15.763880`。轻量结果包已通过 GitHub Release `external-eight-model-comparison-20260714` 同步回本地 `external_eight_model_comparison_plcyf/`，SHA256 校验通过并已解压。注意：外部算法按 oracle-pair 条件评估，不能包装成与内部单图像模型完全公平的同输入对比，客户材料中应写成外部参考/上界。

2026-07-14 已按客户截图要求生成实验部分 Word 文档 `水下裂缝复原算法实验部分整理.docx`，并新增复现脚本 `scripts/build_experiment_section_docx.py`。文档结构严格对应 4 点：1）各种水下裂缝图像复原算法的可视化结果对比图；2）各种算法处理后图像质量指标比较；3）算法复原水下裂缝图像训练曲线、时间；4）消融实验、模块增减消融、外部数据消融结果。文档写入 v107 主模型指标、用户原报告 best_epe_v2 对比、v108 A/B/C/D 外部数据消融指标、v107 100 epoch 训练曲线、早期长训练曲线、后期可视化消融图、ROI/detail panel 和多算法 visual compare。已用 `.venv\Scripts\python.exe` 校验 Word 结构：文件大小 `2,949,983 bytes`、`paragraphs=22`、`tables=3`、`inline_shapes=7`，四个客户要求的小节标题均存在。

2026-07-14 已根据用户提供的原始 Word 详细报告 `水下裂缝扭曲校正实验报告_详细版(1).docx`，生成“用户原结果 vs 我们 v107 最优结果”的单文件 HTML 对比材料 `comparison_user_original_vs_v107.html`。处理流程：使用项目 `.venv` 安装 `python-docx`，提取原报告正文、9 个表格和 9 张内嵌图片到 `docx_extract_user_latest_report/`；基线采用原报告 `best_epe_v2` 120 样本指标（Crack EPE `110.2`、Global EPE `112.6`、Dice `0.0026`、Crack Edge `0.321`、Global Edge `0.308`、Folding `0.570`），对比我们 v107 120 样本指标（Crack EPE `15.763880`、Global EPE `16.620447`、Dice `0.527756`、Crack Edge `0.553365`、Global Edge `0.551721`、Folding `0.022193`），并补充 v107 1000 与全量 10360 评估。新增脚本 `scripts/build_user_original_vs_v107_html.py`，生成指标对比图 `comparison_user_original_vs_v107_assets/metric_comparison.png` 并内嵌原报告曲线/推理/ROI 图片及 v107 100 epoch 曲线。最终 HTML 校验：`image_count=8`、`data_uri_count=8`、`external_refs=[]`，大小约 1.71MB。

2026-07-14 已按用户要求生成面向客户发送的 v107/v108 单页 HTML 汇报材料，输出目录为 `delivery_v107_v108_html/`，压缩包为 `delivery_v107_v108_html.zip`，并进一步生成单文件版本 `delivery_v107_v108_standalone.html`。本次材料包含：v107 最新方法说明、v108 外部数据消融说明、按客户 1-5 点格式整理的可视化结果/指标对比/训练曲线/消融实验/最终结论、v107 100 epoch 完整训练曲线、早期长训练曲线、后期可视化消融曲线，以及 Roboflow underwater crack、Roboflow concrete/blue crack、EUVP 和外部候选综合预览图。已从远端 usmidet 拉回 v107 `train.log` 和 120 评估摘要到 `delivery_v107_v108_html/assets/v107/`，并生成 `v107_epoch_curve_points.csv` 与 `assets/curves/v107_training_curves.png`。用户指出 `UIEB raw 水下图像代表图` 内容为空白后，已从目录版和单文件版报告中移除该代表图；随后按用户要求移除结论部分“三区标准阈值待确认/不建议直接写全部达标”的内部提示段落。最新检查结果：目录版与单文件版均不包含 `三区标准：当前文档先给出实测指标`、`达标 / 接近达标 / 未达标`、`UIEB raw 水下图像代表图` 或 `UIEB_raw890_clear.jpg`；单文件版 `image_count=9`、`data_uri_count=9`，大小约 4.80MB。

2026-07-14 已确认 v108 A/B/C/D 120 补评估 jobs `21024689`-`21024692` 全部完成，退出码均为 `0:0`。正式 120 指标如下：A Roboflow 水下裂缝 `Crack EPE=12.911792`、`Global EPE=13.431700`、`Dice=0.521386`、`Crack edge=0.032756`、`Global edge=0.207698`、`folding=0.015561`；B concrete/blue crack `Crack EPE=18.684168`、`Global EPE=19.480400`、`Dice=0.475168`、`Crack edge=0.424907`、`Global edge=0.447418`、`folding=0.019833`；C UIEB/EUVP style `Crack EPE=16.069843`、`Global EPE=16.749149`、`Dice=0.553720`、`Crack edge=0.332475`、`Global edge=0.437909`、`folding=0.023992`；D all `Crack EPE=15.361706`、`Global EPE=15.822386`、`Dice=0.470117`、`Crack edge=0.120444`、`Global edge=0.241349`、`folding=0.018597`。对比 v107 120 (`Crack EPE=15.763880`、`Global EPE=16.620447`、`Dice=0.527756`、`Crack edge=0.553365`、`folding=0.022193`)：A 和 D 在 EPE/folding 上有局部改善，但边缘保真大幅下降；C 的 Dice 最好但 EPE、edge、folding 不优于 v107；B 整体偏差。当前判断：追加数据不能直接作为“全面有效”的结论，只能作为数据消融材料说明“外部数据能改变几何误差，但会显著冲击边缘结构保真”，不建议直接用 v108 作为最新主模型替代 v107。

2026-07-13 已确认 plcyf 上 v107 后处理 job `21013260` 完成，退出码 `0:0`，输出目录已生成 `eval_best_epe_120/`、`eval_best_epe_1000/`、`eval_best_epe_all_final/`、`visual_comparison_paper/`、`image_detail_roi_review/`、`roi_flow_highfreq/` 和 `paper_postprocess/POSTPROCESS_DONE.md`。v107 关键指标：120 样本 `Crack EPE=15.763880`、`Global EPE=16.620447`、`Dice=0.527756`、`Crack edge=0.553365`、`Global edge=0.551721`、`folding=0.022193`；1000 样本 `Crack EPE=14.845942`、`Global EPE=15.089559`、`Dice=0.581022`、`Crack edge=0.517541`、`Global edge=0.545023`、`folding=0.020609`；全量 10360 样本 `Crack EPE=14.609476`、`Global EPE=15.231524`、`Dice=0.525961`、`Crack edge=0.383267`、`Global edge=0.410554`、`folding=0.025538`。v108 A/B/C/D smoke 训练均已完成并保存 checkpoint，但 Slurm job 标记为失败，原因是训练脚本后置自动评估调用 `utils/evaluate_metrics.py` 时只扫描 `img_dir` 第一层，读不到 v108 多子目录 train_root。四组训练结果：A `Val EPE=18.114px`、`CrackEPE=17.441px`；B `Val EPE=17.942px`、`CrackEPE=17.312px`；C `Val EPE=18.147px`、`CrackEPE=17.495px`；D `Val EPE=18.024px`、`CrackEPE=17.378px`。已最小修改 `utils/evaluate_metrics.py` 的 `list_images()` 为递归扫描，并提交 v108 四组 120 补评估 jobs：A=`21024689` 正在运行，B=`21024690`、C=`21024691`、D=`21024692` 在 plcyf-gpu 排队。

2026-07-13 已按用户要求将 usmidet 上排队的 v107 后处理和 v108 data-ablation smoke 任务迁移到 `plcyf-com-prod-gpu001` 所在 plcyf 集群运行。访问链路为本机 `billy -> 100.94.186.128 -> plcyf-com-prod-gpu001 -> root -> xj62kv`，plcyf 项目目录为 `/net/8k3/e0fs01/users/xj62kv/underwater-crack-correction`，GPU 分区为 `plcyf-gpu`，account 使用历史可用 `mfk-gsr2`。已从 usmidet 同步 v107 输出目录、v108 data-ablation 数据、v106 初始化 checkpoint 和最新训练/评估代码到 plcyf；因 v108 数据中包含指向 `/mnt/users/xj62kv/underwater-crack-correction` 的绝对软链接，已在 plcyf 上创建兼容软链接 `/mnt/users/xj62kv/underwater-crack-correction -> /net/8k3/e0fs01/users/xj62kv/underwater-crack-correction`，随后 v108 dataset smoke 已通过。v107 后处理首次 plcyf job `21011596` 因 plcyf 代码版本旧、不认识 `global_context_flow_unet_image_detail_head_strong` 失败；同步 `models/`、`utils/`、`run_train_slurm.py`、`train_v2.py`、`config_crack.py`、`loss_crack.py`、`MyDataSet.py` 和 `slurm_train_crackwarp.sbatch` 后，已重新提交 v107 后处理 job `21013260`，当前在 `plcyf-com-prod-gpu001` 运行。v108 首次 plcyf jobs `21013201`-`21013204` 同样因旧训练入口失败；同步训练入口后已重新提交 `21013348`-`21013351`，其中 `21013348` v108-A 正在运行，v108-B/C/D 在 plcyf-gpu 排队。usmidet 上旧排队任务 `42728372`、`42765655`、`42765656`、`42765657`、`42765658` 已取消，避免重复运行。

2026-07-13 已确认 v107 最新方法 100 epoch 长训 job `42728275` 正常完成，退出码 `0:0`，总用时 `19:20:32`。训练日志显示完整跑满 100 epoch，最终记录：Train Loss `0.06034`、Train EPE `15.973px`、Val Loss `0.05868`、Val EPE `15.519px`、CrackEPE `14.877px`；已保存 `best_loss.pth`、`best_epe.pth`、`best_crack_epe.pth`、`epoch_100.pth` 和 `final.pth`。v107 训练阶段自动生成的 120 评估已完成，`eval_best_epe_120/eval_summary.json` 关键指标为：`primary_crack_epe_px_mean=15.759034`、`global_epe_px_mean=16.614424`、`primary_warp_crack_dice_mean=0.527820`、`primary_crack_edge_fidelity_mean=0.553387`、`global_edge_fidelity_mean=0.551732`、`folding_rate_mean=0.022201`。依赖后处理 job `42728372` 仍在 `PENDING(Priority)`，尚未生成 1000/full 评估、论文式可视化、ROI/detail/folding 诊断等完整交付材料。v108 A/B/C/D smoke jobs `42765655`-`42765658` 当前仍在排队，尚未开始运行。

2026-07-12 已实施 v108 data-ablation 数据准备和短周期训练提交，用于验证“数据集质量/复杂背景/真实水下扰动/更多裂缝类型”是否能改善可视化方向。新增 `scripts/prepare_v108_data_ablation.py`、`scripts/submit_v108_data_ablation_prepare.sbatch`、`scripts/smoke_v108_data_ablation.py` 和 `scripts/submit_v108_data_ablation_smoke.sh`。远端数据准备 job `42765485` 已完成，输出目录为 `/mnt/users/xj62kv/underwater-crack-correction/data_candidates/v108_data_ablation/`；生成 A/B/C/D 四组训练根目录和 source GT 根目录：A 为原训练集 + Roboflow 水下裂缝，10520 个 pair；B 为原训练集 + 少量 Roboflow concrete/blue crack，10408 个 pair；C 为原训练集 + UIEB/EUVP 水下风格增强，10520 个 pair；D 为 A+B+C 组合，10728 个 pair。已运行 `scripts/smoke_v108_data_ablation.py` 验证四组 `ImageData(..., return_source_gt=True)` 可正常读取，image/label/source shape 均正常。已提交 v108 四个 1 epoch GPU smoke job：A=`42765655`、B=`42765656`、C=`42765657`、D=`42765658`，均使用最新 `global_context_flow_unet_image_detail_head_strong`、从 v106 `best_epe.pth` 初始化、保持 v106 几何/anti-ripple/detail-head loss 设置，仅改变 `TRAINROOT` 与 `SOURCE_GT_ROOT`。v107 100 epoch 长训 job `42728275` 仍是客户主交付实验，当前继续运行；v108 只是数据消融 smoke，不替代 v107。

2026-07-12 已按用户要求把外部候选数据集下载任务直接提交到 usmidet 服务器，用于后续筛选“复杂背景、真实水下扰动和更多类型裂缝样本”。新增 `scripts/download_external_candidate_datasets.sh` 与 `scripts/submit_external_candidate_download.sbatch`；远端 CPU Slurm job `42759575` 已完成，输出目录为 `/mnt/users/xj62kv/underwater-crack-correction/data_candidates/`。UIEB 官方公开数据已下载完成：`UIEB/challenging-60.rar`、`UIEB/raw-890.rar`、`UIEB/reference-890.rar`，合计约 1.5GB；用户提供密码后，已解压到 `data_candidates/UIEB/official_extracted/`，其中 raw-890 为 890 张、reference-890 为 890 张、challenging-60 为 60 张。EUVP 已通过 KaggleHub 成功下载 `pamuduranasinghe/euvp-dataset`，cache 路径为 `/mnt/users/xj62kv/.cache/kagglehub/datasets/pamuduranasinghe/euvp-dataset/versions/1`，项目内软链接为 `data_candidates/EUVP/euvp-dataset-kagglehub`，统计为 35725 个文件、35720 张图片、约 1.23GB。用户提供 Roboflow API key 后，已用远端 `.venv` 安装官方 `roboflow` SDK 并下载两个 underwater crack 候选数据集到 `data_candidates/roboflow_underwater_crack/`：`rov-crack-detection-3o91d__underwater-crack-detection__v3__yolov8`（12012 张导出图片、12012 个 YOLO label，原始项目元信息 4372 张图）和 `underwater-concrete-crack-detection-bg23g__underwater-concrete-crack-6ahb2__v2__yolov8`（1060 张导出图片、1060 个 YOLO label，原始项目元信息 652 张图）。候选 `final-underwater-cracks` API 显示 `versions=0`，没有可下载版本。远端已写入 `data_candidates/DOWNLOAD_STATUS.md`、`data_candidates/UIEB/official_extracted/official_uieb_extract_summary.json`、`data_candidates/EUVP/euvp_kagglehub_summary.json`、`data_candidates/roboflow_underwater_crack/roboflow_sdk_download_manifest.json` 和 `roboflow_screening_summary.json` 说明当前状态；API key 未写入仓库文件。

2026-07-12 已并行启动并完成外部数据小规模筛选，不影响 v107 GPU 长训。新增 `scripts/screen_external_candidate_data.py` 与 `scripts/submit_external_candidate_screening.sbatch`；最终有效远端 CPU job 为 `42764110`，输出目录为 `/mnt/users/xj62kv/underwater-crack-correction/data_candidates/screened_external_candidates_v3/`。筛选覆盖 4 类 source：`UIEB_raw890` 输入 890 张、`EUVP_kaggle_sample` 输入 5000 张、`roboflow_rov-crack-detection...v3` 输入 12012 张、`roboflow_underwater-concrete-crack...v2` 输入 1060 张；合计评分 18962 张图片，每类保留 80 张初选，综合保留 160 张候选。输出包括 `SCREENING_SUMMARY.md`、每类 `*_all_scored.csv`、`*_selected.csv`、`combined_top_candidates.csv`、`selected_images/` 和 `contact_sheets/`。筛选时曾因 UIEB 官方 RAR 密码提示导致 job `42763219` 卡住并取消；随后使用 KaggleHub UIEB raw 镜像完成最终筛选。后续用户提供密码后，官方 UIEB RAR 也已成功解压，后续如需可改用官方解压目录重跑 UIEB 筛选。

2026-07-11 已按客户“最新方法完整跑 100 epoch，并补齐论文实验部分材料”的要求启动 v107 最新方法长训练和论文式交付流程。首次手工提交的 v107 job `42728050` 失败于初始化阶段，原因是 `HARD_SAMPLE_LIST` 指向了远端不存在的 `output_crackwarp_slurm/hard_samples/v98_v99_hard_roi_images.txt`，训练未真正开始。已改用 v106 脚本实际使用的 `scripts/v99_hard_roi_families.txt` 重新提交 v107 job `42728275`：`MODEL_NAME=global_context_flow_unet_image_detail_head_strong`、从 `output_crackwarp_slurm/v106_detail_strength_from_v105_1ep_plcyf/best_epe.pth` 初始化、`EPOCHS=100`、`LR=1e-6`，输出目录为 `output_crackwarp_slurm/v107_latest_method_long100_from_v106_r2`。当前 job `42728275` 在 `gpu` 分区排队，状态为 `PENDING`，原因是可用节点资源暂不可用。已新增并同步远端 v107 论文式后处理脚本，提交依赖 job `42728372`，依赖 `afterok:42728275`，将在长训成功后自动执行 120/1000/all_final 评估、内部/外部可视化对比、detail ROI 复核和 ROI flow 高频/folding 诊断。已新增本地客户交付包构建脚本 `scripts/build_v107_paper_delivery_package.py` 并生成当前状态版 `experiment_submission_package_2026-07-11_v107_paper_ready.zip`，zip 校验 `bad=None`、32 个条目、6,479,299 bytes；由于 v107 尚未完成，当前包中 `v107_metrics_rows=0`，后续训练和后处理完成后需重跑该脚本生成最终版。

2026-07-10 已按用户要求尽量补齐训练曲线。新增脚本 `scripts/generate_available_training_curves.py`，从本地输出、客户材料日志、远端抽取 raw summaries 和最终交付包记录中扫描真实 `train.log`，只基于真实日志生成曲线，不为缺失日志版本伪造曲线。已生成干净版带曲线交付包 `experiment_submission_package_2026-07-10_v1_v106_complete_final_with_curves_clean/` 与 `experiment_submission_package_2026-07-10_v1_v106_complete_final_with_curves_clean.zip`。曲线补充目录为 `03_training_curves/all_available_train_logs/`，包含 `TRAINING_CURVE_COVERAGE_V1_V106.csv`、`TRAINING_LOG_CURVE_SUMMARY.csv`、`all_training_epoch_points.csv`、`all_training_batch_points.csv`、`combined_epoch_epe_curves.png` 和 `per_log_curves/`。本轮共解析 28 份去重训练日志、111 个 epoch 汇总点、51,726 个 batch 点；v1-v106 中 15 个版本可生成真实训练曲线。v100、v101、v102、v104、v105、v106 均已生成曲线；v96、v97、v98 当前本地有评估/可视化成果但未找到可解析 `train.log`，覆盖表标记为 `no_train_log_found`。新版 zip 校验通过：739,612,790 bytes，1070 个条目，`bad=None`，包内 `plcyf/usmidet` 站点名命中数为 0。后续如需要 v96-v98 曲线，必须先从远端进一步找回对应 `train.log`，不能用 summary 伪造。

2026-07-10 根据用户“交付包应该完整反映 v1-v106 所有成果物”的要求，新增完整成果物交付脚本 `scripts/build_v1_v106_complete_delivery_package.py`，并生成最终包 `experiment_submission_package_2026-07-10_v1_v106_complete_final/` 与 `experiment_submission_package_2026-07-10_v1_v106_complete_final.zip`。该包不按内部站点组织，而是按实验阶段和版本组织；完整索引 `00_READ_FIRST/FULL_ARTIFACT_INDEX.csv` 覆盖 2804 条成果物记录，版本索引 `VERSION_PROGRESS_INDEX.csv` 覆盖 v1-v106，其中 50 个版本当前有本地成果物记录。包内实际收录 681 个轻量文本/指标/日志/summary 类成果物、238 张代表性可视化图片、58 个体积可控的阶段性结果包；其余 1825 张图片因全量原图约 1GB，已在索引中完整列出但不全部塞入主包。最终 zip 大小 727,684,496 bytes，1008 个 zip 条目，`bad_entry=None`，包内 `plcyf/usmidet` 站点名命中数为 0。v95-v106 已抽查均有记录，v106 现包含 `train.log` 和 `eval_summary`。后续优先交付 `experiment_submission_package_2026-07-10_v1_v106_complete_final.zip`，旧版摘要包仅作为历史备份。

2026-07-09 根据用户反馈，交付包不应按 `plcyf/usmidet` 站点分层，因为站点信息与用户无关。已更新 `scripts/build_experiment_submission_package.py`，生成新版 `experiment_submission_package_2026-07-09_user_ready/` 与 `experiment_submission_package_2026-07-09_user_ready.zip`（27,667,018 bytes，257 个条目，zip 校验 `bad_entry=None`，包内 `plcyf/usmidet` 字符串命中数为 0）。新版 `07_raw_summaries/` 改为按实验问题分类：`01_long_training/`、`02_comparison_experiments/`、`03_ablation_experiments/`、`04_late_visual_diagnostics/`；原 `06_raw_manifests/` 改为 `06_source_index/`，只保留用户无需感知站点的筛选/跳过清单。后续应优先交付 `experiment_submission_package_2026-07-09_user_ready.zip`，不要再交付旧版 `experiment_submission_package_2026-07-09.zip`。

2026-07-09 已将远端 usmidet/plcyf 回收资料进一步整理为面向用户提交的结构化实验材料包，不再直接交付两个原始大 tar.gz。新增目录 `experiment_submission_package_2026-07-09/` 和压缩包 `experiment_submission_package_2026-07-09.zip`（27,761,516 bytes，zip 校验通过，261 个条目，`bad_entry=None`）。交付包按用户关心的问题分为 `01_long_training/`、`02_comparison_experiments/`、`03_ablation_experiments/`、`04_training_curves/`、`05_visual_examples/`、`06_raw_manifests/`、`07_raw_summaries/`，分别收纳长训练指标和日志、对比实验、消融实验、训练曲线、代表可视化、远端 manifest 与轻量原始 summary/log/CSV 明细。新增脚本 `scripts/build_experiment_submission_package.py` 用于复现整理过程，脚本从现有客户材料和远端 tar.gz 中筛选轻量可追溯材料，明确排除 checkpoint、npy 和海量图片中间件。当前可直接发送给用户的是 `experiment_submission_package_2026-07-09.zip`，原始远端大包仍保留在 `remote_delivery_assets/` 作为内部归档。

2026-07-09 已按用户要求改用 GitHub Release + 本地代理 `http://127.0.0.1:7897/` 完成远端 usmidet/plcyf 资料包回收。远端已创建 Release `remote-delivery-assets-20260709`，将 `plcyf_delivery_assets.tar.gz` 拆为 2 个分卷、`usmidet_delivery_assets.tar.gz` 拆为 11 个分卷，并上传 `SHA256SUMS.txt` 与 `skipped_delivery_manifest.csv`。本地通过代理下载到 `remote_delivery_assets/gh_download_proxy_20260709/`，所有分卷 SHA256 校验通过，并合并生成 `remote_delivery_assets/plcyf_delivery_assets.tar.gz`（156,352,411 bytes，tar 可读 607 项）和 `remote_delivery_assets/usmidet_delivery_assets.tar.gz`（2,118,774,618 bytes，tar 可读 4,841 项）。旧的无代理 gh/scp 下载进程已清理，只保留原本的公司内网 SOCKS 代理 SSH 进程。

2026-07-09 已按用户要求从远端服务器补齐早期 v4-v9 长训练原始 `train.log`，并重新生成客户材料包。远端日志已拉回并归档到 `client_visualization_report_2026-07-09/logs/`，共 6 份；新增 `tables/long_training_curve_points.csv`，包含 v4-v9 共 60 条逐 epoch 曲线点；新增 `curves/long_training_curves.png`，展示 Val EPE、CrackEPE 和 Train Loss 的 10 epoch 走势。`README.md` 和 `EXPERIMENT_APPENDIX.md` 已更新为“原始日志已从服务器补齐”，不再使用“本地日志不全”的表述；新版 `client_visualization_report_2026-07-09.zip` 已覆盖生成。

2026-07-09 已补齐面向实验部分的完整支撑材料，并更新客户材料目录 `client_visualization_report_2026-07-09/`。新增 `EXPERIMENT_APPENDIX.md`、`tables/` 和 `curves/`：表格覆盖 v4-v9 长训练主线指标、UniMatch/SEA-RAFT oracle-pair 外部参考上界、最终消融实验、逐图综合评分对比、v95-v106 后期可视化消融、ROI 局部清晰度；曲线覆盖 v95-v106 主指标柱状图、关键 ROI win count，以及 v105/v106 detail-head 训练诊断。已把 v106 5 张代表性 detail panel 放入 `images/`，并新增 `utils/generate_client_experiment_appendix.py` 作为可复用汇总脚本。当前结论保持不变：实验指标、对比和消融材料已经可以支撑实验章节，但最终可视化仍不建议表述为已经很好，关键瓶颈仍是高边缘裂缝结构恢复不足。

2026-07-09 已完成 v106 `detail-strength probe` 作为最后一次短周期可视化尝试。v106 不推翻主 flow 几何路径，而是在 v105 image-space detail head 基础上新增 stronger detail-head 模型 `global_context_flow_unet_image_detail_head_strong`：提高 `max_detail_delta` 到 `0.18`，将 detail gate 初始 bias 从 v105 的 `-2.0` 放宽到 `-0.75`，新增 `detail_gate_floor=0.25` 和 `detail_patch_base=0.45`，让 detail residual/gate 有更大机会真实打开。训练日志新增 `d_gate_m/d_gate_x/d_gate_a/d_res_m/d_res_x`，用于直接观察 detail gate 均值/最大值/活跃比例和 residual 幅度。新增提交脚本 `scripts/submit_v106_detail_strength_probe_from_v105_plcyf.sh`，默认从 v105 `best_epe.pth` 初始化，使用 `W_SOURCE_IMAGE_DETAIL=0.120`、`LR=1e-6`、`EPOCHS=1`，保留主 flow 的 coordinate、anti-ripple guard、folding、displacement band 约束。远端 `/mnt/users/xj62kv/underwater-crack-correction` 已同步代码并通过 `.venv/bin/python -m py_compile` 与 `bash -n` 检查；Slurm job `42509034` 已在 `gpu` 分区、account `gpo-ifv7xx`、QOS `normal` 完成，退出码 `0:0`，用时 `00:12:38`，输出目录为 `output_crackwarp_slurm/v106_detail_strength_from_v105_1ep_plcyf`。训练末尾 Train EPE `18.154px`、Val EPE `18.363px`、CrackEPE `17.674px`；120 flow-only 指标为 crack EPE `18.956026`、global EPE `19.128481`、Dice `0.493125`、crack edge `0.536711`、global edge `0.547543`、folding `0.015274`。v106 detail 分支数值上确实比 v105 打开：日志中 `d_gate_m` 多数在 `0.03-0.05`，`d_gate_x` 可达 `0.8+`，`d_gate_a` 约 `0.1-0.29`，`d_res_m` 多数在 `0.003-0.008`，`d_res_x` 最高约 `0.15`。已完成 v106 detail-aware ROI 复核：对 v98/v99 hard ROI 图族展开 70 张合成图、导出 298 张 panel；汇总显示 `detail_image` 相对 `flow_corrected` 的 high-edge-grad 有小幅改善，`detail_vs_flow_high_edge_grad_mae_to_source_improve_mean=0.000666`、win `265/298`，edge-weighted improve `0.0000379`、win `144/298`。但人工抽看 `crack0030_08_roi03`、`crack0090_08_roi04`、`crack0052_03_roi01`、`crack0055_07_roi01`、`crack0060_07_roi02` 后判断：v106 的 `detail_residual/detail_gate` 已明显不再全黑，但 `detail_image` 相比 `flow_corrected` 仍只是轻微纹理变化，没有形成一眼可见的裂缝清晰度提升；因此 v106 不能作为最终可视化突破版，建议正式收口，不再继续无限调参。

2026-07-09 已按客户“可视化不行、指标如何、对比/消融数据、发图看看、后续还能不能优化”的沟通需求，整理阶段性 Markdown 材料目录 `client_visualization_report_2026-07-09/`。该目录包含 `README.md` 和 `images/`，文档详细汇总 v95-v105 的主指标、消融实验结论、ROI sharpness 结果、当前可视化问题、客户回复建议和 v106 后续优化边界；图片目录包含 9 张代表性图，包括 v99 ROI panel、v100/v102 flow 高频诊断图，以及 v105 detail-aware panel。该材料可直接作为客户沟通材料或后续打包基础。

2026-07-09 已完成 v105 `image-space local detail/residual head smoke` 的最小实现、远端 PyTorch smoke、Slurm 训练和 detail-aware ROI 复核。根据 v104a 结论，本轮不进入 v104 Stage B，不再把 Source-GT 高边缘结构恢复继续压到主 flow 坐标场；新增 `global_context_flow_unet_image_detail_head`，主 flow 沿用 spatial-cap 稳定路径，默认 `forward(x)` 仍只返回 flow，保持现有评估/推理兼容；显式 `return_detail=True` 时返回 `flow/detail_image/detail_residual/detail_gate`。新增 detail head 的输入使用 detach 后的 base corrected image、decoder 特征和 ROI 先验，Source-GT image detail loss 只监督 `detail_image`，避免结构恢复目标继续反向污染主 flow。新增 `w_source_image_detail` 与 `use_image_detail_output` 默认关闭参数，训练/验证循环已支持 dict 输出并通过 `extract_final_flow()` 取主 flow。远端 `/mnt/users/xj62kv/underwater-crack-correction` 已完成 `.venv/bin/python` smoke：模型输出 shape 正确，`source_image_detail=0.174745` 非零；只开启 detail loss 的隔离反传验证显示 `main_grad=0.0`、`detail_grad=1.433901`，说明 image-space detail loss 不回推主 flow。由于该远端路径当前只有到 v91 的 checkpoint，v105 submit 脚本默认从 v89 `output_crackwarp_slurm/v89_spatial_cap_flowlap1e5_dispband_from_v87_1ep/best_epe.pth` 初始化，并保留 `INIT=...` 覆盖入口。v105 Slurm job `42451293` 已在 `gpu` 分区、account `gpo-ifv7xx`、QOS `normal` 完成，退出码 `0:0`，用时 `00:12:39`，输出目录为 `output_crackwarp_slurm/v105_image_detail_head_from_v89_1ep_plcyf`。训练日志中 `s_imgdet` 持续非零，训练末尾 Train EPE `18.175px`、Val EPE `18.307px`、CrackEPE `17.724px`；120 flow-only 评估为 crack EPE `18.779821`、global EPE `18.833858`、Dice `0.494986`、crack edge `0.537668`、global edge `0.547690`、folding `0.016130`。新增 `utils/export_image_detail_roi_visuals.py` 做 detail-aware ROI 导出和指标复核，已对 v98/v99 hard ROI 图族展开 70 张合成图、导出 298 张 panel 到 `output_crackwarp_slurm/v105_image_detail_head_from_v89_1ep_plcyf/image_detail_roi_review/panels`。结果显示 detail 分支相对主 flow corrected 的局部变化非常小：edge-weighted improve `-0.00000176`、high-edge-grad improve `+0.00000353`、grad improve `+0.00000051`、image MAE improve `-0.00000166`。已人工抽看 `crack0030_08_roi03`、`crack0090_08_roi04`、`crack0052_03_roi01`、`crack0055_07_roi01`、`crack0060_07_roi02` 五个关键 panel：`detail_image` 与 `flow_corrected` 肉眼基本不可分，`detail_residual` 近乎全黑，仅个别 ROI 有极弱细线响应，`detail_gate` 为低响应紫色热图且覆盖偏弱；结论是当前 v105 smoke 证明了解耦链路可行，但 detail gate/residual 确认肉眼过弱，不是清晰度突破版。

2026-07-09 已完成 v104 Stage A `visible-only / weak-geometry probe`，并决定不进入 Stage B。v104a job `20741010` 在 plcyf `plcyf-gpu` 完成，退出码 `0:0`，用时 `00:30:49`；输出目录为 `output_crackwarp_slurm/v104_stagea_visible_only_from_v102_1ep_plcyf`。v104a 从 v102 `best_epe.pth` 初始化，使用 `W_COORD=0.05`、`W_CRACK_COORD=0.05`、`W_SOURCE_ROI_VISIBLE_STRUCTURE=0.080`，关闭 `W_CRACK_FLOW_LAPLACIAN`、`W_CRACK_FLOW_HIGHFREQ` 和 `W_DISP_BAND`，用于单独验证 visible-structure 是否能突破 high-edge-grad 平台。训练日志中 `s_visroi` 全程非零，`c_flow_lap=0`、`c_flow_hf=0`、`disp_band=0`，说明 Stage A probe 设置生效。训练末尾 Train EPE `16.568px`、Val EPE `16.824px`、CrackEPE `16.817px`。120 常规评估为 crack EPE `17.053843`、global EPE `16.950117`、Dice `0.505879`、crack edge `0.514960`、global edge `0.548678`、folding `0.004547`。由于 plcyf 两张 V100 被其它 Python 进程占用约 `31GB` 显存，首次后处理在 ROI sharpness 阶段 OOM；已更新 `scripts/run_v104_stagea_postprocess.sh`，新增 `POST_DEVICE=cpu`、`POST_MODE=compact` 和 `GPU_ID` 支持，并用 CPU compact 口径完成 v104a 后处理。25 个关键 ROI 中 v104a edge-weighted win 为 `14/25`，只达到 v95 平台但未超过预设 `>14/25`，high-edge-grad win 仍为 `2/25`；Source-GT 120 中 edge-weighted win 为 `87/120`，high-edge-grad improve 为 `-0.005105`、win 仍为 `0/120`。ROI flow 高频指标虽然明显改善：`flow_highfreq_gap_to_gt_mean_mean=0.244575`、`flow_lap_gap_to_gt_mean_mean=1.463425`、`roi_folding_rate_mean=0.005077`，但 ROI 平均位移降到 `3.972399px`，25 个 ROI 全部落在 `0-8px` 位移桶，说明弱化几何后更像低位移/低 folding 的保守解，而不是高边缘结构真正恢复。当前判断：v104a 是有效两阶段 probe，但没有达到进入 Stage B 的前置门槛；不提交 v104 Stage B、不扩严格 1000、不继续只加 `W_SOURCE_ROI_VISIBLE_STRUCTURE` 或只加轮次。记录见 `output_crackwarp_slurm/v104_stagea_visible_only_from_v102_1ep_plcyf/visual_review_v104.md`。

2026-07-08 已完成 v103 `gradient conflict probe from v102` 诊断，不启动新训练、不继续调大 `W_SOURCE_ROI_VISIBLE_STRUCTURE`，而是在 v102 checkpoint 上固定 `pred_flow`，对 7 张关键 ROI 图族共 25 个 ROI 直接比较 Source-GT visible-structure、synthetic GT 坐标监督和 anti-ripple guard 的梯度方向。新增 `utils/diagnose_v103_gradient_conflict.py` 与 `scripts/run_v103_gradient_conflict_diagnostic.sh`，远端 plcyf 已运行完成，输出目录为 `output_crackwarp_slurm/v103_gradient_conflict_probe_from_v102`，本地已拉回 `gradient_conflict.csv`、`gradient_conflict.summary.json`、`gradient_conflict_report.md` 和 `v103_roi_key_images.txt`。诊断结果显示：`visible_vs_coord_cos_mean=0.005313`、`visible_vs_coord_conflict_pixel_rate_mean=0.497746`、`visible_vs_guard_cos_mean=0.001706`、`visible_vs_guard_conflict_pixel_rate_mean=0.496637`，说明 visible-structure 与 coord/guard 在全局均值上接近正交，但 ROI 内约一半像素方向冲突。单步 probe 显示 visible 方向可轻微改善 high-edge-grad（`-0.000102`），但会轻微推高 flow highfreq（`+0.000121`）和 flow Laplacian（`+0.001709`）；guard 方向能显著降低 flow highfreq（`-0.011251`）和 flow Laplacian（`-0.207510`），但会轻微伤害 high-edge-grad（`+0.000051`）。当前判断：v103 支持 v100-v102 平台现象来自结构锐化、几何保真和 anti-ripple 之间的局部折中/冲突，不建议继续单阶段叠加小权重；记录见 `output_crackwarp_slurm/v103_gradient_conflict_probe_from_v102/visual_review_v103.md`。

2026-07-08 已完成 v102 `ROI visible-structure reconstruction + v100 anti-ripple guard` 实验、远端训练、120 gate、ROI sharpness、ROI flow 高频归因、focus panels、fixed40/ROI review sheets 导出和本地指标复核。v102 job `20718784` 在 plcyf `plcyf-gpu` 完成，退出码 `0:0`，用时 `00:25:28`；输出目录为 `output_crackwarp_slurm/v102_roi_visible_structure_flowhf_from_v100_1ep_plcyf`。v102 从 v100 `best_epe.pth` 初始化，保留 `W_CRACK_FLOW_HIGHFREQ=0.00004`、`CRACK_FLOW_HIGHFREQ_KERNEL=11`、`CRACK_FLOW_HIGHFREQ_DILATE=9` 与轻量 `W_CRACK_FLOW_LAPLACIAN=0.000015` 作为 anti-ripple guard，同时新增默认关闭参数 `w_source_roi_visible_structure`，本次使用 `W_SOURCE_ROI_VISIBLE_STRUCTURE=0.030`，用于关键 ROI 内 visible structure floor、high-edge gradient floor、正负 ROI margin ranking 和轻量 skeleton 对齐。训练日志中 `s_visroi` 全程非零，`c_flow_hf` 与 `c_flow_lap` 持续非零，说明新增结构目标和 anti-ripple guard 均生效。训练末尾 Train EPE `17.967px`、Val EPE `18.164px`、CrackEPE `17.477px`。120 常规评估为 crack EPE `18.595728`、global EPE `18.703518`、Dice `0.497332`、crack edge `0.537003`、global edge `0.547761`、folding `0.019254`；identity-aware 平均位移 `10.536818px`，不是 identity-like；combined gate 为 `needs_fixed_visual_review`。Source-GT 120 中 image/grad/lap/edge-weighted 仍为正信号，edge win `85/120=70.8%`，但 high-edge-grad improve 仍为 `-0.005455`、win `0/120`。训练后 25 个关键 ROI sharpness 显示 v102 的 edge-weighted win 为 `12/25`，未达到预设 `>14/25`，也未超过 v95 的 `14/25`；high-edge-grad win 仍为 `2/25`。ROI flow 高频诊断显示 v102 基本仍在 v100/v101 anti-ripple 平台附近：`flow_highfreq_gap_to_gt_mean_mean=0.264328`、`flow_lap_gap_to_gt_mean_mean=1.483091`、`roi_folding_rate_mean=0.028501`，其中 `crack0030_08 ROI-3` highfreq/lap/folding 为 `0.501872/2.659568/0.213867`，`crack0090_08 ROI-4` 为 `0.521542/2.634857/0.020325`。本地已拉回关键 JSON/CSV、训练日志和部分 review sheets；结果包流式拉回曾被连接重置，导致本地 review sheets 不完全，但关键指标足够支持结论。当前判断：v102 是有效工程验证，但不是视觉突破版，不建议扩严格 1000；记录见 `output_crackwarp_slurm/v102_roi_visible_structure_flowhf_from_v100_1ep_plcyf/visual_review_v102.md`。

2026-07-08 已完成 v101 `ROI directional structure + flow high-frequency guard` 组合消融、120 gate、ROI sharpness、ROI flow 高频归因、focus panels、fixed40/ROI review sheets 和人工视觉复核。v101 job `20697311` 在 plcyf `plcyf-gpu` 完成，退出码 `0:0`，用时 `00:25:36`；输出目录为 `output_crackwarp_slurm/v101_roi_directional_structure_flowhf_from_v100_1ep_plcyf`。v101 从 v100 `best_epe.pth` 初始化，保留 v100 级 `W_CRACK_FLOW_HIGHFREQ=0.00004` 与轻量 `W_CRACK_FLOW_LAPLACIAN=0.000015` 作为 anti-ripple guard，同时新增默认关闭参数 `w_source_roi_directional_structure`，本次使用 `W_SOURCE_ROI_DIRECTIONAL_STRUCTURE=0.030`，在关键 ROI 内约束梯度方向、soft skeleton 分布和局部梯度统计。训练日志中 `s_dirroi` 全程非零，常见区间约 `0.40-0.54`，`c_flow_hf` 也持续非零，说明新增结构目标和 anti-ripple guard 均生效。训练末尾 Train EPE `17.979px`、Val EPE `18.139px`、CrackEPE `17.457px`。120 常规评估为 crack EPE `18.568134`、global EPE `18.692728`、Dice `0.497477`、crack edge `0.538202`、global edge `0.548092`、folding `0.018384`；identity-aware 平均位移 `10.540px`，不是 identity-like；combined gate 为 `needs_fixed_visual_review`。Source-GT 120 中 image/grad/lap/edge-weighted 仍为正信号，edge win `84/120=70.0%`，但 high-edge-grad improve 仍为 `-0.005436`、win `0/120`。训练后 25 个关键 ROI sharpness 显示 v101 的 edge-weighted win 为 `12/25`，低于 v95 的 `14/25`，high-edge-grad win 仍为 `2/25`；ROI flow 高频诊断显示 v101 基本守住 v100 anti-ripple 下限：`flow_highfreq_gap_to_gt_mean_mean=0.264444`、`flow_lap_gap_to_gt_mean_mean=1.480847`、`roi_folding_rate_mean=0.028213`，其中 `crack0030_08 ROI-3` 较 v100 的 highfreq/lap/folding 还略有改善到 `0.505206/2.679709/0.210449`，但 `crack0090_08 ROI-4` 基本持平。人工抽看 fixed40 与 ROI review sheets 后，v101 没有明显灾难退化，也没有退回 identity，但相比 v100/v98/v99 没有稳定肉眼清晰增益，局部裂缝仍偏软，水纹/糊化/搅动感仍存在。当前判断：v101 是有效组合消融，但不是视觉突破版，不建议扩严格 1000；记录见 `output_crackwarp_slurm/v101_roi_directional_structure_flowhf_from_v100_1ep_plcyf/visual_review_v101.md`。

2026-07-08 已完成 v100 `flow high-frequency guard` anti-water-ripple probe、120 gate、ROI flow 高频归因和 focus panel 复核。v100 job `20646580` 在 plcyf `plcyf-gpu` 完成，退出码 `0:0`，用时 `00:25:01`；输出目录为 `output_crackwarp_slurm/v100_flow_highfreq_guard_from_v98_1ep_plcyf`。v100 从 v98 `best_epe.pth` 初始化，关闭 `s_rank` 与 `s_hroi`，新增默认关闭损失 `CrackFlowHighFreqLoss`，本次使用 `W_CRACK_FLOW_HIGHFREQ=0.00004`、`CRACK_FLOW_HIGHFREQ_KERNEL=11`、`CRACK_FLOW_HIGHFREQ_DILATE=9`，并保留轻量 `W_CRACK_FLOW_LAPLACIAN=0.000015`，用于单独验证 anti-ripple 下限约束。训练日志中 `c_flow_hf` 全程非零，`s_rank=0`、`s_hroi=0`，说明新增约束已生效且未混入 ROI 结构排序目标。训练末尾 Train EPE `18.006px`、Val EPE `18.188px`、CrackEPE `17.509px`。120 常规评估为 crack EPE `18.623941`、global EPE `18.722961`、Dice `0.497682`、crack edge `0.538877`、global edge `0.548001`、folding `0.018347`；identity-aware 平均位移约 `10.533px`，不是 identity-like；combined gate 为 `needs_fixed_visual_review`。Source-GT 120 中 image/grad/lap/edge-weighted 仍为正信号，但 high-edge-grad improve 仍为 `-0.005453`、win `0/120`。25 个关键 ROI flow 高频诊断显示，v100 `flow_highfreq_gap_to_gt_mean_mean=0.264054`、`flow_lap_gap_to_gt_mean_mean=1.479957`、`roi_folding_rate_mean=0.028417`，相比 v99 的 `0.266104`、`1.487278`、`0.031246` 有小幅回收，并接近或略好于 v98。重点 ROI 中，`crack0030_08 ROI-3` 从 v99 highfreq/lap/folding `0.566702/2.821779/0.282715` 回收到 v100 `0.510738/2.695459/0.223633`，三项均好于 v98；`crack0090_08 ROI-4` 也基本从 v99 `0.540913/2.702463/0.040244` 回到 v100 `0.520798/2.617834/0.022087`。当前判断：v100 是有效 anti-ripple 止损验证，但不是视觉突破版，不建议扩严格 1000；记录见 `output_crackwarp_slurm/v100_flow_highfreq_guard_from_v98_1ep_plcyf/visual_review_v100.md`。

2026-07-07 已完成 v99 后续 ROI flow 高频归因诊断。已增强 `utils/diagnose_roi_flow_highfreq.py`：新增多尺度高频 kernel、ROI 平均位移分桶、相对基线模型的逐 ROI delta，以及 `--report_md` 自动生成 Markdown 排名报告；同步更新 `scripts/run_v99_roi_flow_highfreq_after_train.sh`，并新增 `scripts/run_v99_flow_attr_plcyf.sh` 作为 plcyf 复现实用入口。远端 plcyf 已重跑 v99 ROI flow 高频诊断并拉回本地，报告见 `output_crackwarp_slurm/v99_roi_rank_structure_from_v93_1ep_plcyf/roi_flow_highfreq/roi_flow_highfreq_attribution.md`。关键结论：v99 的 water-ripple / 局部高频风险高度集中在 `crack0030_08.png` ROI-3 `480,0,512,64` 与 `crack0090_08.png` ROI-4 `422,430,512,512`；其中 `crack0030_08` ROI-3 的 v99 `flow_lap_gap_to_gt_mean=2.821779`、`flow_highfreq_gap_to_gt_mean=0.566702`、`roi_folding_rate=0.282715`，相对 v98 的 highfreq gap / Laplacian gap / folding 分别退化 `+0.046727`、`+0.091597`、`+0.028809`。25 个 ROI 中 23 个落在 `10-12px` 位移桶，说明当前风险主要集中在 v93-v99 约 10px 平均位移平台上的局部高频残差和 folding，而不是大位移极端样本。v99 视觉复核记录已追加该归因结论。

2026-07-07 已完成 v99 `ROI rank structure` 实验、120 gate、fixed40、ROI、ROI sharpness、ROI flow 高频诊断、review sheets 和本地视觉复核。v99 job `20633793` 在 plcyf `plcyf-gpu`、account `mfk-gsr2`、QOS `normal` 完成，退出码 `0:0`，用时 `00:28:36`；输出目录为 `output_crackwarp_slurm/v99_roi_rank_structure_from_v93_1ep_plcyf`。v99 从 v93 `best_epe.pth` 初始化，新增默认关闭参数 `w_source_roi_rank_structure` 与 `SourceGTVisualConsistencyLoss.roi_rank_structure(...)`，继续使用关键 ROI 图族 `crack0022/crack0030/crack0052/crack0055/crack0060/crack0063/crack0090` 的 hard sampler，`HARD_SAMPLE_WEIGHT=4.0`；同时新增 `utils/diagnose_roi_flow_highfreq.py`，将 anti-water-ripple 单独量化为 ROI flow Laplacian gap、flow highfreq gap 和 ROI folding。训练日志中 `s_rank` 全程非零，约 `0.28-0.43`，说明新增排序/对比式结构约束已生效。训练末尾 Train EPE `18.145px`、Val EPE `18.348px`、CrackEPE `17.605px`。120 常规评估为 crack EPE `18.704172`、global EPE `18.967527`、Dice `0.496866`、crack edge `0.527394`、global edge `0.546461`、folding `0.020665`；identity-aware 平均位移 `10.520px`，不是 identity-like；combined gate 为 `needs_fixed_visual_review`。Source-GT 120 中 image/grad/lap/edge-weighted 仍为正信号，edge win `87/120=72.5%`，但 high-edge-grad improve 仍为 `-0.005490`、win `0/120`。训练后 25 个关键 ROI sharpness 显示 v99 的 edge-weighted win 为 `13/25`，比 v98 的 `11/25` 有所回收，但仍未超过 v95 的 `14/25`；high-edge-grad win 仍为 `2/25`。ROI flow 高频诊断显示 v99 `flow_lap_gap_to_gt_mean_mean=1.487278`、`flow_highfreq_gap_to_gt_mean_mean=0.266104`、`roi_folding_rate_mean=0.031246`，均略差于 v98。人工抽看 fixed40 与 ROI review sheets 后，v99 没有明显灾难退化，也没有退回 identity，但相比 v93/v95/v96/v97/v98 没有稳定肉眼清晰增益，局部裂缝仍偏软，水纹/糊化/搅动感仍存在。当前判断：v99 是有效工程验证，但不是视觉突破版，不建议扩严格 1000；记录见 `output_crackwarp_slurm/v99_roi_rank_structure_from_v93_1ep_plcyf/visual_review_v99.md`。

2026-07-07 已完成 v98 `hard ROI two-stage` 实验、120 gate、fixed40、ROI、ROI sharpness、review sheets 和本地视觉复核。v98 job `20604820` 在 plcyf `plcyf-gpu`、account `mfk-gsr2`、QOS `normal` 完成，退出码 `0:0`，用时 `00:25:16`；输出目录为 `output_crackwarp_slurm/v98_hardroi_twostage_from_v93_1ep_plcyf`。v98 从 v93 `best_epe.pth` 初始化，新增默认关闭参数 `w_source_hard_roi_structure`，并用关键 ROI 图族 `crack0022/crack0030/crack0052/crack0055/crack0060/crack0063/crack0090` 启用 hard sampler，`HARD_SAMPLE_WEIGHT=4.0`；同时保留轻量 `W_CRACK_FLOW_LAPLACIAN=0.00002`、`CRACK_FLOW_LAPLACIAN_DILATE=9` 和 displacement band `10-24px`，用于把“裂缝局部结构恢复”和 “anti-water-ripple” 拆成两段约束。训练日志中 `s_hroi` 与 `c_flow_lap` 均非零，说明新增约束已生效。训练末尾 Train EPE `18.080px`、Val EPE `18.245px`、CrackEPE `17.574px`。120 常规评估为 crack EPE `18.673807`、global EPE `18.808023`、Dice `0.497006`、crack edge `0.537858`、global edge `0.548059`、folding `0.017335`；identity-aware 平均位移 `10.534px`，不是 identity-like；combined gate 为 `needs_fixed_visual_review`。Source-GT 120 中 image/grad/lap/edge-weighted 仍为正信号，edge win `83/120=69.2%`，但 high-edge-grad improve 仍为 `-0.005431`、win `0/120`。训练后 25 个关键 ROI sharpness 显示 v98 的 edge-weighted win 为 `11/25`，低于 v95 的 `14/25` 以及 v93/v96/v97 的 `13/25`；high-edge-grad win 仍为 `2/25`。人工抽看 fixed40 与 ROI review sheets 后，v98 没有明显灾难退化，也没有退回 identity，但相比 v93/v95/v96/v97 没有稳定肉眼清晰增益，局部裂缝仍偏软，水纹/糊化/搅动感仍存在。当前判断：v98 是有效工程验证，但不是视觉突破版，不建议扩严格 1000；记录见 `output_crackwarp_slurm/v98_hardroi_twostage_from_v93_1ep_plcyf/visual_review_v98.md`。

2026-07-07 已完成 v97 `high-edge sharpness guard` 实验、120 gate、fixed40、ROI、ROI sharpness、review sheets 和本地视觉复核。v97 job `20600423` 在 plcyf `plcyf-gpu`、account `mfk-gsr2`、QOS `normal` 完成，退出码 `0:0`，用时 `00:24:42`；输出目录为 `output_crackwarp_slurm/v97_highedge_sharpness_guard_from_v93_1ep_plcyf`。v97 从 v93 `best_epe.pth` 初始化，新增默认关闭参数 `w_source_high_edge_sharpness`，在 Source GT 高边缘裂缝 ROI 内只惩罚预测校正图梯度幅值低于 Source GT 局部下界的区域，用于验证“局部锐度下界”是否能补上 v93/v95/v96 的高边缘裂缝偏软问题；训练日志中 `s_he_sharp` 非零，说明新增约束已生效。训练末尾 Train EPE `18.086px`、Val EPE `18.271px`、CrackEPE `17.560px`。120 常规评估为 crack EPE `18.669830`、global EPE `18.863163`、Dice `0.498568`、crack edge `0.541986`、global edge `0.548365`、folding `0.016818`；identity-aware 平均位移 `10.582px`，不是 identity-like；combined gate 为 `needs_fixed_visual_review`。Source-GT 120 中 image/grad/lap/edge-weighted 仍为正信号，edge win `84/120=70.0%`，但 high-edge-grad improve 仍为 `-0.005411`、win `0/120`。训练后 25 个关键 ROI sharpness 显示 v97 的 edge-weighted win 为 `13/25`，没有超过 v95 的 `14/25`；high-edge-grad win 仍为 `2/25`，与 v93/v95/v96 同档。人工抽看 fixed40 与 ROI review sheets 后，v97 没有明显灾难退化，也没有退回 identity，但相比 v93/v95/v96 没有稳定肉眼清晰增益，局部裂缝仍偏软，水纹/糊化/搅动感仍存在。当前判断：v97 是有效工程验证，但不是视觉突破版，不建议扩严格 1000；记录见 `output_crackwarp_slurm/v97_highedge_sharpness_guard_from_v93_1ep_plcyf/visual_review_v97.md`。

2026-07-07 已在远端 plcyf 直接提交并完成 v96 `patch mask/skeleton artifact guard` 实验、120 gate、fixed40、ROI、ROI sharpness 和 review sheets。v96 job `20545434` 在 `plcyf-gpu`、account `mfk-gsr2`、QOS `normal` 完成，退出码 `0:0`，用时 `00:57:41`；输出目录为 `output_crackwarp_slurm/v96_patch_maskskel_artifactguard_from_v93_1ep_plcyf`。v96 从 v93 `best_epe.pth` 初始化，关闭 v95 的 high-edge-grad align，改用 `W_SOURCE_MASK_SKELETON=0.04` 与 `W_SOURCE_PATCH_DETAIL=0.04` 做局部 Source-GT 对齐，并保留轻量 flow Laplacian 与 displacement band 作为水纹/identity 防护；训练中 `s_mask_skel`、`s_patch`、`c_flow_lap` 均非零。训练末尾 Val EPE `18.310px`、CrackEPE `17.579px`。120 常规评估为 crack EPE `18.659607`、global EPE `18.915247`、Dice `0.498492`、crack edge `0.542479`、global edge `0.548666`、folding `0.016172`；identity-aware 平均位移 `10.613px`，不是 identity-like；combined gate 为 `needs_fixed_visual_review`。Source-GT 120 中 image/grad/lap/edge-weighted 均为正信号，edge win `84/120=70.0%`，但 high-edge-grad improve 仍为 `-0.005394`、win `0/120`。训练后 25 个关键 ROI sharpness 显示 v96 的 edge-weighted win 为 `13/25`，不如 v95 的 `14/25`；high-edge-grad win 仍为 `2/25`，均值虽略好于 v93/v95 但仍明显差于 input。人工抽看 ROI sheets 和 fixed40 后，v96 没有明显灾难退化，也没有退回 identity，但局部裂缝仍偏软，水纹/糊化/搅动感仍存在，未形成稳定肉眼清晰增益。当前判断：v96 是有效工程验证，但不是视觉突破版，不建议扩严格 1000；记录见 `output_crackwarp_slurm/v96_patch_maskskel_artifactguard_from_v93_1ep_plcyf/visual_review_v96.md`。

2026-07-06 已完成 v95 high-edge gradient alignment 实验、120 gate、fixed40/ROI、review sheets 和训练后 ROI sharpness 对比。v95 从 v93 `best_epe.pth` 初始化，新增轻量 `W_SOURCE_HIGH_EDGE_GRAD_ALIGN=0.02`，目标是验证高边缘梯度方向/幅值对齐能否补上 ROI 裂缝偏软和水纹问题。v95 常规 120 指标为 crack EPE `18.5378px`、global EPE `18.8124px`、Dice `0.4990`、crack edge `0.5409`、folding `0.0165`；identity-aware 平均位移 `10.605px`，不是 identity-like；combined gate 为 `needs_fixed_visual_review`。Source-GT 120 中 image/grad/lap/edge-weighted 均有小幅正信号，但 high-edge-grad improve 仍为 `-0.005396`、win `0/120`。训练后 25 个关键 ROI sharpness 对比显示，v95 的 edge-weighted win 为 `14/25`，略高于 v93 的 `13/25`，但 image_mae 仍差于 input，high-edge-grad improve 为 `-0.005913` 且只赢 `2/25`，没有解决核心高边缘结构问题。当前判断：v95 是有效止损实验，不退回 identity 但也不是视觉突破版，不建议扩严格 1000；记录见 `output_crackwarp_slurm/v95_spatial_cap_highedgegrad_from_v93_1ep_plcyf/visual_review_v95.md`。已准备下一轮 v96 脚本：`scripts/submit_v96_patch_maskskel_artifactguard_from_v93_plcyf.sh` 从 v93 起步，关闭 high-edge-grad align，改用显式 `mask/skeleton + patch_detail` 局部 Source-GT 对齐，并保留轻量 flow Laplacian 与 displacement band 作为水纹/identity 防护；配套 gate、fixed40、ROI、ROI sharpness 和 review sheet 脚本均已新增。

2026-07-06 已完成 v95 前置 ROI 局部清晰度诊断，用于量化 v89/v90/v92/v93/v94 在同一批关键 ROI 上的“肉眼是否更清楚”。新增 `utils/diagnose_roi_sharpness.py` 与 `scripts/run_v95_roi_sharpness_diagnostic.sh`，诊断脚本沿用 `export_crack_roi_visuals.py` 的输入裂缝 mask 和 ROI 提取规则，在 7 张关键图、25 个 ROI 上对 `input`、`gt_corrected`、`source_gt`、v89、v90、v92、v93、v94 计算局部清晰度与 Source-GT 贴合指标。远端 plcyf 已完成运行，结果已拉回本地 `output_crackwarp_slurm/roi_sharpness_v89_v90_v92_v93_v94/`。关键结论：v93 在 `edge_weighted_mae_to_source` 和 `grad_mae_to_source` 的 ROI winner 数最多，分别为 `8/25` 和 `9/25`；v90 在 `grad_gap_to_source` 和 `lap_gap_to_source` 上最多，分别为 `13/25` 和 `12/25`；v94 只在 `crack_contrast` 上最高，winner 为 `13/25`，但这更像暗线对比增强，不代表裂缝结构真正清楚。所有模型的 `image_mae_to_source` / `edge_weighted_mae_to_source` 均值都比 input 更差，所有模型的 `high_edge_grad_mae_to_source` 也都比 input 更差，说明当前路线最大短板仍是 Source GT 高边缘梯度结构没有恢复。当前判断：v93 是这组里相对最稳的局部版本，v94 不应继续加轮次；v95 若继续，应围绕“高边缘梯度结构贴合 + 水纹抑制”设计局部监督，而不是继续追求更高暗线对比或更大局部 residual。诊断记录见 `output_crackwarp_slurm/roi_sharpness_v89_v90_v92_v93_v94/roi_sharpness_diagnostic.md`。

2026-07-05 已完成 v94 ROI-only local refinement 实验、plcyf 训练、120 gate、fixed40、ROI、review sheet 拉回和人工视觉复核。v94 是在 v93 patch-detail 权重命中诊断后转向显式局部 refinement 的尝试：新增模型名 `global_context_flow_unet_roi_local_refine`，从 v92 `best_epe.pth` 初始化，主干沿用 v89/v92 稳定 spatial-cap 路线，新增局部残差分支只在暗裂缝核心、高边缘和 patch 上下文共同支持的 ROI 内小幅生效，`max_patch_delta=0.012`，并让 patch mask head 从偏保守状态开始。远端 smoke 已通过：CUDA 可用，v92 checkpoint 宽松加载 `missing=30`、`unexpected=0`，输出形状 `(1, 2, 512, 512)`。v94 job `20526133` 在 `plcyf-gpu`、account `mfk-gsr2`、QOS `normal` 完成，退出码 `0:0`，用时 `00:45:47`；训练末尾最佳 Val EPE `18.409px`、最佳 Crack EPE `17.726px`。120 常规评估为 crack EPE `18.745548`、global EPE `19.009083`、Dice `0.497003`、crack edge `0.540670`、global edge `0.548132`、folding `0.016637`；identity-aware 平均位移 `10.543px`，不是 identity-like；combined gate 为 `needs_fixed_visual_review`，Source-GT image improve `0.000621`、grad improve `0.002844`、lap improve `0.017984`、edge-weighted improve `0.000623`、edge win `83/120=69.2%`，但 high-edge-grad improve 仍为 `-0.005420`。fixed40 已导出 `fixed_visual_v89_v90_v92_v94`，ROI 已导出 7 个关键样本共 25 个 panel，review sheets 已拉回本地。人工复核结论：v94 没有明显灾难性退化，也没有退回 identity-like；但相比 v92/v93 没有稳定肉眼增益，`crack0060_07_roi02`、`crack0090_08_roi01` 等微弱正样本仍偏软，`crack0030_08_roi01`、`crack0052_03_roi01`、`crack0055_07_roi01` 等负样本仍有糊化/水纹/搅动感。当前判断：v94 可作为 ROI-only local refine 方向的有效工程验证，但不是视觉突破版，不建议扩严格 1000；本地记录见 `output_crackwarp_slurm/v94_roi_local_refine_from_v92_1ep_plcyf/visual_review_v94.md`。

2026-07-05 已完成 v93 Source-GT 局部 patch 细节约束实验、plcyf 训练、120 gate、fixed40、ROI、review sheet 人工视觉复核、ROI 三类分类和 patch-detail 权重命中诊断。v93 从 v92 `best_epe.pth` 初始化，继续沿 v89/v92 稳定路线，不从 v91 继续；模型为 `global_context_flow_unet_spatial_cap`，保留 flow Laplacian、位移带、几何稳定、target-risk、Source-GT photo/edge/high-edge 与轻量 mask/skeleton 设置，并新增默认关闭参数 `w_source_patch_detail`。`SourceGTVisualConsistencyLoss.patch_detail(...)` 在 Source GT 高梯度裂缝 ROI 内比较预测校正图与 Source GT 的局部去均值 patch 细节，用于更直接约束裂缝局部暗线和边缘纹理，而不是继续简单加大 mask/skeleton 权重。远端 smoke 已验证 `source_patch_detail` 非零且可反传；v93 job `20523328` 在 `plcyf-gpu`、account `mfk-gsr2`、QOS `normal` 完成，退出码 `0:0`，用时 `00:22:12`，训练末尾最佳 Val EPE `18.283px`、最佳 Crack EPE `17.590px`。120 常规评估为 crack EPE `18.658150`、global EPE `18.906584`、Dice `0.498480`、crack edge `0.541400`、global edge `0.548493`、folding `0.016520`；identity-aware 平均位移 `10.593px`，不是 identity-like；combined gate 为 `needs_fixed_visual_review`，Source-GT image improve `0.000658`、grad improve `0.002854`、lap improve `0.017977`、edge-weighted improve `0.000664`、edge win `84/120=70.0%`，但 high-edge-grad improve 仍为 `-0.005399`。fixed40 已导出 `fixed_visual_v89_v90_v92_v93`，ROI 已导出 `roi_review/panels`，review sheets 已拉回本地 `output_crackwarp_slurm/v93_spatial_cap_patchdetail_from_v92_1ep_plcyf/review_sheets/`。人工复核结论：v93 相比 v92/v89 基本保持稳定，没有看到新的明显大伪影，也没有退回 identity-like；但 fixed40 和 ROI 中“裂缝局部肉眼更清楚”的收益仍不稳定。ROI 三类分类显示：轻微改善候选只有 `crack0060_07_roi02` 和 `crack0090_08_roi01`，且都带有“改善弱/仍有水纹”的限制；多数 ROI 属于基本没变化或判断力弱；`crack0022_03_roi01`、`crack0030_08_roi01`、`crack0052_03_roi01/03`、`crack0055_07_roi01/04`、`crack0060_07_roi01`、`crack0063_07_roi01`、`crack0090_08_roi02` 仍有变糊、水纹或伪影风险。patch-detail 权重诊断已拉回本地 `output_crackwarp_slurm/v93_spatial_cap_patchdetail_from_v92_1ep_plcyf/patch_detail_diag/`：`crack0060_07_roi02`、`crack0090_08_roi01` 等微弱正样本和 `crack0030_08_roi01`、`crack0052_03_roi01`、`crack0055_07_roi01`、`crack0090_08_roi02` 等负样本均显示权重大体能命中真实裂缝或疑似裂缝边缘，但正负样本的 `patch_weight_mean` 没有明显分离，且命中后仍可能糊化或产生水纹。当前判断：v93 可作为 patch-detail 方向的有效诊断实验，但仍不能作为视觉突破版，不建议直接扩严格 1000；下一步应转向显式局部 refinement 或局部清晰度约束，不建议继续单纯调大 `W_SOURCE_PATCH_DETAIL`。本地分类和诊断记录见 `output_crackwarp_slurm/v93_spatial_cap_patchdetail_from_v92_1ep_plcyf/visual_review_v93.md`。

2026-07-05 已完成基于 v89 稳定路线的 v92 轻量 mask/skeleton 结构增强实验，不从 v91 继续。v92 在 `plcyf` GPU 集群运行，输出目录为 `/net/8k3/e0fs01/users/xj62kv/underwater-crack-correction/output_crackwarp_slurm/v92_spatial_cap_flowlap1e5_dispband_maskskel_from_v89_1ep_plcyf`，Slurm job `20514060` 使用 `plcyf-gpu`、account `mfk-gsr2`、QOS `normal`，已 `COMPLETED`，退出码 `0:0`，总用时 `00:25:14`。模型仍为 `global_context_flow_unet_spatial_cap`，从 v89 `best_epe.pth` 初始化，保留 v89 的 flow Laplacian、位移带、几何稳定和 target-risk 设置，仅新增较轻的 `W_SOURCE_MASK_SKELETON=0.02`；训练末尾 Train EPE `18.135px`、Val EPE `18.318px`、CrackEPE `17.645px`，已产出 `best_epe.pth`、`best_crack_epe.pth`、`best_loss.pth` 和 `final.pth`。120 gate 已完成：crack EPE `18.722998`、global EPE `18.925909`、Dice `0.497181`、crack edge `0.539950`、global edge `0.548153`、folding `0.016101`；identity-aware 平均位移 `10.665px`、epe gap `2.178px`，不是 identity-like；combined gate 为 `needs_fixed_visual_review`。fixed40 已在远端导出为 v89/v90/v92 三列主线对比，目录 `fixed_visual_v89_v90_v92`，样本数 `40`；ROI 已导出 7 个关键样本共 25 个 panel，目录 `roi_review/panels`。由于远端 `scripts/` 无写权限且原 `make_v92_review_sheets.py` 只识别旧目录 `fixed_visual_v87_v88_v89_v90_v92`，已在本地脚本补充 fixed40 目录候选兼容，并在远端用临时 Python 从已生成图片补齐 review sheets；远端最终 `review_sheets` 中 fixed40 总览 `4` 页、ROI 总览 `4` 页。已完成 `review_sheets/fixed40_sheet_*.jpg` 与 `roi_sheet_*.jpg` 人工视觉复核，并新增本地记录 `output_crackwarp_slurm/v92_spatial_cap_flowlap1e5_dispband_maskskel_from_v89_1ep_plcyf/visual_review_v92.md`。当前结论：v92 工程链路和核心评估已完整跑通，指标略优于 v89/v90 的同量级区间且没有退回 identity-like；但 fixed40 和 ROI 复核没有看到稳定、肉眼明确的裂缝结构增强，只能作为“指标小幅改善但视觉收益不稳定”的候选，不建议直接扩严格 1000。

2026-07-05 已将本次 v91 训练迁移到用户指定的 `plcyf` GPU 集群并完成训练、120 gate、fixed40、ROI 面板和人工视觉复核。目标项目目录为 `/net/8k3/e0fs01/users/xj62kv/underwater-crack-correction`，数据集、`under-crack-images`、v91 脚本和 v90 checkpoint 均已同步到位；`plcyf-gpu` 分区节点 `plcyf-com-prod-gpu001` 可见 2 张 `Tesla V100-PCIE-32GB`。由于 plcyf 上直接从 PyTorch 官方 cu126 源安装 torch 多次卡住，已保留半成品 `.venv_py39_partial_*` / `.venv_py311_partial_*`，并改为迁移 usmidet 上已验证可用的 `.venv`；迁移后 plcyf `.venv` 体积约 `6.8G`，`torch 2.12.0+cu126`、`CUDA=True`、`device_count=2` 验证通过。v91 plcyf job `20501340` 使用 `plcyf-gpu`、account `mfk-gsr2`、QOS `normal`、输出目录 `output_crackwarp_slurm/v91_spatial_patch_refine_from_v90_1ep_plcyf`，从 v90 `best_epe.pth` 初始化，模型 `global_context_flow_unet_spatial_patch_refine`，已 `COMPLETED`，退出码 `0:0`，`.err` 为 `0` 字节。训练 1 epoch 用时约 `0.56h`，末尾 Val EPE `19.200px`、CrackEPE `18.455px`，已产出 `best_epe.pth`、`best_crack_epe.pth`、`best_loss.pth` 和 `final.pth`。120 常规评估为 crack EPE `19.648266`、global EPE `19.883505`、Dice `0.491443`、crack edge `0.538918`、global edge `0.545821`、folding `0.026540`；相比 v90 的 crack EPE `19.537056`、global EPE `19.711939`、Dice `0.491951`、folding `0.024346` 略退，相比 v89 的 crack EPE `19.094536`、global EPE `19.242613`、Dice `0.494067`、folding `0.018345` 也偏弱。v91 identity-aware gate 显示平均位移 `12.534px`，不是 identity-like；Source-GT 指标中 image improve `0.000507`、grad improve `0.002770`、lap improve `0.017976`、edge-weighted improve `0.000504`、edge win `83/120=69.2%`，但 high-edge-grad improve `-0.005555`，combined gate 为 `needs_fixed_visual_review`。已导出并拉回 v89/v90/v91 fixed40 横向图 `output_crackwarp_slurm/v91_spatial_patch_refine_from_v90_1ep_plcyf/fixed_visual_v89_v90_v91/`，文件数 `43`；已导出并拉回关键 ROI 面板 `output_crackwarp_slurm/v91_spatial_patch_refine_from_v90_1ep_plcyf/roi_review/panels/`，7 个样本共 25 张 panel、目录文件数 `32`。人工抽看 `crack0030_08`、`crack0005_05`、`crack0039_04`、`crack0054_05`、`crack0063_07`、`crack0060_07` 的 fixed40，以及 `crack0030_08_roi01`、`crack0060_07_roi01`、`crack0063_07_roi01` 的 ROI 后，结论是 v91 与 v90 基本同型，没有看到稳定新增的裂缝局部结构恢复，局部仍有波纹、糊化或旋涡状纹理搅动感；因此 v91 不建议作为主线继续扩展，也不建议基于 v91 扩严格 1000。已新增本地复核记录 `output_crackwarp_slurm/v91_spatial_patch_refine_from_v90_1ep_plcyf/visual_review_v91_plcyf.md`。原 `gpuHz/normal` job `41796624` 后续也已完成，状态 `COMPLETED`、退出码 `0:0`、用时 `00:12:36`，输出目录 `output_crackwarp_slurm/v91_spatial_patch_refine_from_v90_1ep`，训练末尾 Val EPE `19.200px`、CrackEPE `18.448px`；它与 plcyf v91 属于重复短训记录，不再需要取消。此前 AWS 公有云 EC2 GPU 初查仍保留结论：`C:\MyFiles\aws.txt` 中凭据当前返回 `InvalidClientTokenId/AuthFailure`，AWS 迁移技术上可行但当前不可直接执行。

2026-07-02 已完成 v84 显式 Source-GT 裂缝结构监督首版实现、远端 smoke、1 epoch 短训、120 gate、combined gate 和 8 张默认代表可视化抽查。`SourceGTVisualConsistencyLoss` 新增 `structure(...)` 分支，先从预测校正图和 Source GT 中提取 soft crack map：局部暗线响应用于强调裂缝主体，灰度梯度响应用于强调裂缝边界、细分叉和强结构，两者相乘后形成结构响应图；loss 只在 Source GT 高结构响应区域做 Charbonnier 对齐，并可继续叠加 `source_valid_mask` 与裂缝 mask。新增默认关闭参数 `w_source_structure`，已贯穿 `config_crack.py`、`train_v2.py`、`run_train_slurm.py`、`slurm_train_crackwarp.sbatch` 和 `scripts/smoke_source_gt_loss.py`，训练日志新增 `s_struct`。本地 `.venv\Scripts\python.exe -m py_compile loss_crack.py config_crack.py train_v2.py run_train_slurm.py scripts\smoke_source_gt_loss.py` 通过；远端 smoke 输出 `source_structure=0.095599` 且反传成功。首次 v84 job `41255739` 因 `sbatch --wrap` 未申请 GPU，日志显示 `Device: cpu`，已取消；r2 job `41256204` 因 checkpoint 路径误写为 `best_e.pth`，排队中取消；正式 r3 job `41256455` 使用 `global_context_flow_unet_spatial_cap` 从 v83 `best_epe.pth` 初始化，`LR=4e-6`、`w_source_photo=0.02`、`w_source_edge=0.02`、`w_source_high_edge=0.08`、`w_source_structure=0.08`、保留 v83 的几何与 target-risk 设置，在 `gpuHz/shortjobs` 完成，退出码 `0:0`。训练中 `s_struct` 稳定非零，约 `0.003-0.028`。120 常规评估为 crack EPE `28.640841`、global EPE `27.817930`、Dice `0.422625`、crack edge `0.522175`、global edge `0.530376`、folding `0.057045`，相对 v83 的 EPE/Dice/edge 退化，folding 略差。identity-aware gate 显示平均位移 `20.610px`，不是 identity-like；combined gate 仍为 `reject_source_gt_regress`：image improve `-0.000921`、grad improve `0.002216`、lap improve `0.017438`、edge win `44.2%`。默认 8 张横向图已拉回本地 `output_crackwarp_slurm/v84_source_structure_from_v83_1ep_r3/fixed_visual_v41_v66_v83_v84/`；抽看 `crack0063_07`、`crack0003_08`、`crack0088_04` 显示 v84 与 v83 基本同型，没有肉眼明确裂缝形态修复增强，局部仍有水纹/边界形变。当前结论：v84 首版证明 source structure loss 工程链路可用，但不能带来视觉突破，不扩严格 1000。

2026-07-02 已完成 v84 soft crack structure map 诊断和 v85 高置信结构核心监督首轮验证。新增 `utils/diagnose_source_structure_map.py`，复刻 v84 的 dark/edge/structure 响应并输出 input、GT corrected、Source GT 的响应面板与统计；默认 8 张结果显示 Source GT edge 响应全图非零，`source_gt_structure_nonzero_ratio_mean=0.391843`，视觉上裂缝主干会亮，但背景颗粒、水泥纹理、阴影斑块也大面积进入响应，说明 v84 的 soft structure target 明显不够干净，不应继续简单提高 `W_SOURCE_STRUCTURE`。随后新增默认关闭参数 `w_source_structure_core`，`SourceGTVisualConsistencyLoss.structure_core(...)` 只选 Source GT 结构响应高分位核心并用响应平方降权背景，已贯穿 `loss_crack.py`、`config_crack.py`、`train_v2.py`、`run_train_slurm.py`、`slurm_train_crackwarp.sbatch` 和 `scripts/smoke_source_gt_loss.py`；本地 py_compile 通过，远端 smoke job `41266246` 完成，输出 `source_structure_core=0.3265` 且可反传。v85 job `41267585` 使用 `global_context_flow_unet_spatial_cap` 从 v83 初始化，`W_SOURCE_STRUCTURE=0.0`、`W_SOURCE_STRUCTURE_CORE=0.08`，保留 v83 的 Source-GT、几何与 target-risk 设置，在 `gpuHz/shortjobs` 完成，退出码 `0:0`；训练日志中 `s_struct=0.00000`、`s_struct_core` 稳定非零。120 常规指标为 crack EPE `28.422064`、global EPE `27.537127`、Dice `0.424290`、crack edge `0.522582`、global edge `0.530492`、folding `0.057962`，相比 v84 略有回收但仍差于 v83。identity-aware 显示平均位移 `20.443px`，不是 identity-like；combined gate 仍为 `reject_source_gt_regress`：image improve `-0.000842`、edge-weighted improve `-0.000897`、edge win `56/120=46.7%`、high-edge-grad improve `-0.005996`。fixed40 已在远端生成 `output_crackwarp_slurm/v85_source_structure_core_from_v83_1ep/fixed_visual_v41_v66_v83_v85/`，文件数 `43`；本地抽看 `crack0063_07`、`crack0030_08`、`crack0061_07` 显示 v85 与 v83 基本同型，没有肉眼明确裂缝形态修复增强，局部仍有水纹/边界形变。当前结论：高置信 structure core 比 v84 全量 soft structure 稍稳，但仍不能突破 Source-GT 视觉瓶颈；不扩严格 1000，也不建议继续沿 source structure 小权重路线微调。

2026-07-02 已完成 v86 显式 Source-GT 裂缝 mask/skeleton 监督首版实现、远端 smoke、1 epoch 短训、120 gate、combined gate 和 fixed40 抽查。新增默认关闭参数 `w_source_mask_skeleton`，`SourceGTVisualConsistencyLoss.mask_skeleton(...)` 从预测校正图和 Source GT 中分别提取高置信裂缝 soft mask，并用软形态学近似 skeleton，对齐裂缝主体与细线骨架；该分支已贯穿 `loss_crack.py`、`config_crack.py`、`train_v2.py`、`run_train_slurm.py`、`slurm_train_crackwarp.sbatch` 和 `scripts/smoke_source_gt_loss.py`，训练日志新增 `s_mask_skel`。远端 smoke job `41286017` 完成，输出 `source_mask_skeleton=0.286945` 且可反传。v86 job `41286202` 使用 `global_context_flow_unet_spatial_cap` 从 v83 初始化，`W_SOURCE_STRUCTURE=0.0`、`W_SOURCE_STRUCTURE_CORE=0.0`、`W_SOURCE_MASK_SKELETON=0.08`，保留 v83 的 Source-GT、几何与 target-risk 设置，在 `gpuHz/shortjobs` 完成，退出码 `0:0`；训练日志中 `s_mask_skel` 稳定非零。120 常规指标为 crack EPE `28.444668`、global EPE `27.544477`、Dice `0.425507`、crack edge `0.523015`、global edge `0.531103`、folding `0.054652`；相对 v85，Dice/edge/folding 略有改善，但 EPE 基本没有改善，且整体仍不优于 v83。identity-aware 显示平均位移 `20.458px`，不是 identity-like；combined gate 仍为 `reject_source_gt_regress`：image improve `-0.000822`、edge-weighted improve `-0.000877`、edge win `56/120=46.7%`、high-edge-grad improve `-0.006069`。fixed40 已在远端生成 `output_crackwarp_slurm/v86_source_mask_skeleton_from_v83_1ep/fixed_visual_v41_v66_v83_v86/`，文件数 `43`；本地抽看 `crack0063_07`、`crack0030_08`、`crack0061_07` 显示 v86 与 v83 基本同型，没有肉眼明确裂缝形态修复增强。当前结论：显式 mask/skeleton 比 v84/v85 的结构响应路线稍干净，但 1 epoch 仍没有足够视觉突破；若要回应“短训不能代表最终效果”，更合理的是选当前最稳的 v83/v86 同系方案跑 80 epoch 长训，而不是继续堆 Source-GT 小 loss。

2026-07-02 已完成 v87 `global_context_flow_unet_spatial_cap` 从 v83 初始化的 80 epoch 长训、120 常规评估、combined gate 和 fixed40 抽查。v87 job `41323340` 输出目录为 `output_crackwarp_slurm/v87_spatial_cap_long80_from_v83`，在 `gpu/normal` 完成，退出码 `0:0`，总用时 `10.90` 小时；训练末尾为 Train Loss `0.08633`、Train EPE `23.768px`、Val Loss `0.08078`、Val EPE `24.028px`、CrackEPE `22.817px`，最佳 Val EPE `24.015px`、最佳 Crack EPE `22.817px`。自动 120 常规评估显示长训相对 v83/v86 明显改善坐标与 Dice：crack EPE `24.747450`、global EPE `24.789375`、Dice `0.459496`、crack edge `0.527016`、global edge `0.534987`、folding `0.059502`。identity-aware gate 显示平均位移 `20.643px`，不是 identity-like；Source-GT gate 出现部分正信号，grad improve `0.002402`、lap improve `0.017896`、edge win `63/120=52.5%`，但 image improve `-0.000053`、edge-weighted improve `-0.000094`、high-edge-grad improve `-0.006136` 仍未过阈值，combined gate 仍为 `reject_source_gt_regress`。fixed40 已生成 `output_crackwarp_slurm/v87_spatial_cap_long80_from_v83/fixed_visual_v41_v66_v83_v87/`，文件数 `43`；本地抽看 `crack0030_08`、`crack0061_07`、`crack0063_07` 显示 v87 确实比 v83 更敢动，部分裂缝位置更接近 corrected GT，但同时出现局部涟漪、糊化和边缘附近形变伪影，仍不能宣布稳定视觉突破或直接扩严格 1000。当前结论：80 epoch 长训证明 1 epoch 筛选低估了该同系模型的常规指标潜力，但 Source-GT 视觉瓶颈仍未真正解决；下一步应围绕 v87 做更严格的视觉归因、ROI/patch 局部 refinement 或伪影抑制，而不是继续只追加同类小 loss。

2026-07-03 已完成 v87 fixed40 全量人工分类和关键 ROI 复核。已将远端 40 张 `fixed_visual_v41_v66_v83_v87` 全量拉回本地，生成 review sheets，并新增 `output_crackwarp_slurm/v87_spatial_cap_long80_from_v83/fixed_visual_v41_v66_v83_v87/visual_review_v87.md` 与 `v87_roi_key_cases.txt`。全图粗分后，ROI 复核进一步收紧结论：v87 虽然比 v83 更敢做局部形变，但多数“看似改善”的样本在局部放大下仍出现涟漪、旋涡、糊化或皱褶状伪影。`utils/export_crack_roi_visuals.py` 已最小更新，新增 `--model_name` 并改用 `models.model_factory.build_model(...)`，以支持 `global_context_flow_unet_spatial_cap` 等 v63 之后模型；本地 `.venv\Scripts\python.exe -m py_compile utils\export_crack_roi_visuals.py` 通过。远端 ROI job `41616145` 已完成，输出 42 个 ROI 文件并拉回本地 `output_crackwarp_slurm/v87_spatial_cap_long80_from_v83/panels/`；关键观察包括：`crack0030_08_roi01_panel.png` 出现强旋涡/褶皱，`crack0052_03_roi01_panel.png` 显示局部像被搅动而非干净靠近 GT，`crack0060_07_roi01_panel.png` 和 `crack0022_03_roi01_panel.png` 也有明显涟漪。当前结论：v87 不应扩严格 1000；下一步应把 v87 作为“伪影暴露最充分”的诊断基线，优先设计 patch/ROI 层面的局部伪影抑制和稳定约束。

2026-07-03 已完成 v88 裂缝 ROI flow Laplacian 伪影抑制首版实现、远端 smoke、1 epoch 短训、120 常规评估、combined gate 和 fixed40 抽查。新增默认关闭参数 `w_crack_flow_laplacian` 与 `crack_flow_laplacian_dilate`，`CrackFlowLaplacianLoss` 在裂缝 soft mask 膨胀 ROI 内比较预测 flow 与 label flow 的像素尺度 Laplacian，用于抑制 v87 中的涟漪/旋涡/皱褶状局部高频形变；已贯穿 `loss_crack.py`、`config_crack.py`、`train_v2.py`、`run_train_slurm.py`、`slurm_train_crackwarp.sbatch` 和 `scripts/smoke_source_gt_loss.py`。本地与远端 py_compile 通过，远端 smoke job `41650079` 完成，输出 `crack_flow_laplacian=136.986816` 且可反传。v88 job `41650442` 使用 `global_context_flow_unet_spatial_cap` 从 v87 `best_epe.pth` 初始化，`LR=2e-6`、`W_CRACK_FLOW_LAPLACIAN=0.0001`、`CRACK_FLOW_LAPLACIAN_DILATE=7`，保留 v87 的 Source-GT、几何和 target-risk 设置，在 `gpuHz/shortjobs` 完成，退出码 `0:0`；训练日志中 `c_flow_lap` 稳定非零，约 `0.4-2.5`。120 常规指标非常强：crack EPE `17.116129`、global EPE `16.927832`、Dice `0.508172`、crack edge `0.536058`、global edge `0.551378`、folding `0.005437`。Source-GT 图像指标也转正：image improve `0.000688`、grad improve `0.002937`、lap improve `0.017726`、edge-weighted improve `0.000695`、edge win `88/120=73.3%`；但 identity-aware gate 显示平均位移仅 `3.887px`，epe-minus-identity 仅 `0.179px`，combined gate 判定 `reject_identity_like`。fixed40 抽看 `crack0030_08`、`crack0052_03`、`crack0060_07`、`crack0063_07` 显示 v88 确实压掉了 v87 的涟漪/旋涡伪影，但输出基本退回 input/v83 保守形态，真实校正量不足。当前结论：flow Laplacian 方向能有效压伪影，但 `1e-4` 权重过强，会把模型推回 identity-like；不扩严格 1000。下一步 v89 应将 `W_CRACK_FLOW_LAPLACIAN` 降到 `1e-5` 量级，并同时启用位移下界约束，避免再次缩回 identity。

2026-07-04 已完成 v89 小权重 flow Laplacian + 位移下界约束的 1 epoch 短训、120 常规评估、combined gate、fixed40 全量复核和 ROI 面板复核。v89 job `41794703` 从 v87 `best_epe.pth` 初始化，使用 `W_CRACK_FLOW_LAPLACIAN=1e-5`、`CRACK_FLOW_LAPLACIAN_DILATE=7`、`W_DISP_BAND=0.005`、`DISP_BAND_MIN_PX=10`、`DISP_BAND_MAX_PX=24`，保留 v87 的 Source-GT、几何与 target-risk 设置，在 `gpuHz/shortjobs` 完成，退出码 `0:0`；训练末尾 Val EPE `18.657px`、CrackEPE `17.962px`。120 常规评估为 crack EPE `19.094536`、global EPE `19.242613`、Dice `0.494067`、crack edge `0.538391`、global edge `0.547271`、folding `0.018345`，相比 v87 明显改善，虽弱于 identity-like 的 v88。combined gate 不再自动拒绝：平均位移 `11.443px`，不是 identity-like；Source-GT image improve `0.000572`、grad improve `0.002819`、lap improve `0.017981`、edge-weighted improve `0.000569`、edge win `80/120=66.7%`，状态为 `needs_fixed_visual_review`。已导出 fixed40 全量 40 张横向对比图、10 个关键样本共 38 张 ROI 面板，并生成本地 review sheets。全量复核显示 v89 比 v87 明显少涟漪/旋涡，比 v88 保留更多恢复量，是目前 flow Laplacian 路线中最好的折中；ROI 复核确认局部伪影下降，但仍有偏保守、局部糊化和少量波纹，尚不能称为干净视觉突破。当前结论：v89 可作为下一轮候选基线，但暂不扩严格 1000；下一步优先做 v90 小调或诊断性 1000，而不是直接写成最终通过版。

2026-07-04 已准备 v90 位移带小调实验与后处理脚本，暂未在本地提交训练，因为本地没有远端 v89 `best_epe.pth` checkpoint。v90 计划从 v89 继续 1 epoch，保持 `global_context_flow_unet_spatial_cap`、`W_CRACK_FLOW_LAPLACIAN=1e-5`、`CRACK_FLOW_LAPLACIAN_DILATE=7`、v87/v89 的 Source-GT、几何与 target-risk 设置，仅将位移带调得稍微更主动：`W_DISP_BAND=0.008`、`DISP_BAND_MIN_PX=12`、`DISP_BAND_MAX_PX=24`，目标是在不明显恢复 v87 涟漪/旋涡伪影的情况下，比 v89 多保留一点裂缝校正量。新增脚本：`scripts/submit_v90_spatial_cap_flowlap1e5_dispband12_from_v89.sh`、`scripts/run_v90_gates_120.sh`、`scripts/run_v90_fixed40_visual.sh`、`scripts/run_v90_roi_visual.sh`、`scripts/make_v90_review_sheets.py`；新增关键 ROI 清单 `output_crackwarp_slurm/v89_spatial_cap_flowlap1e5_dispband_from_v87_1ep/v90_roi_key_cases.txt`，覆盖 `crack0022_03`、`crack0030_08`、`crack0052_03`、`crack0055_07`、`crack0060_07`、`crack0063_07`、`crack0090_08`。本地 `bash -n` 和 `py_compile` 语法检查通过。远端下一步：在远端项目目录执行提交脚本，训练完成后依次跑 v90 120 gate、fixed40、ROI panels 和 review sheets，再与 v87/v88/v89 对比判断是否值得继续。

2026-06-29 已完成 v64-v69 受限单图位移场路线的 120 gate。v64/v65 从 v63/v64 初始化，使用 `single_flow_unet` 和训练尺度 `10-15px` 位移带，120 常规指标显著优于 v41，但在 256 gate 上平均位移仅 `5.898px`/`6.233px`，被判定为 `reject_identity_like`；由此确认训练尺寸与 gate 尺寸存在约 2 倍像素尺度差异。v66 将训练位移带改为 `20-30px` 后平均位移达到 `10.875px`，不再 identity-like，但 source-GT image/grad 退化、edge win 仅 `30.0%`，判定 `reject_source_gt_regress`。v67 使用折中训练位移带 `18-24px`，平均位移 `10.083px`，仍因 source-GT image/grad 退化和 edge win `30.8%` 被拒。v68 在 v67 基础上加轻量 `w_crack_edge=0.005`，v69 在 v67 基础上新增 Slurm `W_PHOTO/--w-photo` 透传并提高 `w_photo=0.45`，二者均未修复 source-GT regress：v68 平均位移 `10.566px`、edge win `30.8%`，v69 平均位移 `10.647px`、edge win `30.0%`。当前结论：`single_flow_unet + 位移带 + 现有 photo/edge loss` 可以摆脱 identity-like，但一旦真实位移达到约 `10px`，source-GT 视觉质量会稳定退化；不应继续只扫位移带、edge 或 photo 小权重，应转向更直接的 source-GT/高边缘视觉约束、样本级可视化归因，或新的结构/模型路线。已生成 v41/v64/v66 fixed40 横向图 `output_crackwarp_slurm/v66_single_flow_unet_dispband020_20_30_from_v65_1ep/fixed_visual_v41_v64_v66/`。

2026-06-29 已对 v41/v64/v66 fixed40 横向图完成 Codex 第一轮人工视觉复核，并新增 `output_crackwarp_slurm/v66_single_flow_unet_dispband020_20_30_from_v65_1ep/fixed_visual_v41_v64_v66/visual_review_v66.md`。40 张样本共同显示：v66 相对 v41 明显更稳，基本避免 v41 大幅采样到错误纹理区域；但 v66 与 v64/input 的视觉差异很小，没有看到稳定、肉眼明确的裂缝真实修复增强。未发现 v66 灾难性退化，但也没有可支撑严格 1000 扩展的明显正样本。当前判断：v66 可作为“受限位移避免 v41 过采样”的诊断证据，不应写成真实视觉突破，也不建议仅凭 v66 扩严格 1000。

2026-06-30 已完成 v70 Source-GT 直接视觉约束的首轮短训、120 gate 和 fixed40 视觉复核。新增 Dataset 可选返回原始裂缝源图，`CrackWarpLoss` 新增 `w_source_photo` 与 `w_source_edge`，训练入口和 Slurm 脚本已透传 `SOURCE_GT_ROOT/W_SOURCE_PHOTO/W_SOURCE_EDGE`，并新增 `scripts/smoke_source_gt_loss.py`。远端 smoke 通过：Source-GT 可按 `crackXXXX_YY.png -> crackXXXX` 反查，新增 loss 可前向和反传。v70 job `40612601` 使用 `single_flow_unet` 从 v66 初始化，`w_source_photo=0.05`、`w_source_edge=0.05`、`disp_band=20-30px`，退出码 `0:0`。120 常规指标相对 v66 小幅改善 EPE/edge：crack EPE `26.319601`、global EPE `24.404989`、crack edge `0.541095`、global edge `0.550715`，但 Dice 降至 `0.440467`、folding 升至 `0.019742`。Source-GT gate 显示 v70 仍退化：image improve `-0.000355204`、edge-weighted improve `-0.000384088`、edge win `48/120`、high-edge-grad improve `-0.006639516`，combined gate 判定 `reject_source_gt_regress`。fixed40 横向图 `output_crackwarp_slurm/v70_sourcegt_photo005_edge005_from_v66_1ep/fixed_visual_v41_v64_v66_v70/` 抽看后确认：v70 仍主要接近 input/v64/v66，没有肉眼明确 Source-GT 修复增强，不建议扩严格 1000。

2026-06-30 已完成 v71 局部 residual refinement 首版实现、远端 smoke、1 epoch 训练、120 gate 与 fixed40 可视化导出。新增 `local_residual_refine_unet`，主体层保持与 `single_flow_unet` 同名同结构，便于从 v66/v70 checkpoint 通过 `strict=False` 继承稳定 base flow；新增 refinement mask/head 只在暗裂缝与高边缘先验区域做小幅 residual flow。远端 smoke 使用 v66 checkpoint 成功，只有新增 refinement head 的 `14` 个 missing keys，无 unexpected keys，CUDA 前向、CrackWarpLoss 和反传正常。v71 job `40738679` 使用 `local_residual_refine_unet` 从 v70 `best_epe.pth` 初始化，`LR=1e-5`、`w_source_photo=0.05`、`w_source_edge=0.05`、`disp_band=20-30px`，在 `gpuHz/shortjobs` 完成，退出码 `0:0`。120 常规指标为 crack EPE `26.705343`、global EPE `24.552637`、Dice `0.442806`、crack edge `0.536987`、global edge `0.536384`、folding `0.009655`；相对 v70，v71 的 folding 明显降低，但 EPE/edge/source-GT 图像质量没有改善。identity-aware 120 显示 v71 平均位移 `20.071px`，不是 identity-like；但 combined gate 仍判定 `reject_source_gt_regress`：image improve `-0.000389`、edge win `37.5%`、grad improve `0.002213`、lap improve `0.014795`。fixed40 横向图已生成在远端 `output_crackwarp_slurm/v71_local_residual_refine_from_v70_1ep/fixed_visual_v41_v64_v66_v70_v71/`。当前结论：v71 首版证明局部 residual refinement 工程链路可用，且能降低 folding，但仍没有形成肉眼可确认的 Source-GT 裂缝修复增强，不扩严格 1000；下一步不应仅继续同结构小幅训练，而应先抽看 fixed40，若无明显改善，则转向更强的局部监督/显式裂缝 mask refinement 或新的结构化单图模型。

2026-06-30 已完成 v72 局部 Source-GT high-edge 监督实现、远端 smoke、1 epoch 短训和 120 combined gate。新增 `SourceGTVisualConsistencyLoss.high_edge(...)`，通过 Source-GT 高梯度区域与裂缝 mask 聚焦局部锐边视觉一致性；`CrackWarpLoss`、`config_crack.py`、`run_train_slurm.py`、`train_v2.py`、`slurm_train_crackwarp*.sbatch` 与 `scripts/smoke_source_gt_loss.py` 已透传 `w_source_high_edge` 并记录 `s_h_edge`。远端 smoke 通过，v72 job `40743519` 使用 `local_residual_refine_unet` 从 v71 `best_epe.pth` 初始化，`LR=8e-6`、`w_source_photo=0.02`、`w_source_edge=0.02`、`w_source_high_edge=0.12`、`disp_band=20-30px`，在 `gpuHz/shortjobs` 完成，退出码 `0:0`。120 常规指标为 crack EPE `26.850410`、global EPE `24.616598`、Dice `0.449247`、crack edge `0.533849`、global edge `0.539000`、folding `0.022682`；相对 v71，Dice 小升但 EPE/edge 没有改善，folding 明显变差。随后生成 `output_crackwarp_slurm/v72_local_refine_source_high_edge_from_v71_1ep/gates_120/` 下的 source-GT、identity-aware 和 combined gate：v72 平均位移 `20.169px`，不是 identity-like，但仍为 `reject_source_gt_regress`，image improve `-0.000368`、edge-weighted improve `-0.000418`、edge win `42/120=35.0%`、high-edge-grad improve `-0.005241`。当前结论：high-edge Source-GT loss 没有修复 v71 的真实视觉瓶颈，不扩严格 1000；下一步不应继续扫同类 source/high-edge 小权重，应转向显式裂缝 mask/结构分支、patch-level refinement，或更强的结构化单图模型。

2026-06-30 已完成 v73 显式结构/patch refinement 首版实现、远端 smoke、1 epoch 短训、120 combined gate 和 fixed40 可视化抽查。新增 `structural_patch_refine_unet`，在 v71 的 `local_residual_refine_unet` 主干和 refinement head 基础上增加 `structure_encoder`、`structure_mask_head` 与 `structure_flow_head`，显式输入 dark/edge/thin/patch 结构先验，只输出小幅结构残差；模型工厂新增该名称，`scripts/smoke_structural_patch_refine_unet.py` 可验证从 v71 checkpoint 宽松加载。远端 smoke 通过，加载 v71 `best_epe.pth` 时 missing `30`、unexpected `0`，CUDA 前向、CrackWarpLoss 和反传正常。v73 job `40743883` 使用 `MODEL_NAME=structural_patch_refine_unet`、从 v71 初始化、`LR=8e-6`、`w_source_photo=0.02`、`w_source_edge=0.02`、`w_source_high_edge=0.08`、`disp_band=20-30px`，在 `gpuHz/shortjobs` 完成，退出码 `0:0`。120 常规指标为 crack EPE `26.601225`、global EPE `24.372036`、Dice `0.451174`、crack edge `0.536966`、global edge `0.540898`、folding `0.019304`，比 v72 略稳但仍没有突破。combined gate 仍为 `reject_source_gt_regress`：v73 平均位移 `20.157px`，不是 identity-like；source-GT image improve `-0.000295`、edge-weighted improve `-0.000342`、edge win `43/120=35.8%`、high-edge-grad improve `-0.005376`。fixed40 已生成并通过 GitHub Release 拉回本地 `output_crackwarp_slurm/v73_structural_patch_refine_from_v71_1ep/fixed_visual_v41_v66_v70_v71_v72_v73/`；抽看 `crack0063_07`、`crack0039_04`、`crack0005_05`、`crack0030_08` 显示 v73 基本接近 v66-v72/input，避免 v41 错误大采样，但没有肉眼明确 Source-GT 裂缝形态修复。当前结论：结构/patch 分支方向比 v72 稍稳，但仍不足以扩严格 1000；若继续结构路线，需要更强的监督信号或显式 mask 目标，而不是仅增加小残差分支。

2026-06-30 已完成 v74 更强单图全局上下文 baseline 的首版实现、远端 smoke、1 epoch 短训、120 gate、combined gate 和 fixed40 抽查。新增 `global_context_flow_unet`，在 `single_flow_unet` 同名主干基础上加入低分辨率 `BottleneckSelfAttention` 和 `global_flow_head`，让模型在 bottleneck 处建模长距离结构并输出低分辨率全局 residual；模型工厂和 `scripts/smoke_global_context_flow_unet.py` 已接入。远端 smoke 使用 v66 checkpoint 成功，missing `23`、unexpected `0`，CUDA 前向和反传正常。v74 job `40754274` 使用 `MODEL_NAME=global_context_flow_unet`、从 v66 初始化、`LR=8e-6`、`w_source_photo=0.02`、`w_source_edge=0.02`、`w_source_high_edge=0.08`、`disp_band=20-30px`，在 `gpuHz/shortjobs` 完成，退出码 `0:0`。120 常规指标显示 v74 更敢修裂缝：crack EPE `23.857065`、Dice `0.470439` 优于 v73，但 global EPE 升至 `25.692404`，folding 暴涨到 `0.110131`，存在明显几何失稳。combined gate 仍为 `reject_source_gt_regress`：v74 平均位移 `22.203px`，edge win `58/120=48.3%` 接近 50%，但 image improve `-0.000609`、edge-weighted improve `-0.000629`、high-edge-grad improve `-0.006692`，仍未过 source-GT 质量阈值。fixed40 已拉回本地 `output_crackwarp_slurm/v74_global_context_flow_from_v66_1ep/fixed_visual_v41_v66_v73_v74/`；抽看 `crack0063_07`、`crack0039_04`、`crack0030_08`、`crack0005_05` 显示 v74 确实比 v73 形变更强，但出现明显底部/边界拉伸、折叠和纹理伪影，未形成干净的 Source-GT 裂缝修复。当前结论：更强全局模型方向有“敢动”的正信号，但必须加入更强几何稳定约束后再试；不扩严格 1000。

2026-06-30 已完成 v75 几何稳定版全局上下文模型的正式 r2 短训、120 gate、combined gate 和 fixed40 抽查。首次 v75 job `40758131` 漏传 `SOURCE_GT_ROOT`，已取消并仅作为无效诊断；正式 v75_r2 job `40758313` 使用 `MODEL_NAME=global_context_flow_unet`、从 v74 `best_epe.pth` 初始化、`LR=6e-6`、`SOURCE_GT_ROOT=under-crack-images/images`、`w_source_photo=0.02`、`w_source_edge=0.02`、`w_source_high_edge=0.08`、`w_fold=0.05`、`w_jacobian=0.0005`、`w_crack_boundary_jacobian=0.0002`、`disp_band=20-30px`，在 `gpuHz/shortjobs` 完成，退出码 `0:0`。120 常规指标相对 v74 略有改善：crack EPE `22.971165`、global EPE `24.683033`、Dice `0.481347`、crack edge `0.542019`、global edge `0.541424`，folding 从 v74 的 `0.110131` 降到 `0.101686`，但仍处高位。combined gate 仍为 `reject_source_gt_regress`：平均位移 `20.545px`，不是 identity-like；edge win `60/120=50.0%`，但 image improve `-0.000365`、edge-weighted improve `-0.000377`、high-edge-grad improve `-0.005969`，仍未真正过 Source-GT 视觉质量阈值。fixed40 已通过 GitHub Release 拉回本地 `output_crackwarp_slurm/v75_global_context_geom_source_from_v74_1ep_r2/fixed_visual_v41_v66_v74_v75/`；抽看 `crack0063_07`、`crack0039_04`、`crack0030_08`、`crack0005_05` 显示 v75 保留了 v74 的较大形变，但底部/边界波纹和拉伸伪影仍明显，裂缝形态没有肉眼明确更接近 Source GT。当前结论：v75 证明几何约束可以小幅缓解但不能根治 v74 的失稳；不扩严格 1000，也不建议继续在同一全局模型上小幅扫几何权重。

2026-06-30 已完成第一轮监督/数据口径重审诊断，新增 `utils/diagnose_gt_target_consistency.py` 并在远端跑 fixed40 与 120。该脚本不加载模型，只比较 input、`.png.npy` label warp 得到的 `GT corrected`、以及原始裂缝 `Source GT` 三者关系，同时统计 label 位移、贴边采样、folding 和边界/中心位移比。fixed40 结果显示 label-corrected 相对 input 更接近 Source GT 的胜率很高：image `97.5%`、edge-weighted `97.5%`、high-edge-grad `100%`；120 结果同样稳定：image `97.5%`、edge-weighted `98.33%`、high-edge-grad `97.5%`。这说明当前 label/GT 目标不是完全错的，`GT corrected` 大体确实比 input 更接近原始 Source GT。但 label 本身存在几何风险：120 平均 label 位移 `16.748px`，贴边采样比例 `6.10%`，label folding rate `3.61%`；fixed40 也类似，平均位移 `15.627px`，贴边采样 `5.85%`，folding `3.28%`。当前结论：下一步不应推翻合成标签，而应在训练和 gate 中显式处理 label 的边界/不可逆高风险区域，例如 valid warp mask、border sampling mask、folding-risk mask 或边界区域降权，避免模型靠整图拉伸和边界 padding 换取 crack 指标。

2026-06-30 已完成 v76 target-risk mask 首版实现、远端 smoke、1 epoch 短训、120 gate、combined gate 和 fixed40 抽查。新增 `target_risk_mask_min_weight/target_risk_border_eps/target_risk_folding_dilate`，默认关闭；启用后 `CrackWarpLoss` 会根据 label 贴边采样和 label 自身 folding 风险构造监督降权 mask，只降低高风险区域的坐标/裂缝坐标监督，不改 label、不删样本。远端 smoke 通过，CUDA 前向和反传正常；训练日志显示真实样本 `t_safe` 约 `0.90-0.95`、`t_w` 约 `0.94-0.96`，说明 mask 只降权少量高风险像素。v76 job `40804740` 使用 `global_context_flow_unet` 从 v75 初始化，`LR=5e-6`、`TARGET_RISK_MASK_MIN_WEIGHT=0.35`、`TARGET_RISK_FOLDING_DILATE=5`、保留 v75 的 Source-GT 与几何约束，在 `gpuHz/shortjobs` 完成，退出码 `0:0`。120 常规指标相对 v75 显示 mask 对几何稳定有帮助：crack EPE `23.014799` 基本持平，global EPE `24.676888` 基本持平，Dice `0.482249` 小升，global edge `0.542248` 小升，folding 从 v75 的 `0.101686` 降到 `0.090655`。但 combined gate 仍为 `reject_source_gt_regress`：平均位移 `20.348px`，edge win `60/120=50.0%`，image improve `-0.000393`、edge-weighted improve `-0.000406`、high-edge-grad improve `-0.005718`。fixed40 已拉回本地 `output_crackwarp_slurm/v76_global_context_target_risk_mask_from_v75_1ep/fixed_visual_v41_v66_v75_v76/`；抽看 `crack0063_07`、`crack0039_04`、`crack0030_08`、`crack0005_05` 显示 v76 的底部/边界伪影比 v75 略轻，但仍明显，裂缝形态没有肉眼明确修复突破。当前结论：target-risk mask 方向有效但力度不足；不扩严格 1000。

2026-06-30 已完成 v77 更硬 valid sampling 约束首版实现、远端 smoke、1 epoch 短训、120 gate、combined gate 和 fixed40 抽查。新增 `ValidSamplingLoss`，通过 `w_valid_sampling/valid_sampling_margin` 直接惩罚预测 flow 贴近归一化采样边界 `0/1` 的区域，并在训练日志中记录 `v_samp/v_risk`；`config_crack.py`、`train_v2.py`、`run_train_slurm.py` 和两个 Slurm 脚本均已透传新参数。远端 CUDA smoke 通过，loss 可前向和反传；v77 job `40810775` 使用 `global_context_flow_unet` 从 v76 初始化，`LR=4e-6`、`W_VALID_SAMPLING=0.08`、`VALID_SAMPLING_MARGIN=0.01`，保留 v76 的 Source-GT、几何约束和 target-risk mask，在 `gpuHz/shortjobs` 完成，退出码 `0:0`。训练中 `v_risk` 从开头约 `3.8%` 降到结尾约 `3.0%`，说明贴边采样惩罚本身生效；但 120 gate 不通过：crack EPE `25.622671`、global EPE `27.393505`、Dice `0.480938`、crack edge `0.552268`、global edge `0.567519`、folding `0.138859`，相对 v76 虽 edge 指标上升但 EPE 与 folding 明显变差。identity-aware 显示 v77 平均位移 `11.587px`，不是 identity-like；combined gate 仍为 `reject_source_gt_regress`：image improve `-0.000815`、grad improve `-0.002010`、edge-weighted improve `-0.000807`、edge win `48/120=40.0%`、high-edge-grad improve `-0.004891`。fixed40 已通过 GitHub Release 拉回本地 `output_crackwarp_slurm/v77_global_context_valid_sampling_from_v76_1ep/fixed_visual_v41_v66_v76_v77/`；抽看 `crack0063_07`、`crack0039_04`、`crack0030_08`、`crack0005_05` 显示 v77 与 v76 基本同型，底部/边界拉伸和波纹伪影仍明显，没有肉眼明确 Source-GT 裂缝修复增强。当前结论：v77 证明“预测贴边采样惩罚”能压低训练中的边界风险日志，但未能转化为视觉质量和 folding 改善；不扩严格 1000，不建议继续只加大同一 valid sampling 权重。

2026-06-30 已完成 v78 Source-GT 有效采样 mask 首版实现、远端 smoke、1 epoch 短训、120 gate、combined gate 和 fixed40 抽查。新增 `source_valid_mask_margin`，默认关闭；启用后不再把预测贴边当作额外 penalty，而是只让 `source_photo/source_edge/source_high_edge` 在预测采样坐标远离 `0/1` 边界的位置计算，避免 Source-GT 视觉约束推动边界 padding 区域继续拉伸。`loss_crack.py` 新增 `_source_valid_mask(...)` 并记录 `source_valid_ratio`，`config_crack.py`、`train_v2.py`、`run_train_slurm.py` 和两个 Slurm 脚本已透传 `SOURCE_VALID_MASK_MARGIN/--source-valid-mask-margin`。远端 smoke 通过，随机 CUDA 样本 `source_valid_ratio` 约 `0.959`，Source-GT loss 可前向和反传。v78 job `40813881` 使用 `global_context_flow_unet` 从 v76 初始化，`LR=4e-6`、`SOURCE_VALID_MASK_MARGIN=0.01`、`W_VALID_SAMPLING=0.0`，保留 v76 的 Source-GT、几何约束和 target-risk mask，在 `gpuHz/shortjobs` 完成，退出码 `0:0`；训练日志 `s_valid` 稳定约 `0.92-0.94`，说明 Source-GT 视觉监督确实避开了少量贴边区域。但 120 gate 仍不通过：crack EPE `25.649332`、global EPE `27.423365`、Dice `0.480767`、crack edge `0.552490`、global edge `0.567952`、folding `0.138947`，几乎复刻 v77，仍明显差于 v76 的 folding `0.090655`。identity-aware 显示 v78 平均位移 `11.607px`，不是 identity-like；combined gate 仍为 `reject_source_gt_regress`：image improve `-0.000818`、grad improve `-0.001994`、edge-weighted improve `-0.000810`、edge win `48/120=40.0%`、high-edge-grad improve `-0.004959`。fixed40 已通过 GitHub Release 拉回本地 `output_crackwarp_slurm/v78_global_context_source_valid_mask_from_v76_1ep/fixed_visual_v41_v66_v76_v77_v78/`；抽看 `crack0005_05`、`crack0030_08`、`crack0039_04` 显示 v78 与 v77 基本同型，底部/边界波纹和拉伸伪影仍明显，没有肉眼明确 Source-GT 裂缝修复增强。当前结论：只给 Source-GT loss 乘预测有效采样 mask 仍不能解决全局上下文模型的几何失稳；不扩严格 1000，v77/v78 这条有效采样小修路线应止损。

2026-06-30 已完成 v79 clean-label subset 数据路线首版实现、远端清单生成、1 epoch 短训、120 gate、combined gate 和 fixed40 抽查。新增 `utils/make_clean_label_subset.py`，只读取 `.png.npy` label，按 `label_border_sample_rate` 与 `label_folding_rate` 组合风险分数排序，生成可直接传给 `--train-filter-list` 的 clean 训练清单。远端在完整 `underwater_crack_v3` 上生成 `output_crackwarp_slurm/v79_clean_label_subset_assets/keep075/`，保留 `7770/10360=75%` 样本；相对全量，保留集 label folding 均值从 `0.031349` 降到 `0.026164`、p95 从 `0.052595` 降到 `0.037532`，border 采样均值从 `0.060417` 降到 `0.055876`，说明清单确实降低了 label 几何风险。v79 job `40813896` 使用 `global_context_flow_unet` 从 v76 初始化，`TRAIN_FILTER_LIST=output_crackwarp_slurm/v79_clean_label_subset_assets/keep075/clean_train_images.txt`，实际匹配训练子集 `7010/9324`，保留 v76 的 Source-GT、几何约束和 target-risk mask，在 `gpuHz/shortjobs` 完成，退出码 `0:0`。120 gate 仍不通过且比 v77/v78 更差：crack EPE `25.704245`、global EPE `27.480225`、Dice `0.479942`、crack edge `0.552167`、global edge `0.568430`、folding `0.143754`。identity-aware 显示 v79 平均位移 `11.663px`，不是 identity-like；combined gate 仍为 `reject_source_gt_regress`：image improve `-0.000822`、grad improve `-0.002005`、edge-weighted improve `-0.000816`、edge win `49/120=40.8%`、high-edge-grad improve `-0.005223`。fixed40 已通过 GitHub Release 拉回本地 `output_crackwarp_slurm/v79_global_context_clean_label075_from_v76_1ep/fixed_visual_v41_v66_v76_v79/`；抽看 `crack0005_05`、`crack0030_08`、`crack0039_04` 显示 v79 没有肉眼明确修复增强，边界/底部伪影仍在。当前结论：仅筛掉 25% label 高风险样本不足以解决当前全局上下文模型的 Source-GT 视觉退化和 folding 反弹；不扩严格 1000。

2026-06-30 已完成 v80 评估样本 label 风险归因诊断。新增 `utils/analyze_eval_label_risk.py`，将 `label_risk_all.csv` 与 120 `eval_per_image.csv`、fixed40 样本清单按图片名对齐，输出 `output_crackwarp_slurm/v80_eval_label_risk_diag_v79_120_fixed40/`。结果显示 120 eval 样本的 label 风险确实略高于全量：border 均值 `0.063632` vs 全量 `0.060417`，label folding 均值 `0.034616` vs 全量 `0.031349`，risk score 均值 `0.749016` vs 全量 `0.697509`；fixed40 则接近全量，risk score 均值 `0.701165`。相关性显示 label 风险与 EPE 有一定关系：`label_disp_px_mean` 与 crack/global EPE 相关约 `0.532/0.527`，`label_folding_rate` 与 crack EPE 相关约 `0.309`；但与模型预测 folding 几乎无关，risk score / label border / label folding / label disp 与 prediction folding 的相关性仅约 `0.025/0.013/0.032/0.030`。当前结论：评估样本 label 风险偏高可以解释一部分 EPE 难度，但不能解释 v77-v79 的 folding 反弹；当前失稳更像是全局上下文模型和现有监督组合导致的预测场问题，而不是评估样本本身全是坏 label。

2026-07-01 已完成 v81 全局 residual 限幅模型首版实现、远端 smoke、1 epoch 短训、120 gate 与 fixed40 远端导出。新增 `global_context_flow_unet_rescap`，保持 `global_context_flow_unet` 层名和结构不变，只将 `max_global_delta` 从 `0.10` 降到 `0.035`，用于直接验证 v77-v79 folding 反弹是否来自全局 residual 自由度过高；smoke 使用 v76 checkpoint 加载结果为 missing `0`、unexpected `0`，CUDA 前向和反传正常。v81 job `40817924` 使用 `global_context_flow_unet_rescap` 从 v76 初始化，保留 v76 的 Source-GT、disp band、fold/jacobian 和 target-risk mask 设置，在 `gpuHz/shortjobs` 完成，退出码 `0:0`。120 gate job `40818329` 完成，输出 `output_crackwarp_slurm/v81_global_context_rescap_from_v76_1ep/gates_120_v3/`：常规指标为 crack EPE `27.350435`、global EPE `25.322380`、Dice `0.433373`、crack edge `0.528688`、global edge `0.541715`、folding `0.036311`。相对 v76/v77-v79，v81 显著压低 folding（v76 `0.090655`，v77-v79 约 `0.139-0.144`），证明“限制全局 residual 输出空间”对几何稳定有效；但 EPE/Dice/source-GT 视觉质量明显不足。combined gate 仍为 `reject_source_gt_regress`：平均位移 `19.678px`，不是 identity-like；image improve `-0.000591`、edge-weighted improve `-0.000626`、edge win `47/120=39.2%`、high-edge-grad improve `-0.006368`。fixed40 已在远端生成 `output_crackwarp_slurm/v81_global_context_rescap_from_v76_1ep/fixed_visual_v41_v66_v76_v81/`，文件数 `43`；因本机到跳板 `100.94.186.128:22` 一度超时，尚未通过 GitHub Release 拉回本地。当前结论：v81 不扩严格 1000；它是有价值的诊断结果，说明限幅能治 folding，但 `0.035` 可能过保守，下一步应先拉回 fixed40 抽看，再决定是否做 v82 中等 residual cap（例如 `0.055-0.065`）或分解 local/global 输出约束。

2026-07-01 已完成 v82 中等 global residual cap 的实现、远端 smoke/训练、120 gate、fixed40 导出拉回和代表样本视觉抽查。新增 `global_context_flow_unet_rescap_mid`，保持 `global_context_flow_unet`/v81 同系结构，仅将 `max_global_delta` 调整为 `0.06`，用于验证 v81 是否只是限幅过保守。v82 训练 job `40938612` 和 120 gate job `40939997` 均完成，退出码 `0:0`；fixed40 导出 job `40942933` 完成，退出码 `0:0`，远端与本地目录 `output_crackwarp_slurm/v82_global_context_rescap_mid_from_v76_1ep/fixed_visual_v41_v66_v76_v81_v82/` 均为 `43` 个文件，并已按“大数据传输使用 gh，不用 scp”的约定通过 GitHub Release 拉回。120 gate 指标为 crack EPE `28.136423`、global EPE `26.888262`、Dice `0.424407`、crack edge `0.519069`、global edge `0.525436`、folding `0.061594`、平均位移 `19.787px`，Source-GT edge win `47.5%`，combined gate 仍为 `reject_source_gt_regress`。抽看 `crack0005_05`、`crack0030_08`、`crack0039_04`、`crack0063_07` 后确认：v82 比 v81 略微恢复形变幅度，但没有出现肉眼明确的裂缝形态修复，底部/右侧边界拉伸和纹理伪影比 v81 更明显。当前结论：v82 不扩严格 1000；简单 global cap sweep 已到边界，下一步应停止继续扫 `max_global_delta`，转向空间自适应 residual cap/边界低 cap + 裂缝核心高 cap，或回到 v66 类更稳的局部修复路线。

2026-07-01 已完成 v83 空间自适应 global residual cap 的实现、远端 smoke、1 epoch 短训、120 gate、fixed40 导出拉回和代表样本视觉抽查。新增 `global_context_flow_unet_spatial_cap`，继承 `GlobalContextFlowUNet`，不新增可学习参数，只按输入图像构造空间 cap map：边界区域使用接近 v81 的低 cap，普通区域使用中间 cap，暗裂缝/高边缘核心区域允许接近 v82 的较高 cap；模型 key 与 v76/v82 兼容，远端 smoke 从 v76 checkpoint 加载为 missing `0`、unexpected `0`，CUDA 前向和反传正常。v83 训练 job `40953666` 完成，退出码 `0:0`；120 gate job `40954388` 完成，退出码 `0:0`；fixed40 导出 job `40955574` 完成，退出码 `0:0`，远端与本地目录 `output_crackwarp_slurm/v83_global_context_spatial_cap_from_v76_1ep/fixed_visual_v41_v66_v76_v81_v82_v83/` 均为 `43` 个文件。由于单个 30MB GitHub Release 下载多次 TLS/stream 断开，最终按用户要求仍使用 `gh` 通道，将远端 tar 拆成 5MB 分片上传到 GitHub Release，并在本机通过代理 `http://127.0.0.1:7897` 下载分片后合并，未使用 `scp`。120 gate 指标为 crack EPE `27.888813`、global EPE `27.124702`、Dice `0.426763`、crack edge `0.522372`、global edge `0.531364`、folding `0.053718`、平均位移 `20.161px`，Source-GT edge win `50.0%`，combined gate 仍为 `reject_source_gt_regress`，image improve `-0.000727`。抽看 `crack0005_05`、`crack0030_08`、`crack0039_04`、`crack0063_07` 后确认：v83 相对 v82 的边界/底部伪影略有收敛，folding 也小幅下降，但视觉上仍没有明确的裂缝形态修复增强，整体只是 v81 稳定性和 v82 动作幅度之间的折中。当前结论：v83 不扩严格 1000；空间 cap 方向有诊断价值，但不足以形成真实修复突破，下一步不应继续做 cap 小修。

2026-06-29 已完成 v41 缩放位移的 120 快速诊断和固定可视化，用于寻找 v41 过度校正与 v63 近 identity 之间的中间幅度。新增 `utils/diagnose_scaled_flow_gate.py` 与 `utils/export_scaled_flow_predictions.py`。远端 job `40565507` 因 CPU/CUDA device 不一致失败，已修复；重提 `40565960` 完成并输出 `output_crackwarp_slurm/v63_vs_v41_diag/v41_scaled_source_gt_gate_120.summary.json`。结果显示：`alpha=0.1` 平均位移 `10.589px`，刚好超过 `10px` 非 identity 阈值，source-GT image/grad/lap/edge-weighted 相对 input 分别为 `+0.000015`、`+0.002487`、`+0.017609`、`+0.000028`，edge-weighted 胜率 `62/120`，但 high-edge-grad 仍为 `-0.005360` 且 `0/120` 胜出；`alpha=0.05` source-GT 更稳但平均位移只有 `5.294px`，仍 identity-like；`alpha>=0.15` 开始明显伤 image/edge。随后 job `40566965` 生成 fixed40 缩放横向图 `output_crackwarp_slurm/v63_vs_v41_diag/fixed_visual_gate_v41_scaled/`，代表图 `crack0063_07` 显示 `alpha=0.05/0.1/0.15` 避免了 v41 的错误大采样，但视觉修复幅度温和。当前判断：下一步不应继续 v41 大位移路线，也不应接受纯 v63 identity-like；更合理的 v64 方向是“受幅度约束的残差校正/轻量非 identity 模型”，目标位移量级先围绕 `10px`，并额外约束 high-edge-grad 或裂缝锐边质量。

2026-06-29 已新增 `utils/report_combined_gate.py`，把 `identity_aware_gate_report_120.json` 与 `source_gt_visual_quality_gate_120.summary.json` 合成统一 gate。远端已生成 `output_crackwarp_slurm/v63_vs_v41_diag/combined_gate_report_120.json` 与 `.md`：v22/v41 因 source-GT image/grad/edge-weighted 退化判定为 `reject_source_gt_regress`，v63 因平均位移 `2.432px` 判定为 `reject_identity_like`。正式结论：v22、v41、v63 当前都不能扩严格 1000；后续候选必须同时满足非 identity 位移、source-GT 图像质量不退化、固定可视化不过度采样。

2026-06-29 在 v63 identity-like 诊断完成后，进一步补充了 source-GT 图像空间 gate。新增 `utils/diagnose_visual_quality_gate.py`、`utils/make_image_list_from_eval.py`、`slurm_visual_quality_gate.sbatch` 和 `slurm_export_source_gt_visual.sbatch`，并扩展 `utils/export_visual_comparison_grid.py` 支持 `--source_gt_dir`，可在横向图中同时显示 `GT corrected`（label warp 口径）和 `Source GT`（原始裂缝源图口径）。远端完成 job `40555786`（label-warp GT fixed40 visual quality）、`40556776`（source-GT fixed40 visual quality）、`40557750`（带 Source GT 列的 fixed40 横向图）和 `40559072`（source-GT 120 visual quality），退出码均为 `0:0`。关键结论：在 source-GT 120 口径下，v63 相对 input 在 image/grad/lap/edge-weighted/high-edge image 上总体为正，分别为 `82/120`、`108/120`、`120/120`、`82/120`、`94/120` 胜出；v22/v41 在 image/grad/edge-weighted/high-edge image/high-edge grad 上整体退化，只在 Laplacian 上有 `106/120` 胜出。代表图 `crack0063_07` 的带 Source GT 横向图显示：`Source GT` 更接近 input/v63，而 v22/v41 大幅采样到另一块纹理区域。当前判断从“v63 只是指标假阳性”修正为：v63 确实接近 identity，但这暴露了现有 v22/v41 大位移校正在源图视觉参照下可能过度校正；下一步应重新定义主 gate，把 source-GT 可视化/图像空间指标、非恒等位移幅度和固定人工可视化共同纳入，而不是继续沿旧坐标 EPE 单指标扩严格 1000。

2026-06-29 已按用户截图确认本地 Slurm 悬浮状态程序 `SLURM xj62kv` 的最新版为 `dist_timeout30_countfix_cst2\SlurmFloatingStatus\SlurmFloatingStatus.exe`。原开机启动快捷方式 `C:\Users\billy.zhou\AppData\Roaming\Microsoft\Windows\Start Menu\Programs\Startup\SlurmFloatingStatus.lnk` 仍指向旧版 `dist\SlurmFloatingStatus\SlurmFloatingStatus.exe`，现已将该旧快捷方式非破坏性移动到 `Startup\disabled-startup-backup\SlurmFloatingStatus_old_20260629_142224.lnk`，并重新创建同名开机启动快捷方式指向 `dist_timeout30_countfix_cst2` 版本；注册表 Run 与计划任务中未发现同名 Slurm 悬浮窗启动项。

2026-06-29 已进入第二轮“更强单图位移场预测 baseline”路线，并完成 v63 首轮 smoke/gate。新增 `single_flow_unet` 作为不依赖 v41 checkpoint 的单图 flow U-Net baseline，通过 `models/model_factory.py` 统一接入训练、评估、flow 诊断和横向可视化；`run_train_slurm.py`、`train_v2.py`、`utils/evaluate_metrics.py`、`utils/export_flow_diagnostics.py`、`utils/export_visual_comparison_grid.py` 与 `slurm_train_crackwarp*.sbatch` 已支持 `model_name`/`MODEL_NAME`。远端 smoke `scripts/smoke_single_flow_unet.py` 已验证 `single_flow_unet` 在 CUDA 上输出 `(1,2,H,W)` 且范围 `[0,1]`，现有 `CrackWarpLoss` 可正常反传。v63 job `40504503` 使用 `MODEL_NAME=single_flow_unet`、`EPOCHS=1`、`LR=2e-5`、不加载 `INIT_CHECKPOINT`，在 `gpuHz/shortjobs` 完成，退出码 `0:0`，输出目录为 `output_crackwarp_slurm/v63_single_flow_unet_baseline_1ep`。120 gate 结果为 crack EPE `17.293095`、global EPE `17.663462`、Dice `0.508231`、crack edge `0.525543`、global edge `0.545865`、folding `0.025443`，相对 v41 的逐图对比目录为 `output_crackwarp_slurm/v63_vs_v41_diag`，`120/120` 综合正收益，`mean_score_new_minus_old=172.098982`。但随后人工查看固定可视化 `output_crackwarp_slurm/v63_vs_v41_diag/fixed_visual_gate/index.md` 中的 `crack0063_07`、`crack0039_04` 发现：v63 输出几乎等同 input/恒等映射，没有接近 GT corrected；v22/v41 反而视觉上更接近 GT。当前修正判断：v63 的低 EPE/低 folding 很可能是“输出接近恒等坐标场”导致的指标假阳性，暴露了坐标标签、评估 EPE 与视觉校正目标之间的口径冲突；v63 不能直接扩严格 1000 或写成突破，下一步必须先修正 gate 判据和坐标方向诊断。

2026-06-29 已新增 `utils/diagnose_identity_gate_mismatch.py`，用于诊断 identity、GT label、旧模型和新模型之间的坐标 EPE、位移幅度和 warp 后图像差异。远端已对 `crack0063_07.png` 与 `crack0039_04.png` 生成 `output_crackwarp_slurm/v63_vs_v41_diag/identity_mismatch_probe.csv`。结果显示：`crack0063_07` 的 identity EPE 为 `15.232px`、v63 EPE 为 `15.304px`、v63 平均位移仅 `2.404px`，而 v41 EPE 为 `131.256px`、平均位移 `130.463px`；`crack0039_04` 的 identity EPE 为 `18.855px`、v63 EPE 为 `20.096px`、v63 平均位移 `2.855px`，而 v41 EPE 为 `108.165px`、平均位移 `105.360px`。这证明当前坐标 EPE 指标把接近 identity 的模型判为更优，而 v41 这种视觉上明显校正的模型反而被坐标 EPE 重罚；后续 gate 必须以视觉校正方向重新定义，不能继续用旧 EPE 单独筛模型。

2026-06-29 已将 `utils/diagnose_identity_gate_mismatch.py` 扩展为同时输出逐图 CSV 与 summary JSON，并在固定 40 张可视化集上完成 identity-aware gate：Slurm job `40509260` 正常完成，输出 `output_crackwarp_slurm/v63_vs_v41_diag/identity_gate_fixed40.csv` 与 `identity_gate_fixed40.summary.json`。固定 40 张统计进一步确认旧 gate 口径问题：identity EPE 均值 `15.626854px`；v63 EPE `16.304981px`，仅比 identity 差 `0.678127px`，平均位移只有 `2.365958px`；v22/v41 EPE 约 `115.3px`，平均位移约 `113.9px`。这说明 v63 的旧指标优势来自贴近 identity，而不是产生真实大幅校正。120 张 identity-aware 诊断 job `40507253` 因 30 分钟短时限超时，没有生成 summary；后续如需 120/1000 统计，应先优化脚本批量推理或分片跑。

2026-06-29 已新增 `utils/report_identity_aware_gate.py`，用于读取 identity-aware summary 并生成 gate 判定。已在本地基于固定 40 张 summary 生成 `output_crackwarp_slurm/v63_vs_v41_diag/identity_aware_gate_report_fixed40.json`：v22 判定为 `needs_visual_review`，平均位移 `113.876px`、相对 identity EPE 差 `99.692px`；v41 判定为 `needs_visual_review`，平均位移 `113.868px`、相对 identity EPE 差 `99.686px`；v63 判定为 `reject_identity_like`，平均位移 `2.366px`、相对 identity EPE 差 `0.678px`。当前新的第一道 gate 结论：任何候选若平均位移很低且 EPE 仅贴近 identity，不应视为修复模型，即使旧坐标 EPE/Dice/edge 看起来更好。

2026-06-29 已完成完整 120 张 identity-aware gate。由于单 job `40507253` 超时，已给 `utils/diagnose_identity_gate_mismatch.py` 增加 `--start`，新增 `utils/merge_identity_gate_shards.py` 合并分片；随后将 120 张拆成三片 `start=0/40/80`，Slurm jobs `40514532`、`40514533`、`40514534` 均完成，合并输出 `output_crackwarp_slurm/v63_vs_v41_diag/identity_gate_120.csv`、`identity_gate_120.summary.json` 与 `identity_aware_gate_report_120.json`。完整 120 统计：identity EPE `16.748393px`；v63 EPE `17.664066px`、平均位移 `2.432436px`、相对 identity EPE 差 `0.915673px`，判定 `reject_identity_like`；v22 EPE `107.773815px`、平均位移 `105.915298px`、相对 identity EPE 差 `91.025422px`，判定 `needs_visual_review`；v41 EPE `107.769330px`、平均位移 `105.910994px`、相对 identity EPE 差 `91.020937px`，判定 `needs_visual_review`。当前正式结论：旧 120 坐标 EPE gate 不适合作为单独筛选标准；新候选必须先过 identity-aware gate，否则像 v63 这样贴近 identity 的模型应立即止损。

2026-06-29 v60/v61/v62 第一轮 120 gate 已完成并止损。三个 r2 job 均正常完成：v60 `40500117`、v61 `40500118`、v62 `40500119`，退出码均为 `0:0`，并已生成 120 评估、相对 v41 的逐图对比、分段分析和固定可视化 gate。v41 120 基线为 crack EPE `105.836586`、global EPE `107.724083`、Dice `0.282961`、crack edge `0.342879`、global edge `0.327785`、folding `0.580521`。v60 `output_crackwarp_slurm/v60_failure_family_sampling_from_v41_gate_1ep_r2` 结果为 crack EPE `107.356674`、global EPE `108.811272`、Dice `0.280758`、crack edge `0.331053`、global edge `0.320331`、folding `0.574479`，相对 v41 逐图 `31/120` 更好、`89/120` 更差，mean score `-2.362558`，balanced improved `7/120`。v61 `output_crackwarp_slurm/v61_hard_negative_mix_from_v41_gate_1ep_r2` 结果为 crack EPE `107.363785`、global EPE `109.910774`、Dice `0.278594`、crack edge `0.327447`、global edge `0.314439`、folding `0.576686`，相对 v41 `25/120` 更好、`95/120` 更差，mean score `-3.215103`，balanced improved `1/120`。v62 `output_crackwarp_slurm/v62_background_focus_aug_from_v41_gate_1ep_r2` 结果为 crack EPE `108.837631`、global EPE `110.699509`、Dice `0.277937`、crack edge `0.317427`、global edge `0.313094`、folding `0.570803`，相对 v41 `8/120` 更好、`112/120` 更差，mean score `-5.222450`，balanced improved `1/120`。三者共同结论：数据采样/背景增强能降低 folding，但明显伤 EPE、Dice 与 edge，不满足 120 gate，不扩严格 1000。固定可视化 gate 已生成：`fixed_visual_gate/index.md` 分别位于 v60/v61/v62 输出目录下，每组 40 张同样本横向图；后续可抽看这些图作为“采样/增强没有带来肉眼修复增强”的视觉证据。

2026-06-29 已开始执行“先冲可视化效果和真实修复能力”的 v60-v62 第一轮数据策略实验。远端已同步 `utils/prepare_next_stage_assets.py` 与 `utils/generate_next_stage_commands.py`，并在完整数据环境生成 `output_crackwarp_slurm/next_stage_v60_assets/`：严格 1000 对齐样本 `1000` 张，v41 相对 v22 为 `518` 张更优、`482` 张退化，固定可视化集 `40` 张，同时输出 `v60_failure_families.txt` 与 `v61_hard_negative_images.txt`。首次使用远端 sbatch 提交 `40499724`-`40499726` 时失败，原因是远端训练入口尚未同步 `--background-aug-mode`，且原 sbatch 为 CRLF 换行；已同步 `run_train_slurm.py`、`train_v2.py`、`MyDataSet.py`、`config_crack.py` 并新增 LF 版 `slurm_train_crackwarp_lf.sbatch`。随后 `40499959`-`40499961` 发现 sbatch 模板漏传 `W_CRACK_EDGE_DIR`，已取消，不作为有效 gate。已补齐 `slurm_train_crackwarp.sbatch` 的 `W_CRACK_EDGE_DIR` 默认值、日志输出和 CLI 透传，并重提正确配置 r2：v60 `40500117` 写入 `output_crackwarp_slurm/v60_failure_family_sampling_from_v41_gate_1ep_r2`，v61 `40500118` 写入 `output_crackwarp_slurm/v61_hard_negative_mix_from_v41_gate_1ep_r2`，v62 `40500119` 写入 `output_crackwarp_slurm/v62_background_focus_aug_from_v41_gate_1ep_r2`。v60 已在 `gpuHz` 上运行，日志确认 v41 checkpoint、`W_CRACK_EDGE_DIR=0.01`、hard sampler `matched=229/9324` 正常；v61/v62 因 `QOSMaxCpuPerUserLimit` 排队。后续只先收 120 gate 和固定可视化结果，不直接扩严格 1000。

2026-06-29 已将 Codex 线程 `019ee235-6d28-75f2-ae02-461414bfc815` 的关键上下文导入当前会话。该线程覆盖本项目从远端 Slurm 环境打通、v35-v59 系列实验止损、v41 当前最强候选确认、可视化优先结论，到 v60-v62 下一阶段工具链落地的完整脉络。当前继续以 v41 作为自研主基线，并以“固定可视化集 + 外部 oracle 上界 + 消融可视化支撑 + v60-v62 数据策略”为下一步推进基础。

2026-06-28 已按用户要求整理新的 Markdown 报告 `可视化效果与方法对比报告_2026-06-28.md`，用于说明当前可视化效果、外部方法视觉上界和消融实验可视化支撑。报告引用 `output_crackwarp_slurm/main_external_ablation_v1/` 中的统一对比图，选取 `crack0003_08`、`crack0063_07`、`crack0006_08`、`crack0088_04` 四个代表样本，并明确结论：v41 作为当前量化指标最强自研候选，但肉眼可见修复效果仍不明显；UniMatch/SEA-RAFT oracle-pair 更接近 GT，可作为视觉上界；消融实验不能只看指标，也需要配合同一样本可视化解释。

2026-06-28 已按“先冲可视化效果和真实修复能力”的下一阶段计划落地首批工具链。新增 `utils/prepare_next_stage_assets.py`，可从 v22/v41 的 `eval_per_image.csv` 生成 v41 退化样本分组、`v60_failure_families.txt`、`v61_hard_negative_images.txt` 和 `fixed_visual_cases.txt`；新增 `utils/generate_next_stage_commands.py`，统一打印 v60/v61/v62 的 Slurm 训练、120 对比、分段分析、固定可视化 gate 和严格 1000 后续命令。训练链路新增 `background_aug_mode=standard/focus`，其中 focus 模式用于 v62 的强背景纹理、光斑、散射和弱裂缝增强；`run_train_slurm.py` 与 `slurm_train_crackwarp.sbatch` 已透传 `BACKGROUND_AUG_MODE` 和 `TRAIN_FILTER_LIST`。本地 `python -m py_compile MyDataSet.py train_v2.py run_train_slurm.py config_crack.py utils/prepare_next_stage_assets.py utils/generate_next_stage_commands.py` 通过；因本地 Python/venv 缺 NumPy，未能执行增强 forward smoke。

2026-06-28 用户重新明确当前需求：总体可视化是重点，外部方法和内部模型不仅要有数据对比，也要有可视化结果对比，消融实验也需要可视化效果支撑；如果本方法难以做出明显修复效果，需要尝试其他方法或模型。已新增 `utils/export_visual_comparison_grid.py`，支持同时加载项目 checkpoint 和外部 `.npy` 预测，生成统一横向可视化面板。已在远端完整数据上生成第一版结果 `output_crackwarp_slurm/visual_comparison_focus/main_external_ablation_v1`，并同步到本地 `output_crackwarp_slurm/main_external_ablation_v1/`，包含 8 张代表样本图、`index.md`、`visual_comparison_meta.json` 和 `visual_comparison_summary.csv`。初步观察：v41/v4/消融模型肉眼修复效果仍不明显，UniMatch/SEA-RAFT oracle-pair 更接近 GT，后续汇报应把“可视化效果不足”作为核心事实，并准备尝试其他模型或方法路线。

2026-06-27 已按用户要求将 `项目当前状况与下一阶段方案汇报_2026-06-26.md` 打包为可发送给用户的 zip：`project_status_report_2026-06-27_with_images.zip`。压缩包内包含 1 个 Markdown 报告和报告引用的 7 张 PNG 诊断图片，保留图片在项目内的相对路径，确保 Markdown 中的图片链接不会丢失。已用 Python `zipfile` 校验压缩包条目数为 8，其中 PNG 为 7、Markdown 为 1，压缩包大小约 11.3MB。

2026-06-27 已继续按用户要求在 `项目当前状况与下一阶段方案汇报_2026-06-26.md` 中新增“接手时指标与当前最好指标对比”。该节基于 `output_crackwarp_slurm/v2/compare_old_vs_v2/all_compare.csv` 的 old 侧 1000 样本均值与当前最强 v41 严格 1000 指标进行对比：crack EPE 从 `116.231326` 降至 `110.782702`，global EPE 从 `114.404534` 降至 `109.630041`，Dice 从 `0.252041` 提升至 `0.260970`，crack/global edge fidelity 分别提升至 `0.227109`/`0.256085`；同时明确指出 folding rate 从 `0.569224` 升至 `0.580707`，仍是三区推进的关键短板。

2026-06-27 已按用户要求继续完善 `项目当前状况与下一阶段方案汇报_2026-06-26.md`，在报告中新增第 10 节“关键过程图片与诊断证据”，并将报告更新时间改为 2026-06-27。新增图片覆盖 v2 早期共同失败、v4 鲁棒边缘阶段改善、v4 后仍存在的困难样本、v53 边界坐标正负样本，以及 v57 gating 后仍未形成稳定收益的止损依据。报告现在不仅包含全项目时间线和指标变化，也包含可直接给用户查看的视觉证据链。

2026-06-26 已继续按用户要求补强 `项目当前状况与下一阶段方案汇报_2026-06-26.md` 的“结果与参数效果变化”内容。新增第 5 节“参数与效果变化归纳”，系统总结从旧模型/v2 到 v59 的参数和策略变化：`W_CRACK_MAG` 对恢复幅度和 EPE 的影响、v4 鲁棒位移/过大位移惩罚/edge 组合的稳定收益、v5/v6 Jacobian 与 ROI 坐标对 edge/folding 的改善及 EPE/Dice 代价、v8-v22 边界/方向/困难样本策略、v41 分段训练的当前最强小正收益、v45-v50 soup 未替代 v41、v51-v59 局部 loss 普遍指标冲突。报告现在不仅有时间线，也有从项目初始到当前的结果演进和参数效果归纳。

2026-06-26 已根据用户反馈继续修订 `项目当前状况与下一阶段方案汇报_2026-06-26.md`：删除“对外表达”“用户汇报”等内部提示口径，将第 8 节改为“项目推进时间线”，按日期补充 2026-06-07 至 2026-06-26 的关键改进过程。新增时间线覆盖 v2 正式训练、诊断脚本建立、v3/v4 损失改进、消融和外部 oracle-pair 方法、v5/v6 几何稳定路线、全量 10360 评估、v8/v16/v22 中期优化、v41 当前最强候选、v51-v59 新 loss 止损，以及 2026-06-26 汇报材料整理。该 Markdown 现在更适合作为用户直接阅读的项目全流程报告。

2026-06-26 已按用户要求扩展 `项目当前状况与下一阶段方案汇报_2026-06-26.md`，不再只写最新 v41-v59 阶段，而是补充为项目全周期汇报。新增内容覆盖：项目接手后的数据与工程闭环、v2/v3/v4 早期主线、4 个消融实验、UniMatch/SEA-RAFT oracle-pair 外部参考上界、v5/v6 几何稳定和边缘增强路线、v8/v16/v22 中期候选，以及 v41-v59 当前最新主线和止损结论。该文件现在可作为向用户汇报“从开始到现在整体项目状况”的主材料。

2026-06-26 已新增面向用户汇报的 Markdown 文件 `项目当前状况与下一阶段方案汇报_2026-06-26.md`。该文件以 v41 当前最强候选为主线，汇总了 v41 严格 1000 指标、相对 v22 的 `518/1000` 小幅正收益、v45-v50 checkpoint soup 止损、v51-v59 新 loss 路线止损，以及当前距离三区论文标准的主要差距。文件同时给出下一阶段建议：固化 v41 作为阶段主模型，转向 v41 失败样本分组、数据采样策略和 metric-loss alignment，而不是继续堆局部 loss 小实验。

2026-06-25 v59 已完成并止损：极轻 adaptive edge 没有修复 v51-v58 暴露的 edge/folding 副作用。v59 `output_crackwarp_slurm/v59_adaptive_edge0001_from_v41_smoke_1ep` 从 v41 初始化，保持 v41/v22 主配置，仅新增 `w_crack_edge_adaptive=0.0001`；Slurm job `39706229` 完成且退出码 `0:0`，运行 `00:28:49`。120 结果为 crack EPE `105.854347`、global EPE `107.712502`、Dice `0.282905`、crack edge `0.337737`、global edge `0.321220`、folding `0.581835`。相对 v41，v59 只有 global EPE 略好，但 crack EPE、Dice、crack/global edge 和 folding 均更弱；逐图对比为 `39/120` 更好、`81/120` 更差，`mean_score_new_minus_old=-0.275809`，其中 crack EPE 退化 `55/120`、Dice 退化 `56/120`、crack edge 退化 `80/120`、global edge 退化 `102/120`、folding 退化 `82/120`，balanced improved 仅 `3/120`。当前结论：adaptive edge 与 topology/boundary/laplacian 一样，不能成为三区主线；不应继续扫该权重或扩严格 1000。当前最强候选仍是 v41，下一步应把 v41 作为报告主模型，并围绕“v41 相对 v22 只有 518/1000 小幅胜出、v51-v59 新 loss 均未过 120 闸门、edge/folding/Dice 稳健性不足”整理三区差距与下一阶段路线。

2026-06-25 已完成 v58 相对 v41/v53 的分段族群分析，并提交 v59 轻量 adaptive edge 验证。`utils/analyze_compare_segments.py` 已在远端对 `output_crackwarp_slurm/v58_vs_v41_diag/all_compare.csv` 和 `output_crackwarp_slurm/v58_vs_v53_diag/all_compare.csv` 生成 `segment_analysis/segment_summary.json`。v58 相对 v41：`39/120` 更好、`81/120` 更差，crack EPE 退化 `57/120`、Dice 退化 `56/120`、crack edge 退化 `72/120`、global edge 退化 `95/120`、folding 退化 `54/120`，balanced improved 仅 `3/120`；退化 family 分布较散，`old_better_all` 前列包括 `crack0002`、`crack0006`、`crack0012`、`crack0004`、`crack0001`、`crack0007`，说明 topology anchor 不是少数族群拖累，而是带来系统性 edge/Dice 副作用。v58 相对 v53：`45/120` 更好、`75/120` 更差，crack EPE 退化 `69/120`、crack edge 退化 `57/120`、global edge 退化 `53/120`、folding 退化 `48/120`，balanced improved 仅 `1/120`；进一步确认局部坐标类 loss 应暂停。基于该结论，已提交 v59 `output_crackwarp_slurm/v59_adaptive_edge0001_from_v41_smoke_1ep`，从 v41 初始化，保持 v41/v22 主配置，只新增极轻 `w_crack_edge_adaptive=0.0001`，目的是验证是否能缓解 v51-v58 普遍暴露的边缘一致性副作用；Slurm job `39706229` 已提交到 `gpuHz`/`shortjobs`，当前因 `Resources` 排队。后续等 v59 开始后只确认 CUDA、checkpoint、`w_crack_edge_adaptive=0.0001` 与 `c_edge_adp` 正常输出；跑完后先看 120，只有同时接近/超过 v41 的 crack/global EPE、edge 和 folding 才考虑严格 1000，否则继续以 v41 作为当前最强候选并转向报告/三区差距材料。

2026-06-25 v58 已完成并止损：拓扑锚点坐标一致性没有形成可扩展主信号。v58 `output_crackwarp_slurm/v58_topology_coord002_from_v41_smoke_1ep` 使用 `w_crack_topology_coord=0.02`、关闭 boundary/gated coord，Slurm job `39668899` 完成且退出码 `0:0`；120 结果为 crack EPE `105.895683`、global EPE `107.747215`、Dice `0.282369`、crack edge `0.338796`、global edge `0.322724`、folding `0.580772`。相对 v57，v58 的 global EPE 和 folding 更稳；但相对 v41/v22，crack EPE、Dice、edge 和 folding 仍无优势，也没有保住 v53 的 crack EPE 小正信号。逐图对比显示 v58 相对 v41 为 `39/120` 更好、`81/120` 更差，`mean_score_new_minus_old=-0.289054`；相对 v53 为 `45/120` 更好、`75/120` 更差，`mean_score_new_minus_old=-0.140376`。当前判断：topology anchor 比 gated boundary 在 folding 上稍稳，但仍无法改善 edge/Dice 和整体胜率，不应扩严格 1000；后续应暂停继续堆局部坐标类 loss，转向更强的诊断驱动策略，例如对 v41/v53 的失败族群做定向数据/采样或重新审视 metric/loss 权重平衡。

2026-06-25 已切换到裂缝拓扑锚点坐标一致性路线并提交 v58。由于 v53-v57 的边界坐标/gated boundary coord 只能带来局部修补，且仍易受强背景边缘牵引，当前暂停继续堆边界坐标实验，改为只在稀疏拓扑锚点位置施加轻量坐标监督。已新增默认关闭的 `w_crack_topology_coord`：`loss_crack.py` 新增 `CrackTopologyAnchorCoordinateLoss`，由 soft crack mask 构造高置信裂缝核心、局部邻域差异和 3x3 轻微膨胀的锚点，近似端点/分叉/高曲率区域；`config_crack.py`、`run_train_slurm.py`、`train_v2.py`、`slurm_train_crackwarp.sbatch` 已完成参数透传和日志输出 `c_topo`。本地 `.venv\Scripts\python.exe -m py_compile loss_crack.py train_v2.py run_train_slurm.py config_crack.py` 通过；远端 `.venv/bin/python -m py_compile ...` 通过，小张量 forward/backward 验证 `crack_topology_coord` 可输出并反向传播。已提交 v58 `output_crackwarp_slurm/v58_topology_coord002_from_v41_smoke_1ep`，从 v41 初始化，关闭 boundary/gated coord，启用 `w_crack_topology_coord=0.02`，保留 `w_crack_boundary_jacobian=0.0002` 和 v41/v22 主配置；Slurm job `39668899` 已提交到 `gpuHz`/`shortjobs`，当前因 `Resources` 排队。后续等 v58 开始后只确认 CUDA、checkpoint、`w_crack_topology_coord=0.02` 和 `c_topo` 正常输出；跑完先看 120，若相对 v41/v22 在 crack EPE 或逐图胜率有正信号且 edge/folding 副作用小于 v53，再考虑进一步验证。

2026-06-25 v57 已完成并确认：gated boundary coord 只能带来局部轻微修补，仍不足以成为三区主线。v57 `output_crackwarp_slurm/v57_boundary_coord_gated002_from_v41_smoke_1ep` 使用 `w_crack_boundary_coord_gated=0.02`、关闭原始 `w_crack_boundary_coord=0.0`，Slurm job `39614043` 完成且退出码 `0:0`；120 结果为 crack EPE `106.001511`、global EPE `107.781853`、Dice `0.283215`、crack edge `0.338369`、global edge `0.323218`、folding `0.581361`。相较 v53，crack EPE、edge 和 folding 没有真正缩小，整体仍弱于 v41/v22。随后对 v57 相对 v53 做逐图比较得到 `mean_score_new_minus_old=-0.233039`、`45/120` 更好、`75/120` 更差；人工查看关键图后发现，`crack0003_08` 这类强背景边缘样本依然会被错误牵引，而 `crack0006_08` 这类弱纹理样本只获得很小局部修补，没有把 edge/folding 重新拉回。当前判断：gated boundary coord 比原始 boundary coord 更“局部”，但仍只是局部修补，不足以成为主线；这条边界坐标方向应暂停继续堆实验，后续更适合根据 `v53_vs_v41_diag` / `v57_vs_v53_diag` 中的正负样本做更细归因，或者直接切换到新的局部拓扑/端点一致性约束。

2026-06-25 已基于 v53 正负样本可视化归因新增 gated boundary coord 路线并提交 v57。人工查看本地同步的关键诊断图 `output_crackwarp_slurm/v53_vs_v41_diag_keylooks` 后，发现 v53 的负样本如 `crack0003_08`、`crack0012_08` 中，边界坐标项容易被强背景边缘、光斑或复杂水下纹理牵引，表现为局部 EPE 热区增加、folding 热图更碎、ROI 面板中预测校正图贴向纹理复杂边缘；正样本如 `crack0001_09` 更像是在弱纹理裂缝区域获得轻微局部对齐收益。基于此已新增默认关闭的 `w_crack_boundary_coord_gated`：`loss_crack.py` 新增 `CrackBoundaryGatedCoordinateLoss`，在边界环带上用 `crack_mask^2` 强化高置信裂缝区域，并用输入图灰度梯度反权重抑制强背景纹理牵引；`config_crack.py`、`run_train_slurm.py`、`train_v2.py`、`slurm_train_crackwarp.sbatch` 已完成参数透传和日志输出 `c_bcoord_g`。本地 `.venv\Scripts\python.exe -m py_compile loss_crack.py train_v2.py run_train_slurm.py config_crack.py` 通过；远端 `.venv/bin/python -m py_compile ...` 通过，小张量 forward/backward 验证 `crack_boundary_coord_gated` 可输出并反向传播。已提交 v57 `output_crackwarp_slurm/v57_boundary_coord_gated002_from_v41_smoke_1ep`，从 v41 初始化，关闭原始 `w_crack_boundary_coord=0.0`，启用 `w_crack_boundary_coord_gated=0.02`，保留 `w_crack_boundary_jacobian=0.0002` 和 v41/v22 主配置；Slurm job `39614043` 已在 `gpuHz`/`shortjobs` 运行，节点 `usmidet-com-prod-gpu006`。启动日志确认 CUDA 可用、GPU 为 `NVIDIA L40S`、v41 checkpoint 路径正确、`w_crack_boundary_coord_gated=0.02` 生效。后续等 v57 完成后先看 120；只有相对 v53 保住 crack EPE 小收益，同时相对 v41/v22 缩小 edge/folding 退化，才考虑进一步 1000 或更小范围确认。

2026-06-25 已完成 v53/v56 的逐图归因准备，开始从“边界坐标到底修了什么”入手做样本级分析。通过 `utils/compare_eval_per_image.py` 对 v53 `output_crackwarp_slurm/v53_boundary_coord002_from_v41_smoke_1ep` 相对 v41 的 120 样本逐图比较，得到 `mean_score_new_minus_old=-0.148678`、`55/120` 更好、`65/120` 更差；已导出新旧优势样本清单和诊断目录 `output_crackwarp_slurm/v53_vs_v41_diag`，其中 `new_better` 前列包括 `crack0001_09`、`crack0005_01`、`crack0011_07`、`crack0012_05`、`crack0006_08`，`old_better` 前列包括 `crack0003_08`、`crack0012_08`、`crack0002_09`、`crack0002_03`、`crack0003_05`。v53 的正样本普遍表现为 crack EPE/edge/Dice 小幅同步改善，负样本则集中在 `crack0003_*`、`crack0012_*` 这类样本上，表现为 crack EPE 上升、Dice 下降或 edge 失真。基于这一轮归因，下一步应优先查看 `output_crackwarp_slurm/v53_vs_v41_diag/flow_new_better`、`flow_old_better`、`roi_new_better`、`roi_old_better` 中这些样本的诊断图，判断问题是边界偏移、分叉形变还是局部过拉伸，再决定是否需要一个更温和的 boundary_coord + edge 组合，或者直接切回新的局部结构约束。

2026-06-25 v56 已完成并止损，当前边界坐标路线的最佳点仍是 v53，但还不够三区稳健。v56 `output_crackwarp_slurm/v56_boundary_coord002_no_bjac_from_v41_smoke_1ep` 在保留 `w_crack_boundary_coord=0.02` 的同时关闭 `w_crack_boundary_jacobian=0.0`，Slurm job `39613869` 完成且退出码 `0:0`；120 结果为 crack EPE `106.000465`、global EPE `107.877609`、Dice `0.283386`、crack edge `0.339154`、global edge `0.323128`、folding `0.581525`。相对 v53/v41/v22，v56 在 crack EPE、edge、folding 上均更弱，逐图综合为 `34/120` 更好、`86/120` 更差，mean score `-0.326371`，说明简单移除边界 Jacobian 会破坏整体稳定性。当前判断：边界坐标路线的相对最优点仍是 v53，而不是 v54/v55/v56；但 v53 仍未同时保住 edge/folding，因此不应直接扩严格 1000。后续应围绕 v53 的正负样本做更细的可视化归因，优先分析 `crack0009_06`、`crack0006_08`、`crack0012_06` 这类正收益图和 `crack0003_*`、`crack0012_08` 这类负收益图，再决定是否需要一个更温和的 boundary_coord + edge 组合，或者直接切到新的局部结构约束。

2026-06-25 v55 已完成并止损，随后提交 v56 拆解边界坐标与边界 Jacobian 的耦合影响。v55 `output_crackwarp_slurm/v55_boundary_coord002_edge0035_from_v41_smoke_1ep` 使用 `w_crack_boundary_coord=0.02` 且将 `w_crack_edge` 提到 `0.035`，Slurm job `39613747` 完成且退出码 `0:0`；120 结果为 crack EPE `106.109993`、global EPE `107.836784`、Dice `0.283690`、crack edge `0.337153`、global edge `0.322519`、folding `0.581444`。v55 虽提高 Dice，但 crack/global EPE、edge 与 folding 均弱于 v41/v22/v53，说明提高 edge 权重没有修复 v53 的 edge/folding 副作用，反而破坏 crack EPE，不扩严格 1000。当前判断：v53 的小正信号可能来自 `w_crack_boundary_coord=0.02`，但它和 `w_crack_boundary_jacobian=0.0002` 叠加后可能带来 edge/folding 副作用；因此已提交 v56 `output_crackwarp_slurm/v56_boundary_coord002_no_bjac_from_v41_smoke_1ep`，从 v41 初始化，保持 `w_crack_boundary_coord=0.02`，关闭 `w_crack_boundary_jacobian=0.0`，其余沿用 v41/v22 主配置。Slurm job `39613869` 已提交到 `gpuHz`/`shortjobs`，当前因 `Resources` 排队。后续等 v56 开始后只确认 CUDA、checkpoint、`w_crack_boundary_coord=0.02`、`w_crack_boundary_jacobian=0.0` 与 `c_bcoord` 正常输出；跑完先看 120，若能保住 v53 的 crack EPE/逐图胜率并减少 edge/folding 退化，再考虑下一步严格 1000 或小范围确认。

2026-06-25 v54 已完成并止损，随后提交 v55 组合修正。v54 `output_crackwarp_slurm/v54_boundary_coord0005_from_v41_smoke_1ep` 使用 `w_crack_boundary_coord=0.005` 从 v41 初始化，Slurm job `39512819` 完成且退出码 `0:0`；120 结果为 crack EPE `105.935326`、global EPE `107.764229`、Dice `0.282900`、crack edge `0.338120`、global edge `0.322534`、folding `0.581138`。相对 v53，v54 降权后未保住 crack EPE 正信号，edge/folding 仍退化；逐图综合相对 v41 为 `35/120` 更好、`85/120` 更差，mean score `-0.149573`，因此不扩严格 1000，也不继续单纯扫描 `w_crack_boundary_coord`。当前判断：v53 的 `bcoord=0.02` 能带来 crack EPE 与逐图胜率小正信号，但需要额外保护 edge/folding；v54 说明简单降权不能解决。已提交 v55 `output_crackwarp_slurm/v55_boundary_coord002_edge0035_from_v41_smoke_1ep`，从 v41 初始化，保持 `w_crack_boundary_coord=0.02`，将 `w_crack_edge` 从 `0.03` 提到 `0.035`，其余沿用 v41/v22 主配置；Slurm job `39613747` 已提交到 `gpuHz`/`shortjobs`。后续等 v55 开始后只确认 CUDA、checkpoint、`w_crack_boundary_coord=0.02`、`w_crack_edge=0.035` 和 `c_bcoord` 正常输出；跑完后先看 120，只有同时保住 v53 的 crack EPE 小收益并明显恢复 edge/folding，才考虑严格 1000。

2026-06-24 v53 已完成，边界坐标约束出现小正信号但仍不足以扩严格 1000。v53 `output_crackwarp_slurm/v53_boundary_coord002_from_v41_smoke_1ep` 使用 `w_crack_boundary_coord=0.02` 从 v41 初始化，Slurm job `39499164` 完成且退出码 `0:0`；训练日志确认 `c_bcoord` 正常输出，Best Val EPE `112.090px`、Best Crack EPE `114.440px`。120 结果为 crack EPE `105.833572`、global EPE `107.769844`、Dice `0.282586`、crack edge `0.340553`、global edge `0.323255`、folding `0.580780`。相对 v41/v22，v53 的 crack EPE 略好，且逐图综合为 `67/120` 更好、`53/120` 更差，但 mean score `-0.050231`，global EPE、Dice、edge 和 folding 仍退化，因此不扩严格 1000。当前判断：边界坐标方向比 Laplacian 更有希望，但 `0.02` 权重偏强，副作用集中在 `crack0003`、`crack0012` 等样本。已提交更保守的 v54 `output_crackwarp_slurm/v54_boundary_coord0005_from_v41_smoke_1ep`，从 v41 初始化，保持 v41/v22 主配置并将 `--w-crack-boundary-coord` 降到 `0.005`，继续保留 `--w-crack-boundary-jacobian 0.0002`；Slurm job `39512819` 已提交到 `gpuHz`/`shortjobs`，当前因 `Resources` 排队。后续等 v54 开始后只确认 CUDA、checkpoint、`w_crack_boundary_coord=0.005` 与 `c_bcoord` 正常输出；跑完先看 120，只有同时接近/超过 v41 的 crack EPE、edge 和 folding 时才考虑严格 1000。

2026-06-24 v52 已完成并止损，随后转入更直接的边界拓扑/边界坐标约束。v52 `output_crackwarp_slurm/v52_lap0001_from_v41_smoke_1ep` 使用 `w_crack_laplacian=0.001` 从 v41 初始化，Slurm job `39475406` 完成且退出码 `0:0`；120 结果为 crack EPE `105.844414`、global EPE `107.818695`、Dice `0.282398`、crack edge `0.338859`、global edge `0.322422`、folding `0.581140`。相对 v41/v22，v52 虽然 crack EPE 接近，但 global EPE、Dice、edge 和 folding 均退化，因此不扩严格 1000；v51/v52 共同说明 Laplacian 二阶结构项当前不应继续做权重扫描。已新增默认关闭的裂缝边界窄环带坐标一致性损失 `w_crack_boundary_coord`：`loss_crack.py` 新增 `CrackBoundaryCoordinateLoss`，复用裂缝 mask 膨胀/腐蚀形成的边界环带，只在边界附近比较预测/GT 逆映射坐标；`config_crack.py`、`run_train_slurm.py`、`train_v2.py` 和 `slurm_train_crackwarp.sbatch` 已完成参数透传与日志输出 `c_bcoord`。本地 `.venv\Scripts\python.exe -m py_compile loss_crack.py train_v2.py run_train_slurm.py config_crack.py` 通过；远端 `.venv/bin/python -m py_compile ...` 通过，小张量 forward/backward 验证 `crack_boundary_coord` 可输出并反向传播。已提交 v53 `output_crackwarp_slurm/v53_boundary_coord002_from_v41_smoke_1ep`，从 v41 初始化、`EPOCHS=1`、`LR=1e-7`、保持 v41/v22 主配置，关闭 Laplacian，新增 `--w-crack-boundary-coord 0.02` 并保留 `--w-crack-boundary-jacobian 0.0002`；Slurm job `39499164` 已提交到 `gpuHz`/`shortjobs`，当前因 `Resources` 排队。后续等 v53 开始后只确认 CUDA、checkpoint、`w_crack_boundary_coord=0.02` 与 `c_bcoord` 正常输出；跑完先看 120，只有明确优于 v41/v22 才扩严格 1000。

2026-06-24 已基于 v41 相对 v22 的严格 1000 退化样本完成第一轮归因，并开始新的局部结构一致性路线。已重新生成 v41 vs v22 严格 1000 对比与分段：v41 相对 v22 为 `518/1000` 更优、`482/1000` 更差，`mean_score_new_minus_old=+0.009029`；old-better 退化段共 `482` 张，均值上 crack EPE、global EPE、Dice、crack/global edge 和 folding 都小幅变差，top 退化家族包括 `crack0086`、`crack0039`、`crack0004`、`crack0036`、`crack0009`。已导出 v22 vs v41 的 flow 诊断：`flow_diag_v22_vs_v41_old_better_all_top40` 与 `flow_diag_v22_vs_v41_crack_epe_regressed_top40`，jobs `39430545`、`39430546` 均完成且退出码 `0:0`。诊断均值显示 old-better top40 中 v41 相对 v22 的 global EPE `+0.009189`、crack EPE `+0.015732`、folding `+0.000328`、mean displacement `+0.007581px`、p95 displacement `+0.062387px`，说明综合退化常伴随轻微位移上冲和 folding 增加；crack-EPE-regressed top40 中 v41 的 global EPE 反而 `-0.017613`、mean/p95 displacement 分别 `-0.012498px/-0.026108px`，但 crack EPE `+0.026021`、folding `+0.000142`，说明局部裂缝结构/拓扑错位不能只靠继续压位移解决。基于此已新增默认关闭的裂缝 ROI 二阶结构一致性损失 `w_crack_laplacian`：`loss_crack.py` 新增 `WarpedImageLaplacianConsistencyLoss`，比较预测/GT 校正图在裂缝 ROI 内的拉普拉斯响应；`config_crack.py`、`run_train_slurm.py`、`train_v2.py` 已完成参数透传和日志输出 `c_lap`。本地 `.venv\Scripts\python.exe -m py_compile loss_crack.py train_v2.py run_train_slurm.py config_crack.py` 通过，远端 `.venv/bin/python -m py_compile ...` 与小张量 forward/backward 验证通过，`crack_laplacian` 可反向传播。v51 `output_crackwarp_slurm/v51_lap0005_from_v41_smoke_1ep` 已完成，Slurm job `39432141` 与 120 评估 job `39457251` 均退出码 `0:0`；120 结果为 crack EPE `105.961197`、global EPE `107.868172`、Dice `0.283231`、crack edge `0.342498`、global edge `0.328161`、folding `0.580665`。相对 v41 120（crack EPE `105.836586`、global EPE `107.724083`、Dice `0.282961`、crack edge `0.342879`、global edge `0.327785`、folding `0.580521`），v51 只在 Dice 和 global edge 有很小提升，但 EPE、crack edge 与 folding 退化，因此不扩严格 1000。已提交更轻的 v52 `output_crackwarp_slurm/v52_lap0001_from_v41_smoke_1ep`，从 v41 初始化、`EPOCHS=1`、`LR=1e-7`、保持 v41/v22 主配置并将 `--w-crack-laplacian` 降到 `0.001`；Slurm job `39475406` 已提交到 `gpuHz`/`shortjobs`，当前因 `Resources` 排队。后续等 v52 开始后只确认 CUDA、checkpoint 和 `w_crack_laplacian=0.001` 正常输出；跑完后先看 120，只有明确优于 v41/v22 才扩严格 1000。

2026-06-24 已完成 adaptive over-mag、adaptive edge、边界 Jacobian 加强与边界 Jacobian 降压路线的阶段性止损，并完成首轮分段样本策略 v41/v42/v43/v44。v35/v36 adaptive over-mag、v37/v38 adaptive edge、v39/v40 边界 Jacobian 加强/降压均未超过 v22。v41 `output_crackwarp_slurm/v41_segment_top5_oldbetter_from_v22_5ep` 使用 v40-v22 old-better top5 家族过滤训练，匹配 `47/9324` 个训练样本；v41 严格 1000 为 crack EPE `110.782702`、global EPE `109.630041`、Dice `0.260970`、crack edge `0.227109`、global edge `0.256085`、folding `0.580707`，相对 v22 为 `518/1000` 更优、`482/1000` 更差，`mean_score_new_minus_old=+0.009029`，仍是当前最强候选。v42/v43/v44 均未超过 v41/v22，`train_filter_list` 过滤训练路线已经止损。已完成低成本 checkpoint soup 验证：v45/v46/v47 分别使用 v22/v41 比例 0.75/0.25、0.50/0.50、0.25/0.75；v47 120 有正信号，但严格 1000 为 crack EPE `110.784101`、global EPE `109.630641`、Dice `0.260959`、crack edge `0.227045`、global edge `0.256023`、folding `0.580764`，相对 v41 为 `493/1000` 更优、`507/1000` 更差，`mean_score_new_minus_old=-0.007325`，不能替代 v41。随后完成靠近 v41 的 soup 细扫：v48 `output_crackwarp_slurm/v48_soup_v22_015_v41_085` 使用 v22/v41=0.15/0.85，v49 使用 0.10/0.90，v50 使用 0.05/0.95。v48 120 最优，但严格 1000 jobs `39429838`-`39429847` 全部完成并合并后，结果为 crack EPE `110.783743`、global EPE `109.632408`、Dice `0.260956`、crack edge `0.227115`、global edge `0.256055`、folding `0.580745`；相对 v22 为 `517/1000` 更优、`483/1000` 更差，`mean_score_new_minus_old=+0.004649`，相对 v47 为 `507/1000` 更优、`493/1000` 更差，`mean_score_new_minus_old=+0.002945`，但相对 v41 为 `489/1000` 更优、`511/1000` 更差，`mean_score_new_minus_old=-0.004380`。当前结论：soup 路线也已止损，v48 只能作为略强于 v22/v47 的平滑参考，不能替代 v41；v41 仍是当前最强候选。下一步应停止继续提交 soup 或过滤训练变体，转向新的局部结构一致性约束、边界/裂缝拓扑一致性诊断，或围绕 v41 相对 v22 的 1000 严格退化样本做可视化归因后再设计新 loss。

2026-06-23 已修正本地 Slurm 悬浮窗最近结束时间的时区换算错误。经远端验证，服务器当前 `date` 为 `-0400 EDT`，原始 `sacct End` 如 job `38789184` 为 `2026-06-22T21:34:57`；设置 `TZ=Asia/Shanghai` 后，`sacct End` 正确变为 `2026-06-23T09:34:57`。此前本地程序只对原始 End 加 8 小时，导致中国时间少算 4 小时。现已在远端查询脚本中加入 `export TZ=Asia/Shanghai`，让 `date` 与 `sacct` 直接输出中国时区；本地 `to_china_time()` 不再加时差，只负责把 `T` 替换为空格并追加 `CST`。真实查询验证显示 `38789184` 显示为 `2026-06-23 09:34:57 CST`，`38668258` 显示为 `2026-06-23 06:40:28 CST`，且最新结束仍排在前面。已重新打包到 `dist_timeout30_countfix_cst2\SlurmFloatingStatus\SlurmFloatingStatus.exe`，`scripts\start_slurm_floating_status_exe.bat` 已指向该最终版。

2026-06-23 已按用户反馈优化本地 Slurm 悬浮窗“最近结束/失败 sacct”区域。远端 sacct 查询由原来的 `tail -n 12` 改为按第 7 列 `End` 倒序排序后取最新 12 条，即最新结束的作业显示在最前面；本地格式化新增 `format_sacct_line()` 与 `to_china_time()`，将 sacct 的 `End` 字段转换为中国时间显示，格式为 `YYYY-MM-DD HH:MM:SS CST`，表头同步显示为 `End(中国时间)`。已通过 `.venv\Scripts\python.exe -m py_compile scripts\slurm_floating_status.py` 和模拟 sacct 行验证，并重新打包到 `dist_timeout30_countfix_cst\SlurmFloatingStatus\SlurmFloatingStatus.exe`；`scripts\start_slurm_floating_status_exe.bat` 已指向该最新版。

2026-06-23 已修复本地 Slurm 悬浮窗“活跃作业个数”和正文不一致的问题。问题原因是 `format_remote_output()` 在空队列时会插入“当前没有 squeue 作业。”提示行，而 `count_active_jobs()` 之前只跳过表头，误把该提示行计为 1 个活跃作业。现已将 `count_active_jobs()` 改为只统计 squeue 区域中以数字 JobID 开头的真实作业行，并加入中文注释说明该边界场景。已通过 `.venv\Scripts\python.exe -m py_compile scripts\slurm_floating_status.py` 和模拟空队列/两作业场景验证：空队列计数为 0，两条作业计数为 2。已重新打包到 `dist_timeout30_countfix\SlurmFloatingStatus\SlurmFloatingStatus.exe`，并将 `scripts\start_slurm_floating_status_exe.bat` 指向该“30 秒超时 + 计数修复”版本。

2026-06-23 已按用户截图反馈调整本地 Slurm 悬浮窗 SSH 查询超时。`scripts/slurm_floating_status.py` 将 `SSH_TIMEOUT_SECONDS` 从 12 秒改为 30 秒，避免公司网络或远端 Slurm 响应稍慢时过早显示“SSH 查询超过 12 秒未返回”。已通过 `.venv\Scripts\python.exe -m py_compile scripts\slurm_floating_status.py` 语法检查。由于旧 `dist\SlurmFloatingStatus` 目录非空且可能仍被运行实例占用，未删除旧包，改用 `dist_timeout30\SlurmFloatingStatus\SlurmFloatingStatus.exe` 生成 30 秒超时新版，并将 `scripts\start_slurm_floating_status_exe.bat` 指向该新版 exe。

2026-06-23 已完成 v33 对目标 old-better 子集的局部诊断，并完成更保守 v34 训练与 120 样本评估。v33-v22 old-better 子集 flow 诊断 job `38668258` 已完成，退出码 `0:0`，处理 60 张 v22 相对 v16 退化样本；v33 相对 v22 在该子集上 global EPE 从 112.222350 降到 111.917436、crack EPE 从 98.133773 降到 97.392449，mean displacement 从 110.614673px 降到 110.319147px，p95 displacement 从 216.983039px 降到 216.844998px，但 folding 从 0.399367 小升到 0.399534。当前判断：v33 的极轻 hard-sample 确实修复了目标退化子集中的位移上冲和 EPE，但对整体 120 样本造成 edge/folding 副作用，因此不能扩严格 1000。基于该局部正信号，已完成更保守的 v34 `output_crackwarp_slurm/v34_v22_oldbetter_hard_w101_lr1e7_from_v22_smoke_1ep`，从 v22 初始化，沿用 v22 主配置，但将 `HARD_SAMPLE_WEIGHT` 从 1.02 降到 1.01、`LR` 从 2e-7 降到 1e-7；Slurm job `38789184` 完成且退出码 `0:0`，训练日志为 Val EPE 112.193px、CrackEPE 114.488px。120 评估 job `38864746` 已完成，结果为 crack EPE 106.159615、global EPE 107.965004、Dice 0.284029、crack edge 0.339831、global edge 0.326265、folding 0.581005。相对 v22，v34 的 crack/global EPE、Dice、crack/global edge 和 folding 均未形成优势；相对 v33，v34 也没有改善主 EPE 和 folding。当前结论：hard-sample 加权路线虽能局部修 old-better 子集，但整体指标副作用无法消除，后续不应继续调整 hard-sample 权重或扩严格 1000，应停止该路线，转向局部结构一致性或自适应位移/边界约束。

2026-06-23 已完成 v22 分段诊断、v33 针对性 hard-sample smoke 训练和 120 样本评估。v22 相对 v16 的分段 flow 诊断 jobs `38367124`、`38367125` 均已完成，old-better 退化组 30 张中，v22 相比 v16 的 global EPE 从 110.810960 升到 111.741332、crack EPE 从 89.811572 升到 90.973733，但 folding 从 0.400452 降到 0.399331，mean displacement 从 109.361815px 升到 110.236486px、p95 displacement 从 214.915099px 升到 215.830917px；balanced 组 30 张中，v22 的 global EPE 从 109.948095 降到 109.818736、crack EPE 从 118.267325 降到 117.606511、folding 从 0.397994 降到 0.396924，mean/p95 displacement 分别从 108.123130px/213.471007px 降到 108.026446px/213.258230px。当前判断：v22 的平衡收益来自轻微位移收缩与 EPE/folding 同降；退化组不是 folding 失控，而是位移幅度小幅上冲导致局部 EPE 变差。基于该结论已完成 v33 `output_crackwarp_slurm/v33_v22_oldbetter_hard_w102_from_v22_smoke_1ep`，从 v22 初始化，使用 v22-v16 old-better 清单、`HARD_SAMPLE_WEIGHT=1.02`、`LR=2e-7`、`EPOCHS=1`，其余主配置沿用 v22；Slurm job `38372454` 完成且退出码 `0:0`，hard sampler 匹配 `446/9324`，训练日志为 Val EPE 112.168px、CrackEPE 114.488px。120 评估 job `38413374` 已完成且退出码 `0:0`，结果为 crack EPE 106.057404、global EPE 107.862572、Dice 0.284838、crack edge 0.339130、global edge 0.326867、folding 0.580784。相对 v22 的 120 样本结果，v33 仅 Dice 更高，但 crack/global EPE、crack/global edge 和 folding 均更差，因此不扩严格 1000。当前结论：极轻 hard-sample 针对 v22 退化样本仍未形成整体正收益，下一步需要确认它是否至少修复了目标 old-better 子集；若目标子集也未改善，应停止 hard-sample 加权路线，转向更明确的局部结构一致性或自适应位移约束。

2026-06-22 已完成 v32 中间 edge 权重 smoke。v32 `output_crackwarp_slurm/v32_edge_w0035_dir001_from_v16_1ep_smoke_1ep` 从 v16 1ep 初始化，配置为 `EPOCHS=1`、`LR=5e-7`、`W_CRACK_EDGE=0.035`、`W_CRACK_EDGE_DIR=0.01`、`W_JACOBIAN=0.0005`、`W_CRACK_COORD_EXTRA=0.1`、`W_CRACK_BOUNDARY_JACOBIAN=0.0002`、`W_CRACK_MAG=0.06`、`CRACK_MAG_OVER_WEIGHT=0.5`。训练 job `38347824` 已完成，退出码 `0:0`，训练日志为 Val EPE 112.231px、CrackEPE 114.435px；120 评估 job `38358412` 已完成，结果为 crack EPE 106.240257、global EPE 108.086151、Dice 0.284370、crack edge 0.337993、global edge 0.325749、folding 0.580410。相对 v22 的 120 样本结果，v32 仅 Dice 略高，但 crack EPE、global EPE、crack edge、global edge 与 folding 均未超过 v22，因此不扩严格 1000。当前判断：`W_CRACK_EDGE=0.035/0.04` 这条 edge 权重扫描已经不能带来三区所需的稳定收益，应停止围绕 v22 的小幅权重微调；当前主候选仍为 v22，下一步应转向样本难度分段、局部结构一致性或更有针对性的可逆位移场约束。

2026-06-22 已完成 v31 edge 权重增强 smoke。v31 `output_crackwarp_slurm/v31_edge_w004_dir001_from_v16_1ep_smoke_1ep` 将 v22 的 `W_CRACK_EDGE` 从 0.03 提高到 0.04，`W_CRACK_EDGE_DIR=0.01` 不变，训练 job `38314116` 完成且退出码 `0:0`，训练日志为 Val EPE 112.224px、CrackEPE 114.368px；120 评估 job `38346102` 完成，结果为 crack EPE 105.850426、global EPE 107.937355、Dice 0.282477、crack edge 0.342458、global edge 0.327448、folding 0.579648。相对 v22 的 120 样本结果，v31 仅 folding 更好，但 crack EPE、crack edge、Dice、global EPE 和 global edge 均未超过 v22，因此不扩严格 1000。当前判断：单纯将 `W_CRACK_EDGE` 提到 0.04 会改善 folding，但会牺牲主体 EPE 和 Dice；下一步可尝试中间值 `W_CRACK_EDGE=0.035`，若仍不能超过 v22，应停止 edge 权重扫描。

2026-06-22 已完成 v30 edge-dir 加强 smoke。v30 `output_crackwarp_slurm/v30_edge_dir0015_from_v16_1ep_smoke_1ep` 将 v22 的 `W_CRACK_EDGE_DIR` 从 0.01 提高到 0.015，其余主配置保持 v22，训练 job `38301032` 完成且退出码 `0:0`，训练日志为 Val EPE 112.027px、CrackEPE 114.396px；120 评估 job `38312638` 完成，结果为 crack EPE 106.124123、global EPE 107.760841、Dice 0.284742、crack edge 0.339301、global edge 0.327506、folding 0.580656。v30 相比 v22 的 Dice 更高，但 crack EPE、crack edge、global EPE 和 folding 均未超过 v22，因此不扩严格 1000。当前判断：edge-dir 强度从 0.005、0.01 到 0.015 的扫描显示 v22 的 0.01 仍是局部最佳；下一步若继续 smoke，应优先测试 `W_CRACK_EDGE` 本身、样本分段或新的局部结构一致性策略，而不是继续加大 edge-dir。

2026-06-22 已完成 v28/v29 的 `W_CRACK_COORD_EXTRA` 双向扫描。v28 `output_crackwarp_slurm/v28_edge_dir001_coord005_from_v16_1ep_smoke_1ep` 将 v22 的 `W_CRACK_COORD_EXTRA` 从 0.1 降到 0.05，训练 job `38227960` 完成且退出码 `0:0`；120 评估 job `38286093` 完成，结果为 crack EPE 106.050812、global EPE 107.832108、Dice 0.283058、crack edge 0.340726、global edge 0.327802、folding 0.580203。v28 的 folding 与 global edge 较稳，但 crack EPE、crack edge 与 global EPE 仍未超过 v22，因此不扩严格 1000。v29 `output_crackwarp_slurm/v29_edge_dir001_coord015_from_v16_1ep_smoke_1ep` 反向将 `W_CRACK_COORD_EXTRA` 提到 0.15，训练 job `38298245` 完成且退出码 `0:0`；120 评估 job `38300101` 完成，结果为 crack EPE 106.428238、global EPE 107.959518、Dice 0.284897、crack edge 0.336416、global edge 0.324202、folding 0.580711。v29 虽 Dice 较高，但 EPE/edge 全面弱于 v22，不扩严格 1000。当前判断：围绕 v22 的 `coord_extra` 小范围扫描不能带来三区所需的稳定增益，应停止该分支；若继续 smoke，应转向 edge-dir 强度、结构方向约束形式、样本分段或新的局部一致性策略。

2026-06-22 已完成 v26/v27 后续判断并提交 v28。v26 `output_crackwarp_slurm/v26_edge_dir001_over06_from_v16_1ep_smoke_1ep` 使用 v22 主配置但将 `CRACK_MAG_OVER_WEIGHT` 从 0.5 微调到 0.6，训练 job `38095455` 完成且退出码 `0:0`；120 评估 job `38153212` 完成，结果为 crack EPE 106.135185、global EPE 107.869186、Dice 0.283999、crack edge 0.339977、global edge 0.327389、folding 0.580870。v26 相比 v24/v25 稍稳，但仍未超过 v22 的 crack EPE 与 crack edge，因此不扩严格 1000。随后提交 v27 `output_crackwarp_slurm/v27_edge_dir001_jac0003_from_v16_1ep_smoke_1ep`，保持 v22 edge-dir 配置但将 `W_JACOBIAN` 从 0.0005 降至 0.0003，训练 job `38161254` 完成；120 评估 job `38217941` 完成，结果为 crack EPE 106.071091、global EPE 107.959030、Dice 0.284333、crack edge 0.338041、global edge 0.324896、folding 0.580880。v27 Dice 略强于 v22，但 crack EPE、edge、global EPE 和 folding 均未超过 v22，因此不扩严格 1000。为继续测试是否能追回 Dice 且保留 edge-dir 收益，已提交 v28 `output_crackwarp_slurm/v28_edge_dir001_coord005_from_v16_1ep_smoke_1ep`，配置为 v22 主配置但将 `W_CRACK_COORD_EXTRA` 从 0.1 降至 0.05；Slurm job `38227960` 已提交到 `gpuHz`，当前等待/启动中。下一步等待 v28 完成并跑 120 评估；若仍不明显优于 v22，应停止这一组局部权重微调，转向新约束或样本分段策略。

2026-06-22 已完成 v24/v25 后续判断并提交 v26。v24 `output_crackwarp_slurm/v24_edge_dir001_over075_from_v16_1ep_smoke_1ep` 使用 `W_CRACK_EDGE_DIR=0.01`、`CRACK_MAG_OVER_WEIGHT=0.75`、`LR=5e-7`，训练 job `38050494` 完成且退出码 `0:0`，训练日志为 Val EPE 112.292px、CrackEPE 114.644px；120 评估 job `38056195` 完成，结果为 crack EPE 106.487923、global EPE 108.143997、Dice 0.282670、crack edge 0.337600、global edge 0.325239、folding 0.581542。当前判断：v24 的 over-weight=0.75 过强，未压出有效收益，EPE、edge、Dice、folding 均弱于 v22，不扩严格 1000。随后提交 v25 `output_crackwarp_slurm/v25_edge_dir001_lr2e7_from_v16_1ep_smoke_1ep`，回到 `CRACK_MAG_OVER_WEIGHT=0.5` 但将 `LR` 降为 `2e-7`，训练 job `38056215` 完成且退出码 `0:0`；120 评估 job `38089423` 完成，结果为 crack EPE 106.128029、global EPE 107.918297、Dice 0.284439、crack edge 0.337698、global edge 0.325386、folding 0.580529。v25 比 v24 稳，但仍未超过 v22 的 crack EPE 105.840302 与 crack edge 0.342899，因此不扩严格 1000。为继续沿 v22 微调方向压制位移过冲，已提交 v26 `output_crackwarp_slurm/v26_edge_dir001_over06_from_v16_1ep_smoke_1ep`，配置为从 v16 1ep 初始化、`EPOCHS=1`、`LR=5e-7`、`W_CRACK_EDGE_DIR=0.01`、`CRACK_MAG_OVER_WEIGHT=0.6`、其余主配置同 v22；Slurm job `38095455` 已在 `gpuHz` 运行，日志确认 checkpoint 加载正常。下一步等待 v26 完成后做 120 评估；只有明显优于 v22/v16 时才扩严格 1000。

2026-06-22 已完成 v23 edge-dir 降权 smoke、v22/v16 flow 诊断，并提交 v24。v23 `output_crackwarp_slurm/v23_edge_dir0005_from_v16_1ep_smoke_1ep` 使用 `W_CRACK_EDGE_DIR=0.005`、其余主配置同 v22，Slurm job `38047355` 完成且退出码 `0:0`，训练日志为 Val EPE 112.121px、CrackEPE 114.514px；120 评估 job `38050482` 完成，结果为 crack EPE 106.082573、global EPE 107.800522、Dice 0.284235、crack edge 0.339801、global edge 0.327640、folding 0.580171。当前判断：v23 比 v22 保守但未超过 v16/v22，不扩严格 1000。v22 vs v16 flow 诊断 jobs `38047356`-`38047358` 均完成；new-better 组 v22 相比 v16 global EPE 115.978->115.197、crack EPE 105.677->105.019、folding 0.400989->0.400858、mean/p95 displacement 115.532/222.476->114.825/221.493；old-better 组 global EPE 109.998->111.047、crack EPE 91.963->93.273、folding 0.399387->0.398273，但 mean/p95 displacement 108.918/213.575->109.944/214.328；joint-failure 组 EPE、folding 和位移幅度均小幅下降。诊断结论：v22 的有效收益来自轻微位移收缩与 edge/方向约束共同降低 EPE/folding；退化样本虽然 folding 下降，但位移幅度上冲导致 EPE 变差。基于此已提交 v24 `output_crackwarp_slurm/v24_edge_dir001_over075_from_v16_1ep_smoke_1ep`，保持 `W_CRACK_EDGE_DIR=0.01`，将 `CRACK_MAG_OVER_WEIGHT` 从 0.5 提到 0.75，以压制 old-better 组的位移过冲；job `38050494` 已在 `gpuHz` 运行，参数打印确认正确。

2026-06-22 已按用户反馈加宽并重新打包本地 Slurm 悬浮窗 exe。`scripts/slurm_floating_status.py` 将紧凑窗口从 `420x150` 调整为 `620x180`，并把任务文本 `wrap` 从 `char` 改为 `none`，避免 4 个作业和长等待原因 `QOSMaxCpuPerUserLimit` 显示不完整或自动断行。已通过 `.venv\Scripts\python.exe -m py_compile scripts\slurm_floating_status.py` 和真实 `query_remote_slurm()` 查询验证，并使用 PyInstaller 6.21.0 重新生成 `dist/SlurmFloatingStatus/SlurmFloatingStatus.exe`；新版 exe 已启动。

2026-06-22 已在 v22 严格 1000 微弱胜过 v16 后继续推进下一步。已提交 v23 `output_crackwarp_slurm/v23_edge_dir0005_from_v16_1ep_smoke_1ep`，从 v16 1ep 初始化，配置为 `W_CRACK_EDGE_DIR=0.005`、其余主配置同 v22，用于验证裂缝梯度方向项减半后能否保留 edge/crack EPE 收益并减少 Dice/folding 代价；Slurm job `38047355` 已在 `gpuHz` 运行，参数打印确认正确。另已提交 v22 vs v16 的 flow 诊断 jobs `38047356`、`38047357`、`38047358`，分别导出 new-better、old-better、joint-failure 各 20 张诊断图；当前因 `QOSMaxCpuPerUserLimit` 暂时排队，等待 v23 释放资源后继续。

2026-06-22 已完成 v22 裂缝梯度方向一致性 smoke 与严格 1000 评估。新增 `w_crack_edge_dir` 默认关闭参数，`loss_crack.py` 中新增 `WarpedImageGradientDirectionLoss`，比较预测/GT 校正图在裂缝 ROI 内的灰度梯度方向；`train_v2.py`、`run_train_slurm.py`、`config_crack.py` 已完成参数透传和日志打印。远端 `.venv/bin/python -m py_compile loss_crack.py train_v2.py run_train_slurm.py config_crack.py` 通过，并用小张量 forward 验证 `crack_edge_dir` 正常。v22 job `38043094` 使用 v16 1ep 初始化，配置为 `EPOCHS=1`、`LR=5e-7`、`W_CRACK_EDGE_DIR=0.01`、`W_CRACK_EDGE=0.03`、`W_JACOBIAN=0.0005`、`W_CRACK_BOUNDARY_JACOBIAN=0.0002`、`W_CRACK_COORD_EXTRA=0.1`、`W_CRACK_MAG=0.06`、`CRACK_MAG_OVER_WEIGHT=0.5`，训练完成且退出码 `0:0`；训练日志为 Val EPE 112.061px、CrackEPE 114.362px。120 样本评估 job `38047328` 完成，结果为 crack EPE 105.840302、global EPE 107.726341、Dice 0.283035、crack edge 0.342899、global edge 0.327734、folding 0.580392。随后严格 1000 分片 jobs `38047330`-`38047339` 全部完成并合并，结果为 crack EPE 110.788558、global EPE 109.633080、Dice 0.260950、crack edge 0.227076、global edge 0.256024、folding 0.580683。逐图对比显示 v22 相对 v16 1ep 为 510/1000 更优、490/1000 更差，`mean_score_new_minus_old=0.016009`；相对 v11-C 1ep 为 535/1000 更优、465/1000 更差，`mean_score_new_minus_old=0.016982`；相对 v8 为 585/1000 更优、415/1000 更差，`mean_score_new_minus_old=0.433224`。当前结论：v22 方向项有效提升 crack edge 与 crack EPE，并对 v16/v11-C 形成微弱综合正收益，但 folding 和 Dice 未同步改善，暂可保留为最新候选，不宜直接扩长 epoch；下一步更适合做 v22 vs v16 的 flow/ROI 诊断，确认收益来自真实裂缝结构对齐而非边缘强度偏置，再考虑更小 `W_CRACK_EDGE_DIR=0.005` 或与 folding guard 组合。

2026-06-22 已按用户要求取消远端 Slurm PushPlus 任务监控。已从 `xj62kv` 用户 crontab 中移除 `/mnt/users/xj62kv/slurm-monitor/monitor_slurm_jobs.py` 的每分钟定时任务，并确认当前无残留监控进程；远端脚本文件和日志目录未删除，后续如需恢复可重新添加 cron。

2026-06-22 已完成 v20 与 v21 两个从 v16 1ep 继续出发的短 smoke。v20 `output_crackwarp_slurm/v20_v16_boundary_jac0003_from_v16_1ep_smoke_1ep` 使用 `W_JACOBIAN=0.0003`、`W_CRACK_BOUNDARY_JACOBIAN=0.0002`、`W_CRACK_COORD_EXTRA=0.1`、`LR=5e-7`、`EPOCHS=1`，训练 job `38043021` 与 120 评估 job `38043052` 均完成；120 样本结果为 crack EPE 106.288704、global EPE 108.027634、Dice 0.285030、crack edge 0.336067、global edge 0.325362、folding 0.580754。v21 `output_crackwarp_slurm/v21_v16_boundary_coord005_from_v16_1ep_smoke_1ep` 保持 `W_JACOBIAN=0.0005` 与 `W_CRACK_BOUNDARY_JACOBIAN=0.0002`，将 `W_CRACK_COORD_EXTRA` 降为 0.05，训练 job `38043053` 与 120 评估 job `38043081` 均完成；120 样本结果为 crack EPE 106.217377、global EPE 107.953545、Dice 0.284485、crack edge 0.339406、global edge 0.326435、folding 0.580846。两者均未超过 v16 1ep 的 120 样本主指标组合，尤其 EPE 与 folding 更弱，因此不做严格 1000；固定 Jacobian / coord_extra 小范围扫描应止损，下一步转向自适应边界权重、样本难度分段或局部结构一致性。

2026-06-22 在 GPU 空闲后继续推进训练。首次 v19 提交 job `38042990` 因直接调用 `run_train_slurm.py` 且只设置环境变量，实际跑成默认 `EPOCHS=50`、`LR=2e-5`、`W_FOLD=0.01`、无初始化 checkpoint，已在运行 1 分 20 秒后取消，避免浪费 GPU。随后显式 CLI 参数重投 v19 job `38042992`，配置为从 v16 1ep `best_epe.pth` 初始化、`EPOCHS=1`、`LR=5e-7`、`W_CRACK_BOUNDARY_JACOBIAN=0.0002`、`W_JACOBIAN=0.0005`、`W_CRACK_COORD_EXTRA=0.1`、`W_CRACK_MAG=0.06`、`W_CRACK_EDGE=0.03`、`CRACK_MAG_OVER_WEIGHT=0.75`，输出目录 `output_crackwarp_slurm/v19_v16_boundary_over075_from_v16_1ep_smoke_1ep`。v19 训练已完成，退出码 `0:0`，训练日志为 Val EPE 112.148px、CrackEPE 114.612px；120 评估 job `38043020` 完成，结果为 crack EPE 106.255287、global EPE 107.905731、Dice 0.285216、crack edge 0.339270、global edge 0.327415、folding 0.580726。当前结论：提高 over-penalty 到 0.75 未改善 v16 1ep 的 EPE/folding 平衡，v19 不应做严格 1000。

2026-06-22 已继续提交 v20 训练 job `38043021`，当前在 `gpuHz` 分区运行。v20 从 v16 1ep `best_epe.pth` 初始化，输出目录 `output_crackwarp_slurm/v20_v16_boundary_jac0003_from_v16_1ep_smoke_1ep`，配置为 `EPOCHS=1`、`LR=5e-7`、`W_CRACK_BOUNDARY_JACOBIAN=0.0002`、`W_JACOBIAN=0.0003`、`W_CRACK_COORD_EXTRA=0.1`、`W_CRACK_MAG=0.06`、`W_CRACK_EDGE=0.03`、`CRACK_MAG_OVER_WEIGHT=0.5`。目的：保留 v16 的边界局部可逆性约束，同时降低全局 Jacobian 压力，观察是否能减少 EPE/Dice 牺牲。下一步等待 v20 完成后先做 120 样本评估，若 120 不优于 v16 1ep 则止损；若 120 有明确收益，再补严格 1000。

2026-06-22 已新增本地桌面 Slurm 悬浮监控小程序。新增 `scripts/slurm_floating_status.py`，使用标准库 Tkinter 实现透明、置顶、可拖动的本地窗口，通过本机 SSH 连接 `billy@100.94.186.128`，再 `sudo -n su - xj62kv` 查询 `squeue` 与最近 `sacct` 记录，默认每 20 秒刷新一次；新增 `scripts/start_slurm_floating_status.bat` 作为 Windows 双击启动器，使用项目 `.venv\Scripts\pythonw.exe` 后台启动。已通过 `.venv\Scripts\python.exe -m py_compile scripts\slurm_floating_status.py`，并用脚本内 `query_remote_slurm()` 验证可读取当前 job `38042992 v19-over075r RUNNING`。本地已启动悬浮窗进程。

2026-06-22 已优化本地 Slurm 悬浮窗布局，解决 `squeue` 最右侧 `NODELIST(REASON)` 信息被窗口裁剪的问题。`scripts/slurm_floating_status.py` 现在使用管道分隔的远端 `squeue` 输出，在本地格式化为“作业主信息一行 + `node/reason` 单独一行”，并把默认窗口宽度调整为 `720x320`、文本换行改为按字符换行；已通过 `.venv\Scripts\python.exe -m py_compile` 和 `query_remote_slurm()` 测试，并重启本地悬浮窗进程。

2026-06-22 已继续优化本地 Slurm 悬浮窗交互。`scripts/slurm_floating_status.py` 中“最近结束/失败 sacct”区域现在默认收起，只展示当前 `squeue` 队列和一行提示；点击标题栏右侧“最近”按钮后才展开最近结束/失败明细，再点击“收起”可恢复紧凑显示。已通过 `.venv\Scripts\python.exe -m py_compile scripts\slurm_floating_status.py`，并重启本地悬浮窗进程。

2026-06-22 已按用户偏好继续压缩本地 Slurm 悬浮窗默认视图。`scripts/slurm_floating_status.py` 现在默认窗口为横向较长、纵向较矮的紧凑模式，只显示活跃 `squeue` 任务行，不再显示“当前队列/最近”标题和收起提示；每个任务压缩为一行，包含 JobID、状态、运行时间、分区、任务名、节点数和节点/等待原因，例如 `38043021 R 5:35 gpuHz v20-jac03 n=1 @gpu001`。点击“最近”展开后窗口自动变高，显示完整 `squeue` 与最近 `sacct` 明细；再次收起后恢复矮窗口。已通过 `.venv\Scripts\python.exe -m py_compile scripts\slurm_floating_status.py` 和真实 `query_remote_slurm()` 查询验证。

2026-06-22 已进一步收窄本地 Slurm 悬浮窗紧凑模式横向宽度。`scripts/slurm_floating_status.py` 将默认紧凑窗口从 `860x150` 逐步调整为 `420x150`，并取消任务行固定列宽填充，当前紧凑行显示为自然长度，例如 `38043021 R 8:31 gpuHz v20-jac03 n=1 @gpu001`，减少右侧空白。已通过 `.venv\Scripts\python.exe -m py_compile scripts\slurm_floating_status.py` 和真实远端查询验证。

2026-06-22 曾尝试为本地 Slurm 悬浮窗增加 Windows 鼠标穿透能力，但实际效果不稳定，已按用户要求撤回。`scripts/slurm_floating_status.py` 现在恢复为普通置顶悬浮窗，保留紧凑尺寸、任务单行显示和“最近”折叠/展开能力。已通过 `.venv\Scripts\python.exe -m py_compile scripts\slurm_floating_status.py` 验证。

2026-06-22 已将本地 Slurm 悬浮窗打包为 Windows exe。使用项目 `.venv` 安装 PyInstaller，并执行 `python -m PyInstaller --windowed --name SlurmFloatingStatus scripts\slurm_floating_status.py` 生成 onedir 版本，产物位于 `dist/SlurmFloatingStatus/SlurmFloatingStatus.exe`。新增 `scripts/start_slurm_floating_status_exe.bat` 作为 exe 启动器；已创建开机启动快捷方式 `C:\Users\billy.zhou\AppData\Roaming\Microsoft\Windows\Start Menu\Programs\Startup\SlurmFloatingStatus.lnk` 指向该 exe。打包产物启动验证通过，当前已运行 exe 版本进程，并停止旧的 `.py` 脚本实例。

2026-06-22 已把本地 Slurm 悬浮窗刷新间隔改为启动参数。`scripts/slurm_floating_status.py` 新增 `--refresh-seconds` 参数，默认值为 `300` 秒，即 5 分钟自动刷新一次；手动点击“刷新”仍会立即查询。`scripts/start_slurm_floating_status_exe.bat` 已支持参数透传，例如可用 `start_slurm_floating_status_exe.bat --refresh-seconds 60` 临时改为 60 秒刷新。已通过 `.venv\Scripts\python.exe -m py_compile`、参数解析烟测和 PyInstaller 重新打包验证；新版 exe 已启动，开机快捷方式无需改动即可使用 5 分钟默认刷新。

2026-06-22 已为本地 Slurm 悬浮窗创建开始菜单快捷方式 `C:\Users\billy.zhou\AppData\Roaming\Microsoft\Windows\Start Menu\Programs\SlurmFloatingStatus.lnk`，指向 `dist/SlurmFloatingStatus/SlurmFloatingStatus.exe`，工作目录为 `dist/SlurmFloatingStatus`。开机自启快捷方式仍位于 `Startup/SlurmFloatingStatus.lnk`，两者互不冲突。

2026-06-22 已按用户要求调整 Slurm 监控通知策略：`scripts/monitor_slurm_jobs.py` 现在只发送作业新增/提交通知和作业结束通知，不再发送作业开始运行通知。脚本仍会在状态文件中标记 `started_notified=True`，用于避免重复处理运行态，但对应分支只写日志“记录作业开始但不通知”，不会调用 PushPlus。远端 `/mnt/users/xj62kv/slurm-monitor/monitor_slurm_jobs.py` 已同步部署，远端 `python3 -m py_compile` 与手动运行均返回 `OK`，`xj62kv` crontab 仍保持每分钟执行。

2026-06-22 已确认 v18 与 v16 2ep 后续评估结果。v18 `output_crackwarp_slurm/v18_boundary_jac00015_from_v11c1ep_smoke_1ep` 使用 `W_CRACK_BOUNDARY_JACOBIAN=0.00015`、其余配置同 v16/v17，从 v11-C 1ep 初始化，Slurm job `38042889` 已完成，`.err` 为空，训练日志为 Val EPE 112.127px、CrackEPE 114.492px。补投 120 评估 job `38042934` 已完成，结果为 crack EPE 106.209442、global EPE 107.876907、Dice 0.285710、crack edge 0.336233、global edge 0.324065、folding 0.580038。随后完成严格 1000 分片评估，jobs `38042939`-`38042943` 与 `38042948`-`38042952` 全部完成并合并，结果为 crack EPE 110.783087、global EPE 109.771667、Dice 0.260307、crack edge 0.223660、global edge 0.252775、folding 0.580318。逐图对比显示 v18 相对 v8 仍有正收益，564/1000 更优、436/1000 更差，`mean_score_new_minus_old=0.209778`；但相对 v16 1ep 只有 410/1000 更优、590/1000 更差，`mean_score_new_minus_old=-0.207437`，相对 v11-C 也只有 436/1000 更优、564/1000 更差，`mean_score_new_minus_old=-0.206464`。当前结论：`0.00015` 边界 Jacobian 不应继续扩训，v18 不能替代 v16 1ep。

2026-06-22 已补完 v16 2ep 稳定性验证。v16 2ep 输出目录为 `output_crackwarp_slurm/v16_boundary_jac0002_from_v16_2ep_lr5e7`，120 样本结果为 crack EPE 106.078812、global EPE 107.807846、Dice 0.284064、crack edge 0.338857、global edge 0.326955、folding 0.579982。首次用远端 shell 循环提交严格 1000 分片 jobs `38042961`-`38042970` 时变量展开异常，多个 job 因 `--shard_index` 缺参失败，剩余错误任务已取消；随后改为显式分片重投 jobs `38042971`-`38042980` 并全部完成。严格 1000 合并结果为 crack EPE 110.769513、global EPE 109.676677、Dice 0.260255、crack edge 0.225739、global edge 0.255750、folding 0.580373。相对 v16 1ep，v16 2ep 只有 474/1000 更优、526/1000 更差，`mean_score_new_minus_old=-0.079075`；相对 v11-C 1ep 为 495/1000 更优、505/1000 更差，`mean_score_new_minus_old=-0.078101`；相对 v8 为 620/1000 更优、380/1000 更差，`mean_score_new_minus_old=0.338140`。当前结论：v16 2ep 相对 v8 仍强，但没有超过 v16 1ep 或 v11-C 1ep；同配置继续加 epoch 的收益不足，应保留 v16 1ep 作为边界 Jacobian 主候选。

2026-06-22 已完成 Slurm 作业监控通知的时区调整。`scripts/monitor_slurm_jobs.py` 新增基于 `zoneinfo` 的时区转换，默认把 Slurm 返回的 `America/New_York` 时间转换为 `Asia/Shanghai` 显示；PushPlus 通知中的提交时间、开始时间、结束时间和监控时间均按中国时区输出。远端 `/mnt/users/xj62kv/slurm-monitor/monitor_slurm_jobs.py` 已同步部署，远端 `config.json` 已增加 `source_timezone=America/New_York` 与 `display_timezone=Asia/Shanghai`，保留 token 仅在远端配置中；远端脚本已通过 `python3 -m py_compile` 并手动运行返回 `OK`，现有 cron 仍按每分钟执行监控。

2026-06-20 已修复服务器 `xj62kv` 工作区中 3 个被清空的关键入口文件：`run_train_slurm.py`、`train_v2.py` 和 `utils/build_hard_sample_list.py`。本地与远端核心代码均确认在提交 `58a6a6b`，并在服务器 `.venv/bin/python` 下通过 `py_compile` 与导入检查，`train_v2.main`、`run_train_slurm.main` 均存在，CUDA 可用且识别到 NVIDIA L40S。随后提交并完成 v10 hard-sample 低权重 smoke：`v10_hard_roi_w15_from_v8mag_smoke_1ep` 使用 `v8_mag_w006_edge_w003_from_v7_10ep/best_epe.pth` 初始化，`HARD_SAMPLE_WEIGHT=1.5`，`W_CRACK_MAG=0.06`、`W_CRACK_EDGE=0.03`、`W_JACOBIAN=0.0005`、`CRACK_MAG_ROBUST_DELTA=0.01`、`CRACK_MAG_OVER_WEIGHT=0.5`，训练 1 epoch 后自动完成 120 样本评估。训练 `.err` 为空，hard sampler 匹配 `510/9324`，训练验证日志为 Val EPE 112.586px、CrackEPE 114.602px；120 样本评估结果为 crack EPE 106.141739、global EPE 108.149773、Dice 0.284543、crack edge fidelity 0.329468、global edge fidelity 0.316225、folding rate 0.580088。当前结论：服务器入口秒退问题已解决，v10 hard-sample 低权重链路可正常训练和评估；下一步应与 v8 同口径 120/1000 样本对比后，再决定是否扩到 10 epoch。

2026-06-20 已补做 `v10_hard_roi_w15_from_v8mag_smoke_1ep/best_epe.pth` 的 1000 样本评估和相对 v8 的逐图对比。1000 样本结果为 crack EPE 110.673798、global EPE 110.101158、Dice 0.259507、crack edge fidelity 0.218327、global edge fidelity 0.244003、folding rate 0.580156。相对 v8 同口径 1000 样本，v10-w15 的 crack EPE、crack edge 和 folding 略有改善，但 global EPE 与 Dice 变差，逐图综合评分仅 438/1000 优于 v8、562/1000 劣于 v8，`mean_score_new_minus_old=-0.083531`。当前判断：`HARD_SAMPLE_WEIGHT=1.5` 的 1 epoch smoke 不能直接扩 10 epoch；hard-sample 确实对 crack EPE 有帮助，但仍会牺牲整体综合质量，下一步应降到 1.0~1.1 的轻权重，或直接转向更精细的 ROI 局部约束。

2026-06-20 已补做 `v10_hard_roi_w12_from_v8mag_smoke_1ep/best_epe.pth` 的 1000 样本评估和相对 v8 的逐图对比。1000 样本结果为 crack EPE 111.075531、global EPE 110.128334、Dice 0.260649、crack edge fidelity 0.215386、global edge fidelity 0.241175、folding rate 0.581857。相对 v8 同口径 1000 样本，v10-w12 的逐图综合评分仅 380/1000 优于 v8、620/1000 劣于 v8，`mean_score_new_minus_old=-0.590504`，整体表现比 w15 更差。当前判断：`HARD_SAMPLE_WEIGHT=1.2` 也不适合继续扩训，hard-sample 路线应暂停为主，若要继续只建议做 1.0~1.1 的极轻权重验证。

2026-06-20 已把后续 hard-sample 极轻权重验证入口补进 `utils/generate_final_experiment_commands.py`，新增 `v10` 阶段可直接打印 `HARD_SAMPLE_WEIGHT=1.05` 的 1 epoch smoke 命令和 1000 样本评估命令，目的是最后确认 hard-sample 是否还能保留微弱正收益；若该步骤仍弱于 v8，则 hard-sample 路线应停止并转向 ROI / 边界约束。

2026-06-20 已将 `utils/generate_final_experiment_commands.py` 的 `--stage` 选项补齐为支持 `v10-hard`，并成功生成极轻权重 smoke 与后续评估命令；当前下一步就是在服务器上直接提交这组命令，完成 hard-sample 路线的最后一次验证。

2026-06-20 已通过 `billy@100.94.186.128` 切换到 `xj62kv`，确认远端项目目录为 `/mnt/users/xj62kv/underwater-crack-correction`，`output_crackwarp_slurm/hard_samples/v10_from_v8mag_family_top80.txt` 与 `v8_mag_w006_edge_w003_from_v7_10ep/best_epe.pth` 均存在。首次提交 job `37587646` 因 shell 引号导致 `INIT_CHECKPOINT` 吞并后续环境变量而秒退，未进入有效训练；随后改用 `env VAR=... sbatch ...` 方式重新提交 job `37587678`，当前 Slurm 状态为 `PD (Resources)`，等待 `gpuHz` 资源。

2026-06-20 已完成 `v10_hard_roi_w105_from_v8mag_smoke_1ep` 的最终验证。Slurm job `37587678` 正常完成，`HARD_SAMPLE_WEIGHT=1.05`、`EPOCHS=1`、从 `v8_mag_w006_edge_w003_from_v7_10ep/best_epe.pth` 初始化，hard sampler 匹配 `510/9324`；训练验证日志为 Val EPE 112.426px、CrackEPE 115.039px，120 样本评估为 crack EPE 106.601074、crack edge 0.328105、Dice 0.284507、folding 0.581398。后续 1000 样本单任务评估 job `37587868` 因 `--time=01:00:00` 超时，4 分片评估 jobs `37588091`-`37588094` 也因每片 250 张超过 1 小时超时；改用 10 分片后发现 `evaluate_metrics.py` 会先分片再应用 `--num`，因此 `--num 1000 --num_shards 10` 实际合并为 10000 样本，仅作为额外全量参考。严格 1000 样本口径最终使用 10 分片、每片 `--num 100`，jobs `37588443`-`37588452` 均完成，合并目录为 `eval_best_epe_1000_strict`。严格 1000 样本结果为 crack EPE 111.178371、global EPE 110.034446、Dice 0.259038、crack edge fidelity 0.220423、global edge fidelity 0.249546、folding rate 0.581139；相对 v8 同口径 1000 样本，v10-w105 只有 402/1000 张逐图综合评分更优、598/1000 张更差，`mean_score_new_minus_old=-0.554497`。当前结论：`HARD_SAMPLE_WEIGHT=1.05` 虽略微提升 edge fidelity，但 crack/global EPE、Dice、folding 和综合胜率均弱于 v8；hard-sample 采样加权路线应停止，不应扩到 10 epoch，下一步转向 ROI 局部对齐、边界一致性或 Jacobian/folding 约束。

2026-06-20 已完成 v11 ROI local-align smoke 与严格 1000 样本评估。新增 `utils/generate_final_experiment_commands.py --stage v11-roi`，从 `v8_mag_w006_edge_w003_from_v7_10ep/best_epe.pth` 初始化，配置为 `EPOCHS=1`、`LR=2e-6`、`W_CRACK_MAG=0.06`、`W_CRACK_EDGE=0.03`、`W_JACOBIAN=0.005`、`W_CRACK_COORD_EXTRA=0.5`、`CRACK_MAG_ROBUST_DELTA=0.01`、`CRACK_MAG_OVER_WEIGHT=0.5`。Slurm job `37588489` 正常完成，`.err` 为空，训练日志为 Best Val EPE 113.429px、Best Crack EPE 115.644px；120 样本评估显示 crack EPE 107.551918、crack edge 0.345170、Dice 0.282417、global EPE 109.398918、global edge 0.334617、folding 0.576504，说明 edge/folding 短样本口径提升明显。随后使用 10 分片、每片 `--num 100` 完成严格 1000 样本评估，jobs `37588960`-`37588969` 均为 `COMPLETED`，合并结果为 crack EPE 111.911628、global EPE 111.001054、Dice 0.257864、crack edge fidelity 0.238528、global edge fidelity 0.268905、folding rate 0.575718。相对 v8 同口径 1000 样本，v11 仅 360/1000 张逐图综合评分更优、640/1000 张更差，`mean_score_new_minus_old=-0.827232`。当前结论：v11 的 `W_JACOBIAN=0.005 + W_CRACK_COORD_EXTRA=0.5` 强化了 edge/folding，但明显牺牲 crack/global EPE 与 Dice，不适合直接扩训到 10 epoch；下一步应降低 Jacobian 权重或重设 ROI 局部坐标约束形式，而不是继续加训练轮数。

2026-06-20 已完成 v11-B 与 v11-C 的温和 ROI/Jacobian 降权验证。v11-B 使用 `W_JACOBIAN=0.001`、`W_CRACK_COORD_EXTRA=0.2`，job `37589061` 完成且 `.err` 为空；120 样本为 crack EPE 106.181900、crack edge 0.334572、Dice 0.283430、global EPE 107.958290、global edge 0.321291、folding 0.580371；严格 1000 样本为 crack EPE 111.109745、global EPE 109.866503、Dice 0.260642、crack edge 0.225932、global edge 0.255774、folding 0.580003，相对 v8 逐图综合评分 513/1000 更优、487/1000 更差，但 `mean_score_new_minus_old=-0.024520`，只能视为接近持平。v11-C 进一步降到 `W_JACOBIAN=0.0005`、`W_CRACK_COORD_EXTRA=0.1`，job `37589386` 完成且 120 样本为 crack EPE 106.064697、crack edge 0.333029、Dice 0.283314、global EPE 107.710747、global edge 0.318261、folding 0.579706；严格 1000 样本分片 jobs `37590480`-`37590489` 全部完成，合并结果为 crack EPE 110.691580、global EPE 109.606030、Dice 0.260483、crack edge 0.223758、global edge 0.253094、folding 0.579550。相对 v8，v11-C 614/1000 张逐图综合评分更优、386/1000 张更差，`mean_score_new_minus_old=0.416242`；相对 v11-B，v11-C 558/1000 张更优、442/1000 张更差，`mean_score_new_minus_old=0.440762`。当前结论：v11-C 是目前 v11 降权系列中最稳的正向配置，既改善 global EPE、edge 和 folding，又没有像 v11-A 那样显著牺牲 Dice；已取消卡在 `gpu` 分区节点保留原因的 10 epoch job `37590750`，改投 `gpuHz` 分区 job `37590757`，配置与 v11-C smoke 相同、`EPOCHS=10`、`--time=08:00:00`，当前为 `PENDING (Priority)`。

2026-06-21 继续在 v11-C 10 epoch job `37590757` 排队期间并行推进训练消融。v11-D 使用 `W_JACOBIAN=0.0005`、`W_CRACK_COORD_EXTRA=0.0`，job `37830065` 已完成，`.err` 为空；120 样本为 crack EPE 106.425652、crack edge 0.331580、Dice 0.283820、global EPE 108.154251、global edge 0.316067、folding 0.581517，相比 v11-C 120 样本在 EPE、edge 和 folding 上均退化，仅 Dice 略高，因此暂不做严格 1000。v11-E 使用 `W_JACOBIAN=0.0005`、`W_CRACK_COORD_EXTRA=0.05`，job `37830066` 已完成，`.err` 为空；120 样本为 crack EPE 106.398857、crack edge 0.329911、Dice 0.284064、global EPE 108.121330、global edge 0.316505、folding 0.581883，同样弱于 v11-C 的 EPE/edge/folding，暂不做严格 1000。v11-F 使用 `W_JACOBIAN=0.0`、`W_CRACK_COORD_EXTRA=0.0`、仅保留 `W_CRACK_MAG=0.06` 与 `W_CRACK_EDGE=0.03`，job `37830252` 已完成；120 样本为 crack EPE 106.091820、crack edge 0.324896、Dice 0.284969、global EPE 107.826012、global edge 0.311294、folding 0.581296，Dice 最强但 edge/folding 弱于 v11-C，说明轻量 Jacobian/ROI 坐标约束仍有助于边缘和可逆性。随后补做 v11-C 从 1ep checkpoint 低学习率续训 2ep，job `37830705` 已完成，输出目录为 `output_crackwarp_slurm/v11c_cont_from_1ep_jac0005_coord01_2ep`；第 2 epoch 刷新 best，训练验证日志为 Val EPE 112.231px、CrackEPE 114.408px，自动 120 样本评估为 crack EPE 106.227081、crack edge 0.330638、Dice 0.285111、global EPE 108.047432、global edge 0.318818、folding 0.580878。该续训提升 Dice 和 global edge，但 crack/global EPE 与 folding弱于 v11-C 1ep，因此暂不扩严格 1000。v11-C 对 v8 的 flow 诊断重投时发现 `utils/export_flow_diagnostics.py` 不支持 `--max_images`，应使用 `--num 20`；两个可选诊断 job `37830806`、`37830807` 已取消，以免占用 GPU 队列影响训练优先级。主任务 `37590757` 已完成，输出目录为 `output_crackwarp_slurm/v11c_roi_local_align_jac0005_coord01_from_v8_10ep`，Slurm 状态 `COMPLETED`，退出码 `0:0`。10 epoch 训练日志中第 6 epoch 刷新 best EPE 112.067px，第 9 epoch 刷新 best CrackEPE 114.274px；自动 120 样本评估为 crack EPE 106.066895、crack edge 0.335034、Dice 0.286126、global EPE 107.623047、global edge 0.323598、folding 0.582312。随后完成严格 1000 样本评估，初次分片提交因 PowerShell 转义把 `$i` 写成 ESC 字符导致 jobs `37832577`-`37832580` 秒退、后续错误分片取消；改为显式分片重投 jobs `37832593`-`37832597` 与 `37832602`-`37832606` 后全部完成并合并。严格 1000 结果为 crack EPE 110.884130、global EPE 109.653934、Dice 0.260843、crack edge 0.227080、global edge 0.257203、folding 0.582112。相对 v8，v11-C 10ep 逐图综合评分 567/1000 更优、433/1000 更差，`mean_score_new_minus_old=0.263783`；但相对 v11-C 1ep，10ep 只有 479/1000 更优、521/1000 更差，`mean_score_new_minus_old=-0.152459`。当前结论：v11-C 10ep 相比 v8 仍有正收益，主要提升 edge/Dice，但不如 v11-C 1ep 稳，且 folding 变差；不应把 10ep 替代为主候选，当前更稳的 v11 系候选仍是 v11-C 1ep。随后补做 v11-C 1ep 相对 v8 的 flow 诊断，jobs `37832623`、`37832624`、`37832625` 分别导出 new-better、old-better、joint-failure 各 20 张诊断图和 `flow_diagnostics.csv`。new-better 组 v11-C 1ep 相对 v8 的 global EPE 从 132.537 降到 130.343、crack EPE 从 136.619 降到 132.918、folding 从 0.403940 降到 0.403396、mean displacement 从 132.453px 降到 130.318px、p95 displacement 从 236.458px 降到 233.837px；old-better 组 global EPE 从 108.916 升到 109.278、crack EPE 从 99.408 升到 100.398、folding 从 0.400911 升到 0.401206，mean/p95 displacement 分别小升 0.347px/0.231px；joint-failure 组 EPE 小幅改善但 folding 从 0.402335 小升到 0.402589，位移幅度下降。诊断结论：v11-C 1ep 的有效收益主要来自对高位移样本做轻微位移收缩并降低 EPE；退化样本则表现为位移略增、folding 略增。下一轮优化应保留轻量位移收缩/edge 收益，同时显式限制 folding 或边界局部可逆性，不应继续简单加 epoch。为此已给 `run_train_slurm.py` 增加 `--w-fold` 参数，并给 `slurm_train_crackwarp.sbatch` 增加 `W_FOLD` 环境变量透传；本地 `run_train_slurm.py` 已通过 `.venv\Scripts\python.exe -m py_compile`，远端 `run_train_slurm.py` 也已通过 `.venv/bin/python -m py_compile`。由于 Windows CRLF 同步后远端 `slurm_train_crackwarp.sbatch` 暂时 `bash -n` 报 EOF，v12 smoke 暂不依赖该 sbatch 文件，改用 `sbatch --wrap` 直接调用 `run_train_slurm.py`。首次 v12 job `37832678` 因 torch 检查命令的引号被 shell 吞掉而秒退；已去掉该检查并重投 job `37832685`，配置为从 `v11c_roi_local_align_jac0005_coord01_from_v8_smoke_1ep/best_epe.pth` 初始化，`W_FOLD=0.03`、`W_JACOBIAN=0.0005`、`W_CRACK_COORD_EXTRA=0.05`、`W_CRACK_MAG=0.06`、`W_CRACK_EDGE=0.03`、`LR=1e-6`、`EPOCHS=1`，输出目录为 `output_crackwarp_slurm/v12_fold_guard_jac0005_coord005_from_v11c1ep_smoke_1ep`；当前 Slurm 状态为 `PENDING (Resources)`。

2026-06-21 已继续完成 v12/v13/v14 轻量消融。v12 folding-guard smoke job `37832685` 已完成且 `.err` 为空，配置为从 v11-C 1ep 初始化、`W_FOLD=0.03`、`W_JACOBIAN=0.0005`、`W_CRACK_COORD_EXTRA=0.05`、`W_CRACK_MAG=0.06`、`W_CRACK_EDGE=0.03`、`LR=1e-6`、`EPOCHS=1`；120 样本结果为 crack EPE 106.308380、crack edge 0.333315、Dice 0.283320、global EPE 108.080658、global edge 0.319131、folding 0.581472，整体弱于 v11-C 1ep，说明直接增加 `W_FOLD=0.03` 没能改善目标平衡，未做严格 1000。v13 over-penalty smoke job `37833694` 已完成且 `.err` 为空，配置为从 v11-C 1ep 初始化、`W_FOLD=0.01`、`W_CRACK_COORD_EXTRA=0.1`、`CRACK_MAG_OVER_WEIGHT=1.0`、`LR=1e-6`、`EPOCHS=1`；120 样本结果为 crack EPE 106.100014、crack edge 0.334248、Dice 0.282972、global EPE 108.019547、global edge 0.319501、folding 0.580915，仅 edge 略优但 EPE/Dice/folding 均不如 v11-C 1ep，未做严格 1000。v14 over-penalty 对照 job `37833818` 已完成且 `.err` 为空，改为从 v8 初始化、`CRACK_MAG_OVER_WEIGHT=0.75`、`W_FOLD=0.0`、`W_JACOBIAN=0.0005`、`W_CRACK_COORD_EXTRA=0.1`、`LR=2e-6`、`EPOCHS=1`；训练日志为 Val EPE 112.197px、CrackEPE 114.520px。补投 v14 120 评估 job `37833932` 已完成，120 样本结果为 crack EPE 106.214943、crack edge 0.334928、Dice 0.285796、global EPE 107.893570、global edge 0.323333、folding 0.580911。v14 严格 1000 分片 jobs `37833942`-`37833951` 已全部完成并合并，结果为 crack EPE 110.810224、global EPE 109.812100、Dice 0.260856、crack edge 0.222224、global edge 0.251491、folding 0.581070。相对 v8，v14 逐图综合评分 516/1000 更优、484/1000 更差，`mean_score_new_minus_old=0.122904`；相对 v11-C 1ep，v14 只有 422/1000 更优、578/1000 更差，`mean_score_new_minus_old=-0.293337`。当前结论：v14 可视为相对 v8 的小幅 edge/Dice 候选，但不如 v11-C 1ep 稳，不应作为主线继续扩训。

2026-06-21 已新增 v15 边界环带 Jacobian 约束并完成严格 1000 评估。代码新增 `CrackBoundaryJacobianLoss`，从裂缝 soft mask 构造窄边界环带，只在裂缝边界附近比较预测/GT 坐标场一阶导数并惩罚负 determinant；`CrackWarpLoss` 新增默认关闭的 `w_crack_boundary_jacobian`，`train_v2.py` 日志新增 `c_bjac`，`run_train_slurm.py` 与 `slurm_train_crackwarp.sbatch` 新增参数透传。新增代码已在本地 `.venv\Scripts\python.exe -m py_compile loss_crack.py train_v2.py run_train_slurm.py` 通过，并同步到远端后用 `.venv/bin/python -m py_compile loss_crack.py train_v2.py run_train_slurm.py` 通过。v15 smoke job `37833985` 已完成，配置为从 v11-C 1ep 初始化、`W_CRACK_BOUNDARY_JACOBIAN=0.0005`、`W_JACOBIAN=0.0005`、`W_CRACK_COORD_EXTRA=0.1`、`W_CRACK_MAG=0.06`、`W_CRACK_EDGE=0.03`、`W_FOLD=0.0`、`LR=1e-6`、`EPOCHS=1`；训练 `.err` 为空，日志中 `c_bjac` 正常出现。补投 v15 120 评估 job `37834090` 已完成，120 样本结果为 crack EPE 106.291733、crack edge 0.341532、Dice 0.283376、global EPE 108.142189、global edge 0.328817、folding 0.580004。v15 严格 1000 分片 jobs `37834102`-`37834111` 已全部完成并合并，结果为 crack EPE 111.098657、global EPE 109.976614、Dice 0.260106、crack edge 0.228076、global edge 0.257875、folding 0.580153。相对 v8，v15 逐图综合评分 493/1000 更优、507/1000 更差，`mean_score_new_minus_old=-0.020601`；相对 v11-C 1ep，v15 429/1000 更优、571/1000 更差，`mean_score_new_minus_old=-0.436842`；相对 v14，v15 464/1000 更优、536/1000 更差，`mean_score_new_minus_old=-0.143505`。当前结论：边界环带 Jacobian 能显著提升 edge，但 `0.0005` 权重伤害 EPE/Dice 和综合胜率，不适合作为主线；下一步若继续该方向，只能降权验证。

2026-06-21 已完成 v16 边界环带 Jacobian 降权 smoke、严格 1000 评估和逐图对比。配置与 v15 相同，但 `W_CRACK_BOUNDARY_JACOBIAN` 从 `0.0005` 降到 `0.0002`，输出目录为 `output_crackwarp_slurm/v16_boundary_jac0002_from_v11c1ep_smoke_1ep`，Slurm job `37834139` 已完成且 `.err` 为空，训练日志为 Val EPE 111.972px、CrackEPE 114.393px。补投 v16 120 评估 job `38042730` 已完成，120 样本结果为 crack EPE 105.957970、crack edge 0.338595、Dice 0.284546、global EPE 107.652229、global edge 0.326745、folding 0.580150。首次严格 1000 分片 jobs `38042735`-`38042744` 因远端 shell loop 变量展开导致 `--shard_index` 缺参而失败/取消；随后显式分片重投 jobs `38042745`-`38042754`，全部完成并合并。严格 1000 结果为 crack EPE 110.770753、global EPE 109.557367、Dice 0.261088、crack edge 0.224801、global edge 0.254913、folding 0.580287。相对 v8，v16 646/1000 张逐图综合评分更优、354/1000 更差，`mean_score_new_minus_old=0.417215`；相对 v11-C 1ep，v16 530/1000 更优、470/1000 更差，`mean_score_new_minus_old=0.000973`；相对 v14，v16 602/1000 更优，`mean_score_new_minus_old=0.294311`；相对 v15，v16 613/1000 更优，`mean_score_new_minus_old=0.437816`。当前结论：v16 是目前最有希望的新候选之一，综合评分略胜 v11-C 1ep，明显胜 v8/v14/v15；它以小幅牺牲 v11-C 的 crack EPE/folding 为代价，换来更好的 global EPE、Dice 和 edge。下一步应优先做 v16 vs v11-C 的 flow 诊断和代表样本 ROI 可视化，再决定是否做 2ep 或 10ep 稳定性验证。

2026-06-22 已完成 v16 vs v11-C 1ep 的 flow 诊断和 ROI 可视化导出。Slurm jobs `38042788`、`38042789`、`38042790` 分别导出 new-better、old-better、joint-failure 各 20 张整图 flow 诊断，jobs `38042792`、`38042793` 导出 v16 new-better / old-better ROI 局部图，分别生成 76 和 73 张 png。new-better 组中 v16 相比 v11-C global EPE 从 110.276155 降到 109.947171、crack EPE 从 100.460579 降到 100.113918、folding 从 0.399927 降到 0.398821、mean displacement 从 108.762206px 降到 108.516747px、p95 displacement 从 214.930606px 降到 214.373684px，说明有效收益来自轻微位移收缩、EPE 下降和 folding 下降。old-better 组中 v16 global EPE 从 121.054142 升到 121.323100、crack EPE 从 124.733307 升到 126.340368、mean displacement 从 119.330020px 升到 119.607402px、p95 displacement 从 224.178569px 升到 224.234502px，但 folding 从 0.402271 降到 0.401473。joint-failure 组中 v16 global/crack EPE 小幅变差，位移幅度小升，但 folding 从 0.402718 降到 0.401335。当前判断：v16 的边界环带 Jacobian 确实在多数组别中降低 folding，并在受益样本上同步降低位移幅度和 EPE；退化样本主要是位移幅度略升导致 crack EPE 变差，而不是 folding 失控。下一步应做 v16 的 2ep 稳定性验证，或进一步加位移幅度 over-penalty/自适应边界权重，避免 old-better 组位移小幅升高。

2026-06-22 已新增并部署 Slurm 作业监控脚本 `scripts/monitor_slurm_jobs.py`。脚本监控 `xj62kv` 用户的 Slurm 作业，识别作业新增/提交和结束事件，并通过 PushPlus 推送通知；作业开始运行只更新内部状态和日志，不再通知。本地脚本不包含 PushPlus token，远端私有配置位于 `/mnt/users/xj62kv/slurm-monitor/config.json`，权限为 `600`。远端部署目录为 `/mnt/users/xj62kv/slurm-monitor/`，状态文件为 `state/jobs_state.json`，日志为 `logs/monitor.log` 和 `logs/cron.log`。已在 `xj62kv` crontab 中部署每分钟执行一次：`/usr/bin/python3 /mnt/users/xj62kv/slurm-monitor/monitor_slurm_jobs.py >> /mnt/users/xj62kv/slurm-monitor/logs/cron.log 2>&1`。脚本已在远端通过 `python3 -m py_compile` 和手动运行验证，并通过 sacct 水位线逻辑避免首次部署回放历史作业；监控日志已捕捉到 job `38042889` 的结束事件，说明定时监控链路可用。

2026-06-19 已同步服务器最新结果提交 `711b8ff`，并在本地将既有文档提交 rebase 到远端最新结果之上。当前最终主候选为 `v8_mag_w006_edge_w003_from_v7_10ep/best_epe.pth`：全量 10360 样本相对 v4 的综合逐图胜率为 5909/10360（57.04%），相对 v7 同为 5909/10360，相对 v9-A 为 5561/10360。v8_mag 的优势集中在 crack/global edge fidelity 与 folding 稳定性，crack EPE 与 Dice 仍只接近 v4，距离“三区稳妥达标”还缺少更明显的 crack EPE/Dice 增益。为继续冲指标，已新增 v10 hard-sample 训练入口：`train_v2.py` 支持从困难样本清单启用 `WeightedRandomSampler`，`run_train_slurm.py` 与 `slurm_train_crackwarp.sbatch` 支持 `HARD_SAMPLE_LIST` / `HARD_SAMPLE_WEIGHT` 参数，新增 `utils/build_hard_sample_list.py` 可从 compare 输出中的 `old_better_images.txt`、`joint_failure_images.txt` 生成困难样本清单。默认参数为空或权重为 1 时不改变历史训练行为。

2026-06-20 已新增会话接力文件 `会话接力说明_2026-06-20.md`，把当前主候选、v10 hard-sample 进展、1000/10360 样本结果、服务器启动入口版本不一致的阻塞点和下一步推进顺序整理成可直接换会话接着做的交接说明。当前最重要的执行动作仍是先对齐服务器代码版本，再用 `run_train_slurm.py` 重新跑低权重 hard-sample smoke，避免继续被秒退任务浪费排队时间。

已完成用户补充代码和数据后的只读确认。当前仓库已经从纯报告资料仓库变为可继续工程诊断和训练优化的算法项目：总计约 21,913 个非 `.venv` 文件、约 27.25GB，其中主训练集 `underwater_crack_v3` 约 26.21GB，训练输出 `output_crackwarp` 约 0.98GB。主数据集配对完整，抽样标签为 `(2,512,512)` 的 `float32`，数值范围稳定在 `[0,1]`。已有训练跑满 50 epoch，最佳 Val EPE 约 117.885px；gate report 显示所有 checkpoint 均为 `below_minimum`，最佳 `best_epe.pth` 的 crack EPE 约 112.021px、Dice 约 0.0026、edge fidelity 约 0.295、global EPE 约 112.606px、folding rate 约 0.570，距离工程门限仍有明显差距。已新增 Slurm 训练入口，但本机 `.venv` 是从旧机器拷贝来的失效环境，当前无法执行 `.venv\Scripts\python.exe`，需要在 Slurm/新机器上重建虚拟环境后再训练。

已完成项目下全部 Markdown 与 Word 文档的只读阅读和目标归纳：文档总体确认本项目的核心目标不是普通图像增强，而是学习水下裂缝扭曲图像的归一化逆映射坐标场，通过 `cv2.remap` 将扭曲裂缝恢复到更接近无扭曲的几何形态。项目评价重点应优先围绕裂缝主体位置、细裂缝分叉、边缘锐度、局部 ROI 对齐、预测恢复幅度和位移场可逆性，而不是只看全图平均损失。普通版实验报告强调模型已稳定收敛并具备一定全局校正能力；详细版报告、gate report、交接文档和工作量说明则共同指出当前结果仍未达到工程或论文级验收标准，下一阶段应以复现诊断、坐标/逆映射一致性排查、Slurm 全量训练、裂缝区域监督增强、folding/Jacobian 约束、ROI 局部细化、对比实验和消融实验为主线。

截至 2026-06-07，本地目录已同步为轻量代码仓库版本：大数据目录 `underwater_crack_v3/`、`under-crack-images/` 和历史输出 `output_crackwarp/` 不在当前本地工作区内，已通过 GitHub Release `data-v1` 分发，并据 `新电脑接续工作指南.md` 记录已在服务器 `usmidet-com-prod-gpu001` 下载、校验、合并和解压完成。当前本地重点文件包括 `requirements.txt`、`run_train_slurm.py`、`slurm_train_crackwarp.sbatch`、`smoke_train_verify.py`、`GITHUB_UPLOAD_PLAN.md`、`SHA256SUMS.txt` 和 `新电脑接续工作指南.md`。下一步主要工作应在服务器上确认数据数量、重建 `.venv`、跑 Slurm smoke，然后再提交正式训练。

截至 2026-06-08，服务器 Slurm 正式 50 epoch 训练已完成。任务 `34827388` 使用 `EPOCHS=50 OUTPUT_DIR=output_crackwarp_slurm/v2 sbatch --partition=gpu slurm_train_crackwarp.sbatch` 提交到 `gpu` 分区，并在 `usmidet-com-prod-gpu001` 正常跑完；`slurm-crackwarp-train-34827388.err` 为空，说明本轮未出现 OOM 或 Python Traceback。输出目录 `output_crackwarp_slurm/v2` 约 964MB，包含 `best_loss.pth`、`best_epe.pth`、`best_crack_epe.pth`、`epoch_5.pth` 至 `epoch_50.pth`、`final.pth`、`train.log` 和 `eval_best_epe_120/`。自动 120 样本评估显示：crack EPE 108.987938、global EPE 111.060646、crack Dice 0.283552、crack edge fidelity 0.308878、global edge fidelity 0.294752、folding rate 0.590619。相比历史 `output_crackwarp/best_epe.pth` 的 120 样本复评结果，EPE 和 Dice 有小幅提升，但 edge fidelity 略低、folding rate 更高，整体仍未达到三区论文目标或工程门限，下一阶段应从坐标/逆映射逻辑、folding 约束、裂缝 ROI 监督和可视化诊断继续优化。

2026-06-08 继续补评 `output_crackwarp_slurm/v2/best_crack_epe.pth` 与 `best_loss.pth`，两者在 120 样本上的评估结果与 `best_epe.pth` 完全一致，说明本轮训练中三个 best 权重很可能对应同一保存时刻或同一 epoch。随后对 `best_epe.pth` 扩大到 1000 样本评估，结果为 crack EPE 113.286263、global EPE 113.095718、crack Dice 0.255906、crack edge fidelity 0.204312、global edge fidelity 0.224700、folding rate 0.590347。该结果比 120 样本更差，说明 120 样本评估偏乐观；后续报告和模型判断应优先采用更大样本或固定验证集指标。

2026-06-08 进一步用同一 1000 样本口径复评历史 `output_crackwarp/best_epe.pth`，结果为 crack EPE 116.231331、global EPE 114.404533、crack Dice 0.252041、crack edge fidelity 0.218584、global edge fidelity 0.242924、folding rate 0.569224。与 v2 相比，v2 的 crack/global EPE 和 Dice 小幅更好，但 edge fidelity 与 folding rate 更差。因此 v2 的真实结论是“位置误差略有改善，但裂缝边缘复原和位移场可逆性没有改善”，不适合作为最终达标模型。

2026-06-08 已从服务器拉取 `f6f4645` 诊断结果，包含 `output_crackwarp_slurm/v2/compare_old_vs_v2/` 和共同失败样本的新旧 ROI 图。`compare_summary.json` 显示 1000 张匹配样本中 v2 综合分更好的有 523 张，旧模型更好的有 477 张，说明 v2 不是稳定全面提升，而是接近五五开的局部改善。共同失败样本高度集中在 `crack0030_xx` 一组，ROI 图显示大形变和边界附近存在明显局部涂抹、重复纹理、不自然边缘和裂缝主体偏移；例如 `crack0030_08`、`crack0030_02`、`crack0063_05` 中 v2 虽然部分纹理位置有变化，但裂缝细节没有稳定复原，部分样本反而抹淡或拉偏。该诊断进一步支持下一步应先修正/增强坐标约束、folding/Jacobian 约束、边缘保持和 ROI 局部监督，而不是继续同配置堆训练轮数。

2026-06-08 新增 `utils/export_flow_diagnostics.py`，用于服务器下一步继续诊断共同失败样本。该脚本同时加载旧模型和 v2 新模型，对指定图片清单导出整图诊断面板，包含输入、旧模型校正、v2 校正、GT 校正、旧/新 EPE 热图、旧/新 folding 热区和旧/新位移幅度图，并生成 `flow_diagnostics.csv`。该脚本需要在服务器 `.venv/bin/python` 下做语法检查和实际运行，本地 Windows 工作区无 `.venv`，暂不能本地执行验证。

2026-06-08 已拉取服务器提交 `d87f9bb`，包含 `output_crackwarp_slurm/v2/flow_diag_joint_failures/` 的 20 张共同失败样本整图诊断面板和 `flow_diagnostics.csv`。CSV 显示这 20 张样本中 v2 全局 EPE 平均从 129.430 降到 123.972，但裂缝区域 EPE 平均从 176.045 升到 177.491；v2 平均位移幅度从 128.289px 降到 121.280px，说明 v2 更偏保守/平滑，全图指标可能改善，但裂缝主体并未稳定对准。典型样本 `crack0041_07` 中 v2 明显优于旧模型，主要因为旧模型位移幅度过大，而 v2 降低位移后 EPE 大幅下降；典型样本 `crack0063_05` 中 v2 严重劣化，全局 EPE 从 114.437 升到 182.805，裂缝 EPE 从 114.093 升到 208.985，且 v2 位移幅度从 115.780px 增到 182.945px，说明 v2 在部分样本上会产生过强或方向错误的局部恢复。当前判断：问题不是单纯“恢复量太小”，而是恢复幅度缺少结构约束，有时过小、有时过大，且没有稳定服务于裂缝 ROI。

2026-06-08 拉取服务器正反样本诊断结果后，确认 v2 改善/退化与位移幅度强相关：`flow_diag_new_better` 的 20 张样本中，v2 裂缝 EPE 全部改善，平均位移幅度从 163.353px 降到 116.598px；`flow_diag_old_better` 的 20 张样本中，v2 裂缝 EPE 全部变差，平均位移幅度从 123.072px 升到 144.373px。由此将下一轮 v3 试验定义为“位移幅度一致性校准”，而不是简单增大或减小恢复量。

2026-06-08 已在本地新增 `DisplacementMagnitudeConsistencyLoss`，并通过 `w_crack_mag` 参数接入 `CrackWarpLoss`、`train_v2.py`、`run_train_slurm.py` 和 `slurm_train_crackwarp.sbatch`。该损失默认权重为 0，不影响当前 v2 复现；服务器可通过环境变量 `W_CRACK_MAG=0.3` 启用，用于约束裂缝 ROI 内预测位移幅度接近 GT 位移幅度，降低样本间过小/过大的恢复幅度漂移。

2026-06-08 服务器运行 `W_CRACK_MAG=0.3 EPOCHS=2 OUTPUT_DIR=output_crackwarp_slurm/v3_mag_smoke sbatch --partition=gpuHz --time=01:00:00 slurm_train_crackwarp.sbatch` 已完成，`.err` 为空但指标灾难性：global EPE 约 298.617、folding rate 约 0.99938。该结果说明新 loss 链路能跑通，但从随机初始化训练 2 epoch 没有诊断价值，且可能把早期位移场推向严重折叠。下一步已改为支持从 v2 `best_epe.pth` 初始化进行短程 fine-tune。

2026-06-08 本地新增 `INIT_CHECKPOINT` / `--init-checkpoint` 入口，训练时只加载模型权重，不恢复 optimizer/scheduler/scaler。该入口用于 v3 fine-tune：从 `output_crackwarp_slurm/v2/best_epe.pth` 初始化，以较小学习率和较低 `W_CRACK_MAG` 验证位移幅度一致性损失是否能在不破坏 v2 几何能力的前提下改善裂缝 ROI 稳定性。

2026-06-09 服务器已完成 v3 fine-tune smoke：`INIT_CHECKPOINT=output_crackwarp_slurm/v2/best_epe.pth W_CRACK_MAG=0.1 LR=5e-6 EPOCHS=2 OUTPUT_DIR=output_crackwarp_slurm/v3_mag_ft_smoke sbatch --partition=gpuHz --time=02:00:00 slurm_train_crackwarp.sbatch`。日志确认已加载 v2 checkpoint，`c_mag` 正常出现，`.err` 为空。120 样本评估结果为 crack EPE 107.956345、crack edge fidelity 0.306520、Dice 0.284974、global EPE 109.795090、global edge fidelity 0.293271、folding rate 0.589715。相较 v2 的 120 样本结果，crack/global EPE 与 Dice 小幅改善，folding 略好，edge 基本持平略低，说明从 v2 初始化后开启 `w_crack_mag=0.1` 是可继续验证的方向；下一步应做 1000 样本评估和 v2-v3 逐图对比，不能直接扩大到 50 epoch。

2026-06-09 已拉取服务器提交 `63a0971`，包含 v3 fine-tune 的 1000 样本评估和 v2-v3 逐图对比结果。v3 1000 样本结果为 crack EPE 112.940239、global EPE 111.840080、Dice 0.259461、crack edge fidelity 0.200950、global edge fidelity 0.221410、folding rate 0.589862。相比 v2 1000 样本结果，v3 的 crack EPE 改善约 0.346px，global EPE 改善约 1.256px，Dice 提升约 0.00355，folding rate 改善约 0.00048，但 crack/global edge fidelity 分别下降约 0.00336/0.00329。逐图综合评分显示 1000 张中 v3 优于 v2 的有 644 张，劣于 v2 的有 356 张，平均综合分提升 1.017554。当前结论：`w_crack_mag=0.1` 微调方向有效，能更稳定地校准位移幅度，但边缘保持仍未改善，下一步应先做 v2-v3 正反样本 flow 诊断，再考虑 10 epoch 小规模 fine-tune，而不是直接长训 50 epoch。

2026-06-09 服务器已完成 v2-v3 正反样本 flow 诊断导出：`flow_diag_new_better` 与 `flow_diag_old_better` 各处理 20 张样本，控制台显示 new-better 组大多从 v2 到 v3 稳定降低 global EPE，例如 `crack0038_01` 从 107.428 降到 104.631；old-better 组中多个高难样本进一步变差，例如 `crack0053_03` 从 151.745 升到 159.044、`crack0063_05` 从 182.805 升到 188.962、`crack0076_04` 从 200.138 升到 206.474。下一步需要读取两组 `flow_diagnostics.csv` 的位移幅度、crack EPE 和 folding 均值，确认 v3 变差样本是否来自位移幅度继续偏大、方向错误或边界 folding。

2026-06-09 已拉取服务器提交 `5b9ca78` 并分析 v2-v3 flow 诊断 CSV/图片。`flow_diag_new_better` 20 张中，v3 相比 v2 的 global EPE 平均从 112.420 降到 110.474，crack EPE 从 116.845 降到 113.667，mean displacement magnitude 从 109.689px 降到 107.996px，p95 displacement magnitude 从 214.854px 降到 212.139px，folding rate 从 0.392338 小升到 0.393053。`flow_diag_old_better` 20 张中，v3 的 global EPE 从 136.226 升到 139.193，crack EPE 从 126.328 升到 134.865，mean displacement magnitude 从 136.213px 升到 139.161px，p95 displacement magnitude 从 247.489px 升到 251.113px，folding rate 从 0.398417 升到 0.400513。代表性图片 `crack0038_01` 显示 v3 通过收缩位移幅度降低 EPE；`crack0053_03`、`crack0079_09`、`crack0100_09` 显示退化样本主要是右侧/边界大形变区域位移幅度进一步变强，导致 EPE 热区更重。当前判断：v3 的 `w_crack_mag=0.1` 不是稳定边缘复原方案，而是一个位移幅度校准信号；它对“v2 位移偏大”的样本有效，但仍会把部分高难样本推向更大恢复幅度，后续应先做更保守的权重或位移幅度上限/鲁棒化约束。

2026-06-09 已拉取服务器提交 `3346ffd`，包含 `W_CRACK_MAG=0.05`、`LR=5e-6`、从 v2 初始化微调 10 epoch 的评估结果。该版本 `output_crackwarp_slurm/v3_mag_ft_w005_10ep/best_epe.pth` 在 1000 样本上达到 crack EPE 111.806763、global EPE 110.886253、Dice 0.260308、crack edge fidelity 0.202303、global edge fidelity 0.224912、folding rate 0.586779。相比 v2，crack EPE 改善约 1.480px、global EPE 改善约 2.209px、Dice 提升约 0.0044、global edge 基本持平略好、folding 改善约 0.00357；相比 `W_CRACK_MAG=0.1` 的 2 epoch smoke，也进一步改善 EPE、Dice、global edge 和 folding。逐图综合评分显示 1000 张中 850 张优于 v2、150 张劣于 v2，global EPE 有 918 张改善，crack EPE 有 802 张改善，folding 有 854 张改善。当前结论：`W_CRACK_MAG=0.05 + 10 epoch` 是目前最稳定的主线候选模型，但 crack edge fidelity 仍略低于 v2，且退化样本集中在 `crack0063`、`crack0079`、`crack0053`、`crack0052` 等高难/大形变组。

2026-06-09 已拉取服务器提交 `c200b54`，包含 `v3_mag_ft_w005_10ep` 的退化样本和共同失败样本 flow 诊断图/CSV。`flow_diag_old_better` 20 张退化样本中，v3 相比 v2 的 global EPE 从 141.950 升到 147.531，crack EPE 从 139.169 升到 152.091，folding rate 从 0.401218 升到 0.405723，mean displacement magnitude 从 142.239px 升到 147.605px，p95 displacement magnitude 从 253.089px 升到 260.220px；其中 crack EPE 没有任何一张改善。`flow_diag_joint_failures` 20 张中，v3 global EPE 从 133.049 小降到 132.855，15 张 global EPE 改善，但 crack EPE 从 179.567 升到 181.797，folding rate 从 0.395344 升到 0.400381，说明 w005_10ep 对整图位置有帮助，但裂缝 ROI 仍有局部过强/折叠和边缘对齐问题。代表图 `crack0079_09`、`crack0063_07` 显示退化主要发生在边缘和大形变区域，v3 位移热图更强、folding 斑点更密；`crack0030_03` 说明共同失败族整体有改善但裂缝细节仍未完全对齐。当前判断：下一阶段应从“继续调低 `W_CRACK_MAG` 或训练更久”转向加入位移幅度上限/鲁棒化、边界约束和裂缝边缘一致性约束。

2026-06-09 已完成下一步代码修改：`DisplacementMagnitudeConsistencyLoss` 新增 `crack_mag_robust_delta` 和 `crack_mag_over_weight`，用于对位移幅度一致性做 Huber 式鲁棒化，并额外惩罚预测位移幅度超过 GT 的部分，降低高难样本 p95 displacement magnitude 被继续推大的风险；新增 `WarpedImageGradientConsistencyLoss`，通过预测校正图与 GT 校正图的灰度梯度差异，在 GT 校正后的裂缝 ROI 附近约束边缘结构。新增参数默认均为 0，保持历史配置不变；Slurm 包装入口已支持 `W_CRACK_EDGE`、`CRACK_MAG_ROBUST_DELTA`、`CRACK_MAG_OVER_WEIGHT` 环境变量。下一步需在服务器 `.venv/bin/python` 下做 `py_compile` 和 2 epoch smoke，本地 Windows 工作区无 `.venv`，未进行 Python 编译验证。

2026-06-09 服务器完成 v4 robust+edge smoke：从 `v3_mag_ft_w005_10ep/best_epe.pth` 初始化，使用 `W_CRACK_MAG=0.05`、`CRACK_MAG_ROBUST_DELTA=0.01`、`CRACK_MAG_OVER_WEIGHT=0.5`、`W_CRACK_EDGE=0.05`、`LR=3e-6`、2 epoch，输出 `output_crackwarp_slurm/v4_robust_edge_smoke`。1000 样本结果为 crack EPE 111.473701、global EPE 110.564407、Dice 0.261134、crack edge fidelity 0.205674、global edge fidelity 0.228518、folding rate 0.585879。相比 `w005_10ep`，crack/global EPE、Dice、crack/global edge 和 folding 全部改善；逐图综合评分 631 张优于 `w005_10ep`、369 张劣于 `w005_10ep`，平均综合分提升 0.698709。top 改善样本包含此前退化严重的 `crack0063_07`、`crack0063_05`、`crack0079_09`、`crack0052_00`，说明鲁棒位移和边缘一致性约束确实压住了一部分大形变退化；剩余 top 退化样本较分散，主要表现为 crack EPE/Dice 局部波动和少量 edge 下降，而非系统性 folding 崩坏。当前判断：v4 方向成立，下一步可从 v4 smoke checkpoint 继续做 8-10 epoch 小规模训练，或保守地微调 `W_CRACK_EDGE` 后做对照。

2026-06-10 服务器任务 `35168809` 已完成 v4 robust+edge 延续训练，输出目录为 `output_crackwarp_slurm/v4_robust_edge_10ep`。完整 120 样本评估已保存到 `eval_best_epe_120/eval_summary.json` 和 `eval_best_epe_120/eval_per_image.csv`：primary crack EPE 106.084343、primary crack edge fidelity 0.325259、Dice 0.284522、global EPE 107.736298、global edge fidelity 0.310216、folding rate 0.582563、crack ratio 0.166787。与 v4 smoke 的 1000 样本指标不能直接横向比较，但从 120 样本口径看，crack/global EPE、edge fidelity 和 folding 均表现较好；下一步必须补做 1000 样本评估和与 `v3_mag_ft_w005_10ep`、`v4_robust_edge_smoke` 的逐图对比后再决定是否作为主模型。

2026-06-10 已完成 `v4_robust_edge_10ep/best_epe.pth` 的 1000 样本评估和逐图对比。1000 样本结果为 crack EPE 110.808922、global EPE 109.764503、Dice 0.260935、crack edge fidelity 0.210758、global edge fidelity 0.236428、folding rate 0.582459。相比 `v3_mag_ft_w005_10ep` 的 1000 样本结果，v4_10ep 的 crack/global EPE、Dice、crack/global edge 和 folding 均改善；逐图综合评分显示 796 张优于 v3_w005、204 张劣于 v3_w005，平均综合分提升 2.007661。相比 `v4_robust_edge_smoke`，v4_10ep 同样继续改善，723 张优于 smoke、277 张劣于 smoke，平均综合分提升 1.308952。当前判断：`v4_robust_edge_10ep` 是目前最强主模型候选，下一步应导出退化样本和共同失败样本的 flow 诊断，确认剩余 204/277 张退化样本是否集中在边界、大形变或裂缝 ROI 局部位移过强。

2026-06-10 服务器已导出 `v4_robust_edge_10ep` 相对 `v3_mag_ft_w005_10ep` 的 flow 诊断图：`flow_diag_v3_better/` 和 `flow_diag_joint_failures/` 各 20 张，并生成对应 `flow_diagnostics.csv`。控制台输出显示，共同失败组中 v4_10ep 在多数高难样本上仍降低 global EPE，例如 `crack0063_05` 从 193.454 降到 188.622，`crack0024_08` 从 178.126 降到 170.589，`crack0053_05` 从 152.892 降到 147.194；`crack0030_xx` 族群也普遍小幅改善但仍处于共同失败集合。`flow_diag_v3_better` 组虽然来自综合评分退化清单，但控制台 global EPE 并非全部退化，说明剩余劣化更可能来自 crack EPE、edge fidelity、Dice 或 folding 等综合项，需要汇总 CSV 中的 crack/folding/位移幅度均值后再判断。

2026-06-10 已汇总 `v4_robust_edge_10ep` flow 诊断 CSV 的 folding 与位移幅度指标。`flow_diag_v3_better` 20 张中，v4 相比 v3 的 folding rate 从 0.400386 小升到 0.402390，但 mean displacement magnitude 从 108.476px 降到 108.220px，p95 displacement magnitude 从 215.772px 降到 214.788px。`flow_diag_joint_failures` 20 张中，v4 folding rate 从 0.400744 小升到 0.402803，但 mean displacement magnitude 从 134.401px 降到 132.662px，p95 displacement magnitude 从 257.981px 降到 254.519px。当前判断：v4_10ep 的剩余退化不是由位移幅度继续放大造成，而是在降低位移幅度和改善 EPE 的同时引入了轻微 folding 增长；下一步若继续优化，应优先考虑 Jacobian/folding 正则或边界平滑，而不是继续压低位移幅度。

2026-06-10 已补充汇总 flow 诊断 CSV 的 global/crack EPE。`flow_diag_v3_better` 20 张中，v4 相比 v3 的 global EPE 从 110.830 降到 110.568，但 crack EPE 从 108.119 升到 110.112，同时 folding rate 从 0.400386 升到 0.402390，说明该组综合评分退化主要来自裂缝 ROI 对齐变差和轻微 folding 增长，而不是全图 EPE 或位移幅度。`flow_diag_joint_failures` 20 张中，v4 global EPE 从 136.136 降到 134.404，crack EPE 从 182.356 降到 180.555，位移幅度也下降，但 folding 仍从 0.400744 升到 0.402803；说明共同失败组整体仍被 v4 改善，只是绝对误差仍高。当前结论：`v4_robust_edge_10ep` 适合作为当前主模型；后续优化重点应转向裂缝 ROI 局部对齐和 folding/Jacobian 约束。

2026-06-10 已按用户 `rl-init` 请求初始化 RecallLoom 项目连续性侧车目录 `.recallloom/`，并通过官方 `recallloom.py validate` 校验，当前无错误和警告。`recallloom.py status` 显示连续性状态为 `initialized_empty_shell`、置信度为 medium，推荐下一步执行 `rl-resume`、`seed_initial_continuity`、审阅 `update_protocol.md` 与 `context_brief.md`。初始化工具默认将 workspace language 设为 `en`；尝试用 `--workspace-language zh-CN --force` 重生成时被工具安全拒绝，因此暂保留现状，不手工修改 RecallLoom 管理文件。

2026-06-10 已读取 Codex 线程 `019eaf6f-2f34-7cb2-80b8-b67b763d775e`（标题“查看项目进展”）的上下文，并将该线程中的项目现状种入 RecallLoom：`context_brief.md` 记录稳定项目目标、范围、来源和边界；`rolling_summary.md` 记录当前 v4 主模型候选、1000 样本指标、flow 诊断结论、筛图下一步和风险；`daily_logs/2026-06-10.md` 追加一条“导入目标线程项目现状”的里程碑记录。当前 RecallLoom 状态已从 `initialized_empty_shell` 推进为 `initialized_seeded`。

2026-06-10 已按 RecallLoom 恢复项目上下文并继续进入报告素材整理阶段，新增 `报告素材筛选建议.md`。本次筛选将样本分为整体提升、共同失败但仍改善、局限/退化三类：建议优先使用 `crack0011_05`、`crack0029_04` 展示 v4 综合提升，使用 `crack0063_05`、`crack0030_08` 展示高难样本仍有改善但未彻底解决，使用 `crack0002_07`、`crack0071_07` 展示少数裂缝 ROI 指标退化。当前报告口径应保持：`v4_robust_edge_10ep` 是当前最强主模型候选，但后续仍需优化裂缝 ROI 局部对齐、folding/Jacobian 约束和边界平滑。

2026-06-10 根据用户明确的四项交付差距（代码全部调好、整体训练完成、效果达到三区及以上并具备实验数据、补全新近对比实验和消融实验），新增 `最终实验推进计划.md` 和 `utils/generate_final_experiment_commands.py`。当前执行策略是先固定 v4 主模型候选并补最终评估，再优先跑 `abl_robust_only`、`abl_edge_only`、`abl_no_over_penalty`、`abl_edge_w003` 等关键消融；外部对比优先调研/适配 2023-2025 的 SEA-RAFT、UniMatch、MemFlow、NeuFlow 等新近 dense matching / optical flow 方法，避免只使用过旧弱基线。

2026-06-11 服务器已完成 v4 主模型最终 1000 样本复评和 4 个 2 epoch 消融 smoke。v4 最终复评结果为 crack EPE 110.757812、global EPE 109.779900、Dice 0.260918、crack edge fidelity 0.207583、global edge fidelity 0.231067、folding rate 0.582812，和此前 1000 样本结果基本一致，说明主模型结果稳定。4 个 smoke 任务 `35277882`、`35277889`、`35277895`、`35277898` 的 `.err` 均为空，训练链路可用。120 样本 smoke 指标显示：`abl_edge_only_2ep` 的 crack/global EPE 最低（106.617/108.150）且 Dice 0.283511；`abl_no_over_penalty_2ep` 的 crack edge 最高（0.317984）且 Dice 最高（0.284091），但 folding 最差（0.585758）；`abl_robust_only_2ep` 和 `abl_edge_w003_2ep` 整体接近但无明显优势。与 v4_10ep 的 120 样本口径相比，四个 2 epoch 消融均未全面超过主模型，因此暂不建议全部扩到 10 epoch；下一步优先对 `abl_edge_only_2ep` 和 `abl_no_over_penalty_2ep` 做 1000 样本评估，再决定是否扩训练。

2026-06-11 已完成 4 个 2 epoch 消融的 1000 样本评估。`abl_edge_only_2ep` 结果为 crack EPE 111.452118、global EPE 110.241234、Dice 0.260914、crack edge 0.202826、global edge 0.227496、folding 0.584606；`abl_no_over_penalty_2ep` 为 crack EPE 111.267204、global EPE 110.403343、Dice 0.260801、crack edge 0.202908、global edge 0.224709、folding 0.585978；`abl_robust_only_2ep` 为 crack EPE 111.650780、global EPE 110.481773、Dice 0.260820、crack edge 0.204112、global edge 0.226439、folding 0.585116；`abl_edge_w003_2ep` 为 crack EPE 111.529793、global EPE 110.527298、Dice 0.260168、crack edge 0.205216、global edge 0.228371、folding 0.585625。与 v4 主模型最终复评（crack EPE 110.757812、global EPE 109.779900、Dice 0.260918、crack edge 0.207583、global edge 0.231067、folding 0.582812）相比，四个 2 epoch 消融均未超过完整 v4；其中 `abl_no_over_penalty_2ep` crack EPE 相对较低但 folding 明显变差，`abl_edge_w003_2ep` crack edge 在消融中最高但 EPE、Dice 和 folding 仍弱于 v4。当前结论：完整 v4 的鲁棒位移、过大位移惩罚和边缘一致性组合仍是当前最稳方案，这 4 个 smoke 不建议扩到 10 epoch；下一步应转向外部新近方法对比适配，或设计新的 folding/Jacobian 正则消融。

2026-06-11 已新增 `utils/summarize_final_results.py`，用于汇总最终主模型和消融实验的 `eval_summary.json` / `compare_summary.json`，并生成 `output_crackwarp_slurm/final_ablation/summary_tables/final_ablation_summary.csv` 与 `final_ablation_summary.md`。汇总表纳入 v3 主线、v4 主模型和 4 个 2 epoch 消融，确认 v4 在 1000 样本上仍是当前最稳主模型：相对 v3，v4 逐图综合评分 796/1000 张更优；相对 4 个消融，v4 分别在 684、732、688、728 张样本上更优。该表可直接作为报告/论文消融实验表的基础版本。

2026-06-11 为紧扣用户“补全新近对比实验”的需求，新增 `utils/evaluate_flow_predictions.py` 作为外部方法通用评估入口。该脚本复用主模型评估口径，支持读取外部方法逐图导出的 `.npy` 预测坐标场，默认格式为与项目标签一致的 `[0,1]` 归一化逆映射坐标，也支持 `pixel_flow` 初步转换。后续 UniMatch、SEA-RAFT、MemFlow、NeuFlow 等方法只要先适配输出到 `pred_dir/xxx.png.npy`，即可用同一指标生成 `eval_summary.json` 和 `eval_per_image.csv`，从而进入最终对比表。

2026-06-11 服务器已完成外部评估入口的 GT sanity check：使用 `underwater_crack_v3` 标签作为预测坐标场评估 10 张样本，crack/global EPE 均为 0，crack/global edge fidelity 均为 1，说明 `utils/evaluate_flow_predictions.py` 的基础评估链路正确。随后确认服务器尚未生成 UniMatch `pred_grid`，并已在项目内 `external_methods/unimatch` 准备 UniMatch 官方仓库和 GMFlow 预训练权重。为继续外部对比适配，新增 `utils/export_unimatch_predictions.py`：该脚本用 GT 标签生成校正图，运行 UniMatch 估计 `GT 校正图 -> 扭曲输入图` 的 flow，再转为项目统一的归一化逆映射坐标场。注意该设置属于 oracle-pair dense matching 对比，报告中必须注明其输入条件不同于单图像主模型。

2026-06-11 服务器完成 UniMatch oracle-pair dense matching 的 1000 样本导出与评估，并已推送结果。UniMatch 1000 样本结果为 crack EPE 3.810645、global EPE 1.676183、Dice 0.715517、crack edge 0.750679、global edge 0.813755、folding rate 0.032984；与 v4 逐图对比中 UniMatch 综合分 1000/1000 张均优于 v4。该结果说明外部 dense matching 上界非常强，可用于论文/报告中的“oracle-pair 参考上界”或“有配对参考图时的 dense matching 对比”，但不能作为同输入条件直接对比主结论。已更新 `utils/summarize_final_results.py`，将 UniMatch 纳入最终汇总表并标注其 oracle-pair 属性。

2026-06-11 为继续补齐新近外部对比，新增 `utils/export_searaft_predictions.py`。该脚本按 SEA-RAFT 官方 README 的 custom 用法适配 Spring-M 配置和 HuggingFace 权重 `MemorySlices/Tartan-C-T-TSKH-spring540x960-M`，同样采用 oracle-pair 设置：先由 GT 标签生成校正图，再估计 `GT 校正图 -> 扭曲输入图` 的 flow，并保存为项目统一的 `pred_grid/xxx.png.npy` 归一化逆映射坐标场。下一步服务器需要 clone `princeton-vl/SEA-RAFT` 到 `external_methods/SEA-RAFT`，安装依赖，先跑 10 张 smoke，再用 `utils/evaluate_flow_predictions.py` 评估方向和指标。

## Recent Changes

- 新增八模型外部/内部对比链路：`utils/export_torchvision_raft_predictions.py`、`utils/export_gma_predictions.py`、`scripts/run_external_eight_model_comparison.sh`、`scripts/submit_external_eight_model_comparison.sbatch`、`scripts/summarize_eight_model_comparison.py`，并新增 plcyf bridge 检查/提交/日志脚本；已将 120 样本横评提交到 plcyf job `21093404`。

- 2026-07-14：确认 v108 A/B/C/D 120 补评估全部完成，并记录正式数据消融指标；结论为新增数据未带来稳定全面提升，v107 仍应作为最新主结果。
- 2026-07-13：确认 v107 plcyf 后处理完成并记录 120/1000/全量指标；v108 四组 smoke 训练完成但自动 eval 因非递归扫描失败，已修复 `utils/evaluate_metrics.py` 并提交四组 v108 120 补评估 jobs。
- 2026-07-13：将 v107 后处理和 v108 A/B/C/D smoke 从 usmidet 迁移到 plcyf；新增 plcyf 桥接/提交/同步脚本，修复 plcyf 绝对软链接兼容路径，同步最新模型与训练评估代码；取消 usmidet 旧排队任务。
- 2026-07-13：确认 v107 job `42728275` 完整完成 100 epoch，并记录训练尾部指标和 120 评估 summary；后处理 job `42728372` 与 v108 smoke jobs 仍在排队。
- 2026-07-12：新增 v108 data-ablation 数据准备、数据 smoke 和 A/B/C/D 短训提交脚本；远端已完成数据准备 job `42765485`，并提交 GPU smoke jobs `42765655`-`42765658`。
- 2026-07-12：新增 `scripts/download_external_candidate_datasets.sh` 和 `scripts/submit_external_candidate_download.sbatch`，用于在远端 CPU 队列下载外部候选数据集；usmidet job `42759575` 已完成，UIEB 三个公开压缩包已落到远端 `data_candidates/UIEB/`。
- 2026-07-12：根据用户提供的 KaggleHub 数据集路径，远端已成功下载 `pamuduranasinghe/euvp-dataset`；项目内 `data_candidates/EUVP/euvp-dataset-kagglehub` 为指向 KaggleHub cache 的软链接，避免重复占用约 1.23GB 空间。
- 2026-07-12：用户提供 Roboflow API key 后，远端使用官方 `roboflow` SDK 成功下载两个 underwater crack 候选数据集：`underwater-crack-detection` v3 YOLOv8 导出和 `underwater-concrete-crack` v2 YOLOv8 导出；下载状态与数量统计已写入远端 `data_candidates/roboflow_underwater_crack/`。
- 2026-07-12：新增外部候选数据筛选脚本并完成最终筛选 job `42764110`，正式输出目录为 `data_candidates/screened_external_candidates_v3/`；共评分 18962 张，按 UIEB/EUVP/Roboflow 四类来源生成 CSV、160 张综合候选和 contact sheets。
- 2026-07-11：新增 `scripts/run_v107_paper_postprocess.sh`，用于 v107 长训完成后自动生成 120/1000/all_final 评估、内部/外部模型可视化对照、v107 detail-head ROI 局部复核和 ROI flow 高频/folding 诊断。
- 2026-07-11：新增 `scripts/submit_v107_paper_postprocess.sh`，支持通过 `TRAIN_JOB_ID=<jobid>` 提交 `afterok` 依赖后处理任务；远端已提交依赖 job `42728372`。
- 2026-07-11：新增 `scripts/build_v107_paper_delivery_package.py`，按客户 5 项验收问题组织材料：可视化结果、指标对比、训练曲线、内部/外部对比、消融实验和三区标准说明；当前状态版 zip 已生成但不含 v107 完成后的指标。
- 2026-07-10：新增 `scripts/generate_available_training_curves.py`，从真实 `train.log` 生成所有可恢复训练曲线、覆盖表和解析点 CSV，并生成 `experiment_submission_package_2026-07-10_v1_v106_complete_final_with_curves_clean.zip`。新版包不含内部站点分层或站点名，推荐替代此前 final 包交付。
- 2026-07-10：新增 `scripts/build_v1_v106_complete_delivery_package.py`，构建 `experiment_submission_package_2026-07-10_v1_v106_complete_final.zip`。新版包通过 `FULL_ARTIFACT_INDEX.csv` 和 `VERSION_PROGRESS_INDEX.csv` 完整反映 v1-v106 成果物，实际收录轻量结果、代表性可视化和体积可控阶段包，并对未全量收录的大量图片给出索引和原因。
- 2026-07-09：按用户反馈重建 user-ready 交付包，去掉 `07_raw_summaries/plcyf` 与 `07_raw_summaries/usmidet` 站点分层，改为按实验问题归类原始 summary/log/CSV；新版压缩包为 `experiment_submission_package_2026-07-09_user_ready.zip`。
- 2026-07-09：新增 `scripts/build_experiment_submission_package.py`，并生成 `experiment_submission_package_2026-07-09/` 与 `experiment_submission_package_2026-07-09.zip`。该包按“长训练指标、对比实验、消融实验、训练曲线图、代表可视化、原始 manifest/summary”重组材料，避免直接交付 `plcyf_delivery_assets.tar.gz` 与 `usmidet_delivery_assets.tar.gz` 两个原始大包。
- 2026-07-09：新增 v106 stronger detail head：`models/single_image_flow_net.py` 为 `ImageDetailHeadGlobalContextFlowUNet` 增加 `detail_gate_bias/detail_gate_floor/detail_patch_base` 参数，并新增 builder `build_global_context_flow_unet_image_detail_head_strong`；`models/model_factory.py` 注册模型名 `global_context_flow_unet_image_detail_head_strong`。
- 2026-07-09：`loss_crack.py` 和 `train_v2.py` 增加 detail 分支数值诊断日志：`detail_gate_mean/max/active_ratio` 与 `detail_residual_abs_mean/max`，训练日志显示为 `d_gate_m/d_gate_x/d_gate_a/d_res_m/d_res_x`。
- 2026-07-09：新增 `scripts/submit_v106_detail_strength_probe_from_v105_plcyf.sh` 并已同步远端提交/完成 Slurm job `42509034`；v106 detail-aware ROI 导出已完成，生成 298 张 panel，并拉回 5 张关键 panel 做人工复核。
- 2026-07-09：新增客户沟通材料目录 `client_visualization_report_2026-07-09/`，其中 `README.md` 回答指标、对比/消融、可视化现状和后续优化问题，`images/` 收集 9 张代表性可视化与诊断图，便于后续直接打包发送。
- 2026-07-09：新增 v105 image-space detail/residual head：`models/single_image_flow_net.py` 增加 `ImageDetailHeadGlobalContextFlowUNet` 和 builder，`models/model_factory.py` 注册 `global_context_flow_unet_image_detail_head`；默认推理仍返回主 flow，显式 `return_detail=True` 时额外返回 detail image、residual 和 gate。
- 2026-07-09：新增 `w_source_image_detail` 与 `use_image_detail_output`，并接通 `config_crack.py`、`loss_crack.py`、`train_v2.py`、`run_train_slurm.py`、`slurm_train_crackwarp.sbatch`；训练日志新增 `s_imgdet`，loss 支持 dict 输出并只监督 `detail_image`。
- 2026-07-09：新增 `scripts/submit_v105_image_detail_head_smoke_from_v102_plcyf.sh`，远端实际提交时因 `/mnt/users/...` 路径缺少 v102 checkpoint，默认改为 v89 checkpoint 初始化，并支持 `INIT=...`、`OUT=...`、`SLURM_PARTITION`、`SLURM_ACCOUNT`、`SLURM_QOS` 覆盖。
- 2026-07-09：v105 已通过本地 `py_compile`、bash 语法检查、远端 PyTorch 前向/反向 smoke 和 detail-loss 梯度隔离验证；远端 Slurm job `42451293` 已完成，产出 `best_epe.pth`、`best_crack_epe.pth`、`final.pth`、`train.log` 和 `eval_best_epe_120/eval_summary.json`。
- 2026-07-09：新增 `utils/export_image_detail_roi_visuals.py`，自包含实现 v105 detail-aware ROI panel 导出、`detail_image/detail_residual/detail_gate` 可视化和局部 Source-GT sharpness 汇总；远端已用 `scripts/v99_hard_roi_families.txt` 展开 70 张合成图并导出 298 张 ROI panel。
- 2026-07-09：人工抽看 v105 五个关键 detail-aware panel，确认 `detail_residual` 近乎全黑、`detail_gate` 低响应且 `detail_image` 与 `flow_corrected` 基本无肉眼差异；该结论与 ROI 指标接近零变化一致。
- 2026-07-09：新增 v104 Stage A / Stage B 提交与后处理脚本：`scripts/submit_v104_stagea_visible_only_from_v102_plcyf.sh`、`scripts/submit_v104_stageb_restore_guard_from_stagea_plcyf.sh`、`scripts/run_v104_stagea_postprocess.sh`、`scripts/run_v104_stageb_postprocess.sh`；已完成 v104a 远端训练和 CPU compact 后处理，并新增 `output_crackwarp_slurm/v104_stagea_visible_only_from_v102_1ep_plcyf/visual_review_v104.md` 记录结论。
- 2026-07-09：`scripts/run_v104_stagea_postprocess.sh` 新增 `POST_DEVICE`、`POST_MODE` 与 `GPU_ID` 参数。`POST_DEVICE=cpu` 可在 plcyf GPU 被占满时禁用 CUDA，`POST_MODE=compact` 只加载 v102 与目标模型，避免 ROI 诊断一次常驻多个 checkpoint 导致显存 OOM。
- 2026-07-09：v104a 结果确认 high-edge-grad 仍未突破：ROI high-edge-grad win `2/25`、Source-GT 120 high-edge-grad win `0/120`；虽 EPE/folding/flow 高频明显改善，但 ROI 平均位移降至 `3.972399px` 且全部落入 `0-8px` 桶，因此不进入 Stage B。
- 2026-07-08：新增 v103 梯度冲突诊断脚本 `utils/diagnose_v103_gradient_conflict.py`，固定模型输出后分别计算 ROI 内 coordinate、visible-structure、flow high-frequency、flow Laplacian 对 `pred_flow` 的梯度，并输出 cosine、冲突像素比例、梯度范数和单步更新后的 ROI sharpness / flow 高频变化。
- 2026-07-08：新增 `scripts/run_v103_gradient_conflict_diagnostic.sh`，脚本内置 v95-v102 复用的 7 张关键 ROI 图族清单，避免远端缺少历史 image list 导致诊断中断。
- 2026-07-08：完成 v103 远端诊断并拉回结果，新增 `output_crackwarp_slurm/v103_gradient_conflict_probe_from_v102/visual_review_v103.md` 记录结论。

- 2026-07-08：新增并验证 v102 ROI visible-structure loss 链路，`loss_crack.py` 新增 `SourceGTVisualConsistencyLoss.roi_visible_structure(...)`，`config_crack.py`、`train_v2.py`、`run_train_slurm.py`、`slurm_train_crackwarp.sbatch` 和 `scripts/smoke_source_gt_loss.py` 已接入 `w_source_roi_visible_structure` / `W_SOURCE_ROI_VISIBLE_STRUCTURE` 与日志项 `s_visroi`。
- 2026-07-08：新增 v102 远端执行和诊断脚本：`scripts/submit_v102_roi_visible_structure_flowhf_from_v100_plcyf.sh`、`scripts/run_v102_gates_120.sh`、`scripts/run_v102_roi_sharpness_after_train.sh`、`scripts/run_v102_roi_flow_highfreq_after_train.sh`、`scripts/run_v102_focus_flow_attribution_panels.sh`、`scripts/run_v102_fixed40_visual.sh`、`scripts/run_v102_roi_visual.sh` 和 `scripts/make_v102_review_sheets.py`。
- 2026-07-08：完成 v102 远端训练与后处理，拉回本地关键 JSON/CSV、训练日志和部分 review sheets，并新增 `output_crackwarp_slurm/v102_roi_visible_structure_flowhf_from_v100_1ep_plcyf/visual_review_v102.md` 记录结论。
- 2026-07-08：v102 结果确认 ROI edge-weighted win `12/25`、high-edge-grad win `2/25`、120 Source-GT high-edge-grad win `0/120`，未达到继续扩严格 1000 的硬门槛。

- 2026-07-08：新增 v101 ROI directional structure 组合消融，`loss_crack.py` 增加 `SourceGTVisualConsistencyLoss.roi_directional_structure(...)`，并贯穿 `config_crack.py`、`train_v2.py`、`run_train_slurm.py`、`slurm_train_crackwarp.sbatch` 与 `scripts/smoke_source_gt_loss.py`；新增 `scripts/submit_v101_roi_directional_structure_flowhf_from_v100_plcyf.sh`、`scripts/run_v101_gates_120.sh`、`scripts/run_v101_roi_sharpness_after_train.sh`、`scripts/run_v101_roi_flow_highfreq_after_train.sh`、`scripts/run_v101_focus_flow_attribution_panels.sh`、`scripts/run_v101_fixed40_visual.sh`、`scripts/run_v101_roi_visual.sh` 和 `scripts/make_v101_review_sheets.py`。v101 远端训练、gate、ROI sharpness、ROI flow 高频归因、focus panel、fixed40/ROI review sheets 已完成并拉回本地，复核记录见 `output_crackwarp_slurm/v101_roi_directional_structure_flowhf_from_v100_1ep_plcyf/visual_review_v101.md`。
- 2026-07-08：新增 v100 anti-ripple 路线，`loss_crack.py` 增加 `CrackFlowHighFreqLoss`，并贯穿 `config_crack.py`、`train_v2.py`、`run_train_slurm.py`、`slurm_train_crackwarp.sbatch` 与 `scripts/smoke_source_gt_loss.py`；新增 `scripts/submit_v100_flow_highfreq_guard_from_v98_plcyf.sh`、`scripts/run_v100_gates_120.sh`、`scripts/run_v100_roi_flow_highfreq_after_train.sh`、`scripts/run_v100_focus_flow_attribution_panels.sh`。v100 远端训练、gate、ROI 高频归因和 focus panel 已完成并拉回本地，复核记录见 `output_crackwarp_slurm/v100_flow_highfreq_guard_from_v98_1ep_plcyf/visual_review_v100.md`。
- 2026-07-07：增强 v99 ROI flow 高频诊断，`utils/diagnose_roi_flow_highfreq.py` 新增多尺度 highfreq kernel、位移分桶、相对 baseline 的逐 ROI delta 和 Markdown 归因报告；`scripts/run_v99_roi_flow_highfreq_after_train.sh` 增加 `--report_md`、`--target_label v99`、`--baseline_label v98` 等参数；新增 `scripts/run_v99_flow_attr_plcyf.sh` 作为远端 plcyf 复现入口。新版诊断已在远端重跑并拉回本地，输出 `roi_flow_highfreq_attribution.md`，用于定位 v99 相对 v98 的 water-ripple 退化 ROI。
- 2026-07-07：新增 v99 ROI rank structure 路线，`loss_crack.py` 增加 `SourceGTVisualConsistencyLoss.roi_rank_structure(...)`，并贯穿 `config_crack.py`、`train_v2.py`、`run_train_slurm.py`、`slurm_train_crackwarp.sbatch` 与 `scripts/smoke_source_gt_loss.py`；新增 `utils/diagnose_roi_flow_highfreq.py` 用于关键 ROI flow 高频、flow Laplacian gap 和 folding 诊断，并新增 v99 hard ROI 图族、远端提交、gate、fixed40、ROI、ROI sharpness、ROI flow 高频和 review sheet 脚本。v99 远端训练与后处理已完成，结果已拉回本地并记录到 `output_crackwarp_slurm/v99_roi_rank_structure_from_v93_1ep_plcyf/visual_review_v99.md`。
- 2026-07-07：新增 v98 hard ROI two-stage 路线，`loss_crack.py` 增加 `SourceGTVisualConsistencyLoss.hard_roi_structure(...)`，并贯穿 `config_crack.py`、`train_v2.py`、`run_train_slurm.py`、`slurm_train_crackwarp.sbatch` 与 `scripts/smoke_source_gt_loss.py`；新增 v98 hard ROI 图族、远端提交、gate、fixed40、ROI、ROI sharpness 和 review sheet 脚本。v98 远端训练与后处理已完成，结果已拉回本地并记录到 `output_crackwarp_slurm/v98_hardroi_twostage_from_v93_1ep_plcyf/visual_review_v98.md`。
- 2026-07-07：新增 v97 高边缘锐度下界约束，`loss_crack.py` 增加 `SourceGTVisualConsistencyLoss.high_edge_sharpness(...)`，并贯穿 `config_crack.py`、`train_v2.py`、`run_train_slurm.py`、`slurm_train_crackwarp.sbatch` 与 `scripts/smoke_source_gt_loss.py`；新增 v97 远端提交、gate、fixed40、ROI、ROI sharpness 和 review sheet 脚本，v97 结果已拉回本地并记录到 `visual_review_v97.md`。
- 2026-06-29：新增 Slurm `W_PHOTO/--w-photo` 透传，`run_train_slurm.py`、`slurm_train_crackwarp.sbatch` 与 `slurm_train_crackwarp_lf.sbatch` 已支持远端实验动态调整 `config.w_photo`，并通过本地/远端 `.venv` 语法检查；该改动用于 v69 验证增强 photometric 保护是否能缓解 source-GT regress。
- 2026-06-29：新增组合 gate 报告脚本和 v41 缩放位移诊断/可视化工具；完成 v41 `alpha=0.05/0.1/0.15/0.2/0.3` 的 120 source-GT 扫描，确认 `alpha=0.1` 是当前最接近“非 identity 且不过度采样”的中间点，但 high-edge-grad 仍未过。
- 2026-06-29：新增 source-GT 图像空间 gate 与带 Source GT 列的横向可视化导出；完成 fixed40/120 诊断，确认 v63 近 identity 但在源图视觉参照下优于 input，而 v22/v41 的大位移校正存在明显过度采样风险。
- 2026-06-29：将本机用户 Startup 中的 `SlurmFloatingStatus.lnk` 从旧版 `dist\SlurmFloatingStatus\SlurmFloatingStatus.exe` 切换到最新版 `dist_timeout30_countfix_cst2\SlurmFloatingStatus\SlurmFloatingStatus.exe`，旧快捷方式已移动到 `Startup\disabled-startup-backup` 备份目录而非删除。
- 2026-06-24：v43 120 评估已完成，crack EPE 略有下降但 global EPE、Dice、global edge、folding 和逐图综合胜率均弱于 v22/v41，因此按止损规则不扩严格 1000。
- 2026-06-24：生成 v44 混合分段训练清单 `output_crackwarp_slurm/hard_samples/v44_v22v16_oldbetter_top20_plus_bg200.txt`，包含 v43 top20 退化家族和 200 个训练 split 背景 stem；第一次 v44 job `39426359` 因清单生成失败而失败，已修正数据集构造参数并重新提交 v44 job `39426366`。
- 2026-06-24：v42 `output_crackwarp_slurm/v42_segment_top5_fold0015_from_v22_5ep` 已完成训练与 120 评估；提高 `W_FOLD=0.015` 虽让 folding 相对 v41 小幅回落，但 EPE/Dice/global edge 退化，且相对 v22 未形成综合优势，因此不扩严格 1000。
- 2026-06-24：生成 v43 top20 分段训练清单 `output_crackwarp_slurm/hard_samples/v43_v22v16_oldbetter_top20_families.txt`，来源为 v22 相对 v16 严格 1000 的 old-better 退化集中家族；已提交 v43 job `39421645`，输出目录 `output_crackwarp_slurm/v43_segment_v22v16_oldbetter_top20_from_v22_5ep`，启动日志确认匹配 `179/9324` 个训练样本并从 v22 checkpoint 正常加载。
- 2026-06-20：通过 `billy` SSH 到 `100.94.186.128`，免密 sudo 切到 `xj62kv`，恢复远端被清空的 `run_train_slurm.py`、`train_v2.py` 和 `utils/build_hard_sample_list.py`，并确认核心代码与本地提交 `58a6a6b` 对齐。
- 2026-06-20：提交并完成 Slurm 任务 `37577083`，输出目录为 `output_crackwarp_slurm/v10_hard_roi_w15_from_v8mag_smoke_1ep`；该任务验证了 v10 hard-sample 低权重训练入口、v8 初始化、loss 参数透传、checkpoint 保存和 120 样本自动评估链路。
- 2026-06-20：补做 `v10_hard_roi_w12_from_v8mag_smoke_1ep` 的 1000 样本评估与逐图对比，确认 w12 比 w15 更弱，hard-sample 采样加权路线暂不宜继续加大。
- 2026-06-20：在 `utils/generate_final_experiment_commands.py` 增加 `v10-hard` 阶段，输出 `HARD_SAMPLE_WEIGHT=1.05` 的极轻权重 smoke 与后续 1000 样本评估命令，作为 hard-sample 路线的最后验证。
- 2026-06-20：远端完成 v10 hard-sample 极轻权重 smoke job `37587678`，并完成严格 1000 样本分片评估与 v8 逐图对比；`HARD_SAMPLE_WEIGHT=1.05` 仍弱于 v8，hard-sample 采样加权路线不再建议继续扩训。
- 2026-06-20：远端完成 v11 ROI local-align smoke job `37588489`，并完成严格 1000 样本分片评估与 v8 逐图对比；v11 提升 edge/folding 但显著损伤 EPE、Dice 和综合胜率，不建议直接扩到 10 epoch。
- 2026-06-20：远端完成 v11-B / v11-C ROI/Jacobian 降权 smoke 与严格 1000 样本评估；v11-C 相对 v8 逐图综合评分 614/1000 更优，平均综合分 `+0.416242`，已提交 v11-C 10 epoch 扩训 job `37590757` 到 `gpuHz` 分区。
- 2026-06-22：新增 `scripts/monitor_slurm_jobs.py`，并部署到远端 `/mnt/users/xj62kv/slurm-monitor/`；通过 `cron` 每分钟监控 `xj62kv` 的 Slurm 作业，作业新增/提交和结束通过 PushPlus 通知，作业开始运行只更新内部状态不通知。本地仓库只保存脚本，不保存 PushPlus token；远端 token 仅保存在权限 `600` 的 `config.json`。
- 新增 `.gitignore`，忽略 `.venv/`、Python 缓存、系统文件和常见编辑器目录。
- 新建项目本地 `.venv`，用于满足本项目 Python 环境约束。
- 新增本项目级 `AGENTS.md`，记录项目目标、当前结构、维护规范、进展、风险和下一步计划。
- 只读解析了两份用户提供的 `.docx` 报告，未修改报告原文。
- 新增 `水下裂缝扭曲校正项目工作内容与工作量说明.md`，作为对外报价和项目排期说明的可编辑源稿。
- 新增 `水下裂缝扭曲校正项目工作内容与工作量说明.docx`，作为可直接发给客户/甲方的 Word 版说明稿。
- 为生成和结构化校验 Word 文档，在项目本地 `.venv` 中安装了 `python-docx`、`pdf2image` 和 `pillow`。
- 用户已补充完整代码、主数据集、原始裂缝图、训练日志、checkpoint、评估结果和可视化结果。
- 已只读统计新增内容：`underwater_crack_v3` 含 10,360 对 `.png` / `.png.npy`，`under-crack-images/images` 含 1,037 张 `.jpg`，`output_crackwarp` 含 13 个 checkpoint 和 gate/eval 结果。
- 已抽样检查 `underwater_crack_v3` 标签范围，样本标签 shape 为 `(2,512,512)`，范围为 `[0,1]`，与项目文档中的归一化逆映射坐标定义一致。
- 已确认当前 `.venv` 缺少训练依赖，只补充安装了用于只读标签检查的 `numpy`，尚未安装 PyTorch 或启动训练。
- 新增 `项目交接与下一步开发计划.md`，用于整文件夹拷贝到另一台电脑后继续开发，记录当前现状、已有指标、环境搭建、小闭环复现和后续优化路线。
- 新增 `run_train_slurm.py`，作为 Slurm 训练包装入口，在不改动原训练主逻辑的前提下覆盖训练数据、输出目录、epoch、worker、梯度累积和学习率等参数，并强制关闭 `restart_training` 以保护历史结果。
- 新增 `slurm_train_crackwarp.sbatch`，默认使用 `gpu` 分区、`gpo-ifv7xx` 账号、`normal` QOS、1 张 GPU、8 CPU、64GB 内存和 48 小时时间限制；训练结束后自动对 `best_epe.pth` 做 120 张样本的小规模评估。
- 更新 `.gitignore`，忽略 Slurm 标准输出和错误日志 `slurm-*.out`、`slurm-*.err`。
- 新增 `requirements.txt`，记录除 PyTorch 以外的训练、评估、可视化和文档维护依赖；PyTorch 仍需按集群 CUDA 环境单独安装 CUDA 12.x 兼容 wheel。
- 只读检查了项目下全部 6 份 Markdown 文档和 3 份 Word 文档，确认项目目标、验收口径、已有训练状态、主要短板、工作量估算和下一步 Slurm 训练需求。
- 完成一次当前项目明显问题只读体检：确认主要风险集中在现有 checkpoint 指标远未达标、`predict.py` 旧推理坐标逻辑可疑、`restart_training=True` 有清空历史输出风险、训练脚本缺少严格断点续训、文档脚本名/命令过时、`.venv` 已失效、folding 约束与评估口径仍需复核。
- 用户删除旧 `.venv` 后，已使用本机 Python 3.11 重新创建项目本地 `.venv`，安装 PyTorch `2.12.0+cu126`、torchvision `0.27.0+cu126` 和 `requirements.txt` 中的训练/评估/文档依赖；本机检测到 CUDA 可用，GPU 为 Quadro T1000。
- 新增 `smoke_train_verify.py`，用于本机最小训练验证。已在 CPU 和 GPU 上分别跑通 2 个样本、64×64、`base_ch=4`、`n_iter=0`、1 epoch 的最小训练和验证，确认数据读取、模型前向、loss、反传、验证和权重保存链路可用。
- 更新 `.gitignore`，忽略 `output_smoke_local*/` 本地 smoke 输出目录。
- 初始化本地 Git 仓库并推送到 public GitHub 仓库 `https://github.com/zh23jemu/underwater-crack-correction`；普通 Git 历史只包含代码、文档、脚本和小型说明文件，不包含大数据、历史训练输出、虚拟环境或本地 smoke 产物。
- 创建 GitHub Release `data-v1`，地址为 `https://github.com/zh23jemu/underwater-crack-correction/releases/tag/data-v1`；已上传 33 个 Release assets，总大小约 23.66GB，包括 `under_crack_images.zip`、`output_crackwarp.zip` 和 `underwater_crack_v3.tar.zst.part-001` 至 `part-031`。
- 已上传 `SHA256SUMS.txt` 到 Release，并在仓库根目录新增同名校验清单；服务器端下载后应优先执行 `sha256sum -c SHA256SUMS.txt` 验证 2 个 zip 和 31 个主数据集分卷是否完整。
- 新增并更新 `GITHUB_UPLOAD_PLAN.md`，记录 Git 内容与 Release 大文件资产划分，以及服务器侧 `gh release download`、分卷合并、完整性校验和解压命令。
- 新增 `新电脑接续工作指南.md`，用于换电脑后继续工作，集中记录 GitHub 仓库、Release 数据、服务器端数据已解压状态、新电脑 clone/pull、服务器环境重建、Slurm smoke、正式训练和后续排查路线。
- 2026-06-07 复核当前轻量仓库状态：本地已不包含大数据目录和历史输出目录，`.gitignore` 已忽略 `underwater_crack_v3/`、`under-crack-images/`、`output_crackwarp/`、`release_assets/`、Slurm 日志、checkpoint 和 smoke 产物。
- 2026-06-07 复核 Slurm 入口：`slurm_train_crackwarp.sbatch` 默认使用 `gpu` 分区、`gpo-ifv7xx` 账号、`normal` QOS、1 GPU、8 CPU、64GB 内存、48 小时，训练后会对 `best_epe.pth` 做 120 样本评估。
- 2026-06-08 根据服务器回传结果记录正式训练进展：Slurm 任务 `34827388` 已完成 50 epoch，输出到 `output_crackwarp_slurm/v2`，`.err` 为空，并生成 `eval_best_epe_120/eval_summary.json` 与 `eval_per_image.csv`。
- 2026-06-08 初步判断 v2 训练结果：`best_epe.pth` 的 120 样本 crack EPE 为 108.987938、global EPE 为 111.060646、crack Dice 为 0.283552；较旧权重略有改善但不足以支撑当前目标，需要进入诊断和结构/损失优化阶段。
- 2026-06-08 追加评估 `best_crack_epe.pth`、`best_loss.pth` 和 `best_epe.pth` 的 1000 样本结果；确认三个 best checkpoint 在 120 样本上指标一致，且 1000 样本评估显示泛化指标回落。
- 2026-06-08 追加旧模型 `output_crackwarp/best_epe.pth` 的 1000 样本复评，确认 v2 相比旧模型只是 EPE/Dice 小幅改善，edge/folding 指标退化。
- 2026-06-08 新增 `utils/compare_eval_per_image.py`，用于从新旧模型逐图评估结果中挑选诊断样本；同时更新 `utils/export_crack_roi_visuals.py`，支持通过 `--image_list` 只导出指定样本的 ROI 可视化。
- 2026-06-08 拉取服务器提交 `f6f4645`，完成对 `compare_old_vs_v2` 和共同失败 ROI 图的本地分析；确认 v2 的改进不稳定，主要问题集中在大形变、边界区域和裂缝细节保持。
- 2026-06-08 新增整图 flow 诊断脚本 `utils/export_flow_diagnostics.py`，为下一步服务器端导出 EPE/folding/位移幅度热图做准备。
- 2026-06-08 修复 `utils/export_flow_diagnostics.py` 直接运行时找不到根目录模块的问题：脚本启动时显式把项目根目录加入 `sys.path`，以兼容服务器命令 `.venv/bin/python utils/export_flow_diagnostics.py ...`。
- 2026-06-08 拉取并分析服务器生成的共同失败样本整图 flow 诊断结果；确认 v2 对全图 EPE 有局部改善，但裂缝 ROI EPE 不稳定，恢复幅度存在样本依赖的过小/过大问题。
- 2026-06-08 拉取并分析 `flow_diag_new_better` 与 `flow_diag_old_better`，确认 v2 的提升来自抑制旧模型过大位移，而退化样本多为 v2 位移幅度异常增大。
- 2026-06-08 新增可开关的裂缝 ROI 位移幅度一致性损失 `w_crack_mag`，默认关闭；Slurm 脚本支持用 `W_CRACK_MAG` 环境变量打开。
- 2026-06-08 根据服务器 v3 从零 smoke 结果修正策略：新增 `INIT_CHECKPOINT` 微调入口，避免新 loss 从随机初始化阶段主导不稳定位移场。
- 2026-06-09 记录 v3 fine-tune smoke 结果：从 v2 checkpoint 初始化、`W_CRACK_MAG=0.1`、`LR=5e-6`、2 epoch 训练链路正常，120 样本指标较 v2 有小幅改善且 folding 未爆炸。
- 2026-06-09 拉取服务器提交 `63a0971`，同步 `output_crackwarp_slurm/v3_mag_ft_smoke/eval_best_epe_1000/` 与 `compare_v2_vs_v3_ft/`，确认 v3 fine-tune 在 1000 样本上综合优于 v2，但 edge fidelity 仍小幅下降。
- 2026-06-09 根据服务器控制台输出记录 v2-v3 正反样本 flow 诊断已生成；尚需将 `flow_diag_new_better/` 与 `flow_diag_old_better/` 的 CSV 和图片同步回本地后做完整归因分析。
- 2026-06-09 拉取服务器提交 `5b9ca78`，完成 v2-v3 正反样本 flow 诊断 CSV 和代表性图片分析；确认 v3 变好组主要伴随位移幅度下降，变差组主要伴随位移幅度和 folding 小幅上升。
- 2026-06-09 拉取服务器提交 `3346ffd`，完成 `v3_mag_ft_w005_10ep` 的 1000 样本结果和逐图对比分析；确认该版本较 v2 和 `W_CRACK_MAG=0.1` smoke 更稳定，是当前主线候选。
- 2026-06-09 拉取服务器提交 `c200b54`，完成 `v3_mag_ft_w005_10ep` 退化样本和共同失败样本 flow 诊断；确认剩余失败主要集中在裂缝 ROI 的局部位移过强、folding 增加和边缘对齐不足。
- 2026-06-09 修改 `loss_crack.py`、`train_v2.py`、`run_train_slurm.py`、`slurm_train_crackwarp.sbatch` 和 `config_crack.py`，接入鲁棒位移幅度约束、过大位移惩罚和裂缝 ROI 校正图边缘一致性损失；默认关闭以保持历史结果可复现。
- 2026-06-09 拉取服务器提交 `2c37686`，分析 `v4_robust_edge_smoke` 的 120/1000 样本评估和与 `w005_10ep` 的逐图对比；确认 v4 在 EPE、Dice、edge fidelity 和 folding 上均进一步改善。
- 2026-06-10 记录服务器 Slurm 任务 `35168809` 已跑完 `v4_robust_edge_10ep`，其 120 样本评估输出已生成，日志片段显示 global EPE、global edge fidelity 和 folding rate 继续保持改善趋势。
- 2026-06-10 完成 `v4_robust_edge_10ep` 的 1000 样本评估和与 `v3_mag_ft_w005_10ep`、`v4_robust_edge_smoke` 的逐图对比；确认 v4_10ep 在当前主指标和多数样本上均为当前最佳候选。
- 2026-06-10 导出 `v4_robust_edge_10ep` 相对 v3_w005 的退化样本和共同失败样本 flow 诊断图；初步观察共同失败组 global EPE 多数继续下降，但退化组需要进一步汇总 crack、edge、folding 和位移幅度指标。
- 2026-06-10 汇总 v4_10ep flow 诊断 CSV 的 folding 与位移幅度指标；确认 v4 剩余副作用更像轻微 folding 增长，而不是位移幅度被继续推大。
- 2026-06-10 补充汇总 flow 诊断 CSV 的 global/crack EPE；确认 v3_better 组的 v4 退化主要来自 crack EPE 上升和 folding 小幅上升，而共同失败组中 v4 仍同时改善 global/crack EPE。
- 2026-06-10 初始化 RecallLoom 侧车目录 `.recallloom/`，生成 `context_brief.md`、`rolling_summary.md`、`update_protocol.md`、`config.json` 和 `state.json`，并完成 `validate` 与 `status` 检查。
- 2026-06-10 从 Codex 线程 `019eaf6f-2f34-7cb2-80b8-b67b763d775e` 导入项目现状到 RecallLoom，更新 `context_brief.md`、`rolling_summary.md`，并追加 `daily_logs/2026-06-10.md` 里程碑记录。
- 2026-06-10 按 RecallLoom 接续项目，新增 `报告素材筛选建议.md`，整理 v4 报告候选样本、指标依据、ROI 图路径、flow 诊断图路径和后续写作口径。
- 2026-06-10 新增 `最终实验推进计划.md`，将代码收口、主模型最终评估、外部新近方法对比、消融实验和论文实验材料整理拆成可执行阶段。
- 2026-06-10 新增 `utils/generate_final_experiment_commands.py`，用于生成 v4 主模型最终评估命令、关键消融 Slurm 训练命令、消融统一评估命令和外部对比方法优先级清单。
- 2026-06-11 服务器完成 v4 主模型最终 1000 样本复评，指标与此前结果基本一致，确认 `v4_robust_edge_10ep` 主模型候选稳定。
- 2026-06-11 服务器完成 4 个 2 epoch 消融 smoke，任务 `.err` 均为空；初步结果显示 `abl_edge_only_2ep` 和 `abl_no_over_penalty_2ep` 更值得继续做 1000 样本评估。
- 2026-06-11 完成 4 个 2 epoch 消融的 1000 样本评估；确认所有单项/弱化配置均未超过完整 v4，消融结果可用于证明完整 v4 组合必要性。
- 2026-06-11 新增 `utils/summarize_final_results.py` 并生成最终主模型与消融实验汇总 CSV/Markdown 表，便于后续直接写报告或论文实验部分。
- 2026-06-11 新增 `utils/evaluate_flow_predictions.py`，为外部新近方法提供统一预测坐标场评估入口；同步更新 `最终实验推进计划.md` 中的外部方法输出格式约定。
- 2026-06-11 完成外部评估入口 GT sanity check，并新增 `utils/export_unimatch_predictions.py`，用于导出 UniMatch oracle-pair dense matching 预测坐标场。
- 2026-06-11 拉取 UniMatch 1000 样本外部对比结果，确认其作为 oracle-pair 上界参考显著强于 v4；同步更新最终结果汇总脚本以纳入 UniMatch。
- 2026-06-11 新增 `utils/export_searaft_predictions.py`，准备将 SEA-RAFT 作为第二个新近外部 oracle-pair dense matching 对比方法。
- 2026-06-11 通过 RecallLoom 接续项目后，新增 `最终实验结果汇总.md`，把 v4 主模型、4 个 2 epoch 消融和 UniMatch oracle-pair 外部上界整理为面向报告/论文实验章节的中文结论稿；当前写作口径明确为：v4 是当前最稳内部主模型，UniMatch 只能作为配对参考条件下的上界，不可写成同输入条件直接公平对比。
- 2026-06-11 继续围绕客户四项核心交付要求收束，新增 `客户需求对齐检查清单.md`，把“代码全部调好、整体训练完成、三区及以上实验完整度、新近对比与消融实验”逐项映射到当前证据、缺口和下一步动作；同时更新 `utils/generate_final_experiment_commands.py`，使 `--stage external` 能直接输出 UniMatch 与 SEA-RAFT oracle-pair 的 smoke、1000 样本评估和 v4 逐图对比命令。
- 2026-06-11 服务器运行 SEA-RAFT smoke 时失败，报错为 `ImportError: cannot import name 'coords_grid' from 'utils.utils'`，根因是 SEA-RAFT 仓库内部 `utils` 包与本项目 `utils` 包重名，服务器脚本仍在导入 SEA-RAFT 前保留了项目 `utils` 模块缓存。应先同步或修补 `utils/export_searaft_predictions.py` 中的 `load_searaft` 导入隔离逻辑，再重新从 10 张 smoke 开始；在预测文件未生成前，不应继续执行评估、对比和查看 summary 命令。
- 2026-06-11 服务器已修复 SEA-RAFT `utils` 包名冲突并完成 10 张 smoke 导出/评估。`export_searaft_predictions.py --num 10 --overwrite` 成功生成 `crack0001_00.png.npy` 至 `crack0001_09.png.npy`；10 张评估结果为 crack EPE 3.064834、global EPE 2.377873、Dice 0.649974、crack edge 0.728619、global edge 0.709878、folding rate 0.039522。当前判断 SEA-RAFT oracle-pair 方向正常，可扩大到 1000 样本并与 v4 主模型逐图对比。
- 2026-06-11 服务器完成 SEA-RAFT oracle-pair 1000 样本导出、评估和与 v4 的逐图对比。结果为 crack EPE 3.140002、global EPE 1.953668、Dice 0.718757、crack edge 0.694224、global edge 0.711150、folding rate 0.030357；相对 v4 的逐图综合评分为 SEA-RAFT 1000/1000 张均优于 v4，mean score new-old 为 228.939246。已在本地新增 SEA-RAFT 的 `eval_summary.json` 和 `compare_summary.json` 轻量摘要，更新 `utils/summarize_final_results.py`、`final_ablation_summary.md`、`最终实验结果汇总.md` 和 `客户需求对齐检查清单.md`，当前新近外部 oracle 上界对比已包含 UniMatch 与 SEA-RAFT 两个方法。
- 2026-06-11 为继续改善模型本身效果并冲刺三区质量，新增 v5 训练代码：`loss_crack.py` 增加像素尺度 `JacobianConsistencyLoss`，同时约束局部一阶导数接近 GT 和负 determinant；`CrackWarpLoss` 新增 `w_jacobian` 与 `w_crack_coord_extra`，后者使用 `crack_mask^2` 强化高置信裂缝核心坐标监督。`config_crack.py`、`train_v2.py`、`run_train_slurm.py` 和 `slurm_train_crackwarp.sbatch` 已接入新参数，`utils/generate_final_experiment_commands.py --stage v5` 可生成 v5 smoke/10ep 命令。默认权重均为 0，不影响历史 v4 复现；本地已通过 `.venv` 语法检查，但本地无 torch，张量 forward 需在服务器验证。
- 2026-06-11 服务器完成 `v5_jacobian_roi_smoke` 2 epoch smoke，任务 `35374406` 的 `.err` 为 0，训练日志确认 `jac=` 与 `c_coord_x=` 正常生效。120 样本评估为 crack EPE 108.720879、global EPE 110.205452、Dice 0.279811、crack edge 0.352710、global edge 0.342142、folding rate 0.573587。相比 v4_10ep 的 120 样本口径，v5 smoke 明显降低 folding 并提升 edge fidelity，但 crack/global EPE 和 Dice 退化；当前参数不应直接扩到 10 epoch，应先降低 `W_JACOBIAN` 和 `W_CRACK_COORD_EXTRA` 做更保守 smoke。
- 2026-06-12 服务器完成更保守的 `v5_jacobian_roi_w005_smoke` 2 epoch smoke，任务 `35440441` 的 `.err` 为 0。120 样本结果为 crack EPE 107.240166、global EPE 109.194359、Dice 0.279766、crack edge 0.346873、global edge 0.334598、folding rate 0.576700。相比第一版 v5，EPE 退化明显收窄；相比 v4_10ep 120 样本，edge fidelity 和 folding 更好，但 crack/global EPE 与 Dice 仍弱。因此该方向有模型改进信号，但还不是最终候选；下一步应做 1000 样本确认，并拆分对照 `W_JACOBIAN=0.005, W_CRACK_COORD_EXTRA=0.0` 与更轻组合 `W_JACOBIAN=0.002, W_CRACK_COORD_EXTRA=0.25`。
- 2026-06-12 服务器完成 `v5_jacobian_roi_w002_10ep_lr1e6` 10 epoch 训练，任务 `35535541` 的 `.err` 为 0，总耗时约 4.34 小时。训练日志显示 Best Val EPE 112.344、Best Crack EPE 114.587。120 样本评估为 crack EPE 106.259941、global EPE 108.024345、Dice 0.281630、crack edge 0.336728、global edge 0.322192、folding rate 0.577763。相比 v4_10ep 120 样本，v5_10ep 的 EPE 已接近 v4，edge/folding 更好，但 Dice 仍略低；下一步必须做 1000 样本评估和 v4 逐图对比后才能决定是否替代主模型。
- 2026-06-12 当前 `v5_jacobian_roi_w002_10ep_lr1e6` 仍缺 1000 样本评估与逐图对比；在没有这一步结果前，不应把它直接写成最终主模型替代。优先完成 1000 样本口径，再和 v4 的 1000 样本 `eval_per_image.csv` 做逐图对比，确认是否真的同时守住 EPE/Dice 并继续改善 edge/folding。
- 2026-06-12 服务器完成 `v5_jacobian_roi_w002_10ep_lr1e6` 的 1000 样本评估和与 v4 的逐图对比。1000 样本结果为 crack EPE 110.827682、global EPE 109.889046、Dice 0.259491、crack edge 0.222985、global edge 0.249800、folding rate 0.577899。相比 v4 最终复评，v5 的 crack/global EPE 与 Dice 小幅变差，但 crack/global edge fidelity 明显提升，folding rate 从 0.582812 降到 0.577899；逐图综合评分 606 张优于 v4、394 张弱于 v4，mean score new-old 为 0.584968。当前判断：v5 可作为“综合质量/边缘与几何稳定性更优”的新候选主模型，v4 仍是“EPE/Dice 稍优”的保守主模型；下一步需要导出 v5 相对 v4 的正反样本 flow 诊断，确认 394 张退化主要来自 EPE/Dice 还是局部 folding/ROI 错位。
- 2026-06-12 服务器已导出 `v5_jacobian_roi_w002_10ep_lr1e6` 相对 v4 的正反样本 flow 诊断：`flow_diag_new_better/` 与 `flow_diag_old_better/` 各 20 张，两个 `flow_diagnostics.csv` 均已生成。控制台输出显示 v5 在 `crack0063_05`、`crack0063_07`、`crack0079_09`、`crack0076_04` 等高难大形变样本上降低 global EPE；old-better 组中部分样本虽然综合评分 v4 更优，但 global EPE 并非全部变差，下一步应汇总 CSV 的 crack EPE、folding 和位移幅度，判断退化来源。
- 2026-06-12 已汇总 v5 相对 v4 的正反样本 flow 诊断 CSV。`flow_diag_new_better` 20 张中，v5 相比 v4 的 global EPE 从 128.520829 降到 126.194326，crack EPE 从 130.365098 降到 124.857238，folding 从 0.405381 降到 0.402061，mean/p95 displacement 从 127.020658/237.701730px 降到 125.005183/233.887090px。`flow_diag_old_better` 20 张中，v5 global EPE 仍从 110.451964 小降到 110.333101，folding 从 0.404257 降到 0.399879，mean/p95 displacement 也下降，但 crack EPE 从 94.409891 升到 98.203701。当前判断：v5 的核心收益是降低 folding、压低位移幅度并提升 edge fidelity；其退化主要来自少数样本 crack ROI 局部对齐变差，而不是全图 EPE 或几何稳定性崩坏。v5 可作为“几何稳定增强版/综合质量候选主模型”，若追求 EPE/Dice 保守最优仍保留 v4。
- 2026-06-14 服务器完成 v6 系列调参：A=`v6_recover_epe_from_v5_smoke`、B=`v6_light_jac_roi_from_v4_smoke`、C=`v6_jac_only_from_v4_smoke` 三个 2 epoch smoke 均 `.err=0`。120 样本中 A 的 EPE 最好，C 的 Dice/edge/folding 最好；1000 样本中 A 更均衡，因此扩到 `v6_recover_epe_from_v5_10ep`。v6 10ep 1000 样本结果为 crack EPE 111.032875、global EPE 109.792915、Dice 0.258757、crack edge 0.231055、global edge 0.259766、folding 0.580705。与 v5 对比，v6 逐图 531/1000 张更好但 mean score new-old 为 -0.014929，说明总体基本持平略弱；与 v4 对比，v6 615/1000 张更好且 mean score 为 0.570039。当前判断：v6 可作为 edge 增强补充实验，不替代 v5；继续加边缘/Jacobian 约束会提升 edge fidelity，但会牺牲 Dice/crack EPE/folding，后续优化应转向更精细的 ROI 局部对齐。
- 2026-06-15 为适配 gpuHz 8 小时时限和全量评估可能超时的问题，`utils/evaluate_metrics.py` 新增 `--start`、`--num_shards`、`--shard_index` 参数，支持把全量评估拆成可重跑分片；新增 `utils/merge_eval_shards.py`，可合并各分片的 `eval_per_image.csv` 并重新计算整体 `eval_summary.json`。后续全量评估建议优先采用分片模式：每个分片独立提交 Slurm，失败时只重跑对应分片，全部完成后再合并。
- 2026-06-15 服务器已完成 v4/v5/v6 的 10360 张全量评估和全量逐图对比。全量 v4 结果为 crack EPE 114.416328、global EPE 111.964767、Dice 0.276011、crack/global edge 0.111250/0.098206、folding 0.583643；全量 v5 为 crack EPE 114.440132、global EPE 112.246910、Dice 0.274965、crack/global edge 0.120241/0.109083、folding 0.579404；全量 v6 为 crack EPE 114.494644、global EPE 112.070282、Dice 0.274207、crack/global edge 0.124766/0.114296、folding 0.582038。全量逐图综合评分中，v5 相对 v4 为 5503/10360 张更优，v6 相对 v4 为 5710/10360 张更优。当前最终口径更新为：v4 是 EPE/Dice 均值最稳的保守主模型，v5 是 folding 最优的几何稳定候选，v6 是 edge 最优的补充实验；v5/v6 综合覆盖面优于 v4，但不能写成全指标碾压。

## Next TODO

- 持续监控 plcyf job `21093404`，完成后拉回 `output_crackwarp_slurm/external_eight_model_comparison/` 中的 `eight_model_metrics_120.csv`、`eight_model_comparison.md`、`visual_comparison_120/` 和日志；若 GMA checkpoint 缺失或失败，按脚本状态说明使用 RAFT-small fallback 并在客户材料里明确标注。

- 将 v107 后处理产物和 v108 A/B/C/D 指标同步回本地，更新最终客户交付包；报告中将 v108 写为数据消融而不是主模型替代方案。
- 监控 plcyf jobs：v107 后处理 `21013260`、v108-A/B/C/D `21013348`-`21013351`；完成后把 plcyf 生成的轻量结果同步回本地，并确认是否还需要从 usmidet 拉取补充材料。
- 等待 plcyf v107 后处理 job `21013260` 完成；完成后确认是否成功生成 `eval_best_epe_120/`、`eval_best_epe_1000/`、`eval_best_epe_all_final/`、`visual_comparison_paper/`、`image_detail_roi_review/` 和 `roi_flow_highfreq/`。
- v107 后处理完成后，将远端 `output_crackwarp_slurm/v107_latest_method_long100_from_v106_r2` 的轻量成果拉回本地，重跑 `scripts/build_v107_paper_delivery_package.py`，生成最终客户交付包。
- 基于 v107 的真实 `train.log` 补充 100 epoch 训练曲线、收敛/过拟合/震荡分析，并在最终包中把 v4-v9、v87、v95-v106、v107 的长训/短训属性分开标注。
- 若仍找不到明确三区阈值，最终 README 中保持“待客户确认阈值，不伪造达标线”的表述；如客户补充阈值，再增加逐项达标/未达标列。
- v106 已完成且没有形成肉眼可见突破；下一步建议停止继续调参，转向整理最终实验材料：长训练指标、对比实验、消融实验、训练曲线、代表性可视化图和局限性说明。
- v106 前建议增加 detail 分支幅度诊断字段，例如 `detail_gate_mean/max`、`detail_residual_abs_mean/max`、ROI 内 active gate ratio，用数值直接判断分支是否打开，而不是只依赖 panel 颜色。
- 如果后续同步 v102/v104 checkpoint 到 `/mnt/users/xj62kv/underwater-crack-correction`，可用 `INIT=output_crackwarp_slurm/v102.../best_epe.pth OUT=... bash scripts/submit_v105_image_detail_head_smoke_from_v102_plcyf.sh` 重跑同口径 v105。
- 可选保守路线：实现 per-ROI 或 per-pixel 梯度冲突门控，只在 `visible_vs_coord_cos > 0`、或者 visible 单步改善 high-edge-grad 且不恶化 flow 高频的区域启用结构恢复目标。

- 不扩 v102 严格 1000；下一步优先诊断 Source-GT 高边缘结构目标与 synthetic GT 几何目标的冲突，例如关键 ROI 内 structure/visible-structure loss 与 coordinate/flow loss 的梯度方向、更新后位移变化和局部清晰度变化。
- 若继续做 v103，建议采用两阶段或分支式 probe：先冻结或弱化几何主目标做局部结构恢复，再恢复几何约束检查是否保真，而不是继续把小权重结构 loss 叠加到单阶段训练。

- 下一步不要扩 v101 严格 1000，也不要继续只调 `W_SOURCE_ROI_DIRECTIONAL_STRUCTURE`、只加 hard sampler、只加轮次或只追求 crack contrast。优先转向 v102：`ROI visible-structure reconstruction + v100 anti-ripple guard`，把关键 ROI 内的可见裂缝结构恢复做成更直接目标，并把 gate 条件设为 ROI edge-weighted win 超过 `14/25`、high-edge-grad win 明显超过 `2/25`、flow folding 不高于 v100/v101 平台。
- 继续单独诊断 Source GT 高边缘结构目标与 synthetic GT 几何目标是否冲突，尤其关注 ROI sharpness 指标命中但视觉仍变糊的样本；必要时按 high-edge-grad 失败样本构建 hard curriculum，而不是继续提高全局 Source-GT 小权重 loss。
- v100/v101 已完成 `crack0030_08 ROI-3` 与 `crack0090_08 ROI-4` 的 focus flow attribution panel；后续若继续归因，可进一步叠加局部 Jacobian/folding 热图与预测校正图，判断残余高频来自边界贴边、局部旋涡、ROI 裁剪边缘还是 Source GT / synthetic GT 目标错配。
- v99 已完成远端训练、120 gate、fixed40、ROI、ROI sharpness、ROI flow 高频诊断和人工复核。当前不建议扩 v96/v97/v98/v99 严格 1000；ROI 排序/对比目标只把 edge-weighted ROI 指标从 v98 的 `11/25` 回收到 `13/25`，仍未突破 high-edge-grad 和肉眼局部清晰度瓶颈。
- 下一步不建议继续只调排序 loss 权重、只加 hard sampler 权重或只加轮次。应把裂缝锐化与 anti-water-ripple 拆得更彻底：先独立定位 flow 高频伪影来自哪些 ROI、频段和位移尺度，再设计关键 ROI 内骨架、边缘方向、局部梯度分布的显式结构恢复目标，或重新审视 Source GT 结构目标与合成 GT 几何目标之间的冲突。
- v96 已完成远端训练、120 gate、fixed40、ROI、ROI sharpness 和人工复核。当前不建议扩严格 1000；下一步不要继续只小幅调 `mask/skeleton + patch_detail` 权重，而应转向更直接的局部清晰度/高边缘结构目标，或设计能同时约束裂缝结构恢复与 anti-water-ripple 的关键 ROI 监督。不建议继续只加轮次、只放大局部 residual，或只追求更高 `crack_contrast`。
- v91 plcyf 训练、120 gate、fixed40、ROI 面板和人工复核均已完成。当前判断：v91 没有稳定超过 v89/v90，不建议继续 v91 patch-refine head 路线，也不建议基于 v91 扩严格 1000。
- 原 `gpuHz/normal` job `41796624` 后续已完成，状态 `COMPLETED`、退出码 `0:0`，不再需要取消；该结果仅作为重复 v91 短训记录保留。
- 下一步若继续优化，建议回到 v89/v90 这条更稳的 flow Laplacian + displacement band 路线，或设计更明确的局部结构约束，而不是继续叠加当前 v91 patch-refine head。
- AWS 公有云路线暂不推进；除非用户提供/轮换有效 AWS 凭据，并确认 GPU 配额、区域、VPC/子网和数据传输路径，否则不要尝试创建 EC2 GPU 实例。
- v73 显式结构/patch refinement 已完成 120 gate 和 fixed40 抽查，常规指标比 v72 略稳但 combined gate 仍为 `reject_source_gt_regress`，不扩严格 1000。下一步若继续结构路线，应加入更明确的裂缝 mask/结构监督或 patch-level 对齐目标；否则切换到更强的结构化单图模型，不再只加小残差分支。
- v75 已验证“全局上下文模型 + 显式几何稳定约束”只能小幅降低 folding，仍不能消除 fixed40 的边界/底部拉伸伪影，combined gate 继续 `reject_source_gt_regress`，不扩严格 1000。下一步不建议继续扫同一模型的几何小权重，应回到监督口径：重审合成标签、Source GT 与 corrected GT 的定义关系，或设计更硬的边界/有效区域 mask，让模型不要通过整图拉伸来换取 crack 指标。
- 第一轮 GT 口径诊断显示 label-corrected 相对 input 在 Source GT 指标上稳定胜出，但 label 自身约有 `6%` 贴边采样和 `3.6%` folding 风险。下一步优先做 v76 数据/监督修正：在 loss 和 gate 中加入 valid warp / border sampling / folding-risk mask，对 label 高风险区域降权或单独惩罚，而不是继续换更强模型。
- v76 target-risk mask 已证明能把 folding 从 `0.101686` 降到 `0.090655`，但 Source-GT combined gate 仍失败，fixed40 只看到伪影略轻。下一步若继续该方向，应做 v77 更硬的有效区域策略：直接对预测采样贴边区域加惩罚或把 Source-GT loss 也乘以 valid sampling mask，同时可尝试更低 `TARGET_RISK_MASK_MIN_WEIGHT=0.15-0.25`；但仍只先跑 120 gate，不扩严格 1000。
- v77 valid sampling 约束已完成 120 gate 和 fixed40 抽查，训练日志中的预测贴边风险确实下降，但 folding 反而升至 `0.138859`，Source-GT edge win 降至 `40.0%`，不扩严格 1000。下一步不建议继续简单提高 `W_VALID_SAMPLING`；应转向更根本的数据/监督口径处理，例如让 Source-GT loss 显式乘有效采样 mask、只在 valid inner crop 内优化视觉质量，或重新生成/筛除含高贴边采样与高 folding label 的训练样本。
- v78 Source-GT 有效采样 mask 已完成 120 gate 和 fixed40 抽查，结果与 v77 几乎一致，说明仅在 loss 里屏蔽预测贴边区域不足以修复全局模型几何失稳。下一步建议停止 v77/v78 这类小约束路线，转向数据层面：筛出 label 高贴边/高 folding 样本，做 clean-label subset 或重新生成更保守的合成标签；同时可准备一个只在 inner valid crop 上评估/训练的诊断版，确认问题是否主要来自边界区域。
- v79 clean-label 75% subset 已完成 120 gate 和 fixed40 抽查，虽然保留集的 label 几何风险明显下降，但模型 folding 反而升至 `0.143754`，Source-GT edge win 仍只有 `40.8%`。下一步不建议继续小幅调整 keep ratio；应做更强的数据诊断：按固定 120/fixed40 检查这些评估样本本身是否属于高风险 label、尝试只训练低风险且覆盖同族评估样本的更严格 subset，或回到合成数据生成逻辑，重新生成边界更保守、可逆性更强的标签。
- v80 评估样本风险诊断显示：120 样本 label 风险略高于全量，且风险与 EPE 有一定相关，但与预测 folding 几乎无关。下一步不建议继续围绕 clean subset/keep ratio 做小修；应转向约束预测场本身的结构，例如降低/冻结 `global_context_flow_unet` 的全局 residual 自由度、回到 v76 之前更稳的 `single_flow_unet` 路线做保守数据验证，或设计显式可逆/边界单调性约束，而不是只筛数据。
- v84/v85/v86 已确认 soft structure、high-confidence structure core 和显式 mask/skeleton 在 1 epoch 下都不能带来 Source-GT 视觉突破，combined gate 均为 `reject_source_gt_regress`，不扩严格 1000。下一步不建议继续调大 `W_SOURCE_STRUCTURE` / `W_SOURCE_STRUCTURE_CORE` / `W_SOURCE_MASK_SKELETON` 或堆叠 photo/edge/high-edge/structure 小权重；为了回应短训不足的问题，应选当前最稳的 v83/v86 同系方案做 80 epoch 长训，再用 fixed40/120/1000 gate 判断是否存在长训后的量变。
- v87 已完成 v83 同系 80 epoch 长训，常规 120 指标显著优于 v83/v86，并且 Source-GT edge win 达到 `52.5%`，但 strict combined gate 仍为 `reject_source_gt_regress`，fixed40 仍可见局部涟漪、糊化和边缘伪影。下一步先不要扩严格 1000，应先对 v87 fixed40 做更系统的人工分组，挑出“确实改善/伪影明显/基本无变化”的样本，再决定是做 ROI/patch 局部 refinement、伪影抑制约束，还是提交 1000 gate 作为长训诊断而非通过性验证。
- v87 fixed40 分类和 ROI 复核已确认：当前主要瓶颈不是“不动”，而是“敢动后局部涟漪/旋涡/糊化”。下一步优先围绕 `crack0030_08`、`crack0052_03`、`crack0055_07`、`crack0090_08` 这类伪影样本设计 patch-level alignment、ROI 内外分区平滑、局部 residual refinement 或 flow 曲率/旋涡抑制诊断；暂不扩严格 1000。
- v88 验证了 flow Laplacian 可以压住局部伪影，但 `W_CRACK_FLOW_LAPLACIAN=1e-4` 明显过强，导致平均位移降到 `3.887px` 并触发 `reject_identity_like`。下一步应做 v89：从 v87 或 v88 前的非 identity 候选初始化，使用更小 `W_CRACK_FLOW_LAPLACIAN=1e-5`，并配合 `W_DISP_BAND`/位移下界把平均位移维持在 10px 以上，再重新跑 120 gate 和 fixed40。
- v89 fixed40 全量和 ROI 复核已确认：`1e-5` flow Laplacian + 10px 位移下界是当前最佳折中，能明显压低 v87 的涟漪/旋涡，同时避免 v88 的 identity-like；但 ROI 仍有偏保守、局部糊化和少量波纹。下一步建议做 v90 微调 `W_DISP_BAND`/位移下界，或做诊断性 1000；仍不建议直接扩严格 1000 或写成最终视觉突破。
- v70/v71/v72/v73/v74/v75 共同说明轻量 Source-GT photo/edge/high-edge loss、局部 residual refinement、结构 patch 分支和更强全局模型都没有带来稳定肉眼修复；后续候选仍必须优先证明 fixed40 上有明确裂缝形态增强，再考虑严格 1000。
- 暂停 v63 严格 1000 扩评；先针对 `crack0063_07`、`crack0039_04` 做坐标场诊断，比较 input、GT label、identity、v41 输出和 v63 输出的位移幅度、EPE、warp 后图像差异，查清为什么 v63 指标极好但视觉几乎不修复。
- 修正 120 gate 判据：除坐标 EPE/Dice/edge/folding 外，必须加入可视化一致的图像空间指标或至少加入“相对 input/identity 的真实校正幅度与 GT corrected 接近度”，避免恒等映射模型通过指标。
- 只有在修复评估口径后，v63 仍在视觉和新 gate 上优于 v41，才考虑严格 1000 或更长 epoch 训练。
- 后续所有新候选先跑 `utils/report_identity_aware_gate.py`；若被判为 `reject_identity_like`，直接止损，不进入严格 1000 或报告主结果。
- 基于 `identity_aware_gate_report_120.json` 重新设计下一版模型/训练目标：候选必须既不是 identity-like，又要在固定可视化集中接近 GT corrected；优先考虑直接优化图像校正后的 photometric/structure 指标，或重新检查 label 到 corrected 图像的坐标方向定义。
- 等 v44 job `39426366` 开始后，先确认日志中 `Train filter enabled` 匹配数、v22 checkpoint 加载和 loss 输出正常；跑完后先做 120 评估。若混入背景样本仍不能超过 v41，就停止当前 `train_filter_list` 过滤训练路线。
- 等 v43 job `39421645` 完成后，先跑 120 样本评估并与 v22/v41 对比；只有在 crack/global EPE、edge 或逐图综合收益明显超过 v41，且 folding 没有继续扩大副作用时，才扩严格 1000。
- 如果 v43 120 不优于 v41，应停止单纯扩大分段家族数量，下一步改为“分段子集 + 少量全量样本混合训练”或更温和的分段采样策略，避免模型只贴合退化族群而损伤通用验证集。
- 不建议继续扩训 `v10_hard_roi_w15_from_v8mag_smoke_1ep`、`v10_hard_roi_w12_from_v8mag_smoke_1ep` 或 `v10_hard_roi_w105_from_v8mag_smoke_1ep`；w15、w12、w105 的 1000 样本逐图综合评分均弱于 v8，hard-sample 采样加权路线已完成最后验证并应停止。
- 不建议直接扩训 `v11_roi_local_align_from_v8_smoke_1ep`；v11 的严格 1000 样本逐图综合评分为 360/1000 优于 v8、640/1000 劣于 v8，说明当前强 Jacobian/ROI 坐标组合只适合作为 edge/folding 诊断，不适合作为主线模型。
- v11-B / v11-C / v11-D / v11-E / v11-F 与 v11-C 10ep 已验证完成。v11-C 10ep 相对 v8 仍正向，但弱于 v11-C 1ep，当前不应继续同配置拉长训练。
- v11-C 低学习率 2ep 续训 `v11c_cont_from_1ep_jac0005_coord01_2ep` 的 120 样本结果未超过 v11-C 1ep 的 EPE/folding 平衡点，暂不做严格 1000；只有后续主 10ep 结果不稳定时，再考虑把该续训作为 Dice/global edge 补充候选复评。
- v11-F 的 120 样本结果表明，去掉 Jacobian 与额外坐标约束后 Dice 略升，但 edge/folding 下滑；因此当前主线仍应保留轻量 Jacobian / ROI 坐标约束，而不是回到纯 mag+edge 配置。
- v11-C 1ep 仍是当前 v11 系列更稳候选；下一步若继续推进三区，应优先做 v11-C 1ep 的代表样本可视化和失败归因，或设计更精细的局部 ROI/边界一致性约束，而不是继续同配置扩训。
- v11-C 1ep flow 诊断显示，正收益主要来自轻微收缩位移幅度并降低 EPE，退化主要伴随位移和 folding 微升；下一步训练方案应围绕“保留位移收缩与 edge 收益，同时约束 folding/边界可逆性”设计，而不是继续增加 `W_CRACK_COORD_EXTRA` 或训练轮数。
- v12、v13、v14 均已完成止损判断；v14 相对 v8 有小正收益，但明显弱于 v11-C 1ep，下一步不应继续同类 over-penalty 或单纯 fold loss 扩训，应回到 v11-C 1ep 主候选并做更精细的边界局部可逆性/裂缝结构一致性设计。
- v16 边界环带 Jacobian `0.0002` 的 2ep 稳定性验证和 v18 `0.00015` 中间权重验证均已完成。v16 2ep 与 v18 相对 v8 仍有正收益，但相对 v16 1ep / v11-C 1ep 均未形成综合胜率优势；下一步不应继续同配置拉长训练或继续在固定边界 Jacobian 权重附近细扫，应保留 v16 1ep 为边界 Jacobian 主候选，并转向自适应边界权重、样本难度分段或局部结构一致性。
- 远端 Slurm 作业监控已部署并启用；后续提交训练、评估、诊断 job 后可通过 PushPlus 自动获知新增/提交和结束事件，不再通知作业开始运行。若需要临时停用监控，可编辑 `xj62kv` 的 crontab 中 `slurm-monitor/monitor_slurm_jobs.py` 这一行，不要删除状态文件，避免恢复后重复通知。
- ROI 局部对齐后续不应只加大坐标 loss 权重；更推荐设计局部边界/细裂缝结构一致性或自适应 ROI 权重，让约束集中在裂缝边缘和高误差区域，避免整图几何被过度拉动。

- 在另一台电脑继续开发时，优先阅读 `项目交接与下一步开发计划.md`，按其中顺序先搭环境、做只读检查和小闭环复现。
- 在 Slurm 集群上训练前，先重新创建项目本地 `.venv`，按 CUDA 12.x 兼容策略安装 `torch torchvision` 和其余训练依赖；不要复用从 Windows/旧电脑拷贝来的 `.venv`。
- 首次 Slurm 训练建议先提交短任务或较小 epoch 复现，确认 `run_train_slurm.py` 输出目录、CUDA、数据读取和评估流程正常后，再扩大到 50-80 epoch。
- Slurm 前建议复用 `smoke_train_verify.py` 或提交 `EPOCHS=2` 的短任务做集群 smoke；确认集群 CUDA、数据路径和输出目录正常后，再提交正式训练。
- 到 Slurm 服务器后，优先 `git clone https://github.com/zh23jemu/underwater-crack-correction.git`，再使用 GitHub Release `data-v1` 下载大文件资产；主训练集需先合并 31 个 `underwater_crack_v3.tar.zst.part-*` 分卷再解压。
- 换到新电脑后，优先阅读 `新电脑接续工作指南.md`；新电脑只需 clone/pull GitHub 轻量仓库，服务器端数据已确认下载和解压，后续主要在服务器上做数据数量确认、重建 `.venv`、跑 Slurm smoke 和正式训练。
- 当前最新优先级：先在服务器执行数据数量确认和 `SHA256SUMS.txt` 校验结果复核；再重建 `.venv` 并安装 CUDA 12.x 兼容 PyTorch；随后提交 `EPOCHS=2 OUTPUT_DIR=output_crackwarp_slurm/smoke sbatch slurm_train_crackwarp.sbatch`。
- 补齐训练环境依赖：优先根据当前机器 CUDA 能力安装 PyTorch CUDA 12.x 兼容版本，再通过 `requirements.txt` 安装 `opencv-python-headless`、`scipy`、`tqdm`、`natsort`、`scikit-image` 等依赖。
- 先做不训练的运行检查：导入模型、加载 `best_epe.pth`、读取少量样本、跑一次小规模推理/评估，确认代码路径、checkpoint 结构和设备可用性。
- 修正文档与代码不一致：项目文档提到 `infer_epoch80.py`，实际文件为 `infer_epoch.py`。
- 梳理 `predict.py` 与 `infer_epoch.py` 的 remap 坐标逻辑，重点排查是否存在 `abs()`、坐标方向、归一化尺度或 `map_x/map_y` 使用不一致导致恢复量偏小。
- 在确认环境可跑后，优先做小样本诊断评估和可视化复现，再决定是否进入完整训练或结构改动。
- 如果需要继续完善对外稿，可根据实际报价策略进一步压缩为一页版、报价单版或合同附件版。
- 如果需要做 Word 视觉级 QA，需在本机安装可命令行调用的 LibreOffice `soffice`，然后重新渲染检查页面布局。
- 服务器下一步优先对 `output_crackwarp_slurm/v2/best_epe.pth` 做更完整评估：扩大到全验证集或更多样本，生成 ROI 局部放大图、误差热图、预测位移场和失败样本，避免只根据 120 张样本下结论。
- 下一轮若继续训练，不建议只沿用相同配置再跑 50 epoch；应先完成预测位移场、ROI 可视化、folding 热区和高误差样本诊断，再决定修改 loss 权重、坐标约束或局部 refinement。
- 基于 1000 样本公平对比，下一步应优先挑选旧模型优于 v2、v2 优于旧模型、两者都失败的样本各若干张，做可视化归因分析。
- 将本地新增脚本同步到服务器后，先运行 `utils/compare_eval_per_image.py` 生成三类图片清单，再分别用旧模型和 v2 模型调用 `utils/export_crack_roi_visuals.py --image_list` 导出 ROI 对比图。
- 对比旧权重与 v2 权重时统一使用当前 `utils/evaluate_metrics.py` 口径，重点比较 crack EPE、global EPE、Dice、edge fidelity 和 folding rate；旧 gate report 的 Dice 口径不应再直接混用。
- 进入优化前先检查 `infer_epoch.py`、`predict.py`、`utils/evaluate_metrics.py` 中 `cv2.remap` 坐标方向、归一化尺度、`map_x/map_y` 顺序和可视化输出是否完全一致，优先排除恢复量偏小的工程原因。
- 优先针对 `crack0030_xx` 共同失败族做专项诊断：检查合成标签的逆映射场、预测 flow 范围、边界采样、folding 热区和 ROI mask 是否对齐；这组样本可作为下一轮 loss/约束调整的回归测试集。
- 服务器拉取 `utils/export_flow_diagnostics.py` 后，先对 `joint_failure_images.txt` 导出整图诊断面板；如果 EPE 热图与 folding 热区集中在边界和大形变区域，优先调整 folding/Jacobian 约束和边界采样；如果位移幅度图整体偏小，则优先排查坐标尺度与恢复幅度约束。
- 下一步需要服务器继续对 `new_better_images.txt` 和 `old_better_images.txt` 分别导出整图 flow 诊断面板，用正反样本确认 v2 改善和退化的共同模式，再决定 loss/结构改动。
- 下一步服务器优先导出 v2-v3 的正反样本 flow 诊断图：分别使用 `output_crackwarp_slurm/v3_mag_ft_smoke/compare_v2_vs_v3_ft/new_better_images.txt` 和 `old_better_images.txt`，对比 v2 `best_epe.pth` 与 v3 `best_epe.pth` 的位移幅度、EPE 热图和 folding 热区。
- 下一步服务器先汇总 `flow_diag_new_better/flow_diagnostics.csv` 与 `flow_diag_old_better/flow_diagnostics.csv` 的均值差异，重点看 global/crack EPE、folding rate、mean/p95 displacement magnitude，再决定是否提交 10 epoch fine-tune。
- 不建议直接按当前 `W_CRACK_MAG=0.1` 跑长训 50 epoch；如果继续小规模训练，优先做更保守的 10 epoch 对照，例如 `W_CRACK_MAG=0.05` 或保留 `W_CRACK_MAG=0.1` 但增加位移幅度鲁棒项/上限项，防止高难样本的 p95 displacement magnitude 继续上升。
- 下一步不要立刻继续 50 epoch；优先对 `v3_mag_ft_w005_10ep/compare_v2_vs_w005_10ep/old_better_images.txt` 和 `joint_failure_images.txt` 导出 flow 诊断图，确认剩余 150 张退化样本是否仍由裂缝 ROI 位移过大、边界大形变或 edge fidelity 下降导致。
- 若 `w005_10ep` 的退化样本也主要是位移幅度过大，则下一轮可考虑 `W_CRACK_MAG=0.03` 的 10 epoch 对照，或在 magnitude loss 中加入 robust/clamp 策略；若主要是边缘变糊，则优先补 edge/gradient consistency，而不是继续调 `W_CRACK_MAG`。
- 下一步代码优化优先级：在 magnitude consistency 中加入鲁棒/上限策略，避免高难样本 p95 displacement magnitude 被继续推大；同时增加裂缝 ROI 边缘/梯度一致性或 edge-preserving 项，解决 EPE 改善但 edge fidelity 仍偏低的问题。
- 服务器 pull 新代码后先执行语法检查：`.venv/bin/python -m py_compile loss_crack.py train_v2.py run_train_slurm.py utils/evaluate_metrics.py`。
- 语法检查通过后建议先跑 2 epoch smoke：`INIT_CHECKPOINT=output_crackwarp_slurm/v3_mag_ft_w005_10ep/best_epe.pth W_CRACK_MAG=0.05 CRACK_MAG_ROBUST_DELTA=0.01 CRACK_MAG_OVER_WEIGHT=0.5 W_CRACK_EDGE=0.05 LR=3e-6 EPOCHS=2 OUTPUT_DIR=output_crackwarp_slurm/v4_robust_edge_smoke sbatch --partition=gpuHz --time=02:00:00 slurm_train_crackwarp.sbatch`。
- smoke 日志中需要确认 `c_mag` 和 `c_edge` 正常出现；若 120 样本评估没有明显退化，再做 1000 样本评估和 v3_w005 对比。
- `v4_robust_edge_10ep` 已通过 1000 样本评估和 flow 诊断，当前可作为主模型候选。下一步应筛选代表性可视化样本，用于报告展示“整体提升、共同失败改善、少数裂缝 ROI 退化”的三类情况。
- 使用 `utils/generate_final_experiment_commands.py --stage all` 生成下一批服务器命令，先做 v4 最终主评估，再跑 `abl_robust_only`、`abl_edge_only`、`abl_no_over_penalty`、`abl_edge_w003` 四个关键消融。
- 4 个 2 epoch 消融 1000 样本均弱于完整 v4，暂不扩到 10 epoch；后续消融应转向 folding/Jacobian 正则、边界平滑或 ROI 局部对齐，而不是继续拉长这 4 个配置。
- 基于 `summary_tables/final_ablation_summary.md` 整理报告中的消融表和文字结论；下一步研发重点转向外部新近方法对比适配。
- 外部对比实验优先从 SEA-RAFT、UniMatch、MemFlow、NeuFlow 等 2023-2025 方法中筛选 4-6 个可适配方法，统一转换输出格式并使用当前 `utils/evaluate_metrics.py` 口径评估。
- 外部方法适配的统一目标是导出 `pred_dir/xxx.png.npy`，shape 为 `(2,H,W)` 或 `(H,W,2)`，优先使用与项目标签一致的归一化逆映射坐标场，然后用 `utils/evaluate_flow_predictions.py` 评估。
- 服务器下一步先运行 `utils/export_unimatch_predictions.py --num 10 --overwrite` 做 UniMatch 预测导出 smoke；若能生成 10 个 `pred_grid/*.npy`，再用 `utils/evaluate_flow_predictions.py` 评估 UniMatch 10 样本结果，确认方向没有明显反掉。
- 基于 UniMatch 1000 样本结果更新最终汇总表；报告中将 UniMatch 写作 oracle-pair dense matching 上界参考，而不是同输入条件单图像校正方法。
- 基于 `最终实验结果汇总.md` 撰写报告实验章节时，应把主模型、消融和外部上界分成三个小节，避免把 UniMatch 的 oracle-pair 指标与 v4 单图像输入结果混写为同条件比较。
- 面向客户汇报时优先使用 `客户需求对齐检查清单.md` 组织材料：先说明四项需求对应的当前完成度，再给出证据文件和剩余缺口，避免只展示分散实验指标。
- 服务器下一步安装/验证 SEA-RAFT，并先导出 10 张 `pred_grid` 做 smoke；如果 EPE 方向正常，再扩大到 1000 样本并纳入最终汇总表。
- SEA-RAFT 下一步必须先修复服务器脚本的 `utils` 包名冲突；修复后执行 `py_compile`，再运行 `export_searaft_predictions.py --num 10 --overwrite`，只有成功生成 `pred_grid/crack0001_00.png.npy` 等预测文件后，才继续 10 张评估、1000 张导出和 v4 对比。
- SEA-RAFT smoke 已通过，下一步直接执行 1000 样本导出、统一评估和 `compare_v4_vs_searaft`；完成后把 `eval_1000/eval_summary.json` 与 `compare_v4_vs_searaft/compare_summary.json` 同步回本地，并将 SEA-RAFT 纳入 `utils/summarize_final_results.py` 与 `最终实验结果汇总.md`。
- UniMatch 与 SEA-RAFT 两个新近外部 oracle-pair 上界对比已补齐；下一步应转向最终报告实验章节整理，包括主模型结果表、消融表、外部上界表、代表性可视化、失败案例、训练曲线和运行成本说明。
- v5 1000 样本评估和正反样本 flow 诊断已完成；下一步优先整理最终报告实验章节，把 `v4_robust_edge_10ep` 写作 EPE/Dice 保守主模型，把 `v5_jacobian_roi_w002_10ep_lr1e6` 写作 edge/folding 与综合质量更优的几何稳定增强候选。
- 若后续继续优化模型，重点不是继续拉长旧消融，而是保留 v5 的 edge/folding 收益，同时追回少数样本的 crack ROI EPE/Dice，可考虑更细的 ROI 局部对齐约束、边界平滑或自适应权重。
- 若需要对外汇报，优先使用 `最终实验结果汇总.md`、`客户需求对齐检查清单.md` 和 `summary_tables/final_ablation_summary.md`，并明确 UniMatch/SEA-RAFT 是 oracle-pair 上界参考，不是同输入公平对比。
- RecallLoom 已完成初始种入；后续继续项目时可先执行 `rl-resume`，重点读取 `.recallloom/rolling_summary.md` 的 v4/v5 双主线、外部上界对比和后续报告整理风险摘要。
- 如果 v2-v3 flow 诊断确认 v3 主要是在校准位移幅度且没有引入新的边界折叠，再提交 10 epoch 小规模 fine-tune：`INIT_CHECKPOINT=output_crackwarp_slurm/v2/best_epe.pth W_CRACK_MAG=0.1 LR=5e-6 EPOCHS=10 OUTPUT_DIR=output_crackwarp_slurm/v3_mag_ft_10ep sbatch --partition=gpu --time=24:00:00 slurm_train_crackwarp.sbatch`。
- v3 后续优化不能只依赖 `w_crack_mag`；需要同步补强 edge fidelity，例如提高边缘保持项、检查 ROI 边缘 mask 对齐，或增加裂缝边缘/梯度一致性消融，否则指标会继续出现 EPE 改善但视觉边缘变差的问题。

## Open Issues

- 八模型外部算法对比当前仍在运行中；RAFT/GMA/UniMatch/SEA-RAFT 均按 oracle-pair 条件评估，不是与内部单图像模型完全同输入条件，客户报告中必须明确写成外部参考/上界，不能包装为完全公平横比。

- v108 追加数据存在指标冲突：A/D 的 EPE 或 folding 有收益，但 edge fidelity 明显下降；C 的 Dice 高但 EPE/edge/folding 不占优。后续如果要继续用外部数据，需要重做采样比例、边缘结构约束或只把外部数据用于风格/鲁棒性消融，不能直接混入主模型长训。
- plcyf 与 usmidet 项目路径不同，v108 数据内存在绝对软链接；当前通过 plcyf 兼容软链接解决，后续若迁移到其它集群，应优先改成相对链接或实体文件包，避免路径依赖。
- v108 当前只是 1 epoch 短周期方向性验证，不能作为客户“最新模型 100 epoch 长训”的主结果；若某组 smoke 明显改善，再决定是否追加更长训练或作为数据消融写入论文实验。
- Roboflow 候选中 `final-underwater-cracks` 显示无可下载版本；EUVP 和 Kaggle UIEB raw 数据位于 KaggleHub cache，项目目录内是软链接，后续打包或迁移时需注意是否要复制实体文件。UIEB 官方 RAR 已用密码解压成功，但当前正式筛选目录 `screened_external_candidates_v3` 仍基于 Kaggle UIEB raw 镜像生成；如需完全使用官方解压目录，可重跑 UIEB 筛选。
- v107 最新方法 100 epoch 长训当前仍在 Slurm 排队，最终指标、训练曲线和可视化结论尚未产生；当前 `experiment_submission_package_2026-07-11_v107_paper_ready.zip` 只是脚本验证版，不应作为最终客户包发送。
- 外部对比按严格论文口径仍需在 v107 完成后补齐同输入传统/可复现外部方法；当前已有 UniMatch/SEA-RAFT oracle-pair 上界，但其输入条件更强，必须保留公平性说明。
- 仓库材料中尚未确认明确的“三区标准”硬阈值；没有阈值前只能提交真实指标和风险判断，不能写成已达标。
- v106 虽然让 detail gate/residual 数值上明显打开，并在 high-edge-grad ROI 指标上有小幅改善，但人工视觉复核仍没有达到最终漂亮可视化效果；继续加轮次或继续放大 detail 分支存在引入伪纹理/过度后处理的风险。
- v105 当前远端 smoke 使用 v89 初始化而非理想的 v102/v100 平台初始化，因为 `/mnt/users/xj62kv/underwater-crack-correction` 当前缺少 v100-v104 checkpoint；该差异会影响与 v102/v104a 的直接对比。
- v105 detail-aware ROI 指标显示 `detail_image` 相对 `flow_corrected` 的变化接近零，当前更像链路 smoke 而不是有效视觉增强；如果继续该方向，需要先解决 detail residual/gate 作用过弱的问题。
- v105 detail head 默认不改变主 flow 输出，因此 flow-only 评估只能说明几何主干表现，不能替代 detail-aware panel 和 ROI sharpness 复核。
- v104a 显示弱化几何约束后模型转向低位移、低 folding 的保守解：25 个关键 ROI 平均位移为 `3.972399px`，全部落在 `0-8px` 桶；这会改善 EPE/folding，但没有带来高边缘裂缝结构恢复。
- v104a 仍未解决核心 high-edge-grad 平台：25 个关键 ROI high-edge-grad win 为 `2/25`，Source-GT 120 high-edge-grad win 为 `0/120`。
- plcyf 当前可能被其它训练进程占满 GPU 显存，ROI 多模型后处理容易 OOM；v104 后处理已支持 CPU compact fallback，但完整多模型后处理仍建议等待 GPU 空闲后再跑。
- v103 证实 visible-structure 与 coord/guard 在 ROI 内约半数像素存在方向冲突，说明单阶段小权重叠加会天然弱化结构恢复信号。
- anti-ripple guard 与 high-edge-grad 目标存在局部拉扯：guard 单步显著降低 flow 高频/Laplacian，但平均会轻微损伤 high-edge-grad。

- v102 仍未解决高边缘裂缝局部结构恢复：120 Source-GT high-edge-grad win 为 `0/120`，25 个关键 ROI high-edge-grad win 仍为 `2/25`。
- v102 的 anti-ripple 指标虽接近 v100/v101 平台，但 `flow_lap_gap_to_gt_mean_mean=1.483091`、`roi_folding_rate_mean=0.028501` 略高于 v100/v101，不能作为更稳的水纹抑制版本。
- v102 结果包流式拉回时连接重置，本地 review sheets 只部分完整落盘；远端完整结果仍在 plcyf 输出目录中。

- v101 证明 ROI directional structure 分支可以在不破坏 v100 anti-ripple guard 的前提下稳定训练，但 120 high-edge-grad 仍为 `0/120`，ROI high-edge-grad win 仍为 `2/25`，说明当前方向/骨架/局部梯度统计约束仍不足以恢复肉眼可见的高边缘裂缝结构。
- v100 证明 anti-ripple guard 能压低 v99 暴露出的少数 ROI 高频/folding 爆点，但 120 high-edge-grad 仍为 `0/120`，说明 flow 高频止损与裂缝锐化/结构恢复仍是两个问题，不能把 v100 当作视觉突破版。
- v99 归因诊断显示 water-ripple 退化并非均匀分布，而是集中在少数 ROI，尤其 `crack0030_08 ROI-3` 的 folding 已到 `0.282715`；后续如果只看 25 ROI 平均值，会低估少数灾难局部对视觉复核的影响。
- v99 证明 ROI 排序/对比式结构目标能稳定参与训练并略微回收 v98 的 ROI edge-weighted 指标，但 120 high-edge-grad 仍为 `0/120`、ROI high-edge-grad 仍为 `2/25`，说明当前局部结构排序目标仍不足以恢复 Source GT 高边缘裂缝结构。
- v99 的 ROI flow 高频诊断略差于 v98，`flow_lap_gap_to_gt_mean_mean=1.487278`、`flow_highfreq_gap_to_gt_mean_mean=0.266104`、`roi_folding_rate_mean=0.031246`，说明裂缝锐化目标可能仍会和 anti-water-ripple 目标互相拉扯。
- v98 证明 hard sampler 能把训练注意力转向关键 ROI 图族，但 ROI edge-weighted win 只有 `11/25`，低于 v95/v93/v96/v97；说明“多采样关键 ROI + 小权重结构 loss”仍不足以恢复高边缘裂缝结构。
- ROI 局部清晰度诊断显示，现有模型在 Source GT 高边缘梯度结构上仍普遍弱于 input；v94 虽然 `crack_contrast` 更高，但没有带来 edge-weighted 或 high-edge-grad 改善。当前不能把 v94 或“更黑/更锐”的局部效果写成视觉突破，否则仍有“暗线对比增强但裂缝结构不清楚”的汇报风险。
- `C:\MyFiles\aws.txt` 中的 AWS access key / secret 已被本地工具读取过，且当前 AWS 返回 `InvalidClientTokenId`，应尽快在 IAM 中禁用或轮换旧 key；后续不要把该 key 写入仓库、日志或命令输出。
- 当前 AWS 凭据不可用，导致无法验证 EC2 GPU 配额、AMI、VPC/子网和 S3/数据传输条件；AWS 迁移仍处于“技术可行但当前不可执行”状态，本次训练已改走 plcyf GPU。
- plcyf 上存在多个保留下来的半成品虚拟环境目录（例如 `.venv_py39_partial_*`、`.venv_py311_partial_*`、`.venv_partial_*`），当前不删除；待 v91 训练和后处理完成后，可在用户确认后再统一清理或归档。
- v66-v69 说明当前模型存在稳定冲突：平均位移达到 `10px` 后不再 identity-like，但 source-GT image/grad 与 edge win 持续退化；现有 label-warp photo、crack edge 与坐标监督不足以代表最终视觉修复目标。
- v70 说明直接加入轻量 Source-GT photo/edge loss 仍不足以解决视觉修复：常规 EPE/edge 可小幅改善，但 Source-GT image/edge-weighted/high-edge grad 继续退化，fixed40 也没有肉眼增强。
- v72 说明进一步加入 Source-GT high-edge 局部监督仍不能解决视觉修复：模型保持非 identity 位移，但 source-GT image、edge-weighted 和 high-edge grad 仍退化，folding 相对 v71 变差；继续扫同类小权重风险较高。
- v73 说明仅增加显式结构/patch 小残差分支还不够：source-GT image/edge-weighted 退化幅度略小，但 edge win 仍只有 `35.8%`，fixed40 未出现肉眼明确裂缝形态修复；继续在同一结构上小幅改参存在收益递减风险。
- v74 暴露新的主要风险：更强全局上下文模型可以改善 crack EPE/Dice 和 edge win，但 folding 会快速失控，并在 fixed40 中表现为边界/底部拉伸伪影；后续若沿 v74 继续，必须把几何稳定作为主约束。
- v75 说明单纯提高 `W_FOLD/W_JACOBIAN/W_CRACK_BOUNDARY_JACOBIAN` 只能小幅降低 folding，不能根治 v74 的边界/底部伪影；继续在同一模型上扫小权重存在明显收益递减，应优先检查监督数据和有效 warp 区域定义。
- GT 口径诊断说明当前 `GT corrected` 本身大体合理，但 label 包含非零贴边采样和 folding 风险；如果 loss 对这些区域等权监督，模型容易学习到边界 padding/局部不可逆形变，表现为 v74/v75 的底部波纹和边界拉伸。
- v76 说明仅对 label 高风险区域做温和监督降权还不足以改变 Source-GT 图像质量；如果继续，需要更直接约束预测 flow 的贴边采样和有效 warp 区域，而不是只降低 GT 风险区域权重。
- v77 说明直接惩罚预测 flow 贴边采样可以降低训练日志中的 `v_risk`，但会把模型推向新的几何折叠/整体失稳状态；边界问题不能只靠一个全局贴边 penalty 解决，必须把有效采样区域、Source-GT 视觉 loss 和 label 风险口径绑定起来，否则容易牺牲 folding 与 Source-GT 图像质量。
- v78 说明把 Source-GT loss 限制到预测有效采样区域也不足以阻止 v77 式 folding 反弹；当前更可能是训练标签高风险区域、全局上下文模型的形变自由度和现有监督口径共同导致的失稳，而不是单一边界 loss 漏约束。
- v79 说明“降低训练集整体 label 风险”不自动等价于改善当前固定评估口径；可能原因包括 clean subset 改变了训练分布、评估样本仍包含高风险标签、全局上下文模型在少量训练后仍沿 v76/v77 失稳方向漂移，或合成标签的 Source-GT 视觉目标与坐标监督仍存在局部冲突。
- v80 说明评估 label 风险不是 v77-v79 folding 反弹的主因；如果继续使用 `global_context_flow_unet`，需要直接限制模型输出空间或全局 residual，而不是期望通过筛掉一部分高风险 label 自然恢复稳定。
- v81/v82/v83 说明全局 residual 限幅是有效的几何稳定手段，但无论单一 cap 还是空间自适应 cap，都只能在 folding/边界伪影和动作幅度之间折中，不能解决 Source-GT image regress 和裂缝形态不修复的问题；后续需要更强监督目标或结构化局部对齐，而不是继续围绕 cap 微调。
- v84/v85/v86 说明单纯把 Source-GT 结构响应、高置信结构核心或显式 mask/skeleton 作为额外 loss 仍不足以解决当前瓶颈：模型不是 identity-like，但 source-GT image/edge-weighted/high-edge 指标继续退化，可视化也没有明显修复增强；潜在问题包括 soft crack/mask 目标仍受背景暗纹理污染、整图 flow 结构难以只修裂缝核心、以及 Source-GT 结构监督与合成 label 坐标监督仍存在局部目标冲突。
- v87 80 epoch 长训说明同系 spatial-cap 模型的常规指标仍有明显训练收益，但视觉 gate 暴露出新的风险：模型更敢做局部形变后，grad/lap 和 edge win 变好，image/edge-weighted/high-edge 仍略退化，fixed40 中出现涟漪、糊化和局部形变伪影；这提示当前问题不是训练轮次不足单独导致，而是缺少对局部伪影和裂缝细节一致性的更强约束。
- v87 ROI 复核进一步确认，很多全图看似接近 corrected GT 的样本，在裂缝 ROI 中会出现旋涡状或皱褶状采样纹理；因此后续视觉验收必须包含 ROI 放大图，不能只看整图横向图或 120 均值指标。
- v88 暴露出新的权衡：局部二阶平滑/一致性约束可以显著降低 folding 和伪影，但如果没有位移下界或恢复量约束，会重新得到低 EPE、低 folding、Source-GT 图像指标转正但接近 identity 的假阳性。
- v63 120 gate 指标相比 v41 出现数量级跃迁，但人工查看固定可视化后确认 v63 视觉上近似恒等映射，未完成明显校正；当前首要风险是历史坐标 EPE gate 与视觉修复目标不一致，可能系统性奖励接近 identity 的预测。
- `utils/diagnose_identity_gate_mismatch.py` 当前串行逐图推理较慢，完整 120 已通过 3 个 40 张分片解决；若要做严格 1000 identity-aware gate，应继续使用分片或先优化为 batch 推理，避免单 job 超时。
- `utils/export_visual_comparison_grid.py` 已支持 `model@single_flow_unet:path` 的混合结构可视化，但同一张横向图中混合不同模型结构时，后续命令必须显式写清 v63 的 `model@single_flow_unet`，否则会按默认 `crack_warp_net` 加载并失败或产生错误结果。
- v44 输出目录中存在失败 job `39426359` 的旧 `slurm-39426359.out`，它只记录清单不存在的失败原因，不代表新 job `39426366` 的训练状态；后续查看日志时应以 `slurm-39426366.out` 和新的 `train.log` 为准。
- v41/v42/v43 的分段训练收益都处在很小量级，v41 严格 1000 虽相对 v22 为 `518/1000` 更优、mean score `+0.009029`，但距离三区论文所需的稳健优势仍明显不足；后续必须同时看严格 1000、逐图胜率、可视化证据和 folding 副作用。
- v43 训练清单从 v22-v16 严格 1000 old-better 家族中取 top20，覆盖比 v41 更大，但仍是强筛选子集，存在过拟合退化家族、损伤常规样本 Dice/folding 的风险。
- 远端曾出现关键入口文件被清空为 0 字节的问题；当前已恢复，但后续提交 Slurm 前仍建议先执行 `py_compile` 和 `hasattr(train_v2, "main")` 之类的轻量检查。
- `utils/export_flow_diagnostics.py` 的限制样本参数是 `--num`，不是 `--max_images`；后续重投 flow 诊断时应使用 `--num 20`，避免再次秒退。
- 在 Windows PowerShell 通过 SSH 提交远端 Slurm 循环时，`$i` 极易被本地或远端 shell 转义破坏；严格评估分片建议显式写出 shard index，或在远端保存脚本后再执行，避免生成带 ESC 字符的 job 名和错误参数。
- 当前远端 `slurm_train_crackwarp.sbatch` 在从 Windows 同步后出现 `bash -n` EOF 报错；短期 v12/v13/v14/v15 smoke 与评估均已绕过该脚本、直接用 `sbatch --wrap` 调 `run_train_slurm.py` 或评估脚本，后续仍需修复 sbatch 文件换行/续行问题后再恢复常规提交方式。
- Slurm 监控依赖远端 `/mnt/users/xj62kv/slurm-monitor/config.json` 中的 PushPlus token 和 `squeue` / `sacct` 路径；本地仓库不保存 token。若远端 Slurm 安装路径变更、PushPlus token 轮换或 cron 被用户覆盖，需要同步更新该配置并手动运行一次 `monitor_slurm_jobs.py` 验证。
- v10 hard-sample 系列 w15、w12、w105 的 1000 样本结果均弱于 v8 综合口径；采样加权可带来局部 edge 或 crack EPE 波动收益，但会持续损伤 global EPE、Dice、folding 或逐图综合胜率，当前不应作为新主线继续扩训。
- 两份报告中的实验配置不完全一致：普通版为 80 epoch、13.14M 参数、数据总量 10370；详细版为 50 epoch、17.84M 参数、数据总量 10360。
- 普通版更强调收敛和视觉效果，详细版则指出所有 checkpoint 未达到工程门限；两份报告若要合并，需要先确认它们是否来自同一实验阶段或不同版本模型。
- 文档口径存在“普通报告较乐观、详细报告与 gate report 较严格”的差异；后续汇报时应以详细版报告和 gate report 的工程门限作为当前真实状态依据，同时保留普通版报告作为早期实验阶段记录。
- `predict.py` 中旧逻辑直接将网络输出坐标喂给 `cv2.remap`，且 `predict_cv` 使用 `np.abs(preds[:, :, 0/1])`，与 `infer_epoch.py` 中 `flow * 511` 的归一化逆映射坐标转换不一致，可能导致恢复幅度、方向和可视化判断失真。
- `loss_crack.py` 中 folding penalty 当前直接对归一化采样坐标求 `det = du_dx * dv_dy - du_dy * dv_dx` 并惩罚负值，后续需要复核它是否正确反映逆映射场相对于像素网格或恒等映射的局部可逆性；当前 gate report 的 folding rate 约 0.57，说明这条约束或其权重仍不足以控制局部折叠。
- 当前 `.venv` 已在本机重建并能完成最小 CPU/GPU smoke；但 Quadro T1000 显存和算力较弱，首次尝试 4 样本、128×128、较宽轻量模型 smoke 超过 5 分钟未完成，说明本机不适合作为正式训练环境。
- 当前 `train_v2.py` 只保存模型权重，没有保存 optimizer、scheduler、scaler 和 epoch 状态，因此还不支持严格断点续训；Slurm 中断后只能从已有权重重新初始化训练，或后续先扩展 checkpoint 结构。
- `.gitignore` 当前忽略 `*.log` 和 `*.pth`，但没有整体忽略 `underwater_crack_v3/`、`under-crack-images/`、`output_crackwarp/` 或 `.npy`；如果后续初始化 Git，需要确认哪些大目录应通过外部数据管理而不是直接入库。
- 现有 `PROJECT_WORKFLOW_GUIDE.md` / `OPTIMIZATION_GUIDE.md` 的命令使用系统 `python`，需要后续统一改为项目 `.venv` 调用方式。
- 已有 gate report 中 checkpoint 路径仍包含原机器绝对路径 `D:\nxy1\GOOD_cnn_new\GOOD_cnn\...`，后续报告或脚本应避免依赖旧路径。
- 现有模型虽然训练收敛，但 gate report 全部未达标，核心瓶颈集中在 crack EPE、Dice、edge fidelity 和 folding rate。
- 当前环境缺少 LibreOffice `soffice`，因此新增 Word 说明稿已完成结构化校验，但未完成页面 PNG 渲染级视觉 QA。
- 当前本地 Windows 工作区没有可用 `.venv`，因此 2026-06-08 新增的诊断脚本尚未在本地完成 Python 编译检查；需要同步到服务器后使用服务器 `.venv/bin/python` 做 `py_compile` 和实际运行验证。
- v3 fine-tune 在 1000 样本上虽比 v2 更稳，但改善幅度仍较小，且 edge fidelity 继续下降；当前只能证明位移幅度一致性损失有进一步实验价值，不能证明已经接近三区论文或工程验收水平。
- RecallLoom 初始化时 workspace language 被设为 `en`，与本项目长期中文维护偏好不完全一致；由于官方工具拒绝直接改语言，暂不手工编辑 `.recallloom/config.json`，后续如需统一中文侧车，应优先查找官方迁移/重建路径。

## Architecture Decisions

- 外部算法横评采用“先统一导出归一化逆映射坐标场，再复用项目 `evaluate_flow_predictions.py` 指标口径”的方式；内部模型继续用 `evaluate_metrics.py` 评估，最终通过汇总脚本合并成同一张 120 样本表，避免为不同外部算法引入不一致指标。

- v108 数据消融采用“只变数据，不变最新模型与 loss”的原则：A/B/C/D 均使用 v106 detail-strength 方法和同一初始化 checkpoint，保证指标变化主要反映外部数据源影响。
- v107 作为“最新方法完整长训”主候选，沿用 v106 的 `global_context_flow_unet_image_detail_head_strong` 和 image-space detail head 路线，从 v106 `best_epe.pth` 初始化，训练 100 epoch；后处理必须同时覆盖常规指标、训练曲线、内部/外部对比、ROI 细节和 flow 高频/folding 风险。
- 客户交付包从 v107 起按验收问题组织，而不是按服务器站点或内部实验目录组织；内部站点名不进入面向客户的主结构。
- v105 采用 corrected-image detail/residual head 解耦路线：Source-GT 局部结构恢复先只训练 image-space 分支，主 flow 继续由 coordinate、anti-ripple guard 和既有几何约束保持稳定。
- v105 的 detail loss 必须保持对主 flow 的梯度隔离；本轮 smoke 已验证只开 `w_source_image_detail` 时 `main_grad=0.0`、`detail_grad>0`，后续修改 detail head 时需要保留该检查。
- v105 的第一次训练采用保守 gate 与小 residual 上限，适合作为工程链路验证；若目标转向可见清晰度提升，应把 detail 分支强度、gate 覆盖和 high-edge residual 监督作为显式实验变量，而不是继续只看主 flow 指标。
- v104a 后不再把“两阶段 Stage B”作为默认下一步；只有 Stage A 明确突破 ROI high-edge-grad 平台且不明显压低位移/恶化 flow 高频时，才恢复几何与 anti-ripple 做 Stage B。
- 后续结构恢复应从主 flow 坐标场中解耦，优先采用 corrected-image 层面的局部 residual/detail 分支或显式门控，而不是继续把可见结构目标直接压到单一 flow 输出上。
- 后续不再把“裂缝锐化”和 “anti-water-ripple” 简单叠成一个单阶段 weighted loss；v103 以后应优先采用两阶段、分支式或梯度冲突门控策略。
- 梯度冲突诊断成为 v104 之前的必要 gate：只有确认结构目标能在关键 ROI 上带来可见 high-edge-grad 增益，且不会明显恶化 flow 高频/folding，才进入短训。

- v102 证明继续在同一阶段叠加可见结构恢复小权重，难以突破 high-edge-grad 平台；后续结构恢复实验应优先拆成冲突诊断、两阶段训练或分支式局部恢复目标。
- anti-water-ripple 继续保持为独立 guard 诊断项，不再把 flow 高频抑制和裂缝锐化目标混成单一调参方向。

- v101 后确认：关键 ROI 结构恢复目标必须以肉眼可见结构、ROI edge-weighted win 和 high-edge-grad win 作为主 gate，不能只用方向/骨架/局部梯度统计 loss 是否非零来判断有效；anti-ripple guard 可作为下限约束保留，但不能替代裂缝结构恢复。
- v100 后确认：anti-water-ripple 应作为独立 guard 保留在候选评估中，重点关注少数 ROI 的 highfreq/lap/folding Top，而不是用全局 EPE 或单一 ROI 平均值替代；但它不能单独解决 Source-GT 高边缘裂缝结构恢复。
- v99 归因后确认：后续 anti-water-ripple 不能只报全局或 25 ROI 均值，必须同时报告相对上一候选的逐 ROI 退化 Top、位移分桶和至少一个局部 folding 最坏样本；少数 ROI 的高 folding 足以否决扩严格 1000。
- v99 后确认：ROI 排序/对比式结构目标不能单独作为视觉突破证据；后续候选必须同时通过 ROI sharpness、120 high-edge-grad、ROI flow highfreq/folding 和人工 fixed40/ROI 复核，才考虑扩严格 1000。
- v98 后确认：关键 ROI 路线后续不能只靠 hard sampler 权重、只加轮次或继续堆 Source-GT 小权重 loss；必须把裂缝结构恢复目标写得更直接，并把 anti-water-ripple 作为可独立诊断的约束。
- 后续候选必须继续同时报告常规 120 指标、identity-aware 位移、source-GT 视觉质量和固定可视化；单独的坐标 EPE/Dice 提升或单独跨过非 identity 位移门槛都不能作为扩严格 1000 的依据。
- 80 epoch 长训候选也必须沿用同一 gate 口径：如果常规指标明显变好但 strict Source-GT gate 仍为 `reject_source_gt_regress`，只能作为“有进步但未过视觉验收”的候选，不应直接进入严格 1000 或写成最终突破。
- ROI 诊断脚本统一通过 `--model_name` 加载模型工厂中的结构；后续 v63 之后的新模型做局部复核时，优先复用 `utils/export_crack_roi_visuals.py`，避免再维护只支持默认 `crack_warp_net` 的旧诊断入口。
- 新增模型统一通过 `models/model_factory.py` 管理，默认模型名保持 `crack_warp_net` 以兼容历史实验；v63 使用 `single_flow_unet`，训练、评估、诊断和可视化都通过 `model_name`/`MODEL_NAME` 显式选择，避免为新 baseline 复制整套脚本。
- v81 起新增 `global_context_flow_unet_rescap`，v82 起新增 `global_context_flow_unet_rescap_mid`，v83 起新增 `global_context_flow_unet_spatial_cap` 作为独立模型名，不直接改变历史 `global_context_flow_unet` 行为；v81/v82 通过 builder 中的 `max_global_delta` 常量控制全局 residual 幅度，v83 通过输入图像先验生成空间 cap map，三者均保持 checkpoint key 兼容，方便公平复现实验和从 v76 checkpoint 直接初始化。
- `single_flow_unet` 采用直接预测 `identity + bounded residual` 的单图位移场路线，输出仍是项目统一的 `[0,1]` 归一化逆映射坐标场；该路线作为第二阶段新 baseline，与 v41/v22 使用同一输入、同一训练集、同一 120/1000 评估和同一固定可视化集。
- 当前将仓库定位为“水下裂缝扭曲校正算法工程与报告整理工作区”，代码、数据、训练输出和报告需要共同维护。
- 保留两份用户原始报告，不直接覆盖或改写原文件。
- `.venv/` 作为本地解析、评估和后续训练环境，已通过 `.gitignore` 排除。
- 对外工作量说明同时保留 Markdown 源稿和 Word 交付稿，便于后续快速改文字并生成客户可读版本。
- Slurm 训练采用包装入口覆盖配置，而不是复制一份训练主脚本；这样保留 `train_v2.py` 作为唯一训练逻辑源，减少后续调参时的维护分叉。
- Slurm 作业监控采用轻量 cron 轮询方案，而不是常驻守护进程；脚本通过 `squeue` 获取当前队列，通过 `sacct` 水位线补捉两次 cron 之间新增并结束的短任务，用 JSON 状态文件去重，降低部署复杂度并避免漏掉短评估 job。
- RecallLoom 采用隐藏侧车目录 `.recallloom/` 维护跨会话项目记忆；该目录当前由 RecallLoom 官方 helper 管理，不手工改写其中的 `config.json`、`state.json` 或 managed markers。
