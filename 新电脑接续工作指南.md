# 新电脑接续工作指南

> 目的：当前电脑上的代码、文档和上传说明已经同步到 GitHub；服务器端大数据也已经下载并解压完成。换到新电脑后，按本文档即可继续本项目开发、连接服务器并准备 Slurm 训练。

## 1. 项目一句话目标

本项目用于“水下裂缝图像几何扭曲校正”。模型需要从水下扭曲裂缝图像中预测归一化逆映射坐标场 `flow (2,H,W)`，再通过 `cv2.remap` 把扭曲图恢复到更接近无扭曲的几何状态。

评价重点不是普通图像增强，而是：

- 裂缝主体位置是否恢复。
- 细裂缝、分叉和边缘是否保真。
- 预测恢复量是否足够。
- 位移场是否可逆，folding rate 是否可控。
- Crack EPE、Global EPE、Dice/IoU、Edge fidelity、Folding rate 是否提升。

## 2. GitHub 与大文件位置

轻量代码仓库：

```text
https://github.com/zh23jemu/underwater-crack-correction
```

大文件 Release：

```text
https://github.com/zh23jemu/underwater-crack-correction/releases/tag/data-v1
```

普通 Git 仓库只保存代码、文档、脚本和小型说明文件。以下大文件不进 Git：

- `underwater_crack_v3/`：主合成训练集，服务器端已解压。
- `under-crack-images/`：原始裂缝图，服务器端已解压。
- `output_crackwarp/`：历史训练输出、checkpoint、评估和可视化结果，服务器端已解压。
- `.venv/`：本地或服务器虚拟环境，均需要重新创建。
- `release_assets/`：Release 下载和打包中间目录。

## 3. 当前已完成进展

### 3.1 本地代码和文档

- 已初始化 Git 仓库，并推送到 public GitHub。
- 已新增 `.gitignore`，排除大数据、训练输出、虚拟环境和本地 smoke 产物。
- 已新增 `requirements.txt`、`run_train_slurm.py`、`slurm_train_crackwarp.sbatch` 和 `smoke_train_verify.py`。
- 已新增 `GITHUB_UPLOAD_PLAN.md` 和 `SHA256SUMS.txt`，用于 Release 资产下载和完整性校验。
- 已完成本机最小 smoke：CPU/GPU 均能跑通微型训练验证，但本机 Quadro T1000 不适合正式训练。

### 3.2 GitHub Release 大文件

- 已创建 Release `data-v1`。
- 已上传 `under_crack_images.zip`。
- 已上传 `output_crackwarp.zip`。
- 已上传主训练集 `underwater_crack_v3.tar.zst.part-001` 到 `part-031`，共 31 个分卷。
- 已上传 `SHA256SUMS.txt` 校验清单。

### 3.3 服务器端数据状态

用户已经在服务器 `usmidet-com-prod-gpu001` 上完成以下操作：

- 成功下载 Release 资产。
- `under_crack_images.zip` 校验 OK。
- `output_crackwarp.zip` 校验 OK。
- 主训练集分卷已合并为 `underwater_crack_v3.tar.zst`。
- 已确认该文件为 `Zstandard compressed data`。
- 已使用 `zstd -d` 解压为 `underwater_crack_v3.tar`。
- 已成功执行 `tar -tf underwater_crack_v3.tar >/dev/null`。
- 已成功执行 `tar -xf underwater_crack_v3.tar`。
- 已解压 `under-crack-images/` 和 `output_crackwarp/`。
- 服务器项目根目录下已经能看到：

```text
underwater_crack_v3
under-crack-images
output_crackwarp
```

## 4. 新电脑第一步

在新电脑上 clone 仓库：

```bash
git clone https://github.com/zh23jemu/underwater-crack-correction.git
cd underwater-crack-correction
```

如果新电脑已经有旧目录：

```bash
cd underwater-crack-correction
git pull
```

新电脑本地不需要下载 24GB 数据集，除非要本地看图或做本地 smoke。正式训练和数据目录都在服务器上。

## 5. 新电脑本地环境可选操作

如果要在新电脑本地跑最小 smoke 或查看代码，可创建本地 `.venv`。

Windows 示例：

```powershell
py -3.11 -m venv .venv
.\.venv\Scripts\python.exe -m pip install --upgrade pip
.\.venv\Scripts\python.exe -m pip install torch torchvision --index-url https://download.pytorch.org/whl/cu126
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
```

检查：

```powershell
.\.venv\Scripts\python.exe -c "import torch; print(torch.__version__); print(torch.cuda.is_available())"
```

注意：新电脑本地没有大数据时，不要跑完整训练。完整训练应该放在 Slurm 服务器。

## 6. 服务器接续检查

从新电脑 SSH 到服务器后，先进入项目目录：

```bash
cd ~/underwater-crack-correction
git pull
```

确认三个大目录存在：

```bash
ls -d underwater_crack_v3 under-crack-images output_crackwarp
```

确认数据数量：

```bash
find underwater_crack_v3 -name '*.png' | wc -l
find underwater_crack_v3 -name '*.png.npy' | wc -l
find under-crack-images/images -name '*.jpg' | wc -l
```

期望：

```text
10360
10360
1037
```

如果数量不对，再回到 `GITHUB_UPLOAD_PLAN.md` 查看 Release 下载、SHA256 校验、分卷合并和解压步骤。

## 7. 服务器虚拟环境

服务器上不要复用任何从 Windows 拷来的 `.venv`，应重新创建。

如果服务器有 Python 3.11：

```bash
python3.11 -m venv .venv
.venv/bin/python -m pip install --upgrade pip
.venv/bin/python -m pip install torch torchvision --index-url https://download.pytorch.org/whl/cu126
.venv/bin/python -m pip install -r requirements.txt
```

检查 CUDA：

```bash
.venv/bin/python -c "import torch; print(torch.__version__); print(torch.cuda.is_available()); print(torch.cuda.get_device_name(0) if torch.cuda.is_available() else 'CPU')"
```

如果 `torch.cuda.is_available()` 为 `False`，不要提交正式训练，先检查 CUDA、驱动、PyTorch wheel、Slurm 分区和 GPU 分配。

## 8. Slurm smoke 测试

先跑短任务，不要直接跑完整 50 epoch。

```bash
EPOCHS=2 OUTPUT_DIR=output_crackwarp_slurm/smoke sbatch slurm_train_crackwarp.sbatch
```

查看任务：

```bash
squeue -u $USER
```

查看日志文件：

```bash
ls slurm-crackwarp-train-*.out slurm-crackwarp-train-*.err
```

如果 smoke 成功，应能看到：

- 数据集正常加载。
- CUDA 可用。
- 训练日志写入 `output_crackwarp_slurm/smoke/train.log`。
- 输出目录中生成 `best_epe.pth`、`best_loss.pth`、`final.pth` 或对应 checkpoint。
- 训练结束后尝试运行小规模评估 `eval_best_epe_120`。

## 9. 正式训练建议

smoke 成功后，再提交正式训练：

```bash
EPOCHS=50 OUTPUT_DIR=output_crackwarp_slurm/v2 sbatch slurm_train_crackwarp.sbatch
```

如果想临时用更快分区：

```bash
EPOCHS=50 OUTPUT_DIR=output_crackwarp_slurm/v2 sbatch --partition=gpuHz slurm_train_crackwarp.sbatch
```

默认脚本设置：

- 分区：`gpu`
- 账号：`gpo-ifv7xx`
- QOS：`normal`
- GPU：1 张
- CPU：8
- 内存：64GB
- 时间：48 小时

## 10. 当前最重要问题

当前模型已经训练收敛，但未达到工程门限。详细版报告和 gate report 显示：

- Crack EPE 约 `112px`，目标 `<=45px`。
- Global EPE 约 `112px`，目标 `<=40px`。
- Dice 约 `0.0026`，目标 `>=0.35`。
- Edge fidelity 约 `0.295`，目标 `>=0.65`。
- Folding rate 约 `0.57`，目标 `<=0.05`。

因此下一阶段不是简单多训几轮，而是先排查基础链路：

1. 统一 `predict.py`、`infer_epoch.py`、`utils/evaluate_metrics.py` 的 `cv2.remap` 坐标逻辑。
2. 确认 `.npy` 标签确实是 distorted 到 clean 的逆映射坐标。
3. 修复或禁用 `predict.py` 中可疑的 `np.abs(preds)` remap 逻辑。
4. 复核 folding penalty 是否正确衡量逆映射场局部可逆性。
5. 检查 Dice 极低是否来自模型问题、mask 提取问题或评估方向问题。
6. 确认评估口径稳定后，再进入 Slurm 全量训练和调参。

## 11. 下一步工作清单

建议按顺序推进：

1. 新电脑 clone/pull GitHub 仓库。
2. 新电脑确认能 SSH 到服务器。
3. 服务器 `git pull`。
4. 服务器确认三个大目录和文件数量。
5. 服务器重建 `.venv` 并安装 PyTorch CUDA 12.x 依赖。
6. 提交 `EPOCHS=2` Slurm smoke。
7. smoke 成功后，先做一轮坐标/remap/评估一致性排查。
8. 排查通过后，提交正式 50 epoch 训练。
9. 根据 gate report 结果决定损失权重、folding 约束和 ROI/refinement 优化方向。

## 12. 关键参考文件

- `AGENTS.md`：项目长期维护记录和当前状态。
- `GITHUB_UPLOAD_PLAN.md`：GitHub Release 大文件下载、校验、合并和解压说明。
- `SHA256SUMS.txt`：Release 资产校验清单。
- `项目交接与下一步开发计划.md`：更详细的项目背景、问题和研发路线。
- `PROJECT_WORKFLOW_GUIDE.md`：项目主流程说明，但其中部分旧命令仍待更新。
- `OPTIMIZATION_GUIDE.md`：优化方向和已做改动记录，但部分旧命令仍待更新。
- `slurm_train_crackwarp.sbatch`：Slurm 训练脚本。
- `run_train_slurm.py`：Slurm 训练包装入口，避免误清空历史结果。
- `smoke_train_verify.py`：本地/小规模训练链路验证脚本。

