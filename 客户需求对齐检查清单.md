# 客户需求对齐检查清单

更新时间：2026-06-15

## 使用目的

本清单用于把当前研发进展直接对齐客户提出的核心交付要求，避免项目后续只堆实验结果，却没有回应客户最关心的验收点。当前客户需求可归纳为四项：

1. 代码全部调好，训练、评估、对比和汇总链路可复现。
2. 整体训练完成，有稳定主模型候选。
3. 效果达到三区及以上论文/报告实验完整度，并具备可支撑的数据。
4. 补全新近对比实验和消融实验。

## 对齐总表

| 客户要求 | 当前状态 | 已有证据 | 缺口 | 下一步动作 |
| --- | --- | --- | --- | --- |
| 代码全部调好 | 主训练、评估、分片续跑、消融、外部对比、v5/v6 优化和汇总链路均已打通 | `train_v2.py`、`run_train_slurm.py`、`slurm_train_crackwarp.sbatch`、`utils/evaluate_metrics.py`、`utils/merge_eval_shards.py`、`utils/evaluate_flow_predictions.py`、`utils/summarize_final_results.py` | 后续主要是报告排版和可视化整理，不再是主代码阻塞 | 固化最终汇总表和报告口径，避免再扩大无关代码范围 |
| 整体训练完成 | 已完成 v4、v5、v6 三条内部主线训练，并完成 10360 张全量评估 | 全量 v4：crack EPE 114.416328、global EPE 111.964767、Dice 0.276011、folding 0.583643；全量 v5：folding 0.579404；全量 v6：crack/global edge 0.124766/0.114296 | 没有单一模型全指标最优，需按 v4/v5/v6 分工表述 | 报告中采用“三版本分工”表述：v4 保守 EPE/Dice，v5 几何稳定，v6 边缘增强 |
| 三区及以上实验数据 | 具备主结果、全量评估、v5/v6 改进结果、消融、两个新近外部上界、逐图对比和 flow 诊断基础 | `最终实验结果汇总.md`、`summary_tables/final_full_summary.md`、`summary_tables/final_ablation_summary.md`、UniMatch/SEA-RAFT 1000 样本 oracle-pair 结果、v4/v5/v6 全量逐图对比 | 还需要把训练曲线、运行成本、最终图表排版和论文式文字整理成完整实验章节 | 整理实验章节：全量主表、1000 快速评估表、消融表、外部上界表、可视化图、失败案例和运行成本 |
| 新近对比实验 | UniMatch 与 SEA-RAFT 均已完成 1000 样本 oracle-pair 对比 | UniMatch：crack EPE 3.810645、global EPE 1.676183、Dice 0.715517、folding 0.032984；SEA-RAFT：crack EPE 3.140002、global EPE 1.953668、Dice 0.718757、folding 0.030357；二者相对 v4 均为 0/1000 | 两者都是 oracle-pair 上界，不是同输入公平对比；MemFlow、NeuFlow 尚未完成结果 | 当前已满足“补充新近外部上界对比”的最低交付要求；若客户要求更多方法，再筛选 MemFlow 或 NeuFlow |
| 消融实验 | 4 个 2 epoch 消融已完成 1000 样本评估 | `abl_edge_only_2ep`、`abl_no_over_penalty_2ep`、`abl_robust_only_2ep`、`abl_edge_w003_2ep` 均弱于完整 v4 | 当前消融足以证明 v4 组合必要性，但还没有 folding/Jacobian 或边界平滑消融 | 报告先使用现有消融；若继续优化，再新增 folding/Jacobian 正则或边界平滑消融 |

## 当前可交付结论

### 1. 代码与流程

当前已经形成从训练到评估、分片续跑、逐图对比、外部方法评估、v5/v6 优化和结果汇总的基本闭环。主模型和消融实验可以通过统一脚本复现；外部方法只要导出 `pred_grid/xxx.png.npy`，即可用 `utils/evaluate_flow_predictions.py` 统一评估。

需要注意的是，正式对客户表述时不宜说“所有代码已经完全最终定版”。更准确的说法是：主模型训练评估链路已经打通，最终实验汇总链路已经建立，v5 进一步改善了 edge/folding 质量，UniMatch 与 SEA-RAFT 两个新近外部 oracle-pair 对比已经补齐。

### 2. 主模型

当前主模型建议采用“三版本分工”口径。`v4_robust_edge_10ep` 是 EPE/Dice 更稳的保守版本；`v5_jacobian_roi_w002_10ep_lr1e6` 是 folding 最优的几何稳定版本；`v6_recover_epe_from_v5_10ep` 是 edge fidelity 最优的边缘增强补充版本。全量逐图综合评分中，v5 有 5503/10360 张优于 v4，v6 有 5710/10360 张优于 v4。

报告中仍应保留局限：v5/v6 的综合覆盖面和专项指标更好，但 EPE/Dice 均值仍不如 v4，不能写成“全指标全面超过 v4”或“所有样本已完全校正”。

### 3. 对比实验

UniMatch 与 SEA-RAFT oracle-pair 已经提供两个非常强的外部上界：二者在 1000 张样本上均 1000/1000 逐图综合优于 v4，说明有配对参考图时 dense matching 可以接近理想几何配准。但这两类方法输入条件更强，不能作为同条件直接压过主模型的公平基线。

当前已经可以支撑“至少两个近年外部方法/上界对比”的报告结构。若后续继续补方法，MemFlow 或 NeuFlow 可作为扩展，但不是当前客户交付的最小阻塞项。

### 4. 消融实验

现有 4 个消融均弱于完整 v4，可以直接用于证明完整 v4 组合的必要性。报告中的建议表述是：

- 仅使用边缘一致性无法稳定替代完整方案。
- 去掉过大位移惩罚会增加 folding 风险。
- 仅鲁棒位移约束或降低边缘权重都不能全面超过完整 v4。
- 因此完整组合更适合作为当前主模型方案。

## 下一步优先级

1. 开始撰写最终报告实验章节，结构固定为：v4/v5/v6 全量主模型结果、1000 样本快速评估、消融实验、新近外部对比、可视化案例、失败分析、运行成本。
2. 将 `final_full_summary.md` 和 `final_ablation_summary.md` 的主表整理成论文/报告表格。
3. 使用 `报告素材筛选建议.md` 挑选整体提升、共同失败改善和局限样本图。
4. 补充训练曲线和运行成本说明，形成更接近客户“三区及以上实验完整度”的交付材料。
5. 若客户继续要求更强模型效果，下一步应围绕“保留 v5/v6 的 folding/edge 收益，同时追回 crack ROI EPE/Dice”继续优化，而不是继续拉长已证明弱于 v4 的 4 个 2 epoch 消融。
