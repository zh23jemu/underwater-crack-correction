# 项目交接与下一步开发计划

> 目的：本文件用于把整个文件夹拷贝到另一台电脑后继续开发。当前不依赖 Git，接手者优先阅读本文件，再查看 `AGENTS.md`、`PROJECT_WORKFLOW_GUIDE.md` 和 `OPTIMIZATION_GUIDE.md`。

## 1. 当前项目状态

当前项目已经不是单纯的报告资料目录，而是一个包含代码、数据、模型权重、训练日志、评估结果和报告材料的水下裂缝扭曲校正算法工程。

核心目标是提升水下裂缝图像的几何扭曲复原效果，重点解决以下问题：

- 当前可视化效果不好。
- 裂缝主体和细小分叉复原不足。
- 预测恢复量偏小，疑似坐标尺度、逆映射方向、`remap` 使用或损失约束存在问题。
- 现有 checkpoint 虽然训练收敛，但没有达到工程门限。
- 后续还需要补齐对比实验、消融实验、定量表格和论文级可视化材料。

当前只做过只读确认和文档整理，没有在本机启动新训练。

## 2. 项目体量与关键目录

只读统计结果：

- 非 `.venv` 文件约 21,913 个。
- 总体量约 27.25GB。
- 主合成训练集 `underwater_crack_v3/` 约 26.21GB。
- 训练输出 `output_crackwarp/` 约 0.98GB。

关键目录和文件：

| 路径 | 作用 |
|---|---|
| `under-crack-images/images/` | 原始真实裂缝图，共 1,037 张 `.jpg`。 |
| `underwater_crack_v3/` | 主合成训练集，共 10,360 张 `.png` 和 10,360 个 `.png.npy` 标签。 |
| `underwater_crack_v3/manifest.json` | 数据生成记录，`samples_per_image=10`，`total_generated=10360`。 |
| `output_crackwarp/` | 已有训练输出，包含日志、checkpoint、评估和可视化结果。 |
| `output_crackwarp/train.log` | 50 epoch 训练日志。 |
| `output_crackwarp/*.pth` | 已有 checkpoint，共 13 个。 |
| `output_crackwarp/gate_report/` | checkpoint 门限评估结果。 |
| `train_v2.py` | 训练入口。 |
| `config_crack.py` | 当前训练配置。 |
| `MyDataSet.py` | 数据读取和增强。 |
| `models/crack_warp_net.py` | 主模型。 |
| `loss_crack.py` | 裂缝优先损失函数。 |
| `infer_epoch.py` | 推理和可视化脚本。 |
| `utils/evaluate_metrics.py` | 裂缝中心指标评估。 |
| `utils/checkpoint_gate_report.py` | checkpoint 横向评估和门限报告。 |

## 3. 数据确认结果

主数据集配对完整：

- `.png` 数量：10,360
- `.png.npy` 数量：10,360
- 缺失标签：0
- 缺失图片：0

抽样检查结果：

- 图片尺寸：`512 x 512`
- 标签 shape：`(2, 512, 512)`
- 标签 dtype：`float32`
- 标签范围：`[0, 1]`
- 标签含义：归一化逆映射坐标场

像素坐标恢复方式应为：

```python
map_x = label[0] * (W - 1)
map_y = label[1] * (H - 1)
corrected = cv2.remap(distorted, map_x, map_y, cv2.INTER_LINEAR)
```

后续排查“恢复量太小”时，要优先确认所有训练、评估、推理脚本都遵循同一套坐标定义。

## 4. 已有训练结果

已有训练跑满 50 epoch。

训练日志结论：

- 总训练时间：约 17.99 小时。
- 最佳 Val Loss：`0.32642`
- 最佳 Val EPE：约 `117.885px`
- 最终 epoch：`50/50`
- 训练集大小：9,324
- 验证集大小：1,036

`output_crackwarp/` 中已有 checkpoint：

- `best_epe.pth`
- `best_loss.pth`
- `final.pth`
- `epoch_5.pth` 到 `epoch_50.pth`，每 5 epoch 保存一次。

gate report 结论：

| 指标 | best_epe.pth 当前结果 | 现有门限 | 状态 |
|---|---:|---:|---|
| Crack EPE | 约 112.021px | <= 45px | 未达标 |
| Global EPE | 约 112.606px | <= 40px | 未达标 |
| Dice | 约 0.0026 | >= 0.35 | 未达标 |
| Edge fidelity | 约 0.295 | >= 0.65 | 未达标 |
| Folding rate | 约 0.570 | <= 0.05 | 未达标 |

判断：模型有收敛趋势，但距离可交付效果很远。当前最明显瓶颈是裂缝区域复原、边缘保真、位移场可逆性和恢复幅度。

## 5. 当前配置重点

当前 `config_crack.py` 中关键配置：

```python
trainroot = './underwater_crack_v3/'
output_dir = './output_crackwarp/'
shape = (512, 512)
gpu_id = 0
workers = 8
epochs = 50
train_batch_size = 1
val_split = 0.1
lr = 2e-5
warmup_epochs = 5
min_lr = 1e-7
weight_decay = 5e-4
gradient_clip = 0.5
w_coord = 1.0
w_smooth = 0.1
w_fold = 0.01
w_photo = 0.15
w_ssim = 0.10
w_freq = 0.10
w_crack_coord = 1.20
w_crack_grad = 0.25
w_crack_freq = 0.15
crack_boost = 8.0
crack_topk = 0.08
use_ema = True
ema_decay = 0.999
accum_steps = 4
mixup_start_epoch = 30
mixup_prob = 0.10
mixup_alpha = 0.05
restart_training = True
checkpoint = ''
```

注意：`restart_training = True` 会在训练开始时清理 `output_dir`。继续开发时，如果要保留已有训练结果，必须先改成 `False` 或另设新的输出目录，例如 `output_crackwarp_debug/`、`output_crackwarp_v2/`。

## 6. 拷贝到新电脑后的环境建议

`.venv/` 不建议跨电脑直接复用。拷贝整个文件夹后，可以保留它作为记录，但新电脑最好重新创建本地虚拟环境。

Windows 建议步骤：

```powershell
py -3.11 -m venv .venv
.\.venv\Scripts\python.exe -m pip install --upgrade pip
```

如果新电脑有 NVIDIA GPU，优先安装 CUDA 12.x 兼容的 PyTorch。当前项目规则建议不要装 CUDA 13.0 wheel，可优先使用 cu126：

```powershell
.\.venv\Scripts\python.exe -m pip install torch torchvision --index-url https://download.pytorch.org/whl/cu126
```

再安装其余依赖：

```powershell
.\.venv\Scripts\python.exe -m pip install opencv-python numpy scipy tqdm pillow natsort scikit-image matplotlib pandas
```

安装后先检查：

```powershell
.\.venv\Scripts\python.exe -c "import torch; print(torch.__version__); print(torch.cuda.is_available()); print(torch.cuda.get_device_name(0) if torch.cuda.is_available() else 'CPU only')"
.\.venv\Scripts\python.exe -c "import cv2, numpy, scipy, tqdm, PIL, natsort, skimage; print('deps ok')"
```

当前仓库没有 `requirements.txt`，建议下一步补一个，避免环境漂移。

## 7. 新电脑上的第一轮只读检查

不要一上来直接训练。建议先跑以下小检查：

### 7.1 检查数据配对

```powershell
.\.venv\Scripts\python.exe -c "from pathlib import Path; d=Path('underwater_crack_v3'); png=set(p.stem for p in d.glob('*.png')); npy=set(p.name[:-8] for p in d.glob('*.png.npy')); print(len(png), len(npy), len(png-npy), len(npy-png))"
```

期望输出类似：

```text
10360 10360 0 0
```

### 7.2 检查标签范围

```powershell
.\.venv\Scripts\python.exe -c "from pathlib import Path; import numpy as np; p=next(Path('underwater_crack_v3').glob('*.png.npy')); a=np.load(p); print(p.name, a.shape, a.dtype, float(a.min()), float(a.max()))"
```

期望：

```text
(2, 512, 512) float32 0.0 1.0
```

### 7.3 检查模型能否导入

```powershell
.\.venv\Scripts\python.exe -c "from models.crack_warp_net import build_crack_warp_net; m=build_crack_warp_net(); print(sum(p.numel() for p in m.parameters())/1e6)"
```

### 7.4 检查 checkpoint 能否加载

```powershell
.\.venv\Scripts\python.exe -c "import torch; from models.crack_warp_net import build_crack_warp_net; m=build_crack_warp_net(); sd=torch.load('output_crackwarp/best_epe.pth', map_location='cpu'); m.load_state_dict(sd, strict=True); print('checkpoint ok')"
```

如果这里失败，先检查模型结构是否和 checkpoint 对得上，不要急着训练。

## 8. 第一轮复现建议

环境和 checkpoint 都确认后，先做小规模推理和评估。

### 8.1 小规模推理

实际文件名是 `infer_epoch.py`，不是文档里旧写法 `infer_epoch80.py`。

```powershell
.\.venv\Scripts\python.exe infer_epoch.py --model output_crackwarp/best_epe.pth --img_dir underwater_crack_v3 --out_dir output_crackwarp/infer_check_best_epe --num 20
```

重点看输出的 `*_compare.png` 和 `*_corrected.png`：

- 校正图是否明显移动。
- 裂缝主体是否更接近目标。
- 是否出现大面积黑边、撕裂、翻折或不自然拉伸。
- 预测 flow 可视化是否和真实 flow 方向一致。

### 8.2 小规模评估

```powershell
.\.venv\Scripts\python.exe utils/evaluate_metrics.py --model output_crackwarp/best_epe.pth --img_dir underwater_crack_v3 --out_dir output_crackwarp/eval_check_best_epe_40 --num 40
```

重点核对：

- `primary_crack_epe_px_mean`
- `global_epe_px_mean`
- `primary_warp_crack_dice_mean`
- `primary_crack_edge_fidelity_mean`
- `folding_rate_mean`

如果新电脑评估结果和现有报告差异很大，先排查环境、随机性、图像读取顺序、resize、坐标转换和 checkpoint 加载方式。

## 9. 优先排查的问题

### 9.1 `predict.py` 与 `infer_epoch.py` 的 remap 逻辑不一致

已发现需要重点查看：

- `infer_epoch.py` 中使用 `map_x = flow_np[0] * (W - 1)`，`map_y = flow_np[1] * (H - 1)`。
- `predict.py` 中存在 `np.abs(preds[:, :, 0])` / `np.abs(preds[:, :, 1])` 的历史逻辑。

`abs()` 很可能会破坏坐标方向，导致可视化结果异常或恢复量判断错误。下一步应统一推理脚本，只保留一种经过验证的坐标转换方式。

### 9.2 `PROJECT_WORKFLOW_GUIDE.md` 和 `OPTIMIZATION_GUIDE.md` 脚本名过时

文档中写的是 `infer_epoch80.py`，实际文件是 `infer_epoch.py`。后续需要统一文档，避免新电脑接手时照文档跑错。

### 9.3 `restart_training=True` 有误删输出风险

`train_v2.py` 中如果 `config.restart_training` 为 `True`，且 `output_dir` 存在，会清理输出目录。正式调试前必须确认是否要保留旧的 `output_crackwarp/`。

建议下一轮训练使用新目录：

```python
output_dir = './output_crackwarp_v2/'
restart_training = True
```

或断点/接续训练时：

```python
restart_training = False
checkpoint = './output_crackwarp/best_epe.pth'
```

但当前 `train_v2.py` 是否完整支持从 `checkpoint` 恢复优化器和 epoch，还需要先读代码确认。

### 9.4 Folding rate 过高

当前 best checkpoint 的 folding rate 约 0.570，远高于门限 0.05。说明预测逆映射场存在大量局部折叠或非单调区域。

优先排查：

- `FoldingPenalty` 是否对归一化坐标场的定义正确。
- `w_fold=0.01` 是否太小。
- 模型输出是否被过强 smooth 或 bounded update 压制。
- 训练标签本身是否在高难样本中存在不可逆区域。

### 9.5 Dice 极低

当前 Dice 约 0.0026，非常低。需要确认：

- Dice 计算用的 crack mask 是否合理。
- `CrackMaskEstimator` 从输入图估计裂缝区域是否稳定。
- 预测校正图和 GT 校正图是否真的在同一空间比较。
- 评估中 mask warp 的方向是否和训练标签方向一致。

## 10. 下一步开发路线

建议按以下顺序推进。

### 阶段 A：环境和小闭环复现

目标：确认新电脑能稳定跑项目，不动模型结构。

任务：

1. 重新创建 `.venv`。
2. 安装 PyTorch CUDA 12.x 兼容版本和项目依赖。
3. 补充 `requirements.txt`。
4. 导入模型，加载 `best_epe.pth`。
5. 小规模推理 20 张图。
6. 小规模评估 40 张图。
7. 对比已有 `diag_eval_best_epe_v2` 和 gate report 指标。

验收：

- checkpoint 能加载。
- 推理能出图。
- 小规模评估能输出 JSON/CSV。
- 指标和已有结果量级一致。

### 阶段 B：坐标和可视化一致性排查

目标：先解决“看起来不对”和“恢复量太小”的基础问题。

任务：

1. 统一 `infer_epoch.py`、`predict.py`、`utils/evaluate_metrics.py` 的坐标转换。
2. 删除或禁用 `predict.py` 中可疑的 `abs()` 坐标逻辑。
3. 增加一个小脚本或调试函数：对同一张图同时展示 distorted、GT remap、Pred remap、flow error、mask ROI。
4. 检查标签方向：确认 `.npy` 是从 distorted 到 clean 的逆映射，还是 clean 到 distorted 的正映射。
5. 检查 `cv2.remap` 的 `map_x/map_y` 输入是否全部为像素坐标。

验收：

- GT label 用 `cv2.remap` 能把 distorted 明显恢复到合理状态。
- Pred 和 GT 的 flow 可视化方向一致。
- 同一 checkpoint 在不同推理脚本下结果一致。

### 阶段 C：评估指标可信度检查

目标：确认 gate report 低分是真问题，不是指标实现错。

任务：

1. 对少量样本手工可视化 crack mask。
2. 检查 `CrackMaskEstimator` 的 topk 和 temperature 是否适合当前裂缝图。
3. 对比 global EPE、crack EPE、edge fidelity、Dice 的逐图排名。
4. 找出 10 张最好样本和 10 张最差样本。
5. 检查 `folding_rate` 对归一化坐标的雅可比定义是否正确。

验收：

- 能解释 Dice 极低、folding rate 极高的主要来源。
- 能确认下一步优化是模型问题、标签问题、评估问题还是推理方向问题。

### 阶段 D：低成本调参

目标：在不大改结构前先试损失和配置。

建议实验：

1. 提高 `w_fold`，观察 folding rate 是否下降。
2. 提高 `w_crack_coord` / `w_crack_grad`，观察 crack EPE 和 edge fidelity。
3. 降低 `w_smooth`，避免过度平滑压制裂缝细节。
4. 检查 `crack_topk`，避免裂缝 mask 过小或过大。
5. 暂停 mixup，排查几何回归是否被混合样本干扰。

每次只改一到两个变量，先跑短训，例如 3-5 epoch 或固定小样本过拟合。

### 阶段 E：模型结构和训练策略优化

目标：在确认基础链路没问题后，再做结构改动。

可选方向：

1. 增加 ROI refinement 分支，专门修裂缝局部。
2. 加强可逆性或单调性约束，降低 folding rate。
3. 引入多尺度监督，让大尺度形变和局部裂缝细节同时收敛。
4. 引入边缘/骨架监督，提升细裂缝连续性。
5. 对困难样本加权采样。

### 阶段 F：对比实验和消融实验

目标：为三区及以上论文或项目验收补实验完整度。

对比实验建议：

- 当前原始模型。
- 修复坐标/推理逻辑后的模型。
- 传统或变分水下畸变校正方法。
- 近期水下非刚性畸变校正方法。
- GMFlow / UniMatch / FlowFormer 类稠密匹配或光流方法。

消融实验建议：

- 去掉裂缝区域加权损失。
- 去掉边缘保持损失。
- 去掉 folding/Jacobian 约束。
- 去掉 ROI/refinement。
- 去掉多尺度结构。
- 去掉 mixup 或关键数据增强。
- 调整恢复量约束或尺度监督。

## 11. 不建议马上做的事情

- 不建议直接从头跑完整 50 epoch，因为当前环境和坐标逻辑还没完成复核。
- 不建议直接覆盖 `output_crackwarp/`，里面有现有日志、checkpoint 和评估结果。
- 不建议一开始就大改模型结构，先确认数据、标签、推理和评估方向。
- 不建议把整个 27GB 项目直接初始化 Git 并提交；当前用户明确说先不用 Git。
- 不建议删除任何现有输出文件。如确实需要清理，只给出建议命令或先备份。

## 12. 建议新增或修复的文件

后续开发建议新增：

- `requirements.txt`：固定依赖。
- `scripts/smoke_check.py`：一键检查数据配对、标签范围、模型导入、checkpoint 加载。
- `scripts/debug_remap_consistency.py`：检查 GT remap 和 Pred remap 的坐标方向。
- `scripts/run_eval_small.ps1` 或 `.bat`：Windows 下小规模评估命令。
- `slurm/train_crackwarp.sbatch`：如果转到集群训练，再补 Slurm 脚本。

建议修复：

- `PROJECT_WORKFLOW_GUIDE.md`：把 `infer_epoch80.py` 改成 `infer_epoch.py`，命令改为 `.venv` 调用。
- `OPTIMIZATION_GUIDE.md`：同上，避免继续写系统 `python`。
- `predict.py`：重点审查 `np.abs()` 坐标逻辑。
- `.gitignore`：如果后续启用 Git，再确认大数据、checkpoint 和日志的管理策略。

## 13. 当前最重要结论

1. 用户代码和数据已经到位，可以继续工程诊断和优化。
2. 数据集配对完整，标签范围初步正常。
3. 当前模型训练收敛但效果远未达标。
4. 下一步应先做环境复现和坐标/推理/评估一致性排查。
5. 只有在小闭环跑通后，才建议进入完整训练、调参、结构优化、对比实验和消融实验。

